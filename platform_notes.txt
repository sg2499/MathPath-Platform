Assessment Builder Phase:

1) * Student level expand all icon along with the expand branch ones already there - IMPLEMENTED

2) * Results tab in admin login has dps wise results, also need to add level wise results in the same tab and manage it through 2
     sub tabs in the same Results page and make it look premium as usual.
   * Also add teacher filter dropdown along with module, lesson, dps already there to check for teacher wise student results for both
     dps and level wise results.
   * Also add the download excel report option in both dps and level wise results tab for each filter applied. ensure and decide which
     fields would be apt and relevant for which report according to the filters for each type of result. This is what i am thinking
     right now but should this feature be here or somewhere else when we go into different types of report generation at all the 
     different levels for tracking?

3) * tracker and results tab show the same thing in the teacher login. can we chnage one of them into something useful or should we
     just remove one of them as both are same? - IMPLEMENTED

4) * The curriculum tab in admin how is it different from the assign dps tab in teaacher? as from both the tabs dps is getting
     assigned to the student. if there is a difference then both should function differently and we should look to redesign that
     part to properly showcase the different abilities. First plese explain to me that what is it doing right now and how is it different
     from each other? - IMPLEMENTED

5) * In admin login, the practice assignments and assessment assignments need to have the level wise and teacher wise filter as
     well. - IMPLEMENTED
   * Also for the students tab in the admin panel we need to have the level filter for checking level wise students info. - IMPLEMENTED
   * In the practice tracker and results tab of the teacher login we need the level filter as well. - IMPLEMENTED