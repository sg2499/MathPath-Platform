# Backend Implementation Plan v1

Stack: FastAPI, PostgreSQL, SQLAlchemy, Pydantic, JWT.

Build order: setup → schema → auth → seed YLM → generator → admin preview → assignment → attempt flow → scoring.
