# Cowork Session Handoff

Last updated: 2026-09-01 (Cowork — PR #494's CI failure (3 `ASSESSMENT_MARKS_MISMATCH` pytest failures) diagnosed as a real backend bug and fixed; PR #494 confirmed merged by Shailesh AND now confirmed LIVE and working -- Shailesh directly: "i have confirmed them live and they work fine". Then: found and fixed why Mock Studio never got the "always-100-marks" Concept Drill/Skill Stacker treatment that assessments already have (the earlier marks-weighting fix only touched scoring, never the admin planning layer or the frontend Section Allocation UI) — built an auto-balance-from-weighted-count fix across every module with a weighted section (MM/IM/PM-L1..L4/BM/YLM), caught and fixed a real regression via the full test suite, and removed the Avg Score column from the mock leaderboard. **This work (4 files: `competition_mock_generation_service.py`, `frontend/lib/api/admin.ts`, `mock-studio/page.tsx`, `leaderboard/page.tsx`) is CONFIRMED MERGED AND DEPLOYED, live and working -- PR #495, commit `d00ec03082bf3fa08c8c41dfe23751d855e61cae`. Confirmed via Render (`mathpath-backend`, deploy `dep-dab66ogu01pc73e69js0`, status `live`, finished 2026-09-01 05:34 UTC) and Vercel (`math-path-platform`, deployment `dpl_E6dsA3ASWMWUaTfirCSxeWKF4ETj`, target production, state READY) -- both serving this exact commit. Delivered outside this Cowork thread (commit authored directly by Shailesh, consistent with this repo's local-Claude-Code-delivers pattern) and confirmed back in this thread with real evidence, not just Shailesh's word. See the entry below for the full technical writeup.**)

## 2026-09-01 update, later still (repo cleanup -- removed garbage files, staged for delivery)

**tl;dr:** Shailesh asked for a repo cleanliness pass before starting new work -- only files that belong should be there. Audited untracked AND tracked content (not just `git status`'s CRLF noise, already explained above) and removed real garbage, verified safe via `git grep` (zero code/CI/doc references to anything removed) and the CI workflow config (`pytest tests -q`, scoped to `backend/tests/` only -- confirms the removed root-level scripts were never part of the test suite).

**Deleted outright (untracked, no git history impact, no push needed):** `_to_delete/` (2 old scratch tarballs a prior session could not delete itself), 5 leftover `.oursresolved` merge-conflict-resolution artifacts dated 2026-08-14 (`routes_admin.py`, `report_export_service.py`, `schema_migration.py`, `CLAUDE_CODE_STATUS.md`, `OPEN_ISSUES.md`), and 2 stray frontend build outputs (`frontend/BUILD_SHA.txt`, `frontend/mathpath-frontend-build.tgz`).

**Staged for removal via `git rm --cached` (tracked -- this is a diff waiting for review + push, Cowork has no GitHub route):**
- 3 accidentally-committed FUSE temp-file duplicates (`.fuse_hidden0000001700000001` /`...1a00000002` / `...1d00000003` under `frontend/app/admin/assessment-blueprints`, `-readiness`, and `/assessments` respectively) -- confirmed these are orphaned copies with no code referencing them by name, and the real `page.tsx` files at each of those 3 paths are untouched and intact.
- 6 one-off personal debug scripts sitting in `backend/` root, never in `backend/scripts/` or `backend/tests/`: `test_add.py`, `test_eval.py`, `test_gamification.py`, `test_null.py` (all query-and-mutate throwaway scripts against `db.query(...).first()`), `wipe_notifications.py` (hardcoded to a specific user's SQLite data), `clear_notifications_sqlalchemy.py` (hardcoded to search for a user named 'Nishant'). None referenced anywhere in code, docs, or CI.

**`.gitignore` updated** to stop this class of file recurring: `*.oursresolved`/`*.theirsresolved`, `.fuse_hidden*`, `frontend/BUILD_SHA.txt`, `frontend/*.tgz`, `*.orig`.

**Deliberately left alone, flagged for a decision rather than deleted unilaterally:** root `SETUP_GUIDE.md` (146 lines, real) vs. `docs/SETUP_GUIDE.md` (20-line stub, looks orphaned) and `CURRENT_STATUS.md` (self-marked superseded since 2026-07-10, never archived) -- both already flagged in `OPEN_ISSUES.md`'s Active section as needing human judgment, not obvious garbage like the items above, so not touched given the explicit instruction not to risk deleting anything useful. `.claude/settings.local.json` and the two new untracked docs (`DAILY_LOGS/2026-09-01.md`, `DEPLOY_PLAYBOOK.md`) are legitimate, not garbage -- left as-is for the next commit.

**Not yet delivered** -- the 9-file `git rm --cached` diff needs review (qa-reviewer) then push (sre-devops) from a local Claude Code session, same as any other change from this sandbox.

## 2026-09-01 update, later (a second Cowork session resumed from project memory and ran a deeper git-log cross-check on top of the same-day cleanup above)

**tl;dr:** Asked to resume and report status, cross-checking every "Active"/"not yet delivered" claim against `git log origin/main` and real deploy state rather than trusting doc prose. Confirmed the same-day cleanup above (14 stale items removed, PR #494/#495 merged-and-live) is accurate. Found two more stale `OPEN_ISSUES.md` Active bullets that cleanup pass missed -- DPS badge detection/colour-repalette work and the wireframe-tangle/backfill-script work were both still listed "PREPARED NOT YET DELIVERED" even though they merged back on 2026-08-03 as PR #418/#419/#420/#421 (confirmed via `git log --grep`), and one of the two bullets even sat right above another bullet that already cited PR #418 as merged. Removed both into a new dated `Resolved Recently` entry in `OPEN_ISSUES.md` citing the real PR numbers, same pattern as the earlier cleanup.

**Also confirmed, not a doc issue:** the working tree shows ~540 files as `git status` "modified" -- verified via `git diff -w --stat` this collapses to exactly the 6 doc files the earlier session in this thread actually edited (94 insertions/63 deletions); the rest is the CRLF line-ending drift this repo's own `OPEN_ISSUES.md` Active section already documents as a known, recurring, harmless pattern (`core.autocrlf` unset on this OneDrive-synced sandbox) -- not real uncommitted work. Also noted, not fixed: `docs/project-memory/CLAUDE_CODE_STATUS.md` is genuinely stale (last "Updated" 2026-08-14, describing the parent-PDF report's 6th/7th polish rounds as "not yet delivered" -- round 6 actually shipped as PR #465 the same day, and round 7's described code changes aren't present anywhere in the current working tree, whitespace-noise aside). Per this repo's own `CLAUDE.md`, that file is meant to be overwritten by the local Claude Code terminal session (the "orchestrator"), not a Cowork session, so left as-is rather than edited here -- flagging for whoever runs that session next.

**Real deploy state, checked directly rather than trusted from docs:** confirmed via the Render and Vercel MCP tools directly this session (see this entry's tool calls) -- summarized in the chat reply back to Shailesh, not duplicated here.

## 2026-09-01 update (PR #494 CI fix confirmed merged; Mock Studio marks-balancing built across all levels; mock leaderboard Score column removed): mock-marks work CONFIRMED MERGED AND DEPLOYED AS PR #495

**tl;dr:** Two separate pieces of work this thread. First, closed out PR #494: its CI check (`MathPath CI/backend-tests`) had failed with 3 `ASSESSMENT_MARKS_MISMATCH` pytest failures (`expectedMarks=100.0`, `actualMarks=140.0`), diagnosed as a genuine backend bug rather than patched away in the tests, fixed, verified with real test execution, and Shailesh confirmed the PR merged ("i have merged, gimme the deploy commands"). Second, Shailesh pasted two Mock Studio "Create Mock" screenshots (MM-L1) and asked directly: "we had applied the 5 mark each logic for the concept drill and skill stacker sums for mock exams as well, but if you see the image above it does not have the parameter which asks for how many questions do these concepts have and then adjust the total number of questions accordingly so that the full marks always comes down to 100 as i had explained it yesterday. it is still not implemented for the mock flow for any of the levels wherever we have these concepts. also for the mock leaderboard we need to have the columns in the leaderboard as follows: rank - student - avg accuracy - avg time. we do not need the avg score there."

**Root cause of the mock-marks gap.** An earlier session's marks-weighting fix only touched `_ResolveCompetitionMockQuestionMarks` (actual scoring) — it never touched the admin-facing planning layer (`CompetitionMockSectionPlan`, which drives the Mock Studio Section Allocation UI) or the frontend itself. Same two-layer-drift bug class already documented elsewhere in this repo for MM/BM assessment blueprints, now confirmed present in the mock flow too.

**Design decision, settled via AskUserQuestion before building:** chose "Auto-balance from weighted count" — admin sets only the weighted section's (Concept Drill/Skill Stacker) question count; every flat section auto-computes its count to fill the remaining marks to exactly 100; "Total Questions" becomes a computed, read-only display whenever a level has a weighted section.

**Backend — `backend/app/services/competition_mock_generation_service.py` (288 changed lines in the final diff).** Added `_CompetitionMockWeightedSectionKeys()` (identifies which sections are 100% weighted-family concepts), `_BalanceCompetitionMockSectionCounts()` (the core balancer: weighted section count x 5 marks, remaining marks split evenly across flat sections, `COMPETITION_MOCK_MARKS_INVALID` if the weighted count alone exceeds 100), `_CompetitionMockSectionPlanSections()` (feeds the GET preview endpoint used by the Section Allocation UI, tagging each section `isWeighted`/`marksPerQuestion`), and `_CompetitionMockLevelSectionConfig()` (dispatches to each module's existing level-config function). All 5 module branches in `CompetitionMockSectionPlan()` (MM/IM/PM/BM/YLM) and the `GenerateCompetitionMockDraft()` entry point were rewired through this shared balancer — zero changes needed to any per-module generator internals, since every module already routes through one shared `_RedistributeSectionCounts()` that honors a fully-populated override map exactly.

**Regression caught and fixed via the full test suite (not tracing).** `test_bm_generate_draft_mock_rejects_mismatched_section_keys_instead_of_silently_saving_empty` broke on the first implementation — an admin override with foreign/mismatched section keys was being silently absorbed and defaulted instead of failing loudly (the same bug class BM hit on 2026-08-07). Fix: `_BalanceCompetitionMockSectionCounts()` now returns `None` (not an all-zero map) when the override keys don't match any real section, so `GenerateCompetitionMockDraft()` falls all the way back to the original pre-fix code path and the pre-existing `COMPETITION_MOCK_GENERATION_EMPTY` check fires exactly as before. Full suite re-run after the fix: 286 passed, 2 warnings, 41.28s.

**Frontend — three files.** `frontend/lib/api/admin.ts`: added `isWeighted`/`marksPerQuestion` to `CompetitionMockSectionPlanItem` (+8 lines). `frontend/app/admin/competition/mock-studio/page.tsx` (98 changed lines): `LiveSections` now branches — weighted sections stay pinned to the admin's chosen count, flat sections auto-float to split `100 - PinnedMarks` evenly; "Total Questions" renders as a read-only computed value instead of an editable input whenever a weighted section exists; added a live `TotalMarks`/`MarksMismatch` check gating the Generate button; Section Allocation badge now shows "{count} Questions x {marks}/100 Marks"; each auto-flat section row shows a read-only computed count with a "Weighted, 5 marks/question" / "Auto, 1 mark/question" label. `frontend/app/student/competition/leaderboard/page.tsx` (`MockLeaderboardPage`, 5 lines removed): dropped the Avg Score/Score `<th>` header, the `animatedScore` computation, and its `<td>` — leaderboard now reads Rank / Student / Avg Accuracy / Avg Time only, as requested.

**Verification.** `py_compile` clean after every backend patch pass. Isolated `tsc --noEmit` on the two changed frontend files (filtering expected unresolved-import noise in this sandbox) — zero syntax/JSX errors. Full backend suite: 286/286 pass. Real end-to-end numeric simulation against a freshly-seeded MM-L1 level (`CompetitionMockSectionPlan`/`GenerateCompetitionMockDraft` called directly) confirmed the persisted `total_marks` lands on exactly 100.0 under multiple admin input scenarios (untouched default, and a bumped weighted-section count).

**Status: CONFIRMED MERGED AND DEPLOYED, live and working.** PR #495, commit `d00ec03082bf3fa08c8c41dfe23751d855e61cae`. Confirmed via Render (`mathpath-backend`, deploy `dep-dab66ogu01pc73e69js0`, status `live`, finished 2026-09-01 05:34 UTC) and Vercel (`math-path-platform`, deployment `dpl_E6dsA3ASWMWUaTfirCSxeWKF4ETj`, target production, state READY) -- both serving this exact commit. `MM_MOCK_GENERATOR_RULES.md` also corrected this session (see its own "Last updated" bump) — its old "10 questions per section by default"/"100-question MM mock output" language was stale and contradicted the new behavior. New `DECISIONS.md` entry records this as a reversal of the earlier "mocks don't need to hit 100" assumption.

**PR #494 status recap:** confirmed merged by Shailesh AND now confirmed LIVE -- Shailesh confirmed directly this session: "i have confirmed them live and they work fine".

**Third piece of work, prompted directly by Shailesh catching a stale status report in a brand-new thread ("i started a new chat thread and asked about if they have everything till what we did last, and i got this response which is backdated as fuck"):** that new thread's summary listed PM-L2, both YLM builds, the parent-report email-removal rework, and the Attempt Review card-sizing polish as "prepared, not yet delivered" -- all four had actually been merged to `main` weeks ago (PR #434/#435/#436, PR #448, PR #460+461+462+464+465, PR #480 respectively), confirmed via `git log origin/main --grep`. Root cause: `OPEN_ISSUES.md`'s `## Active` section is hand-maintained and had drifted stale -- items get delivered via a local Claude Code session (or Shailesh directly) without this file ever being told. Same failure class as the 2026-07-13 documentation-sync bug this project already has a fix-and-learn entry for, now confirmed recurring. **Fixed this session:** cross-checked all 35 `Active` bullets against real git history; removed 14 confirmed-already-merged items into a new dated `Resolved Recently` entry in `OPEN_ISSUES.md` citing the real PR numbers; updated two more in place with new evidence (Render's auto-deploy-on-commit confirmed as the live deploy mechanism via the Render API directly -- `mathpath-backend`, `autoDeploy: yes`, `trigger: new_commit` on every recent deploy; and the DPS practice leaderboard, PR #491/#492, flagged as a possible match for Shailesh's referenced "new DPS leaderboard," needing his confirmation rather than assumed resolved). Also updated `docs/project-memory/README.md`'s canonical read order: added a mandatory step 9/10 requiring every future session to cross-verify any "not yet delivered"/`Active` claim against `git log` before reporting it as current status, and to fix any stale entries found in the same session rather than just privately correcting its own understanding. See `OPEN_ISSUES.md`'s matching 2026-09-01 "stale Active-section cleanup" entry for the full list.

## 2026-08-25 update (three teacher-facing bugs fixed: lesson badge denominator, Attempt Review card wrapping + sizing, DPS timer rule across all 7 seed scripts)

**tl;dr:** Shailesh sent two screenshots — a teacher "Assign Practice" view showing students assigned all 5 sheets of a lesson displaying as "0/2 Cleared" instead of "0/5," and a teacher "Attempt Review" page where the top stat-card row (Score/Accuracy/Benchmark/Questions) had its labels wrapping into vertical letter-stacks ("SCO/RE", "ACC/URA/CY") instead of clean horizontal text — plus a new platform-wide rule for DPS practice-sheet timers. Required a full scan-and-confirm pass before any code ("scan them properly for how they behave right now and then once i give the go ahead only then proceed"), then explicit go-ahead. A later screenshot after deploy caught a follow-up sizing inconsistency on the same Attempt Review cards, fixed in a second small round.

**Bug 1 — lesson badge denominator, `backend/app/services/lesson_progress_service.py`.** `ComputeLessonProgressForStudents`'s `dps_rows` query filtered to `DPS.publication_status == "PUBLISHED"` only, so a lesson with 5 real DPS rows but only 2 published showed "0/2" instead of "0/5." I initially proposed via a clarifying question that concept-drill DPS slots might not count toward the denominator — **Shailesh corrected this sharply and correctly: "who said that concept drill or any that kind of sheet does not get assigned????? that is a different type of sheet but at the end of the day is a dps sheet which is part of those 5 sheets schedule. do not make your own assumptions, pls follow the system properly."** Re-checked `seed_preparatory_module_l2.py` and confirmed a concept-drill slot is inserted via `_ensure_dps()` exactly like any other DPS row, same table, same model — no special-casing needed. Fix: the denominator now counts every DPS row for the lesson (`is_active == True`, any `publication_status`), while `is_assignable_now()` still correctly requires `PUBLISHED` for whether a teacher can actually assign it (new `published_dps_ids` set). Same publication-status-filter bug existed in `IsLessonFullyClearedForStudent` (the "lesson cleared" notification trigger) — fixed identically so the notification can't fire early on a lesson with one still-unpublished sheet.

**Bug 2 — Attempt Review stat-card labels, two byte-identical duplicate route files (`frontend/app/teacher/results/[attemptId]/page.tsx` and `frontend/app/teacher/result/[attemptId]/page.tsx` — confirmed both live/linked, the singular route reached from `assignment-tracker/student/[studentCode]/page.tsx`).** Root cause: `SummaryMetric`'s label `<p>` was nested *inside* the same fixed 44×44px `math-teacher-icon-chip` icon box as the icon itself, instead of as a separate sibling paragraph — the correct pattern already used everywhere else on the platform (`InfoCard` in the same files, `ResultSummary`'s `Metric`, `teacher/students/page.tsx`'s `Metric`). Fixed by pulling the label out as its own paragraph below the icon chip. **Round 2, from a follow-up screenshot after this shipped:** the now-unwrapped top-row cards were still visibly smaller than the InfoCard row directly below (less padding, a bare 16px icon with no colored background vs. InfoCard's 20px icon in a colored chip, a 10px label vs. InfoCard's 12px). Resized `SummaryMetric` to match `InfoCard`'s proportions exactly: `p-4`→`p-5`, icon 16→20px in a new `bg-rose-50 text-rose-700` chip (rose to match this card's existing rose border theme, InfoCard below uses blue), label `text-[10px]`→`text-xs`, grid gap `gap-3`→`gap-4`.

**Bug 3 — DPS practice-sheet timer rule, all 7 content seed scripts.** Shailesh's rule, given directly: "if the sheets have 10 total sums then the timer should always be set at 5 mins and if the sheets have sums greater than 10 then the timer should always be set at 10 mins... if the total sums in a sheet is 10 then total time limit is 5 mins and if it is greater than 10 then the total time should always be 10 mins not less not more only 10." Clarified sub-10-question sheets get 5 minutes too ("sub 10 sheets should also get 5 mins only") — a flat two-tier rule, no third tier. **None of the 7 seed scripts matched this rule before the fix** — some used a 600s/1200s (10min/20min) tiered scheme, others (`seed_intermediate_module.py`, `seed_ylm_phase1.py`, `seed_preparatory_module.py`) used a flat 600s (10min) constant regardless of question count; `seed_intermediate_module.py` even had a stale docstring claiming "use a 5-minute time limit" that never matched its own flat-600s code, fixed as part of the same pass. Every script now uses `300 if question_count <= 10 else 600` (via a small `_dps_duration_seconds()` helper added to the two flat-constant files, `seed_intermediate_module.py`/`seed_master_module.py`, and adjusted directly in the others). **Important platform fact confirmed during this fix, worth remembering:** all 7 seed `seed(db)` functions run unconditionally, on every backend boot (`backend/app/main.py`'s `on_startup()`), and are idempotent upserts — so a plain backend restart after this deploy re-runs every seed script and backfills every existing DPS row's `default_duration_seconds` to the corrected value with zero extra manual step. **Do not run `backend/scripts/fix_dps_duration_seconds.py`** — it's a leftover 2026-07-22 one-time script that bumps any DPS row still at exactly 300s *up* to 600s (the origin of today's bug, a prior blanket bump to 10 minutes); running it now would wrongly revert the just-corrected ≤10-question sheets back to 600s.

**CRLF drift, once again — every file this session touched had drifted to CRLF on the OneDrive-synced working tree by the time it was staged from Shailesh's machine**, including files this same session had written as LF minutes earlier (`lesson_progress_service.py`: 244/244 lines). All 11 touched files (`lesson_progress_service.py`, both duplicate `page.tsx` route files, all 7 seed scripts) were verified via `git diff -w --stat` to show only real content changes before normalizing to LF and delivering. Same recurring root cause already documented elsewhere in this file and in `CLAUDE_CODE_STATUS.md` — always check `git diff -w --stat` against `git diff --stat` before committing in this repo, never `git add -A`.

**Delivery status:**
- **Round 1 (lesson badge + card-wrap + DPS timer), branch `fix/lesson-badge-review-card-dps-timer`: CONFIRMED MERGED AND DEPLOYED.** Shailesh pasted the Render backend deploy log post-merge showing the Master Module and Intermediate Module seed syncs completing with `duration_rule=300s(<=10q)/600s(>10q)` in the summary line — proof the corrected code is live and the auto-backfill-on-restart behavior described above actually happened. Frontend deploy also confirmed live via Shailesh's own follow-up screenshot: Attempt Review's top-row labels render as clean horizontal text, no more letter-stacking.
- **Round 2 (card-sizing polish), branch `fix/attempt-review-card-sizing`: commands handed to Shailesh (branch/commit/push/PR/merge/deploy), execution NOT yet confirmed back in this thread.** Check `gh pr view` for this branch name (or search for a PR titled "Match Attempt Review top-row card sizing to the rest of the platform") before starting new work — merge and deploy if still open.

**Open question, worth resolving before trusting `DEPLOY_PLAYBOOK.md`'s manual SSH steps at face value again: the Render deploy log Shailesh pasted (`https://mathpath-backend.onrender.com`, Render's own build/deploy pipeline, apparently auto-triggered straight off the `main` merge) is a DIFFERENT deploy mechanism than the manual `ssh ubuntu@15.206.108.37` + `systemctl restart mathpath-backend` + `pm2 restart mathpath-frontend` flow documented in `DEPLOY_PLAYBOOK.md` and handed to Shailesh throughout this session per his standing instruction to always provide those commands.** Both could be real (a Render-hosted backend auto-deploying on merge, separate from or in addition to the SSH-reachable EC2-style box), or `DEPLOY_PLAYBOOK.md` could be stale/describing a legacy or parallel environment. This wasn't resolved in this thread — the round-1 backend fix is confirmed live via the Render log alone, so at minimum Render auto-deploy is real and working; whether the SSH steps were also run, or are even still necessary, is unconfirmed. **Ask Shailesh directly, first thing in the next thread, before handing over more SSH deploy commands as though they're the only/authoritative path.**

**Pending work flagged at the close of this thread, NOT documented anywhere in this repo's project-memory — needs Shailesh to restate it fresh next time, do not guess at scope or details:** Shailesh asked this thread to document "the pending revamping work of the leaderboard and the new dps leaderboard that we had to add including the 12 distinct music tracks that i had provided you." A full grep of every file under `docs/project-memory/` for "leaderboard," "music," "track," "DPS leaderboard" turned up plenty of *past* leaderboard bug-fix history (accuracy-mismatch chase, mock leaderboard 0/0% animation bug, DPS badge exclusion from leaderboard `topBadges` — all resolved, all documented above/elsewhere in this file and in `OPEN_ISSUES.md`) but **zero mention anywhere of a planned leaderboard revamp, a separate "new DPS leaderboard" feature, or any music-track asset delivery.** No audio files matching this were found staged in this session's workspace either. This is very likely context from earlier in the same chat thread that got compacted out of this session's carried-forward summary, or from a separate thread entirely — either way it needs to come from Shailesh directly at the start of the next thread: what the leaderboard revamp scope is, what makes the "new DPS leaderboard" distinct from the existing mock-exam/cumulative leaderboards already in `routes_student.py`, and what the 12 music tracks are for (leaderboard ambience? a new badge-unlock-style cutscene, given the existing precedent in `badge-unlock/*.mp3`? something else) and where those 12 files currently live.

Prior "Last updated" (2026-08-05, still later same day (Cowork — PM-L2 built end to end from scratch: Shailesh uploaded the Level 2 workbook + 60 DPS images across 12 lessons and required a full audit-and-confirm pass before any code, then gave explicit go-ahead: "well now go ahead and implement everything across the 3 flows and then check and verify in detail using a deep scan locally only then we will push and merge." New isolated `question_engine/pm_l2/` engine, new Concept Drill question format (3 sub-types, none of them fitting the existing vertical-stack model), new 5-mark/1-mark weighting extended from IM-only into a PM-generic path, new 3-section mock/assessment registry, one real bug caught by testing and fixed. See the entry directly below for the complete writeup. Note: several dated entries between 2026-07-30 and 2026-08-03e exist only in `OPEN_ISSUES.md`/`CLAUDE_CODE_STATUS.md`, not duplicated in full here — check those two files for anything from that window (DPS MCQ-to-free-text conversion, DPS badge system, login-latency fix, and more).)

## 2026-08-05 update, still later same day (PM-L2 built end to end — dedicated engine, new Concept Drill format, section-wise mocks/assessments, 5-mark weighting, exhaustive local verification): PREPARED, NOT YET DELIVERED

**tl;dr:** Immediately after the PM-L1 exhaustive-verification work below shipped-in-principle, Shailesh moved to PM Level 2 as planned. He uploaded `Level 2.xlsx` plus 60 DPS reference images (12 lessons) and set an explicit governing instruction before any code: "go through everything in detail including the concepts, patterns, difficulty progression as we progress through the lessons and everything else in detail as in the workbook and the images. then come back to me with all the findings and i need to make sure that you understand everything in detail before we implement all the 3 flows." Several rounds of audit-and-confirm followed — each answer checked against my own re-derivation, not taken at face value — before he gave the explicit go-ahead quoted in the header above.

**What PM-L2 actually is, structurally.** 12 lessons, same DIRECT/COMPLEMENT-OF-5/COMPLEMENT-OF-10/MIXED-REVISION bead-movement technique family as PM-L1, but introducing a genuinely new question format not present in PM-L1 at all: "Concept Drill" sums, found in specific DPS5 (or DPS6 for one lesson) blocks. Real structural asymmetry across lessons, confirmed against the workbook and deliberately preserved rather than normalized: Lesson 8 has 6 physical DPS blocks (not 5) because of a duplicate "-4" title in the source sheet; Lesson 9 has zero concept-drill content; Lessons 2/3/10/11/12 have concept-drill-ONLY DPS5 (no ordinary vertical block there); Lessons 1/4/5/6/7/8 have both a normal DPS5 block and a separate concept-drill section; Lesson 11 DPS3 escalates mid-sheet from 2-digit (Q1-10) to 3-digit (Q11-20); Lesson 12 DPS4 is mislabelled "3 DIGIT" in the workbook title but its actual data is 2-digit — confirmed a genuine source typo, corrected in the seed, not treated as a new requirement.

**Concept Drill has 3 distinct sub-formats, each independently confirmed against literal workbook ground truth, with two real corrections along the way.** MULTIPLY (`ADD × TIMES`, e.g. 5×12=60) — simple, confirmed on the first pass. DIVIDE (`FROM` / `LESS`) — my first assumption (plain subtraction) was wrong; Shailesh corrected directly: "the students have to keep lessing unless they cannot less anymore without going below 0," i.e. repeated subtraction to the point just before going negative, which is mathematically `FROM mod LESS`. He then added a hard constraint: never generate a DIVIDE pair whose answer is 0, because a zero remainder is guessable without doing the exercise ("the students are smart enough to make out that the answer is gonna be zero") — enforced via `is_guessable_divide_pair()` rejecting any `FROM % LESS == 0` pair, checked up to 200 regeneration attempts. RANGE_SUM (arithmetic-sequence sums, e.g. "sum 2 to 40 in steps of 2") — Shailesh supplied the literal Excel formula, `=SUMPRODUCT(ROW(INDIRECT("1:20"))*2)`, which let me derive the general closed form `step × n(n+1)/2` (with named-sequence overrides for CONSECUTIVE/ODD-labelled sheets) and verify it reproduces all 4 literal workbook answers exactly (210, 420, 465, 225) before it went anywhere near the generator.

**Marks weighting: Concept Drill is 5 marks in DPS and assessments, 1 mark in mocks, and assessment totals must still land on exactly 100.** This mirrors IM's existing Skill Stacker/Concept Drill pattern, per Shailesh's instruction: "this needs to be handled as being handled for other levels right now." The pre-existing weighting machinery in `assessment_engine_service.py` and `assessment_blueprint_service.py` was IM-only (`_CONCEPT_WEIGHTED_MODULES = {"IM"}`). Rather than adding PM to IM's own allow-list (coupling), added a parallel PM-specific `PmQuestionMark()` / `_PM_CONCEPT_WEIGHTED_FAMILIES = {"CONCEPT_DRILL"}` and widened the two gating checks to also treat `module_upper == "PM"` as weighted-eligible. Verified this is an exact, not approximate, no-op for PM-L1 (which has zero CONCEPT_DRILL-tagged sections, so the weighted path mathematically degrades to the old flat-1-mark behavior) via a dedicated regression test plus the full existing PM-L1 suite passing unchanged.

**Dedicated, isolated engine — matching the codebase's own established per-level-engine convention, not importing PM-L1's.** New `backend/app/question_engine/pm_l2/` package (`config.py`, `validators.py`, `operands.py`, `distractors.py`, `generator.py`, `concept_drill.py`) — copied and adapted from PM-L1's engine (same bead-movement classification, same difficulty-staging/near-duplicate-avoidance logic), never imports from `question_engine/pm/`, per Shailesh's explicit requirement: "so that each level works independently and never overlaps or breaks any other level." New seed (`seed/preparatory_module_l2_config.py` + `seed/seed_preparatory_module_l2.py`), wired additively into `main.py`'s `on_startup()` next to the existing PM-L1 seed call.

**VISUAL/ABACUS practice-mode tagging — a new PM-L2-specific dimension, baked into `dps_title` text rather than a new DB column, matching how Learning Path Studio actually renders it.** Confirmed `frontend/app/admin/curriculum/page.tsx` renders `DPS {dps.dpsNumber}: {dps.dpsTitle}` raw with no extra formatting, so a server-side "(Abacus)"/"(Visual)" suffix baked into the seeded title requires zero frontend changes. Untagged workbook sheets default to Abacus per Shailesh: "for the sheets not having any tags means abacus needs to be used and the sheets having visual tags means that visual method needs to be used. this should also be applied to the dps names in the learning path studio."

**Assessment/mock sections are concept-driven, not literal workbook replicas — an explicit, deliberate split from how DPS works.** Shailesh drew the line directly: "for the assessment and mock workflows the sections need to follow the underlying concepts... there we do not need to follow the exact structure of the workbook but the concepts... for dps sheets it is necessary to follow the workbook structure accordingly due to the progression of difficulty and different concepts." Final 3-section structure (his own spec, superseding an earlier 4-section proposal of mine): Section 1 - Add/Less (Abacus), Section 2 - Add/Less (Visual), Section 3 - Concept Drill (5 marks/question) — mirroring how IM isolates Skill Stacker/Concept Drill into its own section. New `PM_L2_COMPETITION_SECTION_DEFINITIONS` + `PM-L2` entry in `PM_COMPETITION_LEVEL_REGISTRY` (`pm_competition_mock_generation_service.py`), with Abacus/Visual pools derived programmatically from the seed's own lesson table (`_pm_l2_addless_pools()`) rather than hand-duplicated, to prevent drift — confirmed genuinely asymmetric (14 Abacus concepts vs 23 Visual concepts, a real partition of the workbook's own tagging, not a mirrored pair). A new, separate collector function (`CollectPmL2CompetitionSectionLockedQuestions`) — PM-L1's own collector was never touched.

**One real bug, caught by testing rather than assumed away — directly validating why Shailesh required a deep local verification pass before push.** Concept-drill mock generation was rejecting nearly every valid question as a false duplicate (1 of 7 required MULTIPLY questions accepted after 28 attempts). Root cause: the dedup signature helper checked `"operands" in question` to route vertical-stack vs. concept-drill questions, but every concept-drill question dict also carries an empty `operands: []` for downstream compatibility — so it always fell into the wrong branch and returned the same constant signature for every concept-drill question regardless of actual content. Fixed by checking `question.get("drill_operands")` first. Re-ran mock generation after the fix: 64/64 questions generated correctly across all 3 sections, 0 errors.

**Verified locally, exhaustively, before any push — per Shailesh's explicit closing instruction for this task.** Custom sweep script exercising the real generation path for every DPS section across all 12 lessons: 61 DPS, 658 questions, 0 arithmetic/structural errors. Live end-to-end assessment generation: 84 questions (80 normal x 1 mark + 4 Concept Drill x 5 marks) totals exactly 100.0 marks. Live end-to-end mock generation: 64 questions across all 3 sections, all answers verified correct. Fresh-deploy simulation (full migration + seed sequence, run twice) confirmed idempotent — no duplicate rows, no errors on re-seed. Full backend suite: 173/173 pass (162 pre-existing + 10 new PM-L2 tests + 1 pre-existing test updated for an intentional, correct error-code change: `ASSESSMENT_QUESTION_COUNT_MUST_BE_100` -> `ASSESSMENT_MARKS_MISMATCH`, since PM now goes through the same weighted-marks validation path as IM). PM-L1 explicitly re-verified unaffected throughout, not just assumed safe by code separation.

**Known gap, flagged honestly rather than glossed over:** the 3 new Concept Drill question types have never been visually checked rendering live in a browser — this Cowork sandbox has no browser/live-server access this session. The backend returns complete `question_text` + MCQ options for each drill type, which should render through whatever generic text+MCQ path already handles IM's own non-arithmetic question types, but this needs a live look before it reaches students.

**Update, same day: live-verified via Claude in Chrome against Shailesh's actual running dev server — found and fixed 3 more real bugs the backend-only sweep above could not have caught.** Shailesh asked directly to use the Chrome extension against his running local system. Clicking "Generate Preview" on Lesson 3 DPS5 (a concept-drill-only DPS) 500'd immediately.

1. **Root cause: the DPS generation dispatcher (`generation_service.py`) was never actually wired to PM-L2 at all.** `generate_preview()` and `persist_question_set()` -- the functions behind both the admin preview button AND real student DPS attempts -- routed every `ModuleCode == "PM"` DPS through PM-L1's `build_pm_config_from_dps`/`generate_pm_question_set` regardless of `level_code`, with zero PM-L2 branch. This is a bigger deal than a preview-button bug: it would have broken every PM-L2 DPS for real students, not just the admin preview. Every earlier "exhaustive" PM-L2 test in this session called the `question_engine/pm_l2` functions directly, bypassing this dispatcher entirely -- which is exactly why it went uncaught until a live click. Fixed via a new `generate_pm_l2_preview()` in `generation_service.py`, dispatched whenever `level_code == "PM-L2"`. This also had to solve a problem no other module in the codebase has: a few PM-L2 DPS (e.g. Lesson 1 DPS5) carry TWO `DPSSection` rows under one DPS (a normal block + a separate concept-drill block) -- every other `build_*_config_from_dps()` only ever reads the first section, so this needed new PM-L2-scoped combining logic that concatenates both sections' generated questions with continuous numbering, tagging each question's `metadata.section_number` for correct DB linkage.
2. **DPS title double-tagging.** A large share of PM-L2's literal workbook titles already say "Abacus"/"Visual" themselves (e.g. "3 Digit Addition & Subtraction - Visual"); the "(Abacus)"/"(Visual)" suffix logic appended the tag unconditionally on top, producing live UI titles like "2 Digit Addition & Subtraction - Visual (Visual)". Fixed by skipping the append whenever the resolved mode word already appears anywhere in the title (case-insensitive substring check) -- verified against all 61 DPS, only the one genuine double-mention in Lesson 11 DPS3's own two-clause title remains (correct, not a bug).
3. **Frontend had no rendering for any of the 3 new Concept Drill display types.** `MathQuestionDisplay.tsx`'s mode switch didn't recognize `CONCEPT_DRILL_MULTIPLY`/`_DIVIDE`/`_RANGE_SUM`, so every concept-drill question silently fell through to the default vertical-stack renderer with empty operands -- a blank box with a floating "?". This was the exact gap flagged as unverified in the entry above, and live verification confirmed it was a real bug, not just an unknown. Added a new `ConceptDrillPromptQuestion` component that renders the question's own natural-language `questionText` as-is (no expression-building fallback, no auto-appended "= ?" since RANGE_SUM's sentence-style prompts don't end in a question mark).

All three re-verified live end to end after fixing: Lesson 3 DPS5 now returns 200 with 2 correctly-rendered questions (34 x 12 = 408; the DIVIDE prompt renders full sentence and reveals the correct 108 mod 13 = 4 answer via Show Answers); Lesson 1 DPS5 (the two-section case) returns 12 questions split correctly into "Section 1: Abacus Add/Less Sums (10 questions)" and "Section 2: Concept Drill (Abacus) (2 questions)", with the two RANGE_SUM questions (210, 420) rendering their full prompts. Added 4 new regression tests exercising the real `generate_preview`/`persist_question_set` dispatch path directly (not just the engine functions) plus a title-double-tagging guard, specifically so this class of bug can't silently reappear. Full suite: **176/176 pass** (172 + 4 new).

**Not yet delivered** — this Cowork sandbox has no GitHub push access; needs a local Claude Code session to review the diff, run qa-reviewer, then sre-devops delivers. Per Shailesh's own words this session, push/merge only happens after local review: "check and verify in detail using a deep scan locally only then we will push and merge." The Concept Drill rendering gap that was the one flagged "known gap" at the end of the entry above is now closed and live-verified, not just fixed in theory.

## 2026-08-05 update, later same day (exhaustive 3-workflow PM-L1 verification -> found + fixed an assessment-generation schema-drift bug): PREPARED, NOT YET DELIVERED

**tl;dr:** After the digit-width fix + generator redesign below, Shailesh asked directly: "so we are sure that everything is pristine for this level and works exactly like the workbook right?" Gave an honest, calibrated answer — automated tests + 4 live spot-checks had been done, but not all 75 DPS individually, and assessment blueprints had never been live-tested at all. His response was explicit and is the governing instruction for this entire session: **"yes i need to be sure that it works fine and accurately across all the 3 workflows. i do not want to push anything onto production which would later again require fixing."**

**Exhaustive backend sweep #1 — every DPS sheet, not a sample.** Built `verify_pm_l1_full.py`: runs the real `generate_preview()` code path (the exact function the admin "Generate Preview" button calls) for all 75 DPS sheets (lessons 1-15 x DPS 1-5) x 12 fresh-seed draws each = 9000 questions, validating per question: count matches, `sum(operands) == correct_answer`, `movement_profile()` reports valid, digit-width matches `digit_pattern` (with the documented COMP10_SUB narrowest-tier exception), DIRECT lessons never leak a complement movement, non-DIRECT lessons' taught technique actually fires on an operand within `target_numbers`, REVISION sheets exercise every taught technique across the sample — plus cross-checking every DPS's title and base-row digit-width directly against the Excel ground-truth JSON. **Result: 75/75 DPS checked, 9000/9000 questions valid, 0 failures.** 81 warnings, all triaged as non-bugs: cosmetic title-spacing differences against the Excel's own inconsistent spacing, one likely pre-existing Excel typo (Lesson 10 DPS3 title says "ADD 7" but should say "ADD 6" per DPS4's own title for the same target), and 8 digit-width findings isolated to lessons 12-15 (targets 6-9) where the real Excel occasionally pads a nominally-1D sheet with 2-digit "escalation" values because only 6 valid single-digit trigger bases exist for those targets — verified genuine (not an extraction bug) via direct `openpyxl` read of the raw `.xlsx`, and judged a legitimate design difference (this session's own Mode A/B redesign already solves the same narrow-pool problem, just differently) rather than something requiring a fix.

**Exhaustive backend sweep #2 — every mock/assessment concept-pool entry.** Built `verify_pm_mock_assessment.py`: (1) generates and validates every one of the 41 `PM_COMPETITION_SECTION_CONCEPT_POOLS` entries directly across all 3 sections (2050+ questions); (2) runs the real `CollectPmCompetitionSectionLockedQuestions()` end-to-end 4 times at the real 60-question default, validating total count, per-section split, no in-paper duplicate operand signatures, and full per-question arithmetic/movement correctness; (3) a field-by-field parity check confirming `PmSectionRegistryConfig` (the assessment code path) and `_build_pm_config` (the mock code path) produce byte-identical `PMConfig` objects for the same concept-pool entry, across all 41 entries x 7 fields — proving the two callers can never silently diverge. **Result: 0 failures, 0 warnings across everything.**

**Live re-verification surfaced a real bug that neither script-level sweep could have caught — the value of testing beyond automation.** Clicking "Generate Preview" on a real PM-L1 assessment blueprint in the admin UI reliably 500'd. Retried once (ruling out a transient dev-server hot-reload hiccup) — reproduced identically. Created an entirely new blueprint from scratch to rule out corrupted state on the first one — reproduced identically there too. This meant the bug was systemic to the assessment-generation orchestration layer, not the PM math engine itself (already proven correct 11000+ times over by the two sweeps above).

**Root-caused via the real Python traceback, asked Shailesh directly for it since this sandbox can't see his terminal.** A clean isolated repro script (fresh in-memory SQLite DB, exact same blueprint config) succeeded every time — meaning the bug was specific to the real deployment's actual database state, not the generation logic. Asked Shailesh to paste the traceback from his running backend's terminal; he did. It showed: `sqlite3.IntegrityError: NOT NULL constraint failed: assessment_questions.lesson_id`, raised inside `GenerateAssessmentVersion()`'s `Db.flush()` when inserting a question with `lesson_id=None` (correct for section-wise assessments — a PM/IM/MM assessment question spans multiple lessons' worth of concepts, so it has no single owning `Lesson` row).

**Root cause: real schema drift between the codebase's models and this deployment's actual database, invisible until exercised.** Alembic revision `e5a2c9f14b7d` ("Add assessment_blueprint_sections, make assessment_questions.lesson_id nullable," dated 2026-07-22 — written specifically for exactly this column, for IM/MM's section-wise assessments) exists in `alembic/versions/` and is the correct fix, but was never actually applied to this deployment's real `mathpath.db`. Confirmed directly: the DB's stamped `alembic_version` (`abe776eff8ee`) doesn't match any revision in the current `alembic/versions/` chain at all — an orphaned pointer, not just "behind." `assessment_blueprint_sections` (a brand-new table that same migration creates) existed fine, because `Base.metadata.create_all()` runs unconditionally at every startup and creates any *missing* table — but `assessment_questions` already existed, so `create_all()` silently skipped it, leaving `lesson_id` at its original NOT NULL forever. This explains why blueprint *creation* (which only touches `assessment_blueprint_sections`) worked fine while preview *generation* (which inserts into `assessment_questions`) didn't — and why this had never been caught before: this is apparently the first time PM's (or plausibly IM's/MM's, sharing the identical code path) section-wise assessment generation was ever actually exercised live in this dev deployment.

**Fix, matching this codebase's own established pattern for exactly this class of problem.** This repo already has ~20 `ensure_*` runtime-migration functions in `backend/app/services/schema_migration.py`, run unconditionally from `main.py`'s `on_startup()` — the codebase's real, actually-relied-on migration mechanism for this deployment, not `alembic upgrade head` (which nothing in this session's tracing found being run anywhere in the normal dev workflow). Added `ensure_assessment_questions_lesson_id_nullable()` following that exact pattern: checks `inspect(engine).get_columns(...)`'s `nullable` flag first and no-ops if already correct (idempotent, safe on every future startup like its siblings); SQLite path does a create-copy-swap table rebuild (SQLite has no `ALTER COLUMN` at all) preserving every column, the unique constraint, both indexes, and both foreign keys exactly as they were; Postgres path is a one-line `ALTER COLUMN ... DROP NOT NULL`. Wired into `main.py` next to `ensure_assessment_reattempt_columns()`.

**Applied itself automatically and was verified live, twice, before being called done.** Saving the two files triggered uvicorn's dev-mode `--reload`, which restarted the process and ran `on_startup()` — confirmed via direct SQLite inspection that the live `mathpath.db`'s `assessment_questions` table was rebuilt correctly (all indexes/constraints/FKs intact, table was empty so zero data-loss risk). Then re-verified live via Claude in Chrome: the original stuck blueprint ("PM-L1 Full Verification Assessment") now generates 100/100 questions successfully; a freshly-created blueprint ("PM-L1 Repro Test 2," made specifically to prove the bug wasn't isolated to one row) also generates 100/100; both blueprints' Question Preview and Coverage Check tabs show real, arithmetically-correct, varied questions across DIRECT_ADD_LESS and COMPLEMENT_OF_5 concept families (spot-checked by hand, e.g. `7+1-3=5`, `100+102+1=203`, `22+2+1=25`, `6-5+4=5` — all correct). Full backend suite re-run after the fix: **163/163 pass**, zero regressions from the schema change.

**Net result: all 3 PM-L1 workflows (DPS, competition mocks, assessments) are now genuinely exhaustively verified, not just assumed correct by extension of "the code looks shared."** This is a materially stronger evidence base than before this session — worth noting given how it was found: automated testing alone (11000+ questions, 0 failures) would have given false confidence that everything was pristine, when a real, systemic, production-blocking bug was sitting one click away in the admin UI the whole time. Live re-verification against the actual running application, not just against the generation functions in isolation, is what caught it.

**Not yet delivered** — this Cowork sandbox has no GitHub push access; needs a local Claude Code session to review the diff (now two independent fixes from the same day: the digit-width/generator-redesign work below, plus this schema-drift fix), run qa-reviewer, and deliver. **Worth a quick follow-up, not yet done:** since IM/MM share the exact same section-wise assessment code path and the same migration history, they were plausibly hitting this same silent bug in this dev deployment too — not confirmed either way, but a fast thing to check before assuming those are clean.

**Leftover test artifacts from this session, left for Shailesh to delete manually** (permanent deletion isn't something Claude does unilaterally): draft assessment blueprints "PM-L1 Full Verification Assessment" and "PM-L1 Repro Test 2" (both 100 questions, PM-L1, DRAFT, never assigned to any student) — see `OPEN_ISSUES.md` for the full running list including the earlier "PM-L1 Verification Mock."

## 2026-08-05 update (PM-L1 audit -> digit-width bug fix -> full generator redesign -> live verification via Claude in Chrome): PREPARED, NOT YET DELIVERED

**tl;dr:** Shailesh asked for a full concept-by-concept audit of the PM-L1 curriculum (built the prior session) against the real source material — "check it end to end whether each and every concept is getting followed right to the minutest of the details" — for DPS sheets, assessments, and mock workflows, before proceeding to PM Level 2. He also asked for a standing fix: anything he uploads (files, images) needs to persist across chat threads so he never has to re-upload. That system is now `docs/reference-materials/PM-L1/` in the connected workspace folder (gitignored, not version-controlled) — holds the real `Bridge Level.xlsx` (source of truth, PM-L1's first 15 lessons are an exact replica of Bridge Module's first 15), a structured JSON extraction of all 75 DPS titles/operand rows/answers, and all 75 DPS reference images.

**Bug found via live generator execution (not just code reading), across 5 lessons, before any fix was made:** `digit_pattern` ("1D" vs "2D" vs "1D_AND_2D") was silently ignored by all 4 complement base-builder functions in `backend/app/question_engine/pm/operands.py` (`_comp5_add_bases`, `_comp5_sub_bases`, `_comp10_add_bases`, `_comp10_sub_bases`). Per the Excel, DPS1/DPS3 are meant to be single-digit-only and DPS2/DPS4 double-digit-only within the same lesson — but both were drawing from the identical candidate pool. Fixed via a new `_comp_tens_values(all_tens_values, digit_pattern)` helper keyed off the *narrowest available tens tier* rather than a hardcoded `tens == 0` check, specifically because `COMP10_SUB`'s tens values never include 0 (subtracting via complement of 10 structurally requires a tens place to borrow from — there's no true single-digit base for that technique, so "1D" for it means "narrowest available," 10-19). Also fixed alongside: `PM_L1_LESSONS` (`backend/app/seed/preparatory_module_l1_config.py`) digit_pattern values corrected across lessons 3-15 to match the Excel exactly (several DPS2/DPS4/DPS5 entries had been "1D" when the Excel shows 2-digit content); `pm_competition_mock_generation_service.py`'s 5 complement concept-pool entries widened from "1D" back to "1D_AND_2D" so the DPS-side fix didn't accidentally narrow mock/assessment variety; a stray "Bead Recognition &" prefix on Lesson 4 DPS5's title (not present in the source Excel) removed; a stale docstring claiming PM falls back to the YLM generator (it doesn't — PM has its own fully independent `question_engine/pm/` package) corrected.

**Fixing the bug correctly surfaced a real tradeoff, handled via explicit user consultation, not a silent fix.** Restricting bases to true single-digit values left some 1D DPS with only 4-5 mathematically possible unique question shapes for 10 slots (confirmed by computing exact pool sizes, not guessing) — a legitimate consequence of correctness, caught by a pre-existing test (`test_every_pm_l1_dps_generates_cleanly_through_the_real_admin_preview_call`) that started failing on "only 4/10 unique question shapes." Rather than silently picking a fix, asked Shailesh directly (AskUserQuestion: ship as-is vs. redesign); he asked for the risk tradeoff explained in prose first, then chose the full redesign.

**Generator redesign, reverse-engineered from real Excel column traces (e.g. column A1: `2 -> (+2, DIRECT) -> 4 -> (+1, COMP5_ADD)`; column A3: `4 -> (+1, COMP5_ADD) -> 5 -> (+2, DIRECT)`):** `operands.py` restructured around two candidate-generation modes per complement question — Mode A ("trigger-first-then-support": the complement technique fires on the very first step, e.g. base=4, +1 triggers COMP5_ADD immediately, then a plain direct step follows) and Mode B ("setup-move-first-then-trigger": a direct step moves the running value into position first, then the complement fires, e.g. base=3, +1 direct to reach 4, then +1 triggers COMP5_ADD). New `_setup_reaching()` finds every (predecessor, delta) pair reaching a target trigger value via one legal DIRECT move, searching both add/sub tables regardless of the lesson's own operation focus; new `_complement_operand_triples()` generates both modes' candidates for a template; new `COMPLEMENT_BASE_BUILDERS` dict maps each complement template to its base-builder, valid targets, and sign. Reuses the existing trusted `DIRECT_ADD_ALLOWED`/`DIRECT_SUB_ALLOWED` lookup tables and `validate_question`/`movement_profile` validators from `validators.py` unchanged — new candidate generation, not new bead-movement rules — to minimize new-bug risk. `_candidate_pool_for_templates()` restructured to route complement templates through the new triple-generator while leaving plain-DIRECT and wide-DIRECT paths untouched.

**Verified twice — automated tests, then live on the actual running platform:** `backend/tests/test_pm_complement_digit_width_segregation.py` (new/extended) asserts 1D DPS never produce a wide base (except the COMP10_SUB narrowest-tier exception), 2D DPS never produce a single-digit base, narrow/wide DPS pairs within a lesson never share a base pool, mock/assessment pools still span both widths, and trigger position genuinely varies across a sheet (built via a new `_trigger_step()` helper on `movement_profile`). Full existing backend suite passes alongside the new tests. **Live-verified via Claude in Chrome** against the real running platform (`127.0.0.1:3000` — not `localhost`, which failed; admin login performed by Shailesh himself, password never typed by Claude, per standing safety practice): Lesson 3 DPS1 (1D, addition, complement-of-5) showed single-digit bases (3, 1, 4...) with both trigger-first and setup-then-trigger questions genuinely present in the same 10-question set; Lesson 3 DPS2 (2D, same lesson) showed double-digit bases (41, 21...) confirming the width split is real; Lesson 4 DPS1 (1D, subtraction, complement-of-5) confirmed the same pattern holds for subtraction; Lesson 12 DPS1 (1D, addition, complement-of-10, generated via the separate `_paired_complement10_lesson` config factory — a different code path than lessons 3-10) confirmed correct there too; a real 60-question PM-L1 competition mock generated via Competition Mock Studio (Section 1 Addition/Section 2 Subtraction/Section 3 Add-Less, 20 questions each) computed every answer correctly end to end. Also incidentally confirmed live: the Lesson 4 DPS5 title-prefix fix rendered correctly in the admin UI.

**Not yet delivered** — this Cowork sandbox has no GitHub push access; needs a local Claude Code session to review the diff, run qa-reviewer, and deliver. One draft artifact left behind from live verification: a competition mock titled "PM-L1 Verification Mock" in the local dev admin (Competition -> Manage Mocks) — never assigned to any student, but its delete confirmation is a hard "cannot be undone" dialog that Claude doesn't execute unilaterally even for its own test data; left for Shailesh to remove if he wants it gone. **Once this ships, Shailesh's stated next step is PM Level 2.**

## 2026-07-30 update (gamification Phase 3, Level Mastery: backend rebuild -> production incident -> rollback -> simpler redesign -> shipped. Full session wrap for continuity into a new thread.)

**tl;dr:** This session picked up Phase 3 ("Level Mastery," 39 badges = 13 curriculum levels x 3 tiers) verification, discovered the backend half had never actually been committed despite being reported "verified" in an earlier session, rebuilt it, shipped it, then shipped a follow-up feature (position-aware badge visibility) that caused a real production outage — every `Student`-loading endpoint 500ing, not just Trophy Room — because a schema migration silently didn't apply in production. Emergency-rolled-back within the same session. Shailesh then proposed a genuinely simpler design for the same visibility problem that needs no schema/migration at all; re-implemented, independently reviewed (caught one real bug pre-ship), fixed, and handed off for push. **As of this entry, the simpler fix is staged in the working tree, not yet pushed** — Shailesh has the exact `apex_deliver.py` command and hasn't confirmed running it back in this thread yet. Check `git log --oneline -5` first thing next session to see whether it went out.

**1. Backend gap found and fixed (PR #409).** Verifying Phase 3's badge visuals, found production's Trophy Room showed zero Level Mastery badges. Root cause: an earlier session's backend implementation (dynamic badge seeding from the `Level` table + mock-completion detection logic) was designed and reportedly self-verified but **never actually committed** — absent from `origin/main`, local `main`, and even the uncommitted working tree. Rebuilt from the documented design: `AchievementEngine.seed_badges()` now dynamically generates 3 badge rows (BASE/SUPER/LEGENDARY) per active `Level` — 13 levels today (YLM x3, PM x4, BM x1, IM x4, MM x1) = 39 badges, zero code change needed when a level is added. New `_evaluate_level_mastery()` in `backend/app/services/achievements.py`: live per-level aggregate of completed mocks (not a stored counter) — BASE >=12 mocks, SUPER >=20 mocks + avg>=85%, LEGENDARY >=30 mocks + (avg>=95% or one 100%). Strict cascade gating (`_has_badge()` helper: SUPER only if BASE already held, LEGENDARY only if SUPER already held) blocks an exploit where one lucky 100% run could otherwise leak LEGENDARY off a low overall average. Naming contract verified byte-for-byte against the already-committed frontend (`frontend/app/dev/badge-preview/page.tsx`). Shipped, deployed, live-verified.

**2. Position-aware visibility, attempt 1 (PR #410) — shipped, caused a production incident, rolled back (PR #411).** Shailesh's next requirement: real students had progress in earlier curriculum levels before this platform existed, with zero in-app mock records for that history — showing a badge for an already-passed, unearnable level is misleading. He also specified the platform's 3 fixed curriculum entry paths (documented permanently in `docs/project-memory/PRODUCT_RULES.md`'s "Curriculum Progression Paths" section — still there, still accurate as background fact, just no longer what badge visibility is built on, see point 3). First attempt: a new `Student.entry_path` column + Alembic migration + logic resolving which of the 3 paths a student is on and projecting which levels are "reachable ahead." Independent adversarial review (dispatched as a subagent before shipping) caught 2 real bugs pre-ship (an `IM`-excluded-from-`MM`'s-onward-set bug that would've hidden every zero-progress IM badge for literally every real student today, and a PM-L1 check ordering bug) — both fixed, re-verified, shipped as PR #410.

**It broke production.** The `entry_path` Alembic migration ran in Render's build step but the column silently never got added to the live database. Every endpoint loading a `Student` ORM row — not scoped to achievements, this hit broadly — started throwing `sqlalchemy.exc.ProgrammingError: column students.entry_path does not exist` (confirmed via Shailesh pasting real Render logs). Fixed via `python .agents/rollback.py --to prod-20260730-142016 --emergency` (PR #411) — safe as a pure code revert specifically because the DB was never actually migrated to that revision, so there was no `alembic_version` pointer to a phantom revision to clean up. Live-reverified: `/api/student/achievements` back to 200, Trophy Room rendering normally. **Root cause of why the migration silently didn't apply was never diagnosed** — needs a Render build-log inspection (the migration step specifically) before any future schema change ships from this codebase; three unconfirmed hypotheses are logged in `CLAUDE_CODE_STATUS.md`'s history.

**3. Position-aware visibility, attempt 2 — Shailesh's simpler redesign, no schema change, shipped after review.** After the incident, Shailesh asked directly: instead of resolving paths, why not just show a locked/zero-progress Level Mastery badge only if it's for the student's CURRENT level, and let subsequent levels' badges appear automatically as the student advances? Confirmed this is strictly better — no path resolution, no migration surface, and it naturally handles the historical-no-record case (a badge with neither an unlock nor real progress just doesn't show) without needing to know *which* path a student took. Implemented as two small additions to `GET /achievements` in `backend/app/api/routes_student.py`: a `level_mastery_level_id_by_code` map (all Levels, not just active ones — see the review finding below for why), and a single condition — skip a locked, zero-progress Level Mastery badge unless `badge_level_id == student.current_level_id`. Already-unlocked or in-progress badges are untouched by this check regardless of level. Verified via an 8-scenario standalone harness (already-unlocked historical badge, in-progress historical badge, current level at 0 progress, future level, historical level with no record for both an MM-current and an IM-current student, just-advanced level, just-left level) — all 8 pass.

**Independent review (dispatched again, same discipline, arguably more warranted given the incident two paragraphs up) caught one real bug before shipping:** the level lookup was originally filtered to `Level.is_active == True` only — if an admin later deactivates a level a student is currently sitting on, that student's own current-level badge would wrongly disappear (the code->level_id lookup returns `None`, which never equals a real `current_level_id`). Fixed by dropping the `is_active` filter entirely (deactivated levels nobody is on just sit unused in the map). Re-verified `py_compile` clean and re-diffed against `HEAD` (CRLF-stripped, this repo's real diffs get buried in whitespace noise otherwise — see the CRLF note in `OPEN_ISSUES.md`) to confirm only the intended lines changed.

**`docs/project-memory/PRODUCT_RULES.md` updated to match:** the "Curriculum Progression Paths" section documents the 3 paths as background product fact (still true, may matter for a future roadmap-style UI), then explicitly documents that Level Mastery visibility does NOT use path resolution, explains why (this incident), states the actual shipped rule, and sets a standing recommendation for future similar features: prefer "current position + already-earned progress" over "resolve path then project forward" unless a real requirement forces the latter.

**Cleanup verified, nothing stray left behind:** grepped the full backend + frontend tree post-rollback for `entry_path`, `resolve_student_entry_path`, `is_level_mastery_level_reachable`, `PATH_1_YLM`/`PATH_2_PM`/`PATH_3_BM`, `_LEVEL_MASTERY_MODULE_ONWARD` — zero hits anywhere in actual code. No orphaned Alembic migration file. No scratch verification scripts accidentally committed (all lived in `/tmp`, never in the repo tree). `git status` shows only the pre-existing local `.claude/settings.local.json` as untracked. The only remaining mentions of `entry_path` anywhere are in this file, `CLAUDE_CODE_STATUS.md`, and `PRODUCT_RULES.md` — intentional history, not dead code.

**Push confirmed live (2026-07-30, same session, after this entry was first drafted).** Merged as PR #412. Live-verified directly via a real student login (Nishant Gantayet, currently MM-L1): Trophy Room shows exactly `MM L1 -- Cleared/Mastered/Perfected` (10/12, 10/20, 10/30 progress) and zero Level Mastery badges for any other level anywhere in the page — the current-level rule working correctly with real production data, no regression. `/api/student/achievements` returns 200. The whole Level Mastery visibility saga (PR #409 backend -> PR #410 incident -> PR #411 rollback -> PR #412 clean fix) is closed.

**What's left — two genuinely separate threads, don't conflate them:**

1. **Level Mastery visuals, still incomplete.** 15 of 39 badge visuals exist (Batch 1 BM-L1, Batch 2 MM-L1, Batch 3 YLM-L1/L2/L3 — all Opus-elevated, live-verified). **Batch 4 (PM-L1 through PM-L4, 12 badges) and Batch 5 (IM-L1 through IM-L4, 12 badges) are not started.** Every remaining design batch must route through an explicit Opus `Agent` call (`model: "opus"`), not this session's own model — a standing correction from earlier in this project, don't repeat the mistake of building design work on Sonnet by default. After Batches 4-5, a full 39-badge verification pass is still needed (distinctness script, contrast, live preview) before the visual side of Phase 3 is actually done. Badges without a shipped visual currently fall back to a generic octagon shape in the Trophy Room (`getShapeStyles()` has no entries for `LevelMastery*` icon names yet) — cosmetic, not blocking, but real.
2. **DPS-side badges: not started at all, not even designed.** The Trophy Room UI already has a "DPS Sheets" tab (`activeTab === "dps"` in `frontend/app/student/achievements/page.tsx`) sitting right next to "Mock Exams," and it currently renders a literal **"DPS Badges Coming Soon"** placeholder — confirmed by reading the component directly. Everything built this session (Level Mastery, and every earlier-session mock badge family — Perfectionist, Speed Demon, Competitor, Unstoppable Streak, Early Bird, Comeback Kid, Sharpshooter, Underdog, Polymath, Podium Finisher, all with MYTHIC tiers per earlier Phase 1/2 work) is Mock-Exam-triggered only; `backend/app/services/achievements.py` has zero DPS-completion detection logic of any kind. The original planning doc (`docs/GAMIFICATION_BADGE_CATALOG_PROPOSAL_2026-07-25.md`) explicitly deferred DPS badges to a later phase, after the mock badge system was fixed and elevated — that mock-side work is now essentially done (10 skill families x MYTHIC + Level Mastery), so **DPS badges are the next real phase of unbuilt work**, not just an unfinished detail. This needs its own design pass from scratch: what actions/thresholds earn a DPS badge, whether it mirrors Level Mastery's per-level structure or is its own shape, and full backend + visual + frontend wiring, same as every mock badge family required.

**Task list state as of this entry (Cowork's tracked task list, IDs stable within this thread — task #37 "Deliver 15 Level Mastery badges to production" is marked in_progress since the underlying PR merged but the follow-up incident/redesign happened on top of it before this final push; #31/#32/#34 [Batch 4, Batch 5, 39-badge verification] are still pending; no task yet exists for DPS badges — worth creating one at the start of that phase).

Last updated: 2026-07-24 (pre-rollout QA closeout phase is now CLOSED OUT — see the newest entry directly below. Final pass: cross-module live verification via 3 real student logins [Rohan Sen/MM, Tanaya Patra/YLM, Anaisha Agrawal/IM read-only] surfaced 2 more real bugs on `student/results/page.tsx` + `accuracyToneClass()`, both fixed, delivered as a second PR, and confirmed live. Whole-phase tally: 16 files fixed across Admin/Teacher/Student, all delivered and live-verified. Only remaining item is an operational decision, not a bug — the `TEMPORARY_ASSESSMENT_READINESS_BYPASS` flag in `backend/app/core/config.py`, awaiting Shailesh's explicit call. See `docs/project-memory/OPEN_ISSUES.md`'s Active section — this phase's items have moved to Resolved Recently there.)

## 2026-07-24 update (pre-rollout QA closeout, final pass — phase CLOSED OUT): cross-module live verification via 3 real student logins found 2 more bugs, fixed and delivered; 16-file tally confirmed pristine

**tl;dr:** Following the prior entry's delivery of PR #400 (2 fixes: `student/practice/page.tsx`, `student/assessments/page.tsx`), Shailesh asked the question this whole phase had been building toward — does the fix hold for *any* student, in *any* module/level, not just the one account tested so far — and explicitly rejected a code-review-only answer: "no no, i want to verify it from all the possible angles every single one of them... nothing should be overlooked or missed, nothing whatsoever. PRISTINE is the benchmark and i will not settle anything other than that." Shailesh logged into two more real student accounts to make that possible: Tanaya Patra (YLM module, zero completed attempts — a data state no prior round had tested) and, after Shailesh drew a hard line ("for anaisha i cannot do that as that is a live student account we cannot meddle with that"), Anaisha Agrawal (IM module, real production account) in **read-only** mode only, per Shailesh's own proposed compromise — no task attempted or submitted on her account at any point.

**2 more real bugs found, same root-cause class as the whole 16-file phase (`0`/`Number("")` used as a "no data" sentinel instead of `null`/`NaN`), surfaced only because Tanaya Patra has zero completed attempts — a code path Rohan Sen's account (which has one real 27% attempt) never exercised:**
1. **`frontend/app/student/results/page.tsx` (the "My Progress" page) — false "AVERAGE ACCURACY 0%" for any student with no completed work**, at all 4 render sites (hero metric, module-level chip, current-level-scope metric, per-level drilldown chip). `AverageValues()` and `HierarchyAverageForRows()` both returned `0` for an empty accuracy array instead of `null`. Fixed: both now return `number | null`; added a new `FormatAccuracyOrDash()` helper used at all 4 sites. Also fixed a latent second bug in the same function while in there — the level-average filter used `.filter(v => v > 0)`, which would have incorrectly excluded any level whose *real* average was genuinely exactly 0%, not just levels with no data — changed to a null-check filter instead.
2. **`frontend/components/common/DetailWorkspaceViews.tsx`'s `accuracyToneClass()` — false "red/low accuracy" tone instead of neutral "no data" tone.** The non-object branch fell back to `numberValue(Value, 0)`; its sibling function `accuracyTone()` a few lines above already correctly used `numberValue(Value, Number.NaN)`. Fixed to match. Only call sites are the two `student/results/page.tsx` chips above, so blast radius was fully understood and contained.

**Verification:** live-confirmed on Tanaya Patra's real Progress page — all 4 sites now show "—" instead of the old false "0%", tone class neutral instead of false-red. Regression-checked against Anaisha Agrawal's real 48% average (unaffected, no regression). Cross-checked across all 3 modules via the 3 logins: Rohan Sen (MM, real completed data), Tanaya Patra (YLM, zero completed attempts), Anaisha Agrawal (IM, real production data, read-only). Every student self-view accuracy display (Dashboard, Progress/Results, Practice, Assessments, Mock Exams, Leaderboard) is now confirmed correct in every data state.

**Delivered:** pushed as a second PR on branch `fix/student-results-accuracy-dash`, squash-merged, confirmed live. **Environmental note for future sessions:** the local terminal is PowerShell, not bash — inline `git commit -m "..."` / `gh pr create --body "..."` strings with embedded quotes or em-dashes get mangled (one attempt produced zero commits silently; another split `gh pr create --body` into bad positional args, erroring "unknown arguments... please quote all values that have spaces"). Fix used: write the message to a file via a PowerShell here-string (`@"..."@ | Set-Content -Encoding utf8 msg.txt`), then `git commit -F msg.txt` / `gh pr create --body-file msg.txt`. Also: `gh pr merge` with no explicit PR number/branch argument operates on whatever's currently checked out — running `git checkout main` first before merging makes it fail with "no pull requests found for branch main"; pass the PR number explicitly instead (`gh pr merge <N> --squash --delete-branch`).

**Whole-phase tally, all delivered and live-verified: 16 files fixed** across this entire pre-rollout QA closeout (spanning several sessions/compactions), every one the same root-cause pattern — a "no data" sentinel using `0`/`Number("")` instead of `null`, surviving an unguarded `Number.isFinite()`/`reduce()`/`??` check and either diluting an average or rendering a false "0%"/false-red tone instead of "—"/neutral.

**Bottom line:** this phase is genuinely closed. Every accuracy/average display across Admin, Teacher, and Student roles, across all 3 curriculum modules and multiple data states (real data, zero data, mixed data), is either fixed and live-verified or was investigated to ground and confirmed correct as designed (false alarms: Concept Drill modulo-not-subtraction math, Grind Heatmap multi-activity pooling, question-weighted mock accuracy, lifetime achievement counters, an Admin History drilldown click-registration automation artifact, a stale search filter — see the prior entry below for full detail on each). The one item left open is a product decision, not a bug: `TEMPORARY_ASSESSMENT_READINESS_BYPASS` in `backend/app/core/config.py` defaults ON, letting teachers assign live assessments regardless of DPS/practice completion (confirmed live via the "Readiness Bypass Active" banner on Ashalatha Gupta's assign-assessment page) — a documented, intentional Phase 8.8 convention awaiting Shailesh's explicit call before rollout.

**Files changed this entry:** `frontend/app/student/results/page.tsx`, `frontend/components/common/DetailWorkspaceViews.tsx`.

## 2026-07-23 update (pre-rollout QA closeout): two full QA sweeps across all 3 logins, then 8 bugs root-caused and fixed — PREPARED, NOT YET DELIVERED

**tl;dr:** Shailesh asked for a full end-to-end QA sweep across Admin/Teacher/Student ahead of a near-term rollout, with an explicit bar: "we need to make sure that whatever we have on the platform right now is completely pristine." Ran two rounds — a first pass across all 3 logins (`docs/QA_AUDIT_2026-07-23.md`), then a deeper sweep covering both teacher accounts and the full Learning Path publish-status (`docs/QA_AUDIT_2026-07-23_DEEP.md`). After Shailesh's final go-ahead ("this is where we need to find, finalise and fix anything and everything that requires fixing"), root-caused and fixed all 8 reproducible bugs found across both rounds in the same session, plus investigated 2 lower-confidence items to a clear conclusion without a code change.

**Headline non-bug finding (round 2):** most MM/IM practice content is genuinely unpublished (DRAFT, not PUBLISHED, in Learning Path Studio) — that's why almost every MM/IM student shows 0% progress. Confirmed with Shailesh as expected test-mode rollout status, not a bug — explicitly out of scope.

**The 8 bugs, fixed this session:**
1. **Assessment Readiness "CLEARED DPS" double-counted a Needs-Re-Attempt sheet as cleared**, identically on Admin/Teacher/Student readiness pages (confirmed on 2 students under 2 different teachers). `assessment_eligibility_service.py` already returns both `completedDpsCount` (PASSED+BELOW_BENCHMARK) and the correct `passedDpsCount` (PASSED only); all 3 frontend pages were reading the wrong one. Fixed all 3 (2 call sites each).
2. **Teacher "My Students" Average Accuracy diluted by no-attempt students** (8%/18% shown on the two teacher accounts, vs. what students who'd actually attempted anything averaged). `teacher/students/page.tsx`'s summary defaulted missing accuracy to `0` instead of excluding it from the denominator — Admin's Practice Control page already did this correctly. Fixed to filter out no-data students first.
3. **Admin Performance Reports didn't reset the Module filter on student switch**, silently showing an all-zero report scoped to the wrong module. Fixed with a new effect in `admin/results/page.tsx` that auto-syncs the Module filter to the newly-selected student's real current module.
4. **Student Assessments page hard-errored.** `AssessmentAssignmentPayload()` in `assessment_engine_service.py` called `float()` directly on nullable `Attempt` fields (`max_score`/`total_score`/`percentage`), crashing with `TypeError` whenever an attempt existed but wasn't fully graded — same pattern also existed in the reattempt-approval payload builder. Added a `SafeFloatOrNone()` helper, used at both call sites.
5. **Mock leaderboard showed "0/0%" for every rank below the podium**, on both leaderboard views — but the backend already returns real percentages for ranks 1-10 and the podium rendered correctly. Root cause: the ranks-4-10 table routed every number through a `requestAnimationFrame` count-up animation starting at 0, which browsers throttle/pause for backgrounded tabs; the podium had no such animation. Fixed by rendering the real values directly and removing the now-dead `useCountUp` hook.
6. **A fresh login could briefly render the previous session's cached data** (observed as implausible numbers on the teacher roster right after switching accounts, self-correcting on reload). Root cause: the single app-wide React Query cache isn't scoped by user identity and nothing cleared it on login/logout. Fixed by clearing it on `lib/auth.ts`'s existing `"mathpath-auth-changed"` event, which now also fires on logout (previously login-only).
7. **One component used the browser's default date locale instead of the shared `en-IN` formatter** (`AssessmentFeedbackRemarkCard.tsx`). Swept the rest of the frontend for the same pattern — everything else that looked like a stray date formatter turned out to be a number formatter (XP/coin grouping); this was the only real instance. Fixed.
8. Two items investigated to a clear conclusion, no code change: Trophy Room badge count (7) vs. visible mock count (6) and the 81%-vs-84.2% average gap both have legitimate non-bug explanations (lifetime achievement counter that correctly doesn't decrement on a superseded assignment; marks-weighted pooled average vs. a naive mean) — flagged as a product decision in `OPEN_ISSUES.md`, not fixed blind. Parent-report resend has no existing retry feature at all (a feature gap, not a regression) — same treatment. The earlier round's "View Result" click-target flakiness looks like a browser-automation artifact (stale coordinates captured before a hover CSS transform), not a real user-facing bug.

**Verification:** `python3 -m py_compile` clean on every changed backend file, `from app.main import app` imports cleanly (185 routes), full existing backend suite **48/48 pytest pass** (had to run from a `/tmp` copy — this sandbox can't write to the OneDrive-mounted `backend/.pytest_cache`, a recurring environment quirk seen in prior rounds too). Frontend: a full-project `tsc --noEmit` timed out before producing any output in this sandbox (same recurring OneDrive-latency limitation noted in the 2026-07-23 "100-marks-always" round further down this file) — every individual edit was instead manually checked against its exact declared TypeScript type. **A local Claude Code session should still run a full `tsc --noEmit`/`next build` before this ships.**

**Not done this session, needs a live browser session with Shailesh:** the 3 planned live-attempt tests (a real DPS practice submission, mock exam submission, and assessment submission via a throwaway test blueprint, all for Rohan Sen/MP-ST-008) and a final live re-verification pass confirming all 8 fixes against the actual running app across all 3 logins.

**Not delivered:** this Cowork sandbox has no GitHub route — changes are prepared in the working tree only. Needs a local Claude Code session to run `qa-reviewer` then `sre-devops`.

**Files changed:** `frontend/app/{admin,teacher,student}/assessment-readiness/page.tsx`, `frontend/app/teacher/students/page.tsx`, `frontend/app/admin/results/page.tsx`, `backend/app/services/assessment_engine_service.py`, `frontend/app/student/competition/leaderboard/page.tsx`, `frontend/components/common/QueryProvider.tsx`, `frontend/lib/auth.ts`, `frontend/components/common/AssessmentFeedbackRemarkCard.tsx`.

## 2026-07-25 update (two rounds that day, both confirmed DELIVERED AND LIVE via real Chrome verification against production, not just merged. Round 1: 6 real bugs Shailesh found live-testing the previous session's #393 "100-marks-always" feature — the prior session's "verified" claim had only checked marks math, never actual rendered content — all fixed, delivered as PR #394 (`a67e90f`), live-verified with a real test blueprint. Round 2, straight after: 3 more real bugs, one of them a systemic, platform-wide bug that had been silently collapsing every IM assessment's multiplication/division/positional/BODMAS-variant questions to one hardcoded pattern instead of each level's real curriculum variety (also made "Find the Position of the First Natural Number" ungenerateable) — root-caused, fixed, a new regression test added, delivered, and live-verified. See the two newest entries below for full detail; the security-audit entries under 2026-07-21 remain exactly as they were, still awaiting Phase 2 delivery. **One decision still needed from Shailesh:** the live "IM L4" published assessment (and possibly other already-published IM assessments) still contains data generated before either 2026-07-25 fix existed and needs regeneration — see `docs/project-memory/OPEN_ISSUES.md`'s Active section for the exact ask.)

## 2026-07-25 update (round 2: IM question-variety systemic bug + BODMAS no-wrap + chip color): 3 more live-found bugs, all root-caused, fixed, tested, delivered, and LIVE-VERIFIED

**tl;dr:** Straight after round 1 (PR #394, below) went live, Shailesh looked at it and found 3 more issues, with an explicit standing instruction this round: "first go through everything and then come back to me with the last fuckin findings, i do not have time to go on waste it on a single fuckin issue" — investigate everything up front, report once, not one bug at a time. All 3 root-caused, fixed, tested, and live-verified against production with a fresh test blueprint after Shailesh's follow-up "fixes are live go verify everything end to end and make sure they are working."

1. **BODMAS questions must never wrap — always a single line, exactly matching the mock-exam convention**, not just "allow wrap instead of overlap" (round 1's stopgap fix, now superseded). Root cause: the Assessment Studio's question preview always rendered the question/answer grid in a narrow ~360px column regardless of question type. The mock exam preview (`mock-studio/[mockId]/page.tsx`) already solves this exact problem via a `needsWideQuestionLayout()` pattern — switching to a full-width single-column grid for `EXPRESSION_WORKSHEET`/`FINANCIAL_TABLE` display types — but the assessment preview never had the equivalent switch. Fixed in `frontend/app/admin/assessment-blueprints/page.tsx`'s `GeneratedQuestionPreview` by copying the identical conditional (checks `displayType`/`display_type` on the question, already correctly persisted by `app/question_engine/im/generator.py` at generation time, so no extra resolution logic was needed on the frontend side unlike mocks, which infer it live).
2. **Multiplication/division only ever generated 2D×2D and 4D/2D, never any of the level's other real digit patterns** — the biggest and most consequential find of the whole day. Root cause, in `backend/app/services/assessment_engine_service.py`'s `ImSectionRegistryConfig()`: every `ConceptSpec` a section's concept-pool entry carries — `multiplicationDigits`, `divisionDigits`, `isLongDivisionEstimation`, `answerPositionDirection`, `positionRange`, `hasSquareTerm`, `bodmasTemplate`, `borrowingMode`, `isDecimal`, `decimalPlaces`, `explicitDigitCount`, `rowCount`, `magnitudeMin/Max`, `fixedTimes`, `addRange`, `skillStackerLinear`, and more — was being discarded except for `conceptFamily`/`title` before being handed to the question generator, so every question for a given concept family silently fell back to `operands.py`'s hardcoded default regardless of which curriculum variant the level actually called for. This wasn't scoped to multiplication/division or to IM-L4 — it affected every IM level and every concept family with more than one pool variant, and as a direct consequence **`answerPositionDirection: "FIND_POSITION"` could never be reached, so "Find the Position of the First Natural Number" questions could never generate at all.** Fixed by merging every `ConceptSpec` field except `conceptFamily`/`title` straight into `GeneratorConfig`, mirroring the pattern MM's equivalent config-builder already uses correctly. Added a new regression test, `test_im_assessment_generation_varies_digit_patterns_and_division_digit_pairs` / position-direction assertions in `backend/tests/test_section_wise_assessment_generation.py`, which generates a real IM-L4 assessment across an 8-section distribution satisfying the 100-marks invariant and asserts real variety in multiplication/division digit pairs and both position directions appearing. 48/48 backend tests pass (47 existing + 1 new), no regressions.
3. **Section-name chip didn't match the section-number chip's blue styling** for IM (and every module except MM) — plain gray instead. Fixed by removing the module-based conditional in `GeneratedQuestionPreview` so both chips unconditionally share `rounded-full math-admin-studio-chip px-3 py-1 text-xs font-black`. (Flagged, not touched: the identical pattern exists in the mock studio's own per-question chips — Shailesh only flagged the assessment view this round, so that one's left as a known, separate item if he wants it later.)

**Delivery:** pushed on branch `fix/im-question-variety-bodmas-chips`, PR opened and merged into `main`, confirmed deployed to production (Render auto-deployed; Vercel needed the manual `manual-prod` Deploy Hook workaround again, same as round 1 — see round 1's entry below for why).

**Live-verified end to end, 2026-07-25, real Chrome session against production (not just the mechanism):** built a fresh IM-L4 test blueprint (92 questions). Confirmed via computed style inspection that both chips share identical class and color. Sampled 11 of 13 multiplication questions via pagination and got real digit-pattern variety spanning all 5 registry patterns (2D×2D through 4D×2D), e.g. `46×88`, `586×9`. Sampled 7 of 13 division questions, same result across all 5 patterns including the long-division-vs-exact-division distinction working correctly. Confirmed both `WRITE_FROM_POSITION` and `FIND_POSITION` position questions now appear in the same generated set (previously only one was ever reachable). Confirmed a 36-character BODMAS expression renders on a single line at full width with no overlap and no wrap. Test blueprint deleted after verification; Assessment Library confirmed back to only the real published assessments, no leftover test data.

**Not yet done:** the live "IM L4" published assessment (and possibly other already-published IM assessments across other levels, since this bug was platform-wide and pre-existing) still contains data generated before this fix existed. Deploying the code fix doesn't retroactively regenerate already-persisted questions — only "Generate Preview" or republish does that. This is a live-data change, not a code change, so it wasn't done unilaterally. **Waiting on Shailesh: regenerate just "IM L4," or run a broader pass across every published IM assessment?** Tracked in `docs/project-memory/OPEN_ISSUES.md`'s Active section.

## 2026-07-25 update (round 1: PR #393 live-testing found 6 real bugs, all fixed, delivered as PR #394, and LIVE-VERIFIED)

**tl;dr:** The previous session's #393 "100-marks-always" feature had been prematurely declared "verified" — only marks math had actually been checked, never real rendered question content. Shailesh live-tested it against production himself and, in very direct terms, reported 6 real, visible bugs with a demand that the platform actually be checked this time, not just claimed fixed. All 6 root-caused and fixed, delivered as PR #394 (`a67e90f`), then genuinely live-verified via a real Chrome session against production with a throwaway test blueprint — not re-asserted from the diff alone.

1. **Skill Stacker/Concept Drill section generating and numbering FIRST instead of last.** A #393-round regression: `distributeWeighted100()` in the blueprint builder (`page.tsx`) constructed weighted section rows before normal section rows, and that array order flowed straight into `AssessmentBlueprintSection.display_order`, which `GenerateAssessmentVersion()` uses to number questions. Fixed by re-sorting the combined row list back into the level's real registry order before returning it.
2. **Multiplication/division questions displaying "+" instead of "×"/"÷".** Pre-existing and platform-wide, not new to #393. `QuestionText()`'s fallback path in `assessment_engine_service.py` was operand-only and ignored the actual operator, and that incorrect text was what got persisted to `AssessmentQuestion` (the frontend prefers persisted text over rebuilding it live, so the bug was invisible until someone looked at real generated output). Fixed by making the fallback operator-aware.
3. **BODMAS questions overlapping the answer-option boxes.** `ExpressionQuestion`'s `nowrap` decision was gated on character count without accounting for the Assessment Studio preview's narrower column width. First fix: always allow wrapping instead of overlapping. (Superseded later the same day by round 2's real fix above, once Shailesh clarified he wanted zero wrapping, not graceful wrapping.)
4. **ANSWER_POSITION values wrapping onto a second line inside their box.** Fixed with dynamic font sizing based on the rendered value's digit length.
5. **Question preview showing 4 chips (section number, section title, concept tag, "Mixed") instead of the 2 the mock-exam convention uses.** Fixed to show exactly 2 (later found in round 2 to still have a color-consistency gap between the two, fixed there).
6. **Step 1 "Skill Stacker/Concept Drill Questions" field visibly misaligned against "Duration Minutes" in the same row.** Fixed with `items-end` on the grid row.

**Live-verified, 2026-07-25, real Chrome session against production:** built a throwaway test blueprint, generated a real assessment version, and confirmed: Skill Stacker section numbered last (not first); real `×`/`÷` symbols rendering on generated multiplication/division questions instead of `+`; BODMAS questions no longer overlapping the answer boxes; ANSWER_POSITION values rendering cleanly on one line regardless of digit count; exactly 2 chips shown per question. Test blueprint deleted after verification.

**A process note carried forward, not a technical one:** this round's starting point — a feature marked "verified" that hadn't actually been looked at — is the reason both this round and round 2 above ended with genuine live-Chrome verification against real rendered output before being reported as done, not a rerun of whatever check had been called "verification" before.

## 2026-07-21 update (security audit Phase 2): 7 defense-in-depth items including full optional Admin 2FA, all implemented and self-verified — PREPARED, NOT YET DELIVERED

**tl;dr:** Straight after Phase 1 merged (PR #362), Shailesh asked to continue into Phase 2 of `docs/SECURITY_AUDIT_2026-07-21.md` — apply all 7 items. First had a real design discussion about password policy: I pushed back on Shailesh's initial proposal to use `first_name-last_name` as the standing password for student/teacher accounts (predictable, low-entropy, and a variant of the exact anti-pattern Phase 0 fixed), recommending random-generated-password-plus-forced-first-login-change instead. He heard the tradeoff and made an informed call: student/teacher accounts are low-stakes enough (no financial data, and while I noted the gamification/leaderboard-tampering risk, he judged it acceptable) that the simple, predictable pattern is fine for initial/admin-reset passwords, with the real password policy applying only when a user voluntarily changes their own password later. That's what got built. All 7 items done on branch `security/phase-2-hardening`, based off `main` at Phase 1's merge commit.

1. **Dependency audit run for real.** `pip-audit`: found 23 advisories across 6 packages. Bumped the 3 that were safely upgradable as direct pins and verified via the full test suite + a live multipart-upload smoke test: `python-jose` 3.3.0→3.4.0 (fixes a JWT-bomb DoS and an algorithm-confusion CVE — directly relevant since this app decodes externally-supplied JWTs on every request), `python-dotenv` 1.0.1→1.2.2, `python-multipart` 0.0.20→0.0.31 (6 advisories, mostly parsing-DoS). Did **not** bump `starlette` (7 advisories) — confirmed via the actual FastAPI 0.115.6 wheel metadata that it hard-pins `starlette<0.42.0`, so fixing it needs a FastAPI upgrade first, which is its own project. Did **not** bump `pytest` (1 advisory) — dev-only, the CVE is about local `/tmp` directory permissions during test runs, irrelevant to production, and 8→9 is a major version bump not worth rushing. `ecdsa`'s advisory has no upstream fix; noted but not actionable — `python-jose[cryptography]` uses the `cryptography` backend for real crypto operations, not the vulnerable pure-Python ecdsa path. `npm audit`: 28 findings (6 high, 22 moderate). Attempted `npm audit fix` in-sandbox; it was still rewriting `package-lock.json` (an 18,674-line diff) when the sandbox's 45-second command cap killed it mid-run. Rather than leave a possibly-inconsistent lockfile in place, reverted immediately — `git checkout` itself briefly failed with "unable to unlink... Operation not permitted" on both files (the same class of transient OneDrive-sync lock documented earlier this session on `.git/index.lock`), worked around via a direct `cp` of the original content back, and a `.git/index.lock` retry cleared on its own a moment later. The safe non-major fixes (`axios`, `brace-expansion`, `form-data`, `js-yaml`) still need `npm audit fix` run in a local terminal, which has no timeout constraint. The remaining findings (`@sentry/nextjs`, `next`) need a semver-major bump each and were left alone deliberately — that's its own dedicated verification pass, not something to fold into a security patch round. Added `.github/dependabot.yml` (pip + npm + github-actions, weekly cadence, major Next.js/Sentry bumps excluded) so this kind of gap gets caught automatically going forward instead of only during a manual audit.
2. **Password policy strengthened, self-service only.** `strong_password_issue()` in `backend/app/core/security.py` (8+ chars, at least one letter, at least one number) is called only from `POST /api/auth/change-password`; mirrored client-side in `AppShell.tsx`'s password-change handler. Admin create/reset-password endpoints in `routes_admin.py` deliberately untouched — still just the pre-existing 6-character floor, no complexity requirement, so `first_name-last_name`-style initial passwords keep working exactly as Shailesh wants.
3. **Lightweight session-revocation mechanism, and a real bug found along the way.** New `User.session_invalidated_at` column (added via the same `ensure_user_columns()` startup-patch pattern already used for `password_changed_at` — no alembic migration needed, matching that existing precedent). `get_current_user()` rejects any token whose `iat` predates it. Exposed via self-service `POST /api/auth/logout-all-sessions` (new "Sign Out Everywhere" row in the account Settings modal, any role) and admin-triggered `POST /admin/teachers/{id}/force-logout` / `POST /admin/students/{id}/force-logout`. While live-testing this against a real database, the old token stayed valid when it should have been rejected — tracked it down to **two separate, real bugs in the pre-existing `password_changed_at` check this new code deliberately mirrored**: (a) SQLite drops timezone info on read-back even for an aware-UTC value, so comparing it against the always-aware `iat`-derived datetime raised `TypeError: can't compare offset-naive and offset-aware datetimes`; (b) far more consequentially, that TypeError (and everything else in the block) was being swallowed by a bare `except Exception: pass` that also wrapped the `api_error(401, ...)` call itself — and `api_error()` *raises* `HTTPException`, which is an `Exception`, so even a correctly-computed "reject this token" outcome had its own 401 silently caught and discarded before it could reach the client. Net effect: **`password_changed_at`'s entire purpose — killing old sessions after a password change — appears to have never actually worked, in any environment, since it shipped**, because the raise was always self-defeating regardless of the timezone bug. Fixed both: a `_as_aware_utc()` helper normalizes and floors precision before comparing (also fixes a same-second re-login race, discovered via testing: JWT `iat` truncates to whole seconds, so an immediate re-login right after a revocation could otherwise get compared against a microsecond-precise revocation timestamp and get incorrectly rejected too), and both checks were restructured so `api_error()` is only ever called after the guarded comparison, never from inside the exception-swallowing block. Verified live end-to-end with real timing: an old token is correctly rejected ~1.5s after either a password change or a force-logout, and a fresh login immediately afterward correctly gets a working token.
4. **File-size guard on bulk-upload.** 5MB ceiling checked before `openpyxl.load_workbook()` ever touches the uploaded stream in `POST /admin/students/bulk-upload` — a real template is a few dozen KB even for hundreds of students.
5. **Optional TOTP-based 2FA, full feature, scoped to Admin/Super Admin.** New `backend/app/core/totp.py` (secret generation, QR-code-as-base64-PNG rendering matching this codebase's existing profile-photo data-URL convention, TOTP verification with 1-step clock-drift tolerance, backup-code generation) — added `pyotp` and `qrcode[pil]` to `requirements.txt`. Five new endpoints under `/api/auth/2fa/*`: `setup` (generates a *pending* secret, not yet active, so a botched QR scan can't lock anyone out), `enable` (confirms with a real code, activates, returns 10 backup codes exactly once), `disable` (requires the current password, so a stolen session token alone can't turn off the very protection 2FA provides), and `verify-login` (redeems either a live TOTP code or a single-use backup code for a real access token). `login()` itself now returns `{twoFactorRequired: true, challengeToken}` instead of a real token when the account has 2FA on. That challenge token is deliberately not a usable bearer token — it carries a `purpose: "2fa_challenge"` claim that `get_current_user()` explicitly checks for and rejects, so a leaked/intercepted challenge token can't be replayed to skip the second factor; verified this directly (challenge token against `/api/auth/me` correctly 401s). Backup codes are bcrypt-hashed at rest (same trust model as passwords, never plaintext) and consumed on use (verified: reusing a spent backup code correctly 401s). Frontend: `LoginClient.tsx` swaps the identifier/password form for a code-entry step when `twoFactorRequired` comes back, reusing the existing `math-input`/`math-button-primary` styling rather than introducing new visual language; `AppShell.tsx` gets a new self-contained `TwoFactorModal` (idle → setup with QR + manual-entry secret → enable → one-time backup-codes reveal → done; or enabled → disable-with-password), reachable from a new "Two-Factor Authentication" row in Account Settings, visible only to Admin/Super Admin accounts. Full lifecycle live-tested end to end: role-gating (Teacher gets 403 on setup), real generated TOTP codes accepted, wrong codes rejected, backup-code login and single-use enforcement, disable requiring the correct password.
6. **httpOnly-cookie migration — still deferred.** Per the original audit's own recommendation ("worth scoping separately rather than bundling into this pass"), not touched.
7. **`python-jose` vs. `PyJWT`/`Authlib` reviewed.** The CVEs surfaced by the dependency audit above are now patched by the 3.4.0 bump, so this is no longer an active vulnerability. `PyJWT` is more actively maintained and this app's JWT usage is fully centralized in 4 functions in one file with no JWE usage to lose, so a future migration would be low-risk if ever wanted — not done here since it's now optional, not urgent.

**Verification:** 36/36 backend pytest pass, run three separate times from a clean scratch copy outside the OneDrive mount (baseline, after the dependencies.py bug fix, after the dependency version bumps) — same `.pytest_cache`-permission workaround as every prior round, this time compounded by `rsync`/`cp -r` of the whole `backend/` tree getting silently truncated across separate sandbox calls (each shell call here is a fresh, isolated process with no background-job carryover), fixed by doing one synchronous `cp -r` of just `app/`+`tests/`+`requirements.txt` per verification pass. 25/25 governance tests pass, untouched by this round. Live end-to-end tests against a real (throwaway, in-memory) database covered: password-policy rejection and acceptance, the force-logout timing fix, and the full 2FA lifecycle (9 separate assertions, detailed above). Frontend: same documented 45-second sandbox cap prevents a full `tsc`/`next build` run — self-reviewed the ~600-line diff across `LoginClient.tsx`/`AppShell.tsx`/`lib/api/auth.ts`/`types/auth.ts` in full, plus ran TypeScript's `transpileModule` API directly against all 4 changed/new files as a syntax-only check (confirms no parse errors, doesn't replace real cross-file type-checking). **Local terminal must run `npm run typecheck && npm run build` for real before this ships**, same as every prior frontend round, and should also run `npm audit fix` for the safe frontend dependency bumps this round couldn't complete in-sandbox.

**Not yet done:** delivery (a local Claude Code session needs to run qa-reviewer then sre-devops to commit/push/PR/merge branch `security/phase-2-hardening`), the deferred `npm audit fix`, then Phase 3 of the audit (a real dynamic scan, confirming Sentry alerting rules exist, an incident-response runbook, encrypted automated backups, a COPPA/FERPA legal-obligations check, a CDN/WAF layer) — not started.

## 2026-07-21 update (security audit Phase 1): 7 high-severity backend hardening items, all implemented and self-verified — PREPARED, NOT YET DELIVERED

**tl;dr:** Straight after Phase 0 merged, Shailesh asked to continue into Phase 1 of `docs/SECURITY_AUDIT_2026-07-21.md` — apply all 7 items. All 7 done on branch `security/phase-1-hardening`, backed off `main` at `4415372` (post-Phase-0).

1. **Exception-handler info leak fixed.** `backend/app/main.py`'s global handler no longer returns `str(exc)` to the client on a 500 — logs full detail server-side (`logger.exception(...)`, Sentry still captures it too when `SENTRY_DSN` is set) and returns a generic message instead.
2. **Security-headers middleware added**, global, in `main.py`: `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, a locked-down `Content-Security-Policy` (appropriate since this is a JSON-only API with no server-rendered pages of its own), and `Strict-Transport-Security`. Uses `setdefault()` throughout so it never overrides a header a specific route already sets on purpose (e.g. the profile-photo endpoint's own `X-Content-Type-Options`).
3. **Fixed the `TEMPORARY_ASSESSMENT_READINESS_BYPASS`/`ASSESSMENT_READINESS_FORCE_STRICT` env-var name mismatch.** `render.yaml` had been setting `TEMPORARY_ASSESSMENT_READINESS_BYPASS: "false"`, clearly trying to turn the bypass off in production — but `backend/app/core/config.py` only ever read a differently-named var, `ASSESSMENT_READINESS_FORCE_STRICT`, which render.yaml never set. Net effect: the readiness bypass had actually been active in production the whole time, contrary to what the deployed config appeared to say. This one changes live behavior depending on how it's fixed, so it was **not** touched without asking first — given the choice between switching production to strict enforcement vs. keeping today's real behavior and just fixing the config to say so honestly, Shailesh chose the latter. `render.yaml` now sets `ASSESSMENT_READINESS_FORCE_STRICT: "false"` (the variable the code actually reads) with a comment explaining the history — no functional change in production.
4. **`SEED_ON_STARTUP=true` in production investigated and confirmed safe.** Read `seed_ylm_phase1.py`'s `seed()` function in full rather than guessing: it's genuinely idempotent, curriculum/master-data-only (creates/updates Module/Level/Lesson/DPS/DPSSection rows for the YLM Golden Steps curriculum), never touches users/students/teachers/assignments/attempts, and its only destructive path (`_delete_legacy_level_lessons`) is narrowly scoped to specifically-identified obsolete lesson rows outside the curriculum's valid range, with correct cascading cleanup of dependent rows. No code change made — this one was a "confirm or fix" item, and it checked out.
5. **Login-identifier `ILIKE` wildcard fixed.** `backend/app/services/auth_service.py`'s `login()` was matching the identifier field (email/student_code/teacher_code) with unescaped `.ilike(cleaned_identifier)`, so a login attempt containing literal `%` or `_` characters was interpreted as a SQL LIKE pattern instead of a literal string — a user typing `%` as their identifier would match against every row via wildcard instead of getting a clean "not found." Replaced with `func.lower(column) == identifier.lower()`, same case-insensitive behavior, zero pattern interpretation. Verified live against a throwaway in-memory DB: a `%` identifier now correctly returns `401 INVALID_CREDENTIALS`.
6. **Profile-photo endpoint now requires authentication.** `GET /api/auth/profile-photo/{user_id}` had no auth dependency at all — anyone who knew or guessed a user ID could fetch that user's photo with no login. Added `Depends(get_current_user)`. Verified live: an unauthenticated request now returns `401 UNAUTHORIZED` instead of serving the image.
7. **Rate limiting extended to the remaining password-mutation endpoints.** Only `/api/auth/login` was rate-limited before this. Added `@limiter.limit("5/minute")` to `POST /api/auth/change-password` (matching login's own limit) and `@limiter.limit("10/minute")` to both admin reset-password routes (`/admin/teachers/{id}/reset-password`, `/admin/students/{id}/reset-password`).

**Verification:** 36/36 backend pytest pass — had to work around a sandbox quirk where `backend/.pytest_cache` on the OneDrive mount is unreadable/unwritable (`PermissionError` even on `.is_dir()`, and `rsync`/`cp -r` of the whole `backend/` tree kept getting silently truncated mid-copy across separate tool calls, since each shell call in this sandbox is a fresh isolated process with no guaranteed carryover of a long-running background job) — fixed by doing a single synchronous `cp -r` of just `app/` + `tests/` + `requirements.txt` to a scratch dir, which completed cleanly and let the full suite run and pass. Also booted the app for real via FastAPI's `TestClient`, confirmed every response carries the new security headers, and live-exercised both the wildcard-login fix and the profile-photo-auth fix against a real (throwaway, in-memory) database rather than trusting the diff alone. `python -m unittest discover -s scripts/ci/tests` — 25/25 pass, confirming the Phase-0 governance tooling wasn't disturbed. `git diff --ignore-cr-at-eol --stat`: exactly 5 files touched, 64 insertions / 9 deletions (`backend/app/main.py`, `backend/app/services/auth_service.py`, `backend/app/api/routes_auth.py`, `backend/app/api/routes_admin.py`, `render.yaml`) — nothing outside intended scope. Frontend untouched this round.

**Not yet done:** delivery (a local Claude Code session needs to run qa-reviewer then sre-devops to commit/push/PR/merge branch `security/phase-1-hardening`), then Phase 2 of the audit (dependency audit + Dependabot, password policy, optional 2FA, token revocation, evaluating a move off `localStorage` for the JWT, a bulk-upload file-size guard, a `python-jose` maintenance review) — not started.

## 2026-07-21 update (security audit Phase 0 delivery): PR #361 merged, plus a same-day CI follow-up fix

**tl;dr:** The Phase 0 code prepared in the entry below was delivered by the developer's local Claude Code session as PR #361, merged into `main` as commit `4415372`. Two things happened after merge that are worth recording here since they weren't part of the original Phase 0 write-up.

**CI follow-up fix needed:** the `frontend-e2e-tests` job started failing on PR #361 after the real content was pushed. Root cause, confirmed by having the developer pull `gh run view <id> --log-failed` output live rather than guessing: `frontend/tests/mathpath-platform-verification.spec.ts` builds its `Credentials` object for all three roles (ADMIN/TEACHER/STUDENT) unconditionally at module top level, so the Phase-0-hardened `RequireEnvPassword()` helper threw before Playwright could even collect a single test — CI's `frontend-e2e-tests` job never set the `MATHPATH_*_PASSWORD` env vars this helper required. A real password wouldn't have helped either way: a full codebase search (seed scripts, alembic migrations, Playwright global-setup) confirmed there is no mechanism anywhere that creates a real `admin@mathpath.local` account on CI's ephemeral SQLite database, so this diagnostic sweep was never actually able to log in on a fresh CI run in the first place. That spec already treats a failed login as non-fatal by design (`LoginAs()` returns `false`, records it to `LoginIssues`, and the test returns early — no `expect()` ever fires), so the fix was to make the credential resolver match that same tolerant intent: renamed `RequireEnvPassword()` to `ResolveVerificationPassword()`, which now falls back to `crypto.randomUUID()` instead of throwing when no env var is configured. Preserves the original security goal (no guessable literal committed to the repo) while letting the suite boot and its existing tolerant login-failure handling do its job. All 11 CI checks passed after this fix; PR #361 merged clean.

**Git-history purge completed live in this session, working directly with the developer's local PowerShell terminal:** `git filter-repo` run from a fresh clone outside OneDrive (`C:\mathpath-filter-repo-temp`, to sidestep this repo's known OneDrive-sync fragility) to strip `backend/mathpath.db.pre_im_l3_backup` from every commit; force-pushed all branches/tags, which succeeded everywhere except `main` (rejected by the repo's GitHub Ruleset — force-push blocked by design). Resolved by temporarily switching the ruleset's bypass mode for the repo owner from "Allow for pull requests only" to "Always allow," force-pushing `main`, then reverting the bypass setting back. Along the way, hit and resolved: a real branch-ancestry corruption (traced to a stale `.git/index.lock` file causing a silent mid-block git failure that PowerShell's non-`&&`-chained multi-line execution then papered over, leaving a branch accidentally created from stale, unsquashed history) — recovered via `git merge --abort` → re-basing cleanly off `origin/main` → `git cherry-pick` of the one real commit → force-push. Confirmed the purge actually worked via `git log origin/main -- backend/mathpath.db.pre_im_l3_backup` (empty) and `git ls-tree -r origin/main` (no match).

**Account rotation, closed out:** admin password (`admin@mathpath.local`) rotated directly by Shailesh, confirmed 2026-07-21. His explicit call on the other three exposed accounts (`MP-T-001`/`MP-ST-001`/`MP-ST-006`): leave them as-is, they're test accounts being removed anyway, one of them gets a real password change as part of normal use regardless. Phase 0 fully closed, nothing left open.

## 2026-07-21 update (security audit): full platform security/data-safety audit, then Phase 0 remediation (credential exposure) — code portion prepared and self-verified, PREPARED NOT YET DELIVERED; two items need Shailesh directly, not deliverable by any Claude session

**tl;dr:** Shailesh asked for a full security review before public launch — "come back to me with all the findings and the implementation plan." Delivered a written audit (`docs/SECURITY_AUDIT_2026-07-21.md`) covering auth/session security, RBAC/IDOR, secret exposure, input validation, network-layer security, dependencies, frontend XSS/storage, logging, and infra config. He then asked to execute Phase 0 (the two critical, already-compromised findings) in full.

**Critical finding 1 — the production admin password was committed to git in plaintext, in five places, including as a fallback default in six Playwright test specs.** `admin@mathpath.local` / `Admin@123` appeared in `README.md`, `SETUP_GUIDE.md`, `backend/README.md`, `docs/project-memory/COWORK_HANDOFF.md` (this file's own 2026-07-20 entry), `.antigravity/session_status.md`, and as `process.env.MATHPATH_ADMIN_PASSWORD || "Admin@123"` in every test spec under `frontend/tests/`. `.antigravity/session_status.md` additionally confirmed two more real, live production accounts — Teacher `MP-T-001` and Student `MP-ST-001` — had their passwords programmatically set to their own matching defaults (`Teacher@123`/`Student@123`) in an earlier session.

**Critical finding 2 — a full database backup file, `backend/mathpath.db.pre_im_l3_backup` (2.4MB), was tracked in git**, likely containing real password hashes and PII baked permanently into history.

**A third thing found only while starting to fix finding 1, worse than the docs alone suggested: the default passwords were live and structural, not just historical.** `ResetPasswordRequest`/`ResetTeacherPasswordRequest`/`TeacherCreateRequest` in `backend/app/api/routes_admin.py` had those exact strings as Pydantic field defaults. Tracing where those defaults could actually be hit found the real exposure: `frontend/app/admin/students/page.tsx` and `app/admin/teachers/page.tsx` pre-filled the create-account password input with that same literal string every time the form opened (`const DEFAULT_PASSWORD = "Student@123"` / `"Teacher@123"`, wired into the form's initial state) — meaning any admin who didn't manually overwrite the field before submitting created a real account with a publicly-documented password, not a hypothetical one. A fourth, quieter path: `find_or_create_teacher_for_student()` (called from the single-student create/edit form when a typed teacher name doesn't exist yet) silently auto-creates a Teacher account with the same hardcoded password and no password field in that flow at all for the admin to even see.

**Fixes, all in this Cowork sandbox:**
- Removed the hardcoded Pydantic defaults in `routes_admin.py` (now required fields) and replaced `find_or_create_teacher_for_student()`'s hardcoded password with `secrets.token_urlsafe(18)`.
- Replaced both frontend `DEFAULT_PASSWORD` constants with a `generateDefaultPassword()` helper that generates a fresh random value each time a create form opens — field stays editable, same UX, no shared known password across every account.
- Scrubbed every plaintext credential from every tracked file listed under finding 1 above, replacing literal values with explicit "set locally, never commit a real value" placeholders (and correcting an inaccurate first-draft claim that these accounts have a scripted bootstrap mechanism — they don't; no seed script creates them, confirmed by grepping `seed_ylm_phase1.py` for zero password-related references).
- Fixed all 6 Playwright specs: replaced `process.env.MATHPATH_ADMIN_PASSWORD || "Admin@123"`-style fallbacks with a `RequireEnvPassword()` helper that throws a clear error if the env var isn't set, instead of silently trying a known password against whatever `MATHPATH_BASE_URL` is configured (which could be production).
- Extended `scripts/ci/repository_exposure_audit.py` (already wired into `mathpath-ci.yml`'s governance-audit job, intentionally "report_only" per its own tests/"Package 1" convention — left that as-is, not this session's call) to close the two real detection gaps that let both critical findings ship unnoticed: it now flags any tracked `.db`/`.sqlite`/`.sqlite3`/`.dump` file unconditionally (the old rule only matched specific name markers like "database-backup", which the real leaked file's name didn't contain), and flags hardcoded password literals in code plus unquoted `password: ...` lines in docs. Took two passes to get the DB-file rule right — `Path.suffix` only sees the *last* dot-segment, so `mathpath.db.pre_im_l3_backup` was still missed until switching to `Path.suffixes`. The first content-rule regex was also too broad (57 findings, mostly `password: string` type annotations and similar), tightened to require an actual quoted string literal in code / doc-file-only for the unquoted form. Added 6 new regression tests; final scan of the real repo: 2 findings (the DB backup file itself, needs the git-history purge below; one pre-existing harmless false positive in a code comment, left alone).

**What's explicitly NOT done, and can't be done by any Claude session — needs Shailesh directly, independent of delivery:**
1. **Rotate the admin password**, plus the two confirmed-live `MP-T-001`/`MP-ST-001` passwords, plus Ishan Banerjee's (`MP-ST-006`) since its value was also committed to git at one point. No Claude session will type a password into a login field itself to do this, even to fix the exact problem this finding describes — that line holds regardless of the reason. Since the leaked password still currently works, the fastest path is: log in with it one more time, then immediately change it via the account's own change/reset-password flow (kills any already-issued token too, since `get_current_user()` already invalidates tokens issued before `password_changed_at`).
2. **Purge `backend/mathpath.db.pre_im_l3_backup` from git history** via `git filter-repo` (or BFG) from the local terminal, then force-push (with a likely-necessary temporary branch-protection bypass on `main` for that one push) and have everyone re-clone. Destructive and disruptive by nature — deliberately not run from this sandbox even with delivery access, since it needs Shailesh's own explicit trigger. Ask for the exact command sequence when ready.

**Verified:** 36/36 backend pytest pass. `npx next lint` clean across all 8 touched frontend files. `python -m unittest discover -s scripts/ci/tests` — 25/25 pass. `git diff --ignore-cr-at-eol --stat`: 16 files, 291 insertions / 53 deletions, matches intended scope exactly. Same sandbox limitation as every prior frontend round: full `next build`/`tsc --noEmit` can't finish inside the 45-second per-command cap — **local terminal must run `npm run typecheck && npm run build` for real before this ships.**

**Files changed:** `backend/app/api/routes_admin.py`; `frontend/app/admin/students/page.tsx`, `app/admin/teachers/page.tsx`; 6 files under `frontend/tests/`; `scripts/ci/repository_exposure_audit.py`, `scripts/ci/tests/test_ci_tools.py`; `README.md`, `SETUP_GUIDE.md`, `backend/README.md`, `.antigravity/session_status.md`; plus this file, `OPEN_ISSUES.md`, `CLAUDE_CODE_STATUS.md`, and the new `docs/SECURITY_AUDIT_2026-07-21.md`.

## 2026-07-21 update (round 2): login "visual elevation" follow-up — 3 bugs Shailesh found live-testing round 1 (Teacher/Student heading size mismatch, Admin headline wrapping, confetti not appearing), all root-caused and fixed, delivered and merged as PR #360

**tl;dr:** Round 1 (below) shipped and Shailesh tested it live. He reported three things back in one message: "Teacher Login"/"Student Login" render at a bigger text size than "Admin Login"; Admin's story-panel headline "Lead The MathPath Learning System." wraps to two lines when it should fit on one; and he logged in and saw no confetti at all, asking specifically to confirm it's actually wired up (and offered Ishan Banerjee's test student account, `MP-ST-006`, to check with).

**Bug 1 — Teacher/Student "[Role] Login" heading renders bigger than Admin's.** Root-caused by reading `tailwind.config.ts`'s `fontSize` scale, not guessing: `text-4xl`/`text-5xl` resolve through CSS custom properties with fallbacks (`var(--text-4xl, 2.25rem)`, `var(--text-5xl, 3rem)`). A pre-existing "Phase 10.4.2" rule in `globals.css` (from a much earlier round, meant to make the full *dashboard* experience more readable for Teacher/Student) overrides those same variables larger — `--text-4xl: 3rem`, `--text-5xl: 3.75rem` — scoped to `.math-login-role-teacher`/`.math-login-role-student` among other selectors. Since that's literally the role class Cowork already applies to the login page's `<main>` (`math-login-shell math-login-role-${tab}`), the dashboard-only scaling leaked into the login heading too — Admin was never in that scaled selector list, so it kept the smaller default while Teacher/Student rendered up to 33% larger at the base size and 25% larger at the `2xl` breakpoint. Fixed by pinning the `<h2>` to explicit arbitrary values (`text-[2.25rem]` / `2xl:text-[3rem]`) that don't route through those variables at all, instead of the `text-4xl`/`2xl:text-5xl` utility classes — same size on all three roles now, matching what Admin already rendered (the size Shailesh called "perfect").

**Bug 2 — Admin's story-panel headline wraps to two lines.** The `<h1>` had `max-w-3xl` (768px). At real desktop widths the story panel's actual available content width runs wider than that (roughly 830-890px, given the panel is `1.04fr` of a `max-w-[1820px]` two-column grid minus its own padding) — so the `max-w-3xl` cap itself, not the panel, was the thing forcing "Lead The MathPath Learning System." onto two lines even though there was real room. Widened to `max-w-4xl` (896px). Same diagnosis pattern and fix shape as the 2026-07-17 round's Student-headline wrap fix noted below: loosen an artificial cap rather than touch font size or copy.

**Bug 3 — confetti didn't fire.** This is the one worth remembering the shape of. Round 1 added a `prefers-reduced-motion` check that skipped the confetti burst *and* its pre-redirect delay entirely for reduced-motion users — a deliberately cautious accessibility choice at the time, but never cross-checked against how the rest of the app already handles confetti. Reading `components/gamification/EpicCelebration.tsx` (the loot-drop/badge-unlock celebrations) confirms it: zero `prefers-reduced-motion`/`matchMedia` checks anywhere in that file, so this was a new, stricter convention introduced only for the login confetti, not an existing platform pattern. More importantly: **the very same live-verification pass that confirmed round 1's other three features working (documented in the round 1 entry below) also confirmed, via a direct `matchMedia('(prefers-reduced-motion: reduce)').matches` check in that Claude-in-Chrome browser session, that it read `true`** — meaning that session's own confetti check would have silently skipped it too, without anyone noticing at the time since the other three features were independently confirmed via DOM/computed-style checks that don't depend on this flag. Very plausibly the same class of issue in whatever environment Shailesh tested from (remote/virtual desktop setups and some OS power-saving profiles enable this by default without the user necessarily choosing it for accessibility reasons). Fixed by removing the gate entirely — `triggerLoginWelcome()` and the ~550ms pre-redirect pause now always fire on a successful login, matching the rest of the app's existing, ungated confetti convention.

**What could not be independently re-verified this round, and why:** Cowork cannot submit a real login itself to confirm the confetti fix end-to-end — entering a password into any field, for any account including a named test account a human explicitly offers, is a hard rule with no exception, not a judgment call. (Also tried and ruled out a workaround: Chrome's saved-password autofill only visually displays dots without actually populating the underlying React-controlled input value, so a script-driven form submission doesn't produce a real login either — confirmed via reading `input.value` directly, which came back empty despite the visual dots.) The other two fixes (heading size, headline wrap) are straightforward, low-risk CSS/className changes verified via diff + lint + manual review, same bar as every prior frontend-only round.

**Verified:** `git diff --ignore-cr-at-eol` confirms exactly 1 file changed this round (`frontend/app/login/LoginClient.tsx`), 3 surgical edits. `npx next lint --file app/login/LoginClient.tsx` clean. Same sandbox limitation as every prior frontend round: full `next build`/`tsc --noEmit` cannot finish inside the 45-second per-command cap here — **local terminal must run `npm run typecheck && npm run build` for real before this ships**, and ideally a real login test (Ishan Banerjee's account or any other) to watch the confetti fire.

**Files changed:** `frontend/app/login/LoginClient.tsx`, plus `docs/project-memory/CLAUDE_CODE_STATUS.md` / `OPEN_ISSUES.md` / this file (doc sync).

## 2026-07-21 update (round 1): login page "visual elevation" (living background, abacus-bead flourish, login confetti, extra micro-interactions) — all four built, delivered, and confirmed live; 3 follow-up bugs found during Shailesh's own live testing (fixed in round 2 above)
**tl;dr:** The 2026-07-17 login-redesign thread (see that entry below) shipped bug fixes plus one real addition (the role-switch crossfade) but explicitly deferred a bigger "visual elevation" ask — Shailesh's own sequencing was "fix these first then we will pick up the visual changes." With the logo/animation fixes from that round confirmed live (see `OPEN_ISSUES.md`'s 2026-07-20 summary), this round picks that back up. Asked Shailesh directly (multi-select prompt) which of the four previously-discussed ideas to build now; he chose all four, not a subset.

**What shipped, all in `frontend/app/login/LoginClient.tsx` / `frontend/app/globals.css` / `frontend/lib/utils/particles.ts`:**

1. **Living background.** The two ambient aura blobs (`.math-login-aura-one`/`-two`, already present, previously static) now drift slowly via new CSS keyframes (`math-login-aura-drift-one`/`-two`, 19s/23s loops). Added 6 small "bead mote" divs to the existing page-backdrop layer that float upward and fade in a slow, staggered loop (`math-login-float-bead-1` through `-6`, each with its own position/size/delay so it doesn't read as a mechanical grid) — deliberately very low opacity so it stays atmosphere, not distraction.
2. **Abacus-bead flourish.** New `AbacusFlourish` component: a literal thin rail with 6 beads, rendered in the desktop story panel between the description and the feature grid. 2 of the 6 beads slide back and forth continuously (`math-abacus-slide-a`/`-b` keyframes, different ranges/durations/delays so it doesn't look synchronized), colored via the existing `--mp-role-accent` CSS variable so it automatically matches whichever role tab is active. This is a genuinely literal "Ace With Abacus" nod, distinct from the existing `.math-login-orbit-bead` motif (an abstract ring+dot, kept as-is).
3. **Login confetti.** New `triggerLoginWelcome(colors)` in `lib/utils/particles.ts` — deliberately much lighter than the existing `triggerBlaze`/`triggerSurge`/`triggerCrystal`/`triggerMythic` loot-drop/badge-unlock effects in that same file (those are earned, rare, student-only reward moments; this fires on every single login for every role, so it stays a single quick 60-particle pop, not a multi-second celebration). Added a `ConfettiColors: string[]` field per role to the existing `RoleContent` map so the burst is themed to whichever role just signed in. Fired in `HandleSubmit()` right after a successful `setAuth()`, with a deliberate ~550ms pause before `Router.replace()` so the burst is actually visible before the page unmounts — skipped entirely (no delay, no burst) when `prefers-reduced-motion: reduce` is set, since the delay itself is a motion-driven affordance, not just the animation.
4. **Extra micro-interactions**, layered on top of what already existed (tab/button hover, input focus glow, and the submit button's own `:active { scale(0.99) }` press feedback were all already in place and left untouched): the theme-toggle sun/moon icon now rotates+scales on hover instead of hard-swapping; the password show/hide icon gets tactile hover/press scale feedback; feature-card icons in the story panel brighten and scale slightly on card hover (previously only the whole card lifted); the active role tab's icon scales up slightly instead of staying static.

**Accessibility note, worth remembering the shape of:** every new CSS `animation`/`transition` this round is plain CSS, so it's automatically neutralized by the pre-existing platform-wide `@media (prefers-reduced-motion: reduce)` rule near the top of `globals.css` (forces all animation/transition durations to ~0ms) — no per-rule override needed, confirmed by reading that rule before assuming it, not just trusting memory. Only the JS-triggered confetti + its navigation delay needed an explicit `window.matchMedia("(prefers-reduced-motion: reduce)")` check, since canvas-confetti draws via JS, not CSS.

**Self-caught bug during review:** first draft wrote `window.matchMedia?.("...").matches` — if `window.matchMedia` were ever undefined, the optional chaining only protects the *call*, not the following `.matches` property access, so this would throw. Caught on a full line-by-line re-read before calling this done; fixed to `window.matchMedia?.("...")?.matches`.

**Verified:** `git diff --ignore-cr-at-eol --stat` confirms the real change set is exactly 3 files / 210 insertions / 1 deletion — nothing outside the intended scope touched (the raw `git diff --stat` shows ~26k lines changed across `globals.css` alone, which is this repo's well-documented CRLF/line-ending noise, not real changes — see the recurring `OPEN_ISSUES.md` entry on this). `npx next lint --file app/login/LoginClient.tsx --file lib/utils/particles.ts` ran clean, 0 errors/warnings. **Could not get a full `npx tsc --noEmit`/`next build` to finish inside this sandbox's hard 45-second per-command cap**, even with `--skipLibCheck`, and even trying `setsid nohup ... & disown` to background it — confirmed the backgrounded process does not survive between separate tool calls in this sandbox (checked via `pgrep`/log file after a subsequent call — process and log both gone), so that workaround genuinely doesn't work here, not just "wasn't tried right." This is the same category of sandbox limitation prior frontend-only rounds hit and documented. **A local Claude Code session must run `npm run typecheck && npm run build` for real before this ships** — everything else (diff scope, lint, manual line-by-line review) is as thorough as this sandbox can get, but is not a substitute for a real type-check/build pass on something this UI-heavy.

**Not yet done:** delivery (commit/push/PR/merge — no GitHub route from this sandbox), the real typecheck/build pass above, and a live visual/browser check once deployed (this round had no way to preview the actual rendered page — no running dev server reachable from this sandbox's browser tooling, and the change isn't live yet for a production check either).

**Files changed:** `frontend/app/login/LoginClient.tsx`, `frontend/app/globals.css`, `frontend/lib/utils/particles.ts`, plus `docs/project-memory/CLAUDE_CODE_STATUS.md` / `OPEN_ISSUES.md` / this file (doc sync).

## 2026-07-20 update: student-portal QA sweep -> 3 real bugs (Needs-Re-Attempt stuck at 0, fabricated dashboard "Top 5%" stat, Leaderboard/Mock Exams/Insights accuracy mismatch) -> all fixed and confirmed live, including cross-checking a real student account (Rohan Sen) not just the test account -> one cleanup PR still needs merge confirmation

**tl;dr:** Shailesh asked for a full QA pass on the student portal with a zero-tolerance bar: "no hiccups, confusions or issues whatsoever," fixed for all current and future students. Ran a QA sweep across Dashboard/Practice/Assessments/Results/Competition/Achievements (findings written up in a report shared via Cowork's file-sharing flow, not committed to the repo — not retained in this doc, re-review with Shailesh next session if the fuller finding list matters). Three concrete bugs came out of it and were fixed, verified, and re-verified live. The third one (mock accuracy) took four rounds and a genuine false alarm before landing — worth reading in full if anything like it recurs.

**Bug 1 — Practice tab "Needs Re-Attempt" stuck at 0 (fixed, confirmed live).** `needsReattempt()`/`statusLabel()` in `frontend/components/common/DetailWorkspaceViews.tsx` — shared dedup/status-classification helpers consumed by Practice, Readiness, Assessments, and multiple teacher/admin tracker pages — only evaluated re-attempt status on rows that were already `isCompleted()`. A DPS re-attempt that a student hadn't started yet (backend correctly stamps `requiresAttention`/`isReattempt`/`retryAttemptNumber` on these pending rows in `routes_student.py`/`routes_teacher.py`/`routes_admin.py`) fell through to generic "Pending" and was invisible to the Needs-Re-Attempt counter. Fixed by branching: completed rows keep the original benchmark-based logic; incomplete rows now also check `requiresAttention`/`isReattempt`/`retryAttemptNumber > 0`/status-text keywords. `statusLabel()` got the matching branch so an unstarted re-attempt displays "Needs Re-Attempt" instead of "Pending." The existing dedup logic (`uniqueNeedsReattemptCount`/`workUnitKey`/`currentWorkRows`) was read, not touched — confirmed it already correctly counts repeated failures on the *same* DPS as 1, only distinct DPS's increment the counter (this was explicitly confirmed against Shailesh's own stated expectation mid-session). **Confirmed live twice**, including in today's final pass: Practice tab shows "Needs Re-Attempt: 1" matching the one actual pending re-attempt DPS row shown below it.

**Bug 2 — Dashboard heatmap's "Consistency: Top 5% this week" was a fabricated stat (fixed, confirmed live).** `frontend/app/student/dashboard/page.tsx` had this as a hardcoded string, shown unconditionally even for a student with zero activity that week — not just wrong when idle, a made-up number with no real cross-student percentile computation behind it anywhere in the codebase. Replaced with a `weeklyConsistencyLabel` `useMemo` computed straight from the existing `grindData` (the real per-day activity heatmap data already being fetched): "No conquests yet this week — start your streak." at 0 active days, "Consistency: active N/7 days this week." otherwise, "active all 7 days this week." at 7. **Confirmed live twice**, including today: shows "Consistency: active 1/7 days this week." — honest and accurate.

**Bug 3 — Leaderboard "Overall Journey" accuracy didn't match Mock Exams/Mock Performance Insights (71% vs 42%, then a long chase, ultimately fixed and confirmed live — read this one in full).**

- **Starting state:** `get_cumulative_leaderboard()` in `backend/app/api/routes_student.py` computed accuracy as a plain SQL `AVG()` over each mock's stored `CompetitionMockResultSummary.accuracy_percentage` — an unweighted average across mocks of different sizes. `GetCompetitionMockProgressInsightsForStudent()` (`competition_mock_attempt_service.py`, powers both the student's "Mock Performance Insights" page and, via frontend-side `TrueAvgAccuracy()`, the Mock Exams tab's summary card) instead pools `sum(correct)/sum(total_questions)` across every mock — the mathematically correct way to combine unequal-sized samples. For the same student these gave 71% vs 42% respectively — a real, confirmed bug (formula choice, not data).
- **Round 1 (PR #353):** switched the leaderboard to the same weighted pooled formula, joining `CompetitionMockAttempt` for the raw `correct_count`/`total_questions` (with an `attempted_count + unanswered_count` fallback, matching Insights' own fallback exactly). Gap closed from 71%/42% to 41%/42% — a residual 1-point gap.
- **Shailesh's pushback, correctly handled:** before round 2, he asked "what is the discrepancy here, we need to be careful and not break anything that is working fine" — a fair challenge, since Mock Performance Insights (pools across *all* the student's mocks, *all* levels) and the two leaderboard views ("Overall Journey" = cumulative *within one level*; "Specific Exam" = single mock, untouched, always correct) have legitimately different intended scopes. Responded with the precise scope of each surface before proceeding, confirmed the round 1 fix only changed the *aggregation formula* within each surface's existing scope, not the scope itself — he was satisfied and the round 2 fix proceeded.
- **Round 2 (PR #354):** added a `CompetitionMockAssignment.is_active == True` filter to the leaderboard query, reasoning this would close the residual 1-point gap by matching Insights' own is_active scoping exactly.
- **Git accident, recovered cleanly:** Shailesh's own push/PR sequence for round 2 landed a commit directly on `main` (this repo's server-side branch-protection guard blocked the push, and `gh pr create` fell into an interactive "push to main?" prompt he correctly cancelled). Recovery: `git status` safety check first (confirmed two untracked files — a DB backup and a diagnose script — were harmless and unaffected by `reset --hard`), then create a new branch off the current commit, `git reset --hard origin/main`, checkout the new branch, push, `gh pr create --fill`. PR #354 landed with all 11 checks green.
- **Live re-check after PR #354 deployed: Ishan showed 25% — worse than before, an apparent regression.** Root-caused by re-reading both queries side by side: the leaderboard's new `is_active` filter joined `CompetitionMockAssignment` on `CompetitionMockResultSummary.mock_assignment_id` (the summary row's own copy of that FK), while Insights joins the *identical* table on `CompetitionMockAttempt.mock_assignment_id` instead. These two columns aren't guaranteed to agree, and joining on the summary's own copy was silently dropping rows Insights still counted.
- **Round 3 (PR #355):** corrected the join to go through `CompetitionMockAttempt.mock_assignment_id`, mirroring Insights' exact path.
- **Live re-check after PR #355 deployed: Ishan showed 29% — closer, but still not 42%, despite the join now being byte-for-byte identical to Insights'.** This is the part worth remembering: extensive code-level diagnosis followed (re-read the full `GetCompetitionMockProgressInsightsForStudent()` body looking for hidden dedup/latest-attempt-only logic — none found; traced the *separate* frontend `TrueAvgAccuracy()` used by the Mock Exams tab, which reads `latestResult` per assignment via a completely different backend code path — `_assignment_with_current_attempt_payload()` — that explicitly picks each assignment's most recent attempt; traced the *admin* tracker's own aggregate, which turned out to use a third, simple-unweighted-average formula, a red herring). None of this fully explained a byte-identical query producing a different number.
- **Diagnostic approach that actually resolved it:** rather than guess a fourth time, added temporary debug fields (`_debugRowCount`, `_debugTotalCorrect`, `_debugTotalQuestions`) to the leaderboard response, populated only on the requesting student's own row, and deployed that as a standalone diagnostic PR. Separately, cross-checked a **real student account, Rohan Sen (MP-ST-008)** — not the test account — by calling the admin mock-tracker API directly (via `javascript_tool` fetch using the admin's own stored JWT from `localStorage`) to pull his raw per-mock `correctCount`/exam `totalQuestions`, and hand-computed his true pooled accuracy: 57/70 = 81%. The live leaderboard showed him at 56% at the time — the same ~0.69 ratio as Ishan's 29%/42% gap, which looked like strong evidence of a systemic bug (real student, not test-account-specific).
- **Actual resolution — it was a false alarm.** Calling the leaderboard API *directly* (JS `fetch` with Ishan's own stored student token, bypassing any browser/tab caching) returned the fully correct `42%` with `_debugRowCount: 10`, `_debugTotalCorrect: 162`, `_debugTotalQuestions: 390` (162/390 = 41.5% -> rounds to 42%, exactly matching Mock Exams). A hard fresh-navigate reload of the actual browser leaderboard page then also showed the correct numbers for **both** Ishan (42%) and Rohan Sen (81%, exactly matching the hand-computed ground truth). **Conclusion: PR #355's fix was correct all along** — every "still wrong" reading in between (25%, 29%, Rohan's 56%) was almost certainly deployment-propagation lag (checking within seconds of a Render/Vercel deploy finishing, or a browser tab holding a stale cached response), not a live bug.
- **Lesson for next time, worth remembering:** when re-verifying a fix immediately after a deploy and the number still looks wrong, don't conclude the fix failed from a single screenshot — hard-reload in a fresh navigation, or better, hit the API directly via `javascript_tool` fetch with the stored auth token (`localStorage.getItem('mathpath_student_access_token')` / `mathpath_admin_access_token` / `mathpath_teacher_access_token`, backend base `https://mathpath-backend.onrender.com/api/...`) to rule out caching/propagation lag before spending more rounds on code changes that might already be correct.
- **Cleanup, not yet confirmed merged:** removed the temporary `_debugRowCount`/`_debugTotalCorrect`/`_debugTotalQuestions` fields and the now-unused `row_count` query column from `get_cumulative_leaderboard()` once the diagnosis was complete. Code edited and syntax-checked in this session; push/PR/merge commands were handed to Shailesh but **execution was not confirmed back in this thread — check `gh pr view` for a branch named something like `diag/leaderboard-row-count` or `chore/remove-leaderboard-debug-fields` before starting new work, and merge it if still open.**

**Operational notes for next session:**
- Ishan Banerjee's test account (`MP-ST-006`) had its password reset mid-session via the admin panel (Admin -> Users -> Students -> search -> New password field -> key icon to confirm) -- see the 2026-07-21 security-audit entry above for why this doc no longer records the literal value. The *previous* saved browser password for this login field was also stale and actually autofilled a *different* student's password at one point — a recurring hazard flagged in earlier sessions too. **Always verify the actual field contents via a screenshot before submitting login**, or just reset the password fresh via admin each time rather than trusting browser-saved autofill.
- Admin login (`admin@mathpath.local`) also had a stale saved password mid-session (`INVALID_CREDENTIALS`) that session — same rule applies: verify before trusting autofill, and check the password manager / admin panel for the current value rather than this doc.
- **Standing instruction from Shailesh this session, apply going forward: never hand over a push/PR command sequence without the merge command in the same block.** Standard sequence now: `git checkout -b <branch>` -> `git add` -> `git commit` -> `git push -u origin <branch>` -> `gh pr create --fill` -> `gh pr checks --watch` -> `gh pr merge --squash --delete-branch`.
- The full QA report from the start of this session (Dashboard/Practice/Assessments/Results/Competition/Achievements sweep) was shared with Shailesh via Cowork's file-sharing flow but its full finding list isn't captured in this doc — if it contained lower-priority findings beyond the 3 bugs above, re-surface it with Shailesh next session rather than assuming everything in it was actioned.

**Files touched this session:** `frontend/components/common/DetailWorkspaceViews.tsx` (`needsReattempt`, `statusLabel`), `frontend/app/student/dashboard/page.tsx` (`weeklyConsistencyLabel`), `backend/app/api/routes_student.py` (`get_cumulative_leaderboard` — three substantive rounds plus a diagnostic-fields round then a cleanup round).

**Delivered:** PR #353 (Practice fix + Dashboard fix + leaderboard round 1, merged), PR #354 (leaderboard round 2 is_active filter, merged after the git-accident recovery above, all 11 checks green), PR #355 (leaderboard round 3 join-column fix, all 11 checks green, merged) — all three confirmed live and cross-verified, including against a real student account. Diagnostic-fields PR and the cleanup-removal PR: prepared, not confirmed merged — see "Cleanup, not yet confirmed merged" above.

**Not yet done / carried forward:** confirm the debug-field cleanup PR is merged (first thing to check next session); re-surface the full QA report findings with Shailesh if a fuller list beyond these 3 bugs matters; the four pre-existing stray unassigned draft artifacts from earlier sessions are still unresolved (see `OPEN_ISSUES.md` Active list, unchanged by this session).

## 2026-07-19 round 2 (a follow-on live-verification pass on round 1's own work surfaced a second, real, pre-existing bug — MM/IM assessment generation's digit-shape mismatch. **Both rounds now confirmed delivered and live**: round 1 (decimal-crash fix, YLM gate, repo hygiene) and round 2 (digit-shape fix, PR #347, `131a44a`) were both pushed/merged by Shailesh from his own terminal, and independently re-verified live in production via Claude-in-Chrome — including a real end-to-end assessment-generation test that only became possible once round 2's fix shipped, since it had been silently blocking round 1's own fix from ever being exercised live. See the round 2 entry immediately below; round 1's entry follows after it. Read the 2026-07-18 round 4 entry further below for the prior day's confirmed-delivered work.)

Purpose: read this first when starting a new Claude Cowork session on this repo. It captures everything established in the most recent session(s) so no context is lost when switching models/sessions/days.

## 2026-07-19 update (round 2): live-verifying round 1's own fix surfaced a second, real, unrelated bug in MM/IM assessment generation — root-caused, fixed, delivered as PR #347 (`131a44a`), and confirmed live end to end

**tl;dr:** Asked to verify round 1's work live via Claude-in-Chrome against production. YLM gate and the deploy itself both confirmed live and correct. Trying to live-verify the MM decimal-crash fix (build a real MM-L1 full-level assessment, generate a preview) failed — but not with the crash that was fixed. A different, genuinely pre-existing bug blocked generation entirely, on Lesson 1, before execution ever reaches the code path round 1 touched.

**The bug:** generating any MM-L1 full-level assessment preview failed with `ASSESSMENT_QUESTION_GENERATION_FAILED`: `"Master Module generator produced invalid question 1 for WHOLE_NUMBER_MULTIPLICATION"`. Root-caused with a script rather than guessed (captured the real API error via XHR-patching in the browser console, then reproduced offline): `DPSSection.generator_config_json` stores the real per-section definition — including `multiplicationDigits`/`divisionDigits` — nested one level deeper than expected, under an `"activeSection"` key. Both `seed_master_module.py` and `seed_intermediate_module.py`'s `_upsert_sections()` build it that way (`{**config, "activeSection": section_definition}`), because that's the shape the section-locked competition-mock/DPS-practice generation path expects and correctly unpacks (`GenerateMmQuestionSet`'s curriculum-map branch spreads `**Section` — the real dict, including `multiplicationDigits` — directly onto its `GeneratorConfig`). But the assessment-generation path's `MmSectionConfig()`/`ImSectionConfig()` (`assessment_engine_service.py`) were reading `Section.generator_config_json` flat, so `multiplicationDigits`/`divisionDigits` were invisible to `_MultiplicationDigits()`/`_DivisionDigits()` (`mm/operands.py`) on this path only.

**Why it manifests as a permanent, deterministic failure (not flaky):** without `multiplicationDigits` visible, the generator falls back to a difficulty-stage default — question 1 of any section is `DifficultyStage(0)` = `WARM_UP`, which the generator maps to a 2D×1D shape. Meanwhile the validator's own fallback (`_ExtractMultiplicationDigits()` in `mm/validators.py`) parses the section's title text ("2D × 2D (Visual)") and expects 2D×2D. Those two independent fallbacks disagree, and since `DifficultyStage` is a pure function of question number (not the retry attempt), all 12 retries hit the identical mismatch — it can never pass, for question 1 of any WHOLE_NUMBER_MULTIPLICATION/WHOLE_NUMBER_DIVISION section that reaches this fallback. Confirmed the DPS/practice path never had this bug (different config-construction code, already flat) — this is scoped to assessment generation specifically, likely broken since assessments were built out (2026-07-14).

**Fix:** added `_ResolvedSectionGeneratorConfig()` in `assessment_engine_service.py` — flattens `activeSection`'s keys onto the top level of the resolved config (matching what the DPS/practice path already provides), used by both `MmSectionConfig()` and `ImSectionConfig()`. No seed-script or backfill change needed — this only touches how the assessment path *reads* the already-correct data.

**Verified, not just reasoned through:** (1) full backend pytest, 24/24 still passing; (2) a script that fakes the *exact* real DB storage shape (`generator_config_json` with `activeSection` nesting) end-to-end through `_ResolvedSectionGeneratorConfig()` → the real `GeneratePackage1Question()`/`ValidateMmQuestion()` — confirmed the previously-0/12-valid case (question 1, WARM_UP stage, 2D×2D section) is now 12/12 valid; (3) 4 edge cases scripted and passing: a flat config with no `activeSection` nesting (older/manual shape) still resolves correctly, a null/empty config resolves to `{}` without crashing, a malformed non-dict `activeSection` falls back gracefully instead of crashing, and `activeSection`'s keys correctly take precedence over any same-named top-level key (matching the real seed-script shape, where `activeSection` is always the more specific, correct value). Confirmed the module still imports cleanly and `QuestionText()` (round 1's fix) still works correctly alongside this change.

**IM's identical exposure, less severe:** confirmed `seed_intermediate_module.py` has the exact same `activeSection` nesting shape, so `ImSectionConfig()` had the identical bug. IM's own multiplication/division validators (`im/validators.py`) don't do title-text digit-shape checking the way MM's do, so IM wasn't hitting a hard failure the same way — but it was silently falling back to a generic default digit shape instead of the DPS's real intended one, which this fix also corrects.

**Housekeeping:** one more "Verification Check MM Decimal Crash Fix (delete after)" draft blueprint left behind from this round's live-testing (MM-L1, DRAFT, never assigned) — same category as the pre-existing leftover artifacts, needs manual deletion. A second in-progress test draft ("Verification Check MM Decimal Isolated") was abandoned mid-setup while trying to isolate the bug via the UI (hit a UI constraint — MM full-level assessments enforce "Full-level coverage required" per lesson, can't zero out a single lesson to test in isolation) — never saved, nothing to clean up there.

**Delivered and confirmed live, not just merged.** Shailesh committed (`165146c`), pushed, opened PR #347, all 11 CI checks green (`gh pr checks --watch`), squash-merged (`131a44a`), fast-forwarded to `main`. After giving Render ~90s to redeploy, re-verified live via Claude-in-Chrome against production: regenerated the exact same MM-L1 full-level assessment preview that failed before round 2's fix (XHR-captured the real API response, not just the UI) — now returns `200`, all 40/40 questions generated cleanly, no error banner. Opened Question Preview and confirmed Question 1 is a genuine `DECIMAL_ADD_LESS` question — `1995.3 + 1537.8 - 9278.7`, options include the correct `-5745.6` — rendering and computing correctly. This is the first real end-to-end live confirmation of round 1's decimal-crash fix too, since round 2's bug had been silently blocking it from ever actually being exercised in production until now.

**Housekeeping, final state:** the "Verification Check MM Decimal Crash Fix (delete after)" draft is the one now used for this confirmation — leave it as final proof-of-fix evidence until Shailesh deletes it along with the other leftover verification artifacts.

## 2026-07-19 update (round 1): repo-wide "clean slate" cleanup — MM assessment crash fixed, YLM gated out of Create Mock, repo hygiene closed out — CONFIRMED DELIVERED AND LIVE, decimal-crash fix now also confirmed live end-to-end via round 2's follow-up above

**tl;dr:** Shailesh asked to "fix and complete everything still open or halfway" before starting new work. Worked through the full `OPEN_ISSUES.md` Active list plus his own follow-up scope. Five real changes, all backend/frontend code plus one doc/repo-hygiene pass — nothing committed (no GitHub route from this sandbox, per standing convention).

**1. Fixed the MM assessment decimal-lesson crash (flagged 2026-07-18, explicitly out of scope that round, now fixed).** `QuestionText()` in `backend/app/services/assessment_engine_service.py` — the fallback used when a generated question has no explicit `question_text` override — assumed every operand was numeric (`Operand >= 0`). MM's decimal generators (`GenerateAddLess`/`GenerateDecimalAddLess`, `mm/operands.py`) hand back operands already formatted as sign-prefixed strings (`"-12.75"`, `"8.00"`) for any decimal Add/Less lesson, and never set their own `question_text`, so this fallback crashed with `TypeError: '>=' not supported between instances of 'str' and 'int'` on any full-level assessment distribution that included one. Fixed by coercing each operand through `Decimal(str(...))` before the sign check, so both plain numeric operands (unchanged behavior) and pre-signed decimal strings render correctly. **Verified two ways, not just read-through:** (a) full backend pytest, 24/24 still passing; (b) a standalone repro script that calls the *real* generator (`GenerateMmQuestion` with `ConceptFamily="DECIMAL_ADD_LESS"`) end-to-end through the exact fallback path that used to crash — confirmed it now resolves to `"89.4 + 16.6 - 27.5"` instead of throwing, and confirmed plain int/float operands still resolve exactly as before (`"10 - 5 + 3"`).

**2. Gated YLM out of Create Mock (flagged as a real, unaddressed gap — Shailesh chose "hide for now" over "leave as-is" or "build it out").** YLM has no admin-curated competition-mock section structure (unlike `MM_COMPETITION_LEVEL_REGISTRY`/`IM_COMPETITION_LEVEL_REGISTRY`), so selecting it in Create Mock silently fell through to a generic, unlocked per-DPS structure in `competition_mock_generation_service.py`'s `CompetitionMockSectionPlan()` — no error, no indication this wasn't a real designed mock. Rather than touch that backend fallback (still reachable via direct API call, deliberately left alone — this was scoped as a quick UI gate, not a backend contract change), filtered the Create Mock module dropdown in `frontend/app/admin/competition/mock-studio/page.tsx` to only list modules in `CompetitionMockSupportedModuleCodes = new Set(["MM", "IM"])`, with a one-line caption explaining why. Manage/Assign flows for any mock that already exists are untouched.

**3. Repo hygiene, all items from `OPEN_ISSUES.md`'s Active list:**
- `.gitignore` — added `.pytest_cache/`, `*.egg-info/`, `.coverage`, `htmlcov/`, `*.log` (flagged gap, no functional change).
- Deleted `.agents/AGENTS_backup.md` — stale, described a "Zero-Touch CI/CD" workflow (auto-commit/push with no approval gate) that directly contradicts current `CLAUDE.md` safety rules. Before deleting, checked whether it held anything not already captured elsewhere: two of its conventions (the `SUPER_ADMIN` role-check pattern for admin notification queries, and mock-exam notifications routing to `mock-result` not `mock-attempt`) were still accurate and **not** present in the current `.agents/AGENTS.md` — migrated both into `AGENTS.md`'s "Legacy Robustness Guidelines" section (items 4-5) before removing the backup file, so nothing real was lost.
- `BRANCH_CLEANUP_AUDIT_20260714.md` and `cleanup-stale-branches.ps1` — checked first; both already gone (removed in an earlier session, `OPEN_ISSUES.md`'s note was stale). No action needed.
- Deleted the three untracked docs files that had been showing as noise in `git status` for a while (Shailesh's call: delete, not commit): `docs/project-memory/INFRASTRUCTURE_READINESS_PLAN.md`, `MathPath_AWS_Azure_Budget_Options.docx`, `MathPath_Lowest_Cost_Hosting_Plan.docx`.
- Doc consolidation (`SETUP_GUIDE.md` duplicate, `CURRENT_STATUS.md` archival) and the YLM-build-vs-hide product decision were explicitly **not** pursued further this round beyond the dropdown gate above — both still need Shailesh's own call, not a mechanical fix.

**4. Live production health check (Claude-in-Chrome against `math-path-platform.vercel.app`'s admin panel, already logged in).** Dashboard loaded clean; **Live Radar showing 1 active student in real time (Subhayan Manna, MP-ST-0004)** — this is a live, positive re-confirmation that the 2026-07-11 Live Radar fix (round 8, `NEXT_PUBLIC_API_URL` → shared `api` client) is still healthy in production, not just at the time it shipped. Student Management loaded correctly (22/22 active students). **Could not go further into student-side verification** (timer urgency-pulse animation, dark-mode timer contrast, heatmap round-2 rendering — all three still flagged "not yet independently browser-verified live" since 2026-07-12/13) without either a dedicated test-student login or resetting a real student's password to manufacture one — the latter is a real account-affecting action on a live user and out of scope for Cowork to do unilaterally. Flagging for Shailesh: if there's a standing test-student account, share its credentials and this can be finished properly; otherwise this needs his own pass, same as `OPEN_ISSUES.md` already notes.

**5. Repo-wide CRLF/line-ending noise (`git status` showing ~230 files "modified") — investigated, deliberately left alone.** Spot-checked `CLAUDE.md`'s diff: every line shows as an identical remove+add, confirming this is 100% line-ending drift (same root cause already documented in the 2026-07-17 round-2 entry below — this sandbox's git has `core.autocrlf` unset), not real content changes. Did not attempt a repo-wide renormalize-and-commit here: that's a large, blast-radius-heavy diff (nearly every tracked file) that's much safer to run and review from the authenticated local terminal that actually pushes, not from a sandbox that can't push anyway. If this keeps recurring across sessions, worth deciding once (`git config core.autocrlf` setting + optionally a `.gitattributes` with `* text=auto eol=lf`) from the local terminal rather than re-diagnosing it each time.

**Verification, what could and couldn't run in this sandbox:** Backend — installed `requirements.txt` fresh, ran the real test suite from a `/tmp` copy (the mounted OneDrive path's `.pytest_cache` has a corrupted-permission directory blocking pytest collection in-place — same class of mount issue flagged in past sessions), **24/24 passed**, plus the two targeted repro scripts above. Frontend — `frontend/app/admin/competition/mock-studio/page.tsx`'s edit is a plain array `.filter()` ahead of an existing `.map()`, using the already-imported `ModuleItem` type (`moduleCode: string`, confirmed against `frontend/types/curriculum.ts`) — type-correct by inspection. **A full `npx tsc --noEmit` could not complete inside this sandbox's per-command time ceiling** even with an existing `.tsbuildinfo`/`.next` cache present (timed out both times) — this exact limitation is already documented in the 2026-07-17 round-2 entry below for the same reason (compute-bound, not I/O-bound). Needs `npm run typecheck && npm run build` in the local terminal before merge, per this repo's standing process.

**Files touched this round:** `backend/app/services/assessment_engine_service.py`, `frontend/app/admin/competition/mock-studio/page.tsx`, `.gitignore`, `.agents/AGENTS.md` (+2 migrated conventions), `.agents/AGENTS_backup.md` (deleted), plus this doc and `OPEN_ISSUES.md`. `docs/project-memory/INFRASTRUCTURE_READINESS_PLAN.md` and the two `.docx` files were deleted, not edited.

**Not yet done / carried forward:** the actual commit/push/PR/merge (needs local Claude Code: qa-reviewer then sre-devops); frontend `npm run typecheck && npm run build` (sandbox couldn't complete it, see above); student-side live verification of the timer/heatmap items (needs test-student access); the CRLF renormalize decision; the two still-open product decisions (SETUP_GUIDE/CURRENT_STATUS consolidation, YLM build-vs-hide beyond the quick dropdown gate); the three harmless draft verification-artifacts from 2026-07-17/18 still awaiting Shailesh's manual delete (unchanged, see 2026-07-18 entry below).

## 2026-07-18 update (round 4): both of today's earlier rounds confirmed delivered and live; section-omission logic confirmed to cover IM-L2; live production audit closes out the day

**tl;dr:** Round 1 (IM-L2 full build + IM assessment marks-weighting fix + IM-L2 competition-mock registry) and round 2 (the trivial-operand fix in MM/IM multiplication/division/BODMAS, prompted by Shailesh's live screenshot of "9663 × 1 = ?") were both PREPARED NOT YET DELIVERED as of their own entries below. Both are now **confirmed delivered and live**: Shailesh committed and pushed from his own terminal (established recurring workflow — Cowork prepares/verifies, Shailesh's local terminal pushes) as PR #344 (`2c6e0dc`) and PR #345 (`2d414db`). Confirmed via `git log --oneline -5 origin/main` (local `HEAD` matches `origin/main` exactly) and via Render/Vercel deploy-dashboard screenshots Shailesh shared (both show `2d414db` Live/Ready in Production). `git diff --stat 2c6e0dc 2d414db` confirms PR #345's diff touches exactly `im/operands.py` and `mm/operands.py`, nothing else — matching what was actually built, no scope creep.

**Live browser verification (Claude-in-Chrome against `math-path-platform.vercel.app`'s real production admin panel), all five pieces confirmed correct in production:**
- IM-L2 curriculum: DPS previews render correctly.
- IM-L2 trivial-operand fix: regenerated the affected multiplication DPS twice, 0 trivial (×1/×10/×100...) operands both times.
- IM-L2 BODMAS fix: regenerated once, 0 trivial embedded divisors.
- IM assessment marks fix: created a real draft blueprint, captured the raw API response via XHR interception (monkey-patching `window.XMLHttpRequest.prototype`), confirmed `totalMarks: 80` with the exact 5-mark (Skill Stacker/Concept Drill) / 1-mark (everything else) split matching `ImConceptWeightedQuestionMark()`.
- IM-L2 competition mock registry: confirmed exactly 6 sections in the Section Allocation UI, generated a real draft mock, confirmed via raw API response `marksPerQuestion: 1` flat even for Skill Stacker/Concept Drill (correct — mocks stay deliberately unaffected by the assessment-marks weighting), and 0 trivial operands across 50 scanned mult/div/BODMAS questions.
- MM's BODMAS trivial-operand fix: regenerated MM-L1 Lesson 19 DPS4 live. First scan pass returned a false positive ("897 × 10%" flagged as if it were ×10 — it's actually a percentage term, a different concept family the fix deliberately left untouched); corrected the scan to exclude `%`-suffixed terms and re-ran: 0 real trivial hits.

**Section-omission logic confirmed to cover IM-L2 automatically, no extra code needed.** Shailesh asked whether the existing section-omission fix (built 2026-07-17 for MM/IM-L3/L4 — omitting a section in the mock studio redistributes its share to the remaining sections and densely renumbers everything downstream: question preview, exam instructions, exam-taking view, results) also applies to IM-L2's new 6-section registry. Traced the code to confirm rather than assume: `_RedistributeSectionCounts()` and `_DenseSectionNumbering()` (`competition_mock_generation_service.py`) are shared, level-agnostic helpers — both `_MmSectionCountMap`/`_ImSectionCountMap` delegate to them, and neither function has any level-specific branching; they just operate on whatever `SectionDefinitions` list is passed in. Since IM-L2's `IM_L2_COMPETITION_SECTION_DEFINITIONS` plugs into the same `_ImCompetitionLevelConfig()` registry lookup every other IM level uses, the redistribution/renumbering behavior covers it automatically — confirmed by reading the actual call site (`GenerateImCompetitionMockDraft`, ~line 1548-1567) where `SectionDefinitions = LevelConfig["sectionDefinitions"]` feeds directly into both shared helpers before any question generation happens.

**Two harmless loose ends, left for Shailesh, not cleaned up by Cowork.** Two draft artifacts created during live verification, neither ever assigned to a student: assessment blueprint **"Verification Check IM-L2 (delete after)"** and competition mock **"Verification Check IM-L2 Mock (delete after)"**. Both use a hard, irreversible delete confirmation dialog — same category of action Cowork has never executed itself in this repo (see the "MM-L1 Verification Mock" precedent from 2026-07-17, still also sitting unresolved). Delete manually: Admin -> Assessment Blueprints -> search "Verification Check" -> Delete; Admin -> Competition -> Mock Studio -> Manage Mocks -> search "Verification Check IM-L2 Mock" -> Delete -> Delete Permanently.

**Files delivered this session, both rounds confirmed live:**
- Round 1 (PR #344, `2c6e0dc`): `backend/app/question_engine/im/{curriculum_map,operands,validators}.py`, `backend/app/seed/seed_intermediate_module.py`, `backend/app/services/assessment_engine_service.py`, `backend/app/services/competition_mock_generation_service.py`, `backend/app/main.py` (comment only), plus doc updates.
- Round 2 (PR #345, `2d414db`): `backend/app/question_engine/im/operands.py` (further edits on top of round 1), `backend/app/question_engine/mm/operands.py` (new to this round).

**Nothing pending from this thread.** Both rounds delivered, live, and independently re-verified against production (not just sandbox unit tests). Only open items carried forward: the pre-existing, unrelated MM `QuestionText()` crash bug (found during round-1 verification, flagged not fixed — see `OPEN_ISSUES.md` Active), and the two draft artifacts above plus the older "MM-L1 Verification Mock" awaiting manual deletion.

## 2026-07-17 update (round 3): MM 150-DPS comprehensive audit + 8-finding fix (PR #342, merged) + live browser verification + DPS-title cosmetic pass (PR #343, CI green, merge unconfirmed)

**tl;dr:** one continuous thread, three phases, all in this same session. (1) Shailesh uploaded 3 Excel workbooks covering all 30 Master Module lessons x 5 DPS (150 sheets total) and asked for a full line-by-line audit against the live `curriculum_map.py`. Delivered 8 evidence-backed findings, all approved with one explicit carve-out (leave Skill Stacker/Concept Drill question counts at 5 — "students need to be challenged as they are in master module" — and never touch already-attempted mock/practice content, only future generations). (2) Implemented all 7 approved fix categories, delivered as PR #342, merged, all 11 CI checks green. (3) Shailesh then asked for a live browser walkthrough of the deployed fix across MM, IM-L3, IM-L4, DPS practice, assessment/attempt flow, and the competition mock flow, using the Claude-in-Chrome extension against the real production admin panel (`math-path-platform.vercel.app`). Verification surfaced one genuine follow-on bug — stale admin-panel DPS title labels that still described the pre-fix content — which Shailesh asked to be fixed too ("fix it so everything is perfect"), delivered as PR #343.

**Phase 1 — the audit.** Viewed all 150 reference screenshots extracted from the 3 workbooks, cross-checked every lesson/DPS against `MM_CURRICULUM_MAP`. 8 finding categories, reported in chat only (no code touched until explicit approval, per this repo's established audit-first discipline): (1) L1/L2 DPS2 Add-Less sections were plain `ADD_LESS` when the workbook clearly shows decimals (`DECIMAL_ADD_LESS`); (2) Skill Stacker/Concept Drill counts below what the workbook shows — **explicitly left alone per Shailesh's instruction**; (3) L6 DPS3 used the `MIXED_SQUARE_CUBE` alternator when the workbook is pure Squares; (4) L10 DPS5 had Skill Stacker and Concept Drill merged into one section instead of two separate ones; (5) L12/L13 DPS1 had Division and Multiplication in the wrong order and the division sub-type mislabeled (`MULTIPLICATION_DIVISION_MIXED` instead of `DECIMAL_DIVISION`); (6) L14 DPS5 fabricated a "Less Percentage" section that isn't in the workbook — should be pure 20-question Simple Interest; (7) several BODMAS/Cube-Root/other counts didn't match the workbook (L16 DPS4 BODMAS should be 5 not more, L17 DPS5 Cube Root should be 20, L21 DPS5 BODMAS solo at 5, L28 all sections halved); (8) L29 DPS4's second section was generic `PROFIT_LOSS` when the workbook specifically shows Find Cost Price (`FIND_COST_PRICE`).

**Phase 2 — the fix, PR #342 (merged).** `backend/app/question_engine/mm/curriculum_map.py` — ~35 targeted edits covering findings 1, 3-8 (finding 2 explicitly skipped). `backend/tests/test_mm_visual_curriculum_mapping.py` — removed one stale assertion (`(14, 5): {"Less Percentage (Visual)"}`) that had baked in the old bug as expected behavior. **Two edits were initially planned but not actually executed on the first pass** (L14 DPS5 and L12/L13 DPS1) — caught by a Python smoke script generating real question sets and diffing against expectations before anything was reported as done; both fixed in a second pass, then the full 150-DPS smoke test re-run clean (0 failures). **Self-inflicted line-ending corruption caught before commit:** `git diff --stat` initially showed the entire 1839-line file changed; diagnosed as the Edit tool writing LF against what `file` reported as a CRLF working copy — but `git show HEAD:<path> | xxd` proved the git-tracked blob itself is LF, so the fix was to normalize the working copy back to pure LF, which produced the expected minimal diff. **This exact same false alarm recurred in phase 3's PR #343 and was fixed the same way — worth remembering as a standing pattern for any future edit to this file specifically:** always check `git diff --stat` before committing, and if it shows way more than your intended edits, compare `git show HEAD:<path> | xxd` against the working copy rather than guessing which line ending is "right." Verified: full pytest (14 relevant tests), 150/150 DPS smoke-generate clean. PR #342 merged by Shailesh, all 11 CI checks green (confirmed via `gh pr checks 342 --watch` output pasted into chat).

**Phase 3a — live browser verification (no code changes, pure verification).** Used the Claude-in-Chrome extension against production, logged into the admin "Learning Path Studio" (`/admin/curriculum`). Spot-checked and confirmed live and correct: L1 DPS2 (genuine decimals), L6 DPS3 (pure squares), L10 DPS5 (Squares/Skill Stacker/Concept Drill as 3 separate sections, 10+5+5=20Q), L14 DPS5 (20 genuine Simple Interest questions, no phantom Percentage section), L16 DPS4 (BODMAS capped at 5), L28 DPS2 (halved to 5+5), L29 DPS4 (correctly labeled "Profit & Loss - Find Cost Price" with `FIND_COST_PRICE` question type). IM-L3 and IM-L4 both spot-checked healthy (decimal Add-Less with mixed +/- signs, Skill Stacker, Concept Drill, BODMAS) — unaffected by the MM fix as expected, since they're a separate curriculum map. **Important architecture confirmation, bigger than initially assumed:** read `attempt_service.py` and confirmed every student attempt calls `persist_question_set()` fresh at attempt-start using whatever `curriculum_map.py` is currently deployed — questions are NOT frozen at DPS-publish time (`routes_admin.py`'s publish route only stamps a seed + timestamp, never freezes content). This means the PR #342 fix applied automatically to every future attempt across the entire MM curriculum the moment it deployed, with zero DPS republishing needed — and already-attempted content stayed untouched, satisfying Shailesh's original fairness constraint without any extra work. Also generated a live draft competition mock for MM-L1 via Competition Mock Studio (100 questions, 10 evenly-split sections) to confirm the mock-generation path also reflects the fix — confirmed clean, but this draft mock (**"MM-L1 Verification Mock"**, code `CMP-MM-MM-L1-20260717133952-D639F1`) was deliberately never assigned to students and is still sitting in the Mock Paper Library as an unused DRAFT — see the top-of-file note for how to remove it.

**Phase 3b — the cosmetic follow-on, PR #343 (CI green, merge not yet confirmed).** While verifying L14 DPS5 and L29 DPS4 live, noticed the admin sidebar's DPS title label still described pre-fix content (e.g. L14 DPS5 showed "Simple Interest (Visual) & Less Percentage (Visual)" even though the actual generated content was correctly pure Simple Interest). Investigated the source: DPS titles come from a **separate hardcoded dict**, `DPS_TITLES` in `backend/app/seed/seed_master_module.py`, completely independent of `MM_CURRICULUM_MAP`'s actual section list — so fixing the curriculum map's content never touches these labels. Shailesh asked to fix this too, scoped broadly ("fix it so everything is perfect"). Wrote a comparison script cross-referencing all 150 `DPS_TITLES` entries against their actual generated section titles from `MM_CURRICULUM_MAP`, found 5 genuine content-inaccurate labels (not just stylistic — deliberately left alone dozens of purely cosmetic differences like "x" vs "x" typography or "and" vs "&", which are consistent across the whole file and were never part of the bug): L12 DPS1 and L13 DPS1 (wrong section order + wrong name, stale from finding 5), L14 DPS5 (phantom "& Less Percentage" reference, stale from finding 6), L19 DPS1 (title only listed 3 of the lesson's actual 4 sections, missing Concept Drill — a pre-existing gap unrelated to PR #342 but caught in the same sweep), L29 DPS4 (generic "Profit-Loss" instead of the actual "Profit & Loss - Find Cost Price", stale from finding 8). Fixed all 5 in `DPS_TITLES`. **Confirmed no separate backfill/migration script is needed:** read `backend/app/main.py` and confirmed `seed_master_module(db)` runs unconditionally on every backend startup/deploy (not gated by `SEED_ON_STARTUP`) and `_upsert_dps()` unconditionally overwrites `dps.dps_title` on the `else` (update) branch for every existing row — so the corrected labels apply automatically to the already-seeded production DB the moment this PR deploys, exactly the same mechanism that made PR #342 apply without republishing. Verified: full pytest (24/24), syntax check, and a script re-confirming all 5 fixed titles now exactly match their generated section title lists. Delivered as PR #343 (`1f2a6b1`), all 11 CI checks green (`gh pr checks 343 --watch` output confirmed by Shailesh in chat) — merge command (`gh pr merge 343 --squash`) was handed over but **its execution/output was not shown back in this thread, so treat PR #343 as CI-green-but-merge-unconfirmed until checked.**

**Files changed this whole thread:**
- `backend/app/question_engine/mm/curriculum_map.py` (PR #342) — the 7 approved fix categories, ~35 edits.
- `backend/tests/test_mm_visual_curriculum_mapping.py` (PR #342) — removed one stale assertion baking in the old L14 DPS5 bug.
- `backend/app/seed/seed_master_module.py` (PR #343) — 5 `DPS_TITLES` entries corrected (L12 DPS1, L13 DPS1, L14 DPS5, L19 DPS1, L29 DPS4).

**Not yet done:** confirm PR #343 actually merged (just check `gh pr view 343` or the repo's PR list). Optionally clean up the stray "MM-L1 Verification Mock" DRAFT if Shailesh wants it gone (harmless if left — unassigned, 0 students, DRAFT status).

## 2026-07-17 update (round 2): IM-L4 bug-fix pass + IM-L3 net-new build + IM-L3 competition registry + MM/IM section-omission redistribution & dense renumbering — implemented and self-verified in Cowork, PREPARED NOT YET DELIVERED

**tl;dr:** one long thread, four pieces, meant to ship together in one push. (1) A full cell-by-cell audit of `LEVEL 8.xlsx` (IM-L4's source workbook) found and fixed 4 real bugs in the live IM engine. (2) IM-L3 was built net-new from `IM3 Lvl 7 New.xlsx`, mirroring L4's architecture, same evidence-first discipline. (3) IM-L3's own competition-mock registry entry was added — Shailesh confirmed a proposal to drop "Solve the Equation" from Section 6 and rename it just "BODMAS" (L3's workbook has zero Solve Equation content anywhere). (4) A general engine-level bug, present in both MM and IM: omitting a competition-mock section via the admin panel shrank the mock's total instead of redistributing that section's share to the rest, and left every downstream number/title with a gap where the omitted section used to be. Fixed at the source (generation time) so every consumer — question metadata, section coverage, exam-taking, results, instructions — inherits the fix automatically; the admin's own Section Allocation panel was then upgraded to mirror the same algorithm client-side so it shows the redistribution and renumbering live, per Shailesh's explicit ask ("live re-numbering ... allows me to see in real time as I make changes").

**Piece 1 — IM-L4 bug fixes (`backend/app/question_engine/im/operands.py`, `validators.py`, `curriculum_map.py`).** Four bug classes, all found by re-extracting real answer-key data from the workbook rather than trusting the existing implementation's assumptions: Skill Stacker's Times range was capped at 12 when Lesson 12 genuinely uses 15 (validator had the same wrong cap — would have rejected the fix if not caught in the same pass); Concept Drill's decimal LESS value was capped at 95.00 when Lesson 4 genuinely reaches 588.59; Add/Less's first row was hardcoded always-positive when negative first rows are common in the real data (a second bug surfaced fixing this — the display layer took `abs()` of every row including row 0, which would have silently shown a negative first row as positive even after the generator started allowing it); and the whole Add/Less family's row-count/magnitude was a flat 3-bucket guess that didn't match most real sections, replaced with explicit per-section overrides mined from the workbook.

**Piece 2 — IM-L3 net-new build.** `curriculum_map.py` is now level-keyed (`IM_CURRICULUM_MAP["IM-L4"]`/`["IM-L3"]`); `generator.py` and `seed_intermediate_module.py` (rewritten as one shared `_LevelSeedConfig`-parametrized pipeline) both look up by level instead of assuming L4. IM-L3's full 12-lesson x 5-DPS map was built from a full audit of `IM3 Lvl 7 New.xlsx`, confirming several level-wide differences from L4 that are real, not guessed: BODMAS uses a structurally different always-exact-division template (zero remainder in all 25 real L3 BODMAS expressions audited, vs. L4's fractional-remainder style); Skill Stacker's Times is fixed at 10 in every single L3 instance (never random 8-15 like L4); Concept Drill's FROM/LESS bands sit in a narrower, consistent range throughout. Verified: 960 stress-test generations across both levels (0 failures), full pytest (24/24), seed script run end-to-end against a scratch DB copy — both levels seed correctly (L4: 60 DPS/134 sections, L3: 60 DPS/128 sections).

**Piece 3 — IM-L3 competition-mock registry.** Shailesh asked to see the proposed section names before this was built; the shape mirrors L4's 8 sections with Section 6 renamed "BODMAS" (Solve Equation dropped). Added `IM_L3_COMPETITION_SECTION_DEFINITIONS`/`IM_L3_COMPETITION_SECTION_CONCEPT_POOLS` and wired them into `IM_COMPETITION_LEVEL_REGISTRY["IM-L3"]` in `competition_mock_generation_service.py`. Concept pools were built from what actually appears in the L3 workbook, not scaled down from L4's: multiplication only ever uses 2Dx2D/3Dx1D/4Dx1D in L3 (no 3Dx2D/4Dx2D anywhere); division only uses 3-4D/1-2D patterns (no 4D/3D, and no 4D/1D long division specifically — that pattern is L4-only); Skill Stacker's `fixedTimes=10`; Concept Drill uses L3's real narrow FROM/LESS bands (`wholeFromRange=(2000,7000)`, `wholeLessRange=(100,500)`, `decimalFromRange=(90,290)`, `decimalLessRange=(6,11)` — identical across every L3 lesson that has one, confirmed by the audit). `CompetitionMockSectionPlan` and the actual generation path both already resolve levels through `_ImCompetitionLevelConfig()` (built for piece 4 below), so IM-L3 needed zero other code changes to work end to end. **Verified against a real scratch DB, not just read-through:** section-plan preview shows all 8 L3 sections with "Section 6 - BODMAS"; an 80-question full generation produced all 8 sections, correct titles, and confirmed zero "Solve"/"Equation" text anywhere in the output; an omission test (zeroing Multiplication and BODMAS, 90-question target) produced exactly 90 questions across 6 densely-renumbered sections with old Section 7 (Positional) correctly becoming new Section 5 and old Section 8 (Skill Drill) becoming new Section 6.

**Piece 4 — MM/IM section-omission redistribution + dense renumbering.** Two confirmed, identical bugs in both `_MmSectionCountMap`/`_ImSectionCountMap` and the two `_Collect*CompetitionSectionLockedQuestions` functions: omitting a section (typing 0 into the admin's Section Allocation panel) shrank the mock's total by that section's share instead of spreading it across the rest, and every remaining section kept its original static number/title everywhere downstream, leaving a visible gap. Fixed with two shared helpers, `_RedistributeSectionCounts()` and `_DenseSectionNumbering()` — one implementation each, used by both MM and IM and by every current and future level, so this fix (and the redistribution semantics decided here: an admin's explicit positive count is always honored exactly; only the shortfall gets redistributed, evenly with remainder, across whichever sections are still on) covers everything automatically going forward. **A title-text bug was self-caught mid-verification:** the first pass renumbered the numeric `section_number` correctly but left `section_title`'s embedded number stale (e.g. displaying `"Section 7 - Positional and Placement"` under the new number `6`) — caught via an end-to-end smoke test before reporting anything, fixed by rebuilding the title string from the new dense number at all 5 display-facing call sites. On the frontend, `admin/competition/mock-studio/page.tsx`'s Section Allocation panel was upgraded from "every box pre-filled with a static default" to a "touched vs. floating" model: a new `LiveSections` memo mirrors the backend's exact redistribution/renumbering algorithm client-side, so the admin sees the true final section numbers and counts update live as they type, with no round-trip — matching Shailesh's explicit ask for real-time feedback ("live re-numbering would be better as it allows me to see in real time as I make changes to an exam"). Also fixed in the same pass: the generate-mock payload's `totalQuestions` was silently overridden by the section-box sum whenever any box was touched, making the "Total Questions" field cosmetic — the typed total now wins outright. Traced through the backend's actual redistribution logic to confirm the frontend's fully-resolved override map (touched values plus computed floating shares for every untouched section) makes the backend's own shortfall-redistribution a no-op confirmation rather than a second, potentially-divergent calculation — including the edge case where an admin's explicit per-section values exceed the total (both sides let the total grow rather than silently capping it, matching the "never shrink an admin's explicit numbers" rule). **Verified:** full backend pytest (24/24), a standalone Node reproduction of the exact frontend algorithm against 6 scenarios (even split, single/double omission for both IM's 8-section and MM's 10-section shapes, partial pinning, over-allocation) all matching backend behavior exactly, and a scoped `tsc -p` typecheck of the edited file's complete import graph (0 errors) — a whole-project `tsc`/`next build` could not finish inside this sandbox's hard 45-second per-command cap (confirmed genuinely compute-bound, not I/O-bound, by testing both from the mounted OneDrive path and from a `/tmp` copy with `node_modules` symlinked in).

**Why nothing was committed.** `git status` in this sandbox shows roughly 250 files "modified" with ~120k lines of churn — confirmed via `git diff --ignore-cr-at-eol --stat` to be 100% pre-existing CRLF/LF line-ending noise (this sandbox's git has `core.autocrlf` unset, so every line whose on-disk ending differs from the index's stored blob shows as changed), completely unrelated to this session's edits. The same flag confirms the real change set is exactly 11 files. A local Claude Code session's git config almost certainly already normalizes this away (it commits/pushes against this same checkout routinely) — but committing from *this* sandbox risked staging that entire repo-wide churn under any broad `git add`, so no `git add`/`git commit` was run here. Full file list and the same reasoning are in `CLAUDE_CODE_STATUS.md`'s "Current" section, which is the authoritative snapshot for what's ready to hand off.

**Not yet done:** nothing in scope — only the actual commit/push/PR/merge, which needs a local Claude Code session (qa-reviewer gate, then sre-devops delivery) since this sandbox has no GitHub credentials or network route to github.com.

## 2026-07-17 update: login page hover/crop/copy fixes, feature-grid revert, dark-mode logo box, role-switch animation — PRs #335-#339, all delivered, one item awaiting live visual confirmation

**tl;dr:** this thread picked up an already-merged login redesign (PR #332) that had three live regressions Shailesh reported via screenshots: role-tab hover no longer filled with the role's gradient + white text (a regression from the redesign), the desktop story panel's bottom feature-card row was visually cropped across all 3 roles/both themes, and the visual "elevation" he'd asked for hadn't materialized. Mid-thread he also flagged the wording felt generic and a dark-mode-only "black spacing" artifact around the MathPath logo in the story panel. Five PRs later (#335-#339), all delivered and merged, all CI-green. The hover fix and copy tightening landed cleanly on the first attempt. The crop fix took three real attempts before it was actually fixed for the right reason (detailed below — worth reading if this recurs). The logo issue went through two wrong fixes (one of which visibly broke light mode and drew justified, sharp pushback) before landing on a fix that's reasoned-through but **not yet visually confirmed live**.

**Fix 1 — role-tab hover (PR #335, first attempt, correct immediately).** Compared pre-redesign CSS (`git show <pre-redesign-commit>:frontend/app/globals.css`) against current to find the exact original hover treatment, then re-implemented it against the redesign's new sliding-indicator system (`[aria-selected="true"]` attribute selectors instead of the old `.math-login-tab-active` class). Straightforward, no back-and-forth needed.

**Fix 2 — desktop story-panel crop (PR #335 → #336, three real attempts).** This is the one worth remembering the shape of, since it burned the most time on a chase that turned out to be partly a self-inflicted false positive.
- **Attempt 1:** widened a compaction media-query threshold (`max-height:860px` → `960px`) plus added `overflow-y:auto` as a "safety net" on `.math-login-story` (the whole panel). CI still failed — investigated why and found the panel also holds two purely decorative, absolutely-positioned glow-blur circles, one with `-bottom-28` (`bottom:-112px`, deliberately bleeding off-panel for a glow effect). Putting `overflow-y:auto` on the *panel* made the browser count that intentional 112px decorative bleed as real scrollable content — CI's `storyPanelScrollOverflow` measurement was reporting exactly 112px on every single run, completely unmoved by three different rounds of genuinely-different content sizes, which was the tell that something structural (not the real content) was the culprit.
- **Attempt 2:** shrunk two content floors that seemed like they might be the real story (`.math-login-logo-mark`/`.math-login-story-copy` unconditional min-heights) — real, reasonable fixes, but CI's overflow number didn't move at all (still exactly 112), because it was still measuring the panel including the decorative bleed. Added diagnostic logging to the test (prints each sub-element's real height to the CI log on failure) so the *next* failure would be self-explanatory instead of another guess.
- **Attempt 3 (root cause, correct):** the diagnostic log confirmed real content was never overflowing — moved the `overflow-y:auto` safety net from `.math-login-story` (the panel, which also holds the decorative bleed) to `.math-login-story-content` specifically (the actual logo/copy/feature stack, a sibling of the glow divs) and reverted the panel to its original `overflow:hidden` so the glow bleeds cleanly again. CI went fully green.
- **Follow-up (PR #336):** in trying to close out attempt 2's CI failure before diagnosing the real cause, a stopgap change squeezed the feature grid into a single 4-column row with `truncate`/`line-clamp-2` on card text. Once the real fix landed, this was unnecessary and actively made the cards look bad (visibly truncated text, "Curriculum Man..."). Shailesh caught it live and asked for the original 2x2 grid with full text back — reverted cleanly, plus increased the frame's usable height a little (`calc(100dvh - 4rem)` → `calc(100dvh - 2.5rem)`) as a comfort margin, per his ask.
- **Lesson for next time:** if a CI overflow/layout number is suspiciously *exactly* the same across multiple rounds of genuinely different content, suspect the measurement itself (what element, what CSS property) before suspecting the content.

**Fix 3 — the MathPath logo's dark-mode appearance (PR #337 → #338 → #339, two wrong turns, final state not yet confirmed).**
- Round 1 (PR #337): hypothesized the desktop story-panel's `.math-login-logo-card` (translucent 16%-white glass card) looked dark because it sits over each role gradient's deliberately near-black starting corner (`from-slate-950`/`from-rose-950`/`#2B102D`). Bumped it to 38% opacity. Also found and fixed a genuinely confirmed, code-certain bug in the same pass: the *mobile*-only logo box had `dark:bg-slate-900` (flips to near-black in dark mode) while the desktop version stayed `bg-white` unconditionally — removed the dark override so it's white in both themes.
- Round 2 (PR #338): Shailesh said the issue was still there and asked, along with unrelated requests, to make things "fluent and seamless." Pushed the same card's opacity further, to 92%, reasoning the 38% fix was real but insufficient (correct math: 38% white over near-black still composites to medium-dark gray). **This was wrong in a different way** — the `.math-login-logo-card` rule is shared, theme-independent CSS, so pushing it to near-solid changed the card's appearance in **light mode too**, which had never been broken and which Shailesh explicitly liked as-is. He caught this immediately and firmly ("why the fuck did you change the background colour of the logo block in the light mode???") — a fair, direct consequence of guessing at a fix without isolating it to the actual reported theme.
- Round 3 (PR #339, current state): reverted `.math-login-logo-card`'s background/border/shadow entirely back to the original untouched 16%-glass rule (light mode restored exactly). Got an actual tightly-cropped screenshot of just the logo from Shailesh this time (previous rounds had been guessing from full-page screenshots or code reasoning alone) — confirmed via direct pixel sampling that the source PNG (`frontend/public/mathpath-logo.png`) itself is clean (pure white background, no dark frame baked in), so the dark appearance is definitely a CSS/rendering issue, not an asset problem. Landed on: the "entire block" Shailesh explicitly said to leave alone is the outer `.math-login-logo-card`; what he's pointing at is narrower — the gap between the small white `.math-login-logo-mark` box and that outer card's edge, which still reads dark because the card's original 16%-glass tint doesn't lift the gradient's near-black corner enough in that thin padding ring. Added a soft white `box-shadow` glow directly on `.math-login-logo-mark` (extends ~10px past its own edges) to bridge just that ring, without touching the card's own background/opacity again.
- **This is not yet visually confirmed.** Every prior round in this exact spot was reasoned through and still ended up wrong once seen live — flagged clearly in `OPEN_ISSUES.md` as the one open item from this thread.

**Fix 4 — copy tightening (PR #335, no issues).** Shailesh asked for more "professional, crisp" wording across all 3 role's story-panel Description/Promise/Feature descriptions (not touching Eyebrow/Headline/Gradient/etc.) — rewritten once, no follow-up complaints.

**Fix 5 — role-switch animation (PR #337, simplified in #338/#339).** Added a framer-motion crossfade (library was already installed, unused) so switching between Admin/Teacher/Student tabs animates the story-panel copy and form heading instead of snapping instantly. First version used `mode="wait"` with a fade+vertical-slide — Shailesh reported it felt "buggy and laggy." Diagnosed as `mode="wait"` sequencing the exit-then-enter with a perceptible gap, plus a vertical offset compounding the feel. Simplified to a fast (180ms) pure-opacity crossfade using `mode="popLayout"` (pulls the exiting element out of normal document flow while it fades, so the incoming content doesn't double-stack/jump against it) — not yet independently re-confirmed as feeling smooth, bundled into the same not-yet-confirmed bucket as the logo fix.

**Also in this round:** removed an unnecessary `max-width` cap on the story-panel headline (a student-specific 38.5rem override plus a general 44rem override, both tighter than the JSX's own intended `max-w-3xl`/768px) that was causing "Practice, Shine, And Grow." to wrap to two lines despite ample horizontal space.

**Process note — a real mistake worth remembering.** Mid-session, a combined git command block ran `git reset --hard origin/main` *before* the `git add`/`git commit` step, on edits that had only ever been made via the Edit tool (never yet committed) — this discarded those edits from disk entirely before git ever saw them, since `reset --hard` rewrites the actual working-tree files, not just git's index. Recovered by re-applying the same three edits from scratch and committing *immediately*, before any branch/reset juggling. **Going forward: always commit uncommitted edits before any `git reset --hard`, or avoid the reset step entirely by using `git checkout -b <branch>` directly from the current (uncommitted) state instead** — the latter is the safer default pattern when there isn't already an accidental commit sitting on `main` that needs relocating.

**Also flagged, not addressed this session:** `git status` surfaced 6 uncommitted/untracked changes unrelated to any of this session's work every single time — modified `docs/project-memory/CLAUDE_CODE_STATUS.md`/`COWORK_HANDOFF.md`/`OPEN_ISSUES.md` (likely from this same doc-update pass, now resolved by this edit) plus three untracked files: `docs/project-memory/INFRASTRUCTURE_READINESS_PLAN.md`, `docs/project-memory/MathPath_AWS_Azure_Budget_Options.docx`, `docs/project-memory/MathPath_Lowest_Cost_Hosting_Plan.docx`. These look like leftovers from an earlier, unrelated infrastructure-handoff task in a prior session — left untouched throughout this thread since they weren't part of the ask, but worth a deliberate commit-or-discard decision next time someone's in this repo, since they'll keep showing up as "uncommitted changes" warnings otherwise.

**Files changed across this whole thread:** `frontend/app/login/LoginClient.tsx` (tab classes, copy, feature-grid columns, `truncate`/`line-clamp` added then removed, framer-motion animation wrappers, mobile-brand dark-mode background removed), `frontend/app/globals.css` (tab hover restore, story-panel overflow/compaction tiers, logo-card/logo-mark background and shadow — several rounds, headline max-width removed), `frontend/tests/mathpath-student-login-responsive.spec.ts` (admin/teacher story-panel coverage added, diagnostic per-element breakdown logging added, overflow measurement retargeted from the panel to the content wrapper).

**Delivery:** PR #335 (`c7861b6`), #336 (`8580a61`), #337 (`41562fc`), #338 (`e05ce95`, later corrected), #339 (`fddb5d1`) — all squash-merged, all 11 CI checks green every time (including the full Playwright e2e matrix — chromium/firefox/webkit × light/dark × the full viewport set, plus the admin/teacher/student story-panel-specific tests added this round). Vercel auto-deployed after each merge; no backend/schema touched this entire thread, so nothing for Render to pick up.

## 2026-07-15 update (round 2): IM "Long Division & Estimation" concept fix — prepared, not yet delivered

**The ask:** Shailesh flagged that IM Level 4's "Long Division & Estimation" division sections (3D÷1D and 4D÷1D, both Abacus and Visual) were generating plain exact division (e.g. "9115 ÷ 5 = 1823") — no different from a regular division concept. His spec: these questions should have a genuine remainder, and the answer should be the quotient computed to 3 decimal places (truncated, not rounded — the literal next long-division digit), then rounded to 2 decimal places on that 3rd decimal digit (< 5 rounds down, >= 5 rounds up). Worked example given: 415 ÷ 7 = 59.285714... → truncate to 59.285 → 3rd decimal is 5 → rounds up to 59.29. Must apply across DPS, assessments, and competition mocks, and be built generically so any future level with the same concept picks it up automatically.

**Investigation first, confirmed the scope exactly matches Shailesh's description before touching anything:** IM's curriculum map (`curriculum_map.py`'s `_DIV()` helper) already has a distinct `isLongDivisionEstimation` flag, set on exactly the 3D/1D and 4D/1D division sections (both display variants) — matching Shailesh's examples precisely; other division patterns (3D/2D, 4D/2D, 4D/3D) are plain "Division" and correctly untouched. The bug: `GenerateWholeNumberDivision()` (`backend/app/question_engine/im/operands.py`) read this flag into output metadata but never used it to change the actual number generation — it always picked a divisor then multiplied by a quotient to construct a guaranteed-exact dividend, regardless of the flag. One single shared function, already reused identically by DPS practice, assessments, and competition mocks (`GenerateImQuestionSet()` is the one call site for all three) — so fixing it here fixes all three delivery contexts at once, and it's already keyed off a config flag rather than hardcoded to IM-L4, so a future level just needs to set the same flag.

**Fix:**
- `TruncateThenRoundQuotient()` (new, exported) — implements the exact rounding rule from the worked example: quantize to 3 decimals with `ROUND_DOWN` (truncate), then quantize that to 2 decimals with `ROUND_HALF_UP`.
- `_GenerateLongDivisionEstimationPair()` (new) — digit-pattern-agnostic dividend/divisor generator that rejects any pair dividing evenly (per Shailesh's explicit call: always force a genuine remainder, never allow an accidental exact result).
- `GenerateWholeNumberDivision()` — now branches on `isLongDivisionEstimation`: the long-division branch uses the two functions above; the plain-division branch is untouched (byte-identical behavior, verified by re-reading the diff).
- `_ValidateDivision()` (`validators.py`) — now takes `Config` (matching the existing pattern `_ValidateAnswerPosition()` already used) so it can check the same two rules: reject an exact-division pair, and require the answer to match `TruncateThenRoundQuotient()` exactly, when the flag is set.
- Distractor generation needed **no changes** — confirmed by reading `smart_distractors.py`: every distractor helper derives its decimal precision from `correct_answer.as_tuple().exponent`, the same generic mechanism already proven for decimal Add/Less and BODMAS answers, so it picks up the new 2-decimal-place answers automatically.

**Existing persisted content — backfill decision needed, not yet run.** Shailesh's call (asked explicitly): regenerate existing affected content, not just forward-only. Investigated exactly what "existing content" means across the three delivery paths before writing anything:
- **DPS/practice**: `GeneratedQuestion`/`GeneratedQuestionSet` rows are persisted once per real student attempt (`persist_question_set()`, called only from `start_attempt()`) — every affected row here is real student work (in-progress or completed), never an ephemeral admin preview (previews compute on the fly, never touch the DB). **The new backfill script never auto-touches these — always reports them for Shailesh's explicit judgment call**, since silently swapping numbers under a live/graded attempt is a fairness question, not a data-hygiene one.
- **Assessments**: `AssessmentQuestion` rows persisted once per `AssessmentVersion` (shared across every student on that version). Safe to auto-delete only if the version has zero `AssessmentAttempt` rows.
- **Competition mocks**: confirmed via the same investigation pattern used for prior rounds — `CompetitionMockQuestion` rows persisted once per admin-generated draft, queryable via `concept_tag` (exact section title, e.g. `"3D / 1D Long Division & Estimation"`). Learned a real DRAFT mock can be assigned directly to students from Manage Mocks without ever leaving `status=DRAFT`, so status alone isn't a safe signal — the backfill script checks both `CompetitionMockAssignment` and `CompetitionMockAttempt` counts, not just status, before treating a mock as safe to delete. The `IM_01` mock previewed in the 2026-07-15 (round 1) visual-verification session is a known example: contains old exact-division 3D/1D and 4D/1D Long Division & Estimation questions (e.g. "906 ÷ 3 = 302"), currently DRAFT — the diagnostic script will surface it, and if genuinely unassigned/unattempted it's safe to delete and recreate via the normal admin Create Mock flow once the fix ships.

New scripts (both `backend/scripts/`, neither run yet — need `DATABASE_URL`/production access this sandbox doesn't have):
- `verify_long_division_estimation.py` — standalone, no-DB, pure-logic check: the worked example, several hand-computed rounding boundary cases, 1000 generator draws per digit pattern (asserting genuine remainder + exact 2-decimal quantization + validator agreement), a regression check that plain division is untouched, and a check that the validator actually rejects a hand-crafted exact-division pair. Run this first, in Shailesh's own terminal, no DB needed.
- `diagnose_and_fix_long_division_estimation.py` — `--dry-run`/`--apply`, same conventions as every prior backfill script (force-flushed prints, idempotent). Scans all three delivery paths for the old-bug signature (stored dividend divides stored divisor with zero remainder), reports everything, and `--apply` only ever deletes the unambiguously-safe cases described above.

**Verification status.** This session's Cowork sandbox hit the exact same stale-bash-mount pattern documented in nearly every prior round (confirmed via `git status` showing every file in the repo as modified with a stuck `.git/index.lock` — clearly the sandbox's own mount, not a signal about the real changes; no git operations attempted in that state). `verify_long_division_estimation.py` was run for real in Shailesh's own terminal and **caught one genuine bug this sandbox couldn't**: `TruncateThenRoundQuotient()` used `ROUND_DOWN` without importing it (`operands.py`'s `from decimal import ...` only had `ROUND_HALF_UP`) — a plain missing-import `NameError`, fixed in one line. Re-ran clean after the fix: **21/21 checks passed** -- the worked example (415/7 -> 59.29), 5 hand-computed rounding-boundary cases, 4000 generator draws total (1000 each across 3D/1D and 4D/1D, both Visual and Abacus) all producing a genuine remainder + exactly-2-decimal answers + passing `ValidateImQuestion()`, the plain-division regression check (unaffected, still exact), and the validator correctly rejecting a hand-crafted exact-division pair.

**Second real bug caught by actually running the diagnostic script against production** (`diagnose_and_fix_long_division_estimation.py --dry-run`, run for real by Shailesh with the Render External Postgres URL): came back "0 affected rows" across all three areas, including competition mocks — which looked wrong at the time, since the `IM_01` mock previewed earlier that same day visibly had old exact-division 3D/1D and 4D/1D Long Division & Estimation questions. Root cause found and fixed: the script's competition-mock filter used the DPS/assessment section-title format (`"3D ÷ 1D Long Division & Estimation (Visual)"`, from `curriculum_map.py`'s `_DIV()` helper) but competition mocks actually use a completely different naming convention (`"3D / 1D Long Division & Estimation"`, no Visual/Abacus split, plain `/` not `÷` — from `competition_mock_generation_service.py`'s separate `IM_DIVISION` pool-config list). Fixed properly, not just patched: traced `GenerateCompetitionMockDraft()`'s own row-building code and confirmed the original generator's `is_long_division_estimation` metadata flag survives intact into `CompetitionMockQuestion.metadata_json`, so the script now filters all three areas on that one metadata flag instead of any section-title/concept_tag string (documented in a code comment so this doesn't get "simplified" back to the fragile version later).

**Re-ran against production after the fix -- still 0 found, and this time that's genuinely correct**, not a filter bug: Shailesh deleted the existing draft competition mocks (`IM_01`, `Srijan Practice`) before the re-run, so there was no old buggy content left to find. Combined with 0 DPS/assessment findings (no student has attempted this concept yet), **there is nothing to backfill** -- a clean slate. The diagnostic/backfill script (`diagnose_and_fix_long_division_estimation.py`) stays in the repo for future use (e.g. if this class of bug recurs, or for a future level's equivalent concept) but has no action to take right now.

**Still not run: `pytest`/typecheck/build.** That, plus the standard commit/push/PR/CI/merge sequence, is what's left before this is actually delivered.

**Files changed this round:**
- `backend/app/question_engine/im/operands.py` — `TruncateThenRoundQuotient()` (new), `_GenerateLongDivisionEstimationPair()` (new), `GenerateWholeNumberDivision()` branches on `isLongDivisionEstimation`.
- `backend/app/question_engine/im/validators.py` — `_ValidateDivision()` now takes `Config`, checks both new rules when the flag is set.
- `backend/scripts/verify_long_division_estimation.py` — new, not yet run.
- `backend/scripts/diagnose_and_fix_long_division_estimation.py` — new, not yet run (needs production `DATABASE_URL`).

## 2026-07-15 update (round 1): live visual verification of the 2026-07-14 IM mock-studio fixes — confirmed clean, nothing pending

**tl;dr: the one open item from yesterday is closed.** Logged into the production admin panel (`math-path-platform.vercel.app`) via a live Chrome session and opened the real `IM_01` (IM-L4) competition mock's Question Preview and Coverage Check tabs — not just typecheck/build/unit-test results.

**BODMAS/Solve Equation rendering:** checked multiple Section 6 questions, including a squared-term expression (`751 ÷ 19 + 79² + 74 × 66 + 7879 - 2540`). All rendered cleanly on one line in the wide layout, no clipping or overlap — confirms `needsWideQuestionLayout()` (PR #328) works live. Also spot-checked a Skill Stacker question (Section 8) — clean.

**Equal concept distribution:** pulled the full 100-question Coverage Check breakdown across all 8 sections. Every section splits as evenly across its concepts as the math allows — Section 6 (3 concepts, 13Q) splits 5/4/4, Section 7 (2 concepts, 12Q) splits 6/6, Section 8 (2 concepts, 10Q) splits 5/5, Sections 1-4 (5-6 concepts each, 13Q) all within 1 question of even. Confirms the `_CollectImCompetitionSectionLockedQuestions()` rewrite (PR #328) holds in real generated data.

**One non-blocking observation:** a separate attempt to generate a brand-new draft mock via Create Mock hung the browser tab and the draft never appeared in the library afterward. Could not reproduce against a fresh tab / the existing mock, so this reads as a possible Chrome-extension hiccup rather than a confirmed app bug — flagged in `OPEN_ISSUES.md` in case it recurs for real. No code changed this session — pure verification.

## 2026-07-14 update: IM Level 4 in assessments + competition mocks, equal-distribution/layout fix, level-registry robustness, and full repo/branch hygiene cleanup — all delivered, verified, nothing pending

**tl;dr: nothing pending from this thread.** Four rounds today, all delivered and merged: (A) IM Level 4 wired into assessments and competition mocks with full section-allocation/omission parity with MM — PR #326. (B) IM competition-mock equal concept distribution fix + the real root cause of the admin mock-studio BODMAS display bug — PR #328. (C) Level-keyed registry robustness for MM/IM competition mock structures, so an unconfigured future level fails loudly instead of silently generating the wrong content — PR #329. (D) A full repo/branch hygiene sweep — not code, pure repo housekeeping — that root-caused and fixed a recurring Windows git corruption bug and deleted 300 stale branches. The one thing genuinely still open: **visual verification**. Typecheck/build passed for the layout fix and the distribution-fix logic was verified via isolated unit tests, but nobody has actually opened the admin mock-studio preview and looked at a freshly generated IM mock to confirm the BODMAS questions render cleanly and the section breakdown looks evenly distributed by eye. Do that first thing tomorrow if it hasn't happened yet.

### Round A — IM Level 4 wired into assessments + competition mocks, delivered as PR #326

Carried over from Shailesh's request: apply IM Level 4 to assessments and competition mocks "with all the conventions and guidelines in place," and confirm the smart-MCQ-distractor fix already reaches them. Investigation first (per Shailesh's explicit instruction, since no students had yet attempted any assessments — "this is the best time to ensure everything is fixed and in place"): found assessments were universally broken for MM/IM, silently degrading every lesson to trivial single-digit add/less regardless of the real concept, because `SectionConfig()`/`enrich_config_with_lesson_rule()` always routed through YLM's generic operand builder. IM competition mocks had no structured section system at all (MM already had one).

**Fix — assessments (module-aware, level-agnostic):** added `MmSectionConfig()`/`ImSectionConfig()`/`_GenerateMmAssessmentBatch()`/`_GenerateImAssessmentBatch()` to `assessment_engine_service.py`, dispatched purely on module code so every lesson in every level of MM and IM now generates from the real curriculum instead of falling through to YLM's generic path. A `forceSingleSection` `GeneratorConfig` flag was added to both `mm/generator.py` and `im/generator.py` so a caller that's already resolved one specific lesson/DPS pair (assessments, competition mocks) can't have it silently re-expanded by the curriculum map's multi-section auto-expansion.

**Fix — IM competition mocks (8-section structure, exact spec from Shailesh):** Section 1 Add/Less (Abacus), 2 Add/Less (Visual), 3 Multiplication, 4 Division, 5 Squares, 6 BODMAS and Solve Equation, 7 Positional and Placement, 8 Skill Stacker and Concept Drill — 100 questions / 30 minutes, no challenge-lesson floor (uses all 12 IM Level 4 lessons). `competition_mock_generation_service.py` gained `IM_COMPETITION_SECTION_DEFINITIONS`/`IM_COMPETITION_SECTION_CONCEPT_POOLS`, admin section-count allocation matching MM's existing no-auto-redistribution convention exactly (admin decides every section's count, including 0 to omit — nothing gets automatically rebalanced), and the student-facing mock-instructions endpoint already correctly relays the real (omission-aware) section composition with no code changes needed there.

**Verification:** `pytest` 20 passed, `npm run typecheck && npm run build` clean — both run for real in Shailesh's own terminal (this sandbox's usual stale-bash-mount issue recurred and was worked around the same way as every prior round). All 11 CI checks green. Merged, no `--admin` bypass.

### Round B — IM competition mock equal concept distribution + mock-studio layout width, delivered as PR #328

Two distinct bugs, delivered together per Shailesh's instruction to merge them as one PR.

**Bug 1 — BODMAS questions displayed clipped/overlapping in the admin mock-studio preview.** First attempt (delivered separately as PR #327, `MathQuestionDisplay.tsx` — allowed wrapping past 26 characters instead of hard-clipping) made it *worse*, not better — Shailesh's exact words: "this makes it look even weirder and shittier." Re-investigated and found the true root cause: `mock-studio/[mockId]/page.tsx`'s wide-single-column layout was gated on `isMmExpressionQuestion()`/`isMmFinancialQuestion()`, both hardcoded to `isMasterModuleMock()` — so IM's BODMAS/Solve Equation questions (which already carry `displayType: "EXPRESSION_WORKSHEET"` from the generator) always got squeezed into MM's narrow 420px two-column layout regardless of length, and PR #327's wrapping fix had nowhere near enough room to lay out cleanly in that constrained space. Fixed by generalizing the layout decision to a new `needsWideQuestionLayout()` helper that checks the actual resolved display type instead of the module, so any module's long-form questions get the same wide treatment MM already had.

**Bug 2 — one concept in a competition-mock section could dominate the others.** Shailesh's requirement: every concept within a section must get an equal share of that section's questions, not just "intended" via a pre-computed schedule but actually realized in the generated output. Root cause: `_CollectImCompetitionSectionLockedQuestions()`'s while-loop generated a batch of 5 candidate questions for ONE concept per outer iteration and then greedily consumed however many were needed to fill remaining slots — so one concept's batch could silently eat multiple consecutive slots the schedule had intended for a different concept, the moment any duplicate got skipped. Rewrote it to generate exactly one question per pre-scheduled slot, in strict schedule order, with retries confined to that slot's own concept (`IM_COMPETITION_SLOT_MAX_RETRIES`) — verified in isolation with a synthetic 10-question/3-concept section: was skewed before, now splits evenly 4/3/3 every time.

**Files changed:** `frontend/components/common/MathQuestionDisplay.tsx` (PR #327, first attempt, kept), `frontend/app/admin/competition/mock-studio/[mockId]/page.tsx` (`needsWideQuestionLayout()`), `backend/app/services/competition_mock_generation_service.py` (`_CollectImCompetitionSectionLockedQuestions()` rewrite).

**Delivery:** PR #327 tagged `prod-20260714-1816`; PR #328 tagged `prod-20260714-1856`, all 11 CI checks green both times, backend health-checked after each merge.

### Round C — level-keyed registry for MM/IM competition mocks, delivered as PR #329

Shailesh asked, after confirming all three modules/levels are otherwise solid (see the platform-wide audit below): "tell me if there is a way to make it more robust so that no problems occur in the future." Root issue: MM's and IM's competition-mock section/concept definitions were bare module-level Python constants (`MM_COMPETITION_SECTION_DEFINITIONS`, `IM_COMPETITION_SECTION_DEFINITIONS`, etc.) referenced directly by the generation code, with no level check at all — a request for a competition mock at any *other* MM or IM level (there's only one of each live today, MM-L1/IM-L4, but Shailesh confirmed IM will eventually have 4 levels, each with its own distinct mock rules) would not error, it would silently generate that other level's structure under the wrong level's name.

**Fix:** `MM_COMPETITION_LEVEL_REGISTRY`/`IM_COMPETITION_LEVEL_REGISTRY`, keyed by `level_code`, referencing the existing MM-L1/IM-L4 data verbatim (zero behavior change for the live levels). `_MmCompetitionLevelConfig()`/`_ImCompetitionLevelConfig()` are the only way generation code may resolve a level's structure, and both fail loudly with a clear `..._LEVEL_NOT_CONFIGURED` error — at both the admin preview stage (`CompetitionMockSectionPlan`) and actual generation — for any level not yet registered. `_MmSectionCountMap`/`_ImSectionCountMap`/`_MmCompetitionChallengeLessons`/`_MmCompetitionOrderedConceptSchedule` were all threaded to take the resolved config as a parameter instead of reading the bare global, so once IM-L1/L2/L3 (or a second MM level) get their own registry entry, they'll actually use their own data, not just be safely rejected. IM's per-question `competitionDifficultyProfile` metadata also now reflects the real `level_code` instead of a hardcoded `"IM_COMPETITION_LEVEL4"` string.

**Confirmed safe for live students before implementing:** competition-mock generation is a strictly one-time admin action (`GenerateCompetitionMockDraft`) that persists to the DB; the student attempt flow reads only those persisted rows through a completely separate service file, so this refactor could not affect an already-generated mock or a mock a student is mid-attempt on.

**New tests:** 2 in `test_mm_competition_mock_generator.py`, 2 new in `test_im_competition_mock_level_registry.py` — prove the registry stays intact for the live levels and that an unregistered level fails loudly instead of silently reusing another level's content. 24 total backend tests passed (up from 20).

**Platform-wide audit that prompted this (no code changed, investigation only):** confirmed via subagent audit that assessments and the DPS/practice smart-distractor fix are both solid across all three modules and every level (assessment dispatch is purely module-code-based, not level-scoped, so it correctly covers every lesson everywhere). Competition mocks were the one area not level-generic (now fixed above). **Confirmed gap, not addressed today:** YLM has zero competition-mock support — selectable in the admin dropdown but falls through to a generic per-DPS path with none of MM/IM's section-locked structure. No comment or gate anywhere marks this as an intentional product decision; it reads as an unaddressed gap. Flagged in `OPEN_ISSUES.md`.

**Delivery:** PR #329, tag `prod-20260714-2019`, all 11 CI checks green, backend health-checked.

### Round D — repo-wide branch/ref hygiene cleanup (not code — pure repo housekeeping)

Started from Shailesh asking to permanently fix a recurring "filename too long" warning on `git pull`, caused by one stray branch with an extremely long auto-generated name. Investigation revealed this was systemic, not a one-off: an automated delivery tool (`apex_deliver.py`) generates branch names from full task descriptions, producing names well past Windows' ~260-character path limit once combined with the repo's deeply-nested OneDrive path — and a **full repo audit found 430 non-`main` branches total**, only 14 of which were active/recent.

**Safety established before touching anything:** confirmed (via `.github/workflows/mathpath-ci.yml` and repo-wide search) that nothing deploys from or watches any branch but `main` — branch deletion structurally cannot affect the live platform, only whether old history stays reachable. Two independent subagent investigations (byte-for-byte tree-hash comparison against `main`, not just ancestor-checking, which this repo's delivery pattern of reapplying fixes as new commits makes unreliable on its own) confirmed every one of the ~230 "needs a closer look" branches — including an apparent abandoned 3D/gamification/VFX initiative (two explicit `apex-revert-complete-rollback-to-*` commits confirm it was deliberately killed, though the broader gamification work that mattered did ship separately) and a `phase-2` through `phase-6` engineering-hardening series — had its content already merged into `main` under differently-worded commits, specifically double-checked on the highest-stakes item (`phase-5-security`, confirmed byte-identical to main's merged PR #134).

**Safety net:** a full `git bundle --all` backup of every branch's complete history was made before any deletion (`mathpath-full-branch-backup-20260714.bundle`, shared with Shailesh) — recoverable via `git bundle unbundle` if anything is ever needed later. **Deliberately left untouched:** 8 branches/worktrees actively checked out by Gemini/Antigravity (a separate tool Shailesh also uses) under `.gemini/antigravity/...` — not part of this cleanup at all, since Shailesh confirmed he's still actively switching between that tool and Claude/Cowork based on usage limits.

**Executed:** a generated PowerShell script deleted 300 branches (77 local, 223 remote) plus 18 auto-pruned stale tracking refs. Two real obstacles hit and fixed along the way: (1) a stuck `packed-refs.lock` from an interrupted git operation, and intermittent per-ref `.lock` collisions from OneDrive's real-time sync watcher locking files mid-write — fixed with a retry-with-backoff wrapper around every git call plus a defensive lock sweep at script start; (2) 4 branches were checked out in separate `apex_deliver.py` worktrees under `AppData/Local/MathPathDelivery/worktrees/` (confirmed stale, ~4 weeks old, content already merged) — removed via `git worktree remove` before their branches could be deleted. **Root cause of the corruption fixed:** `git config core.longpaths true` (this exact class of corrupted-ref issue happened once before, 2026-07-10, and was reverted after a one-off unblock — this time it's deliberately being kept enabled, since the actual root cause is systemic long branch names, not a one-off).

**Final state confirmed clean:** `git status` shows `main` up to date with `origin/main`, `git branch -a` shows 28 entries (main + 14 active branches + the 8 preserved Gemini branches, some counted twice as both local and remote-tracking). Two leftover files from this process (`BRANCH_CLEANUP_AUDIT_20260714.md`, `cleanup-stale-branches.ps1`) are safe to delete — Cowork's sandbox hit a permission wall trying to remove them itself, left for Shailesh to delete directly. Also flagged, not actioned: `.agents/AGENTS_backup.md` (stale, describes an old workflow that contradicts current CLAUDE.md safety rules) and a few minor `.gitignore` gaps (`.pytest_cache/`, `*.egg-info/`, `.coverage`, `htmlcov/`, generic `*.log` — harmless today since nothing of that kind is currently tracked, but worth adding defensively).

## 2026-07-13 update: IM Level 4 marks-per-question fix — delivered as PR #323, verified end to end. Nothing pending.

**tl;dr: nothing pending from this thread.** This is a direct follow-up to the IM + Level 4 launch (PR #321, then a small docs close-out as PR #322 — both already covered in `CLAUDE_CODE_STATUS.md`'s "Last completed milestone" section). Shailesh asked for one more scoring correction on top of that launch: IM's Concept Drill and Skill Stacker sections only carry 2 questions per sheet (by design — they're meant to be worth more per question, not repeated many times), so scoring them at the same flat 1 mark/question as every other IM concept made a sheet's total marks come out non-whole and undervalued those two concepts. Requirement: Concept Drill and Skill Stacker questions score 5 marks each (0 if wrong), every other IM question stays at 1 mark, and this must not touch MM or YLM's scoring at all.

**Design (proposed, approved by Shailesh with "go ahead" before any code changed):** added a nullable `DPSSection.marks_per_question` column. `NULL` (the default, and the value for every existing section in every module) means "inherit `DPS.marks_per_question`" — today's behavior, completely unchanged. Only IM's seed script sets a real value (`5.0`) on Concept Drill/Skill Stacker sections specifically. Scoring (`attempt_service.py`) now looks up each question's own section's override before falling back to the DPS's flat value, both when computing `max_score` at attempt start and when awarding marks per question at submit time.

**Files changed:**
- `backend/app/models/models.py` — nullable `DPSSection.marks_per_question` column.
- `backend/app/services/schema_migration.py` — `ensure_dps_section_marks_column()`, same self-heal convention as every other column added this way in this repo.
- `backend/app/main.py` — wired the new migration function into `on_startup()`.
- `backend/app/seed/seed_intermediate_module.py` — sets `marks_per_question=5.0` on Concept Drill/Skill Stacker sections only; every other IM section stays `None`.
- `backend/app/services/attempt_service.py` — new `_SectionMarksPerQuestion()` helper (section override if set, else the DPS's flat value) and `_ComputeDpsMaxScore()` (sums `question_count x effective marks` across a DPS's sections); `start_attempt()`'s `max_score` and `submit_attempt()`'s per-question scoring loop both now go through these instead of the old flat `total_questions x marks_per_question` calculation.
- `backend/app/api/routes_student.py` — `/student/assignments` now reports `totalMarks` via `_ComputeDpsMaxScore()` instead of the old flat multiply, so the student-facing assignment list shows the correct total for IM sheets with mixed marks.

**Verification — actually run, not just reasoned about.** This session's Cowork sandbox hit the same recurring stale-bash-mount bug as prior rounds (all 5 edited files showed phantom mid-line truncation via `bash`/`python3 -c "ast.parse(...)"`, confirmed spurious by re-reading every file fresh via the Read tool — all 5 were correct on disk). Rather than fight the sandbox further, wrote a standalone, no-server-needed verification script (`verify_marks_per_question.py`, throwaway SQLite DB) and had Shailesh run it directly in his own terminal, where the files aren't stale. **All 12 checks passed**, covering both: (1) an IM-style DPS (10 Add/Less@1 + 2 Skill Stacker@5 + 2 Concept Drill@5) — confirmed `max_score == 30`, and a real `submit_attempt()` call with a mix of correct/wrong/unanswered across all three sections landed on the exact expected `total_score`/`correct_count`/`wrong_count`/`unanswered_count`; (2) an MM/YLM-style DPS (single flat section, no override) — confirmed `max_score` matches the old pre-existing flat formula exactly, i.e. zero regression for every module besides IM.

**Delivery — two real process hiccups worth remembering for next time, both self-corrected:**
1. First delivery attempt tried a direct `git push` to `main` (my mistake — should have used a feature branch from the start, same as PR #321/#322/#323's actual predecessors). Blocked by the repo's server-side "MathPath safety guard" ("direct pushes to main are blocked"). Recovered cleanly with no data loss: `git branch feature/im-marks-per-question` (captures the already-made commit), `git reset --hard origin/main` (realigns local `main`), `git checkout feature/im-marks-per-question`, then pushed the branch and opened a PR normally.
2. Also caught: the developer ran the Phase 3 (PR merge) commands before Phase 2 (add/commit/push) had actually been run in an earlier attempt — `gh pr create` correctly refused ("head branch main is the same as base branch main") since there was nothing new to PR yet. Re-sequenced correctly once caught.

**Delivered as PR #323**, merged via `gh pr merge --squash --delete-branch` after `gh pr checks --watch` showed all 11 checks green (Vercel, Vercel Preview Comments, backend-tests, ci-summary, delivery-console-lint, frontend-build, frontend-e2e-tests, frontend-typecheck, governance-audit, mathpath-generator-validation, repository-safety) — no `--admin` bypass, this touches live scoring logic. Confirmed fast-forward merged into `main` (`b80ed95..3657624`). Nothing pending from this thread — IM + Level 4, including this scoring correction, is fully delivered and live.

## 2026-07-13 update: premature mock-exam auto-submit investigation + fix — delivered as PR #318, verified

Students reported mock exams auto-submitting while the on-screen timer still showed several minutes remaining. Investigated end-to-end before any code changed, per Shailesh's explicit instruction. Backend deadline enforcement (`expires_at = started_at + duration_seconds`, re-checked on every answer-save and page load via `EnsureCompetitionAttemptActiveOrSubmit`/`SubmitCompetitionMockAttempt`) was confirmed correct — no clock-skew or premature-expiry bug there.

**Root cause: the client-side countdown, not the server.** `frontend/hooks/useAttemptTimer.ts` (shared by mock exam, assessment, and DPS/practice attempt pages — unchanged since the initial commit) counted down by blindly decrementing a number via chained `setTimeout`, with no anchor to real wall-clock time and no resync on tab-focus regain (the app's global React Query config has `refetchOnWindowFocus: false`). Backgrounded/throttled tabs (student switches apps, phone locks, alt-tabs) freeze or lag the on-screen timer while the server's real deadline keeps advancing; the next network call (an answer save) correctly detects real expiry and submits, while the frozen display still showed time left. Reproducible for any student who leaves the tab mid-attempt, on any of the three timed activity types — not a one-off, which is exactly why only "some" students reported it (only those who actually backgrounded the tab).

**Fix (frontend-only, no backend/schema touched):**
1. `useAttemptTimer.ts` now anchors to an absolute deadline (`Date.now() + initialSeconds*1000`) and recomputes remaining time from that deadline on every tick, self-correcting regardless of setTimeout throttling. Added a `visibilitychange` listener that immediately recomputes on tab-focus regain and fires an optional `onVisible` callback.
2. All three attempt pages (`mock-attempt`, `assessment-attempt`, `attempt`) now pass `query.refetch()` as that callback, so returning to a backgrounded tab immediately re-pulls true attempt state from the server instead of trusting stale local time.
3. Separately found and fixed in the same session (a UI report, not the auto-submit bug): `.math-timer-critical`'s pulsing animation for the sub-5-minute urgency state referenced the wrong keyframes name (`math-timer-critical` vs. the actual `@keyframes mathTimerCritical`) — CSS silently no-op'd, so the timer sat static red instead of pulsing. One-line fix in `globals.css`.
4. `TestTimer.tsx` had no dark-mode variants on its non-urgent (blue) state — hardcoded light Tailwind classes with no `dark:` counterparts. Added dark-mode contrast classes for both states, and removed a redundant inline `style` override that would have silently blocked any future dark-mode class styling on the icon.

**Files changed:** `frontend/hooks/useAttemptTimer.ts`, `frontend/app/student/competition/mock-attempt/[attemptId]/page.tsx`, `frontend/app/student/assessment-attempt/[attemptId]/page.tsx`, `frontend/app/student/attempt/[attemptId]/page.tsx`, `frontend/app/globals.css`, `frontend/components/student/TestTimer.tsx`.

**Verification:** `npm run typecheck && npm run build` run for real in the developer's own terminal (clean, 41/41 routes) — this session's sandbox bash mount showed the same phantom-corruption pattern as prior rounds (a correctly-closed file read back mid-token truncated via bash `cat`/`tsc`), confirmed spurious via `git diff -w` and a fresh re-read through the Read tool showing the real content was always correct. Delivered as **PR #318** (squash commit `fa246c0`), all 11 CI checks passed, confirmed merged. **Not yet independently browser-verified live** — flagged as a follow-up: confirm the pulsing animation renders in both themes at ≤5 minutes, and confirm a genuinely backgrounded tab past expiry resyncs correctly instead of showing stale time.

## 2026-07-13 update: repo-wide cleanup audit — delivered as PR #315, verified

Shailesh asked for a full sweep of the entire repo (not just the student portal) for dead/unused files and anything bloating or slowing the platform down. Found and removed: a 946MB duplicate nested clone (`MathPath-Platform/`, tracked as a broken gitlink with no `.gitmodules`, including its own 459MB `node_modules`), 4 unreferenced root files (`original_page.tsx`, `teacher-rules.css`, `trigger_gamification_sync.py`, `trigger_production_sync.py`), 5 pre-git versioning artifacts from an old folder-duplication workflow (3 "STABLE" READMEs, `VERSION.txt`, `STABLE_PACKAGE_DO_NOT_EDIT_DIRECTLY.txt`), and an empty untracked `mathpath.db`. Everything was confirmed unreferenced by any code, docs, or CI config before being flagged — nothing removed blind. **Delivered as PR #315 (squash commit `040a946`), all 11 CI checks passed** (backend-tests, frontend-build, frontend-typecheck, frontend-e2e-tests, governance-audit, repository-safety all green), confirming nothing actually depended on any of it. Full detail in `OPEN_ISSUES.md`'s matching entry. Also flagged, not a code fix: `frontend/node_modules` (818MB) and `frontend/.next` (2.4GB), while properly gitignored, sit inside the OneDrive-synced folder — several GB of build cache/packages OneDrive is likely continuously trying to index, a plausible contributor to this session's repeated stale-sync issues. Worth excluding both from OneDrive sync locally.

## 2026-07-13 update: line-by-line student-portal audit + unified economy system — delivered as PR #312, backfilled, verified

**tl;dr: all fixes below are live in production (PR #312, squash commit `6145dca`, all 11 CI checks passed), and all three new backfill/correction scripts have been run to completion.** `backfill_practice_dps_economy.py --apply` — 8 historical practice/DPS attempts (Sakshi Agarwal x4, Shailesh Gupta x1, Meera Chatterjee x3, the same set from the PR #311 notification backfill), 210 XP / 7 coins awarded total. Hand-verified against the formula: each attempt was a 300s (5-min) DPS at weight 1.0, so base XP = `GAMIFICATION_MINUTE_RATE × 5 × 1.0 ≈ 27.78`, and every attempt's award matched its accuracy band exactly (e.g. 90% accuracy → 2.0x → round(55.56) = 56 XP / 3 coins; 40% accuracy → 0.5x → 14 XP / 0 coins). `backfill_assessment_economy.py --apply` and `fix_assessment_auto_submitted_timestamps.py --apply` both found 0 attempts affected — no assessment completions exist historically yet, consistent with PR #311's finding.

**Also this round — a docs-sync bug was found and fixed (PR #313).** After PR #312 merged, `CLAUDE_CODE_STATUS.md` and `OPEN_ISSUES.md` were discovered to still carry stale "prepared, not yet delivered" content on `main` itself (not just a locally-stale read) — confirmed via `git reset --hard origin/main` pulling in the old snapshot. Root cause: a gap between when Cowork's Edit-tool writes land on the OneDrive-synced disk and when the developer's terminal's `git add` captures those bytes for a commit — the actual committed PR #312 content for these two files was an intermediate, pre-delivery snapshot, not the final one. Corrected via a small docs-only PR #313 (squash commit `9871632`). **Lesson for future sessions:** don't trust a memory doc's self-reported status at face value if there's any doubt — cross-check against `git log`/PR history, since the doc can lag what's actually committed.

Shailesh asked for an exhaustive, literal line-by-line audit of the entire student portal (beyond the pattern-level audit earlier the same day) -- every frontend page, every backend route, every calculation, notification, achievement, and leaderboard, "nothing left out." Collector's Vault stayed explicitly deferred. Findings were reported in full before any code changed. Summary of what was found, and what's now built in response:

**Finding 1 (critical) — the economy system didn't do what the product told students it did.** `EconomyService.award_xp_and_coins()` was only ever called from mock exam completion. Practice/DPS and assessment completions awarded zero XP/coins, despite the dashboard's "Daily Objective" card explicitly promising "Complete your assigned DPS Sheets to build speed, accuracy, and earn MathCoins." Achievements were unaffected -- all 27 badges are consistently and intentionally mock-exam-scoped by design (every badge description says "Mock Exam").

**Finding 2 (high) — assessments had the same AUTO_SUBMITTED timing bug round 10 fixed for mocks, just never patched here.** `_SubmitAssessmentAttemptCore()` always stamped `submitted_at = now()` and derived `time_taken_seconds` from raw elapsed wall-clock time, auto or manual, so a late-detected lazy auto-submit could report a longer time-taken than the assessment's own `duration_seconds` allowed and mis-date the completion.

**Finding 3 (low, latent)** — `attempt_service.py`'s `submit_attempt()` hardcoded 1 mark per question, ignoring `DPS.marks_per_question`. Not live-exploitable (every DPS is seeded at 1, no admin path changes it) but a future trap.

**Findings 4-6 (dead code, cleanup)** — `recalculate_streaks.py` referenced a column (`CompetitionMockResultSummary.submitted_at`) that doesn't exist on the model and would crash if ever run; `NotifyPracticeReattemptUnlocked()` was fully built and never called anywhere; `GET /student/notifications` was a ~70-line synthesized endpoint nothing in the frontend calls (the real panel uses `GET /api/notifications`).

Shailesh's decision on the economy gap: all three activities (DPS, assessments, mocks) need to earn XP and coins, under one system that's fair and dynamic across every module/level combination (durations vary per DPS/assessment/mock, admin-configured), robust against ever paying a "buggy" amount, and applied both retroactively and going forward.

**The design landed on, and what's now built:**

One shared formula -- `EconomyService.evaluate_activity_performance()` -- used by DPS, assessment, and mock completion alike: `XP = round(GAMIFICATION_MINUTE_RATE x (allotted duration_seconds / 60) x accuracy_multiplier x ACTIVITY_WEIGHTS[type])`, coins = 5% of XP (unchanged ratio from the original mock formula), zero coins below 50% accuracy. The accuracy multiplier is the exact curve already live for mocks (0.5x/1.0x/1.5x/2.0x/2.5x), extracted into a shared `_accuracy_multiplier()` helper so "how much better performance pays" is decided in exactly one place for all three activity types. `ACTIVITY_WEIGHTS = {"DPS": 1.0, "ASSESSMENT": 1.3, "MOCK": 1.5}`, calibrated so a standard 60-minute mock at the 1.0x band lands close to the pre-existing 500 XP baseline -- no existing student's mock-earned trajectory changes.

Deliberately keyed on the activity's **allotted** `duration_seconds` (fixed, admin-configured, correct from the moment an attempt starts), not `time_taken_seconds` (derived after submission, and exactly the field with the timing bug found in this audit). Two reasons this matters: paying off a field that can itself be wrong would let that bug corrupt payouts too, and this program explicitly rewards speed -- paying by time *taken* would mean a fast, accurate student earns less than a slow one for identical accuracy, which is backwards. Clamped to 1-180 minutes so a misconfigured (zero, negative, or absurd) duration can never produce a broken payout. Because every module/level configures its own duration per activity, this never needs manual re-tuning as new content is added.

Loot-pack drops (Alpha Pack / Elite Chest) stay mock-exclusive for now -- Collector's Vault, where a dropped pack would actually be seen, isn't built out for DPS/assessment content yet.

**New idempotency guard, mirroring the exact pattern already proven three times this week:** a `gamification_processed_at` column on `Attempt` and `AssessmentAttempt` (mirroring `CompetitionMockAttempt`'s existing one), claimed atomically (`UPDATE ... WHERE IS NULL`), so the economy award runs exactly once per attempt regardless of which completion path fires it -- deliberately a *separate* column from `notification_processed_at` (added last round) so the two guards can never block or race each other. Two new Alembic migrations (`c2f7a9d3e451` for `attempts`, `d4a1b8e6f723` for `assessment_attempts`, chained off last round's head `a1e6838c5ea3`) plus matching `schema_migration.py` self-heal functions, wired into `main.py`'s startup.

**Files changed this round:**
- `backend/app/services/economy_service.py` -- `_accuracy_multiplier()` extracted; new `evaluate_activity_performance()`; old `evaluate_assignment_performance()` kept (not deleted) since `backfill_mock_gamification.py`, already run to completion, still references it.
- `backend/app/services/attempt_service.py` -- `marks_per_question` fix in `submit_attempt()`; new `_process_attempt_gamification_side_effects()`, called alongside the existing notification side-effects call.
- `backend/app/services/assessment_engine_service.py` -- AUTO_SUBMITTED timestamp fix in `_SubmitAssessmentAttemptCore()` (mirrors round 10 exactly); new `_ProcessAssessmentGamificationSideEffects()`.
- `backend/app/services/competition_mock_attempt_service.py` -- economy hook switched from the old flat `evaluate_assignment_performance(base_xp=500)` to the shared `evaluate_activity_performance(activity_type="MOCK")`.
- `backend/app/models/models.py` -- `gamification_processed_at` on `Attempt` and `AssessmentAttempt`.
- `backend/alembic/versions/c2f7a9d3e451_*.py`, `d4a1b8e6f723_*.py` -- new migrations.
- `backend/app/services/schema_migration.py`, `backend/app/main.py` -- two new self-heal functions + startup wiring.
- `backend/app/services/recalculate_streaks.py` -- fixed the nonexistent-column reference (kept as a manual reconciliation tool, not deleted).
- `backend/app/services/practice_notification_service.py` -- deleted `NotifyPracticeReattemptUnlocked()` (dead, unused, would have duplicated `NotifyPracticeFreshPracticeAssigned()` if ever wired in).
- `backend/app/api/routes_student.py` -- deleted the dead synthesized `GET /notifications` route.
- `backend/scripts/backfill_practice_dps_economy.py`, `backend/scripts/backfill_assessment_economy.py` -- new, one-time historical XP/coin backfill (not yet run), calling the real production gamification functions.
- `backend/scripts/fix_assessment_auto_submitted_timestamps.py` -- new, one-time retroactive correction (not yet run). Note: assessments have no stored auto-vs-manual flag the way mocks do (`ResultStatus()` only ever sets CLEARED/NEEDS_RE_ATTEMPT, never a literal AUTO_SUBMITTED status), so this script detects affected rows via the mathematically reliable signal instead: `time_taken_seconds > duration_seconds`, which is physically impossible except as a symptom of this exact bug.

**Verification status:** same confirmed sandbox bash-mount staleness as every round this week -- `py_compile` on the 5 brand-new files (2 migrations + 3 scripts) passed clean, but the same command against pre-existing edited files threw a phantom `SyntaxError` at `models.py:1028`, a line nowhere near any edit made this round; manually re-read via the Read tool and confirmed correct (`created_at = Column(DateTime(timezone=True), server_default=func.now())`, properly closed). Every edited file was manually re-read end-to-end after editing. Real verification then ran in the developer's own terminal before push: `pytest tests/ -q` — 20 passed; `npm run typecheck && npm run build` — clean, 41/41 routes. Shipped as PR #312, confirmed deployed on both Vercel and Render, all three backfill scripts run to completion (see tl;dr above).

## 2026-07-13 update (earlier the same day): full student-portal audit, five fixes — delivered as PR #311, backfilled, verified

**tl;dr: all five fixes below are live in production (PR #311, commit `434f357`, all 11 CI checks passed, merged cleanly), both backfill scripts have been run to completion, and there's nothing pending from this thread.** `pytest tests/ -q`: 20 passed. `npm run typecheck && npm run build`: clean. Delivered from the developer's own terminal in two grouped pushes (verify+commit+push+PR, then watch CI+merge) per Shailesh's request to minimize back-and-forth.

Backfill results: `backfill_practice_attempt_notifications.py --apply` found and notified 8 historical attempts missing their notification — Sakshi Agarwal (4), Shailesh Gupta (1), Meera Chatterjee (3) — creating 30 notifications total (student/teacher/admin per attempt). `backfill_assessment_attempt_notifications.py --apply` found 0 attempts affected — no historical gap existed for assessments (consistent with #2 below being a safety net for a failure mode, not a bug that had already fired historically).

## 2026-07-13 original findings: full student-portal audit, five fixes — prepared, not yet delivered (superseded by the delivery summary above)

After round 10 shipped, Shailesh asked for a from-scratch audit of the entire student side of the platform ("every single detail... nothing should be left out") given students were actively attempting exams that day, specifically to catch any other instance of the bug class round 9/10 had just surfaced. Full findings were reported first (auth, notification commit patterns, mock/practice/assessment attempt lifecycles, frontend guards) with no code changes, then Shailesh approved fixing everything found except Collector's Vault (explicitly deferred — still needs real backend work, tackled separately once the rest of the portal is confirmed solid).

**1. Practice/DPS lazy-auto-submit notification gap (critical, same bug class as pre-round-9 mocks).** `attempt_service.py`'s `ensure_active_or_auto_submit()` — the lazy fallback triggered by a plain GET once a practice/DPS attempt's timer has expired server-side — called `submit_attempt()` directly, which graded the attempt correctly but never notified anyone: `NotifyPracticeAttemptSubmitted()` (and the retry-assignment notification) only ever lived in the two explicit route handlers (`/attempts/{id}/submit`, `/attempts/{id}/auto-submit`), never inside `submit_attempt()` itself. Fixed by moving both notifications into a new `_process_attempt_notification_side_effects()` called from inside `submit_attempt()`, gated by a new `notification_processed_at` column (migration `bfa28b9fc380`) claimed atomically, so every completion path gets identical treatment exactly once. Route handlers simplified to just call `submit_attempt()` and return — no more duplicate notification logic.

**2. Assessments had no server-side safety net at all (high).** Unlike mocks and practice sheets, `GetAssessmentAttemptForStudent()` had no lazy-completion fallback whatsoever — an assessment whose client-side timer-driven auto-submit call never reached the server (tab closed at time-up, crash, dropped network) would stay `IN_PROGRESS` forever, no scheduled sweep anywhere to catch it. (One partial, narrower version of this check already existed inline in `SaveAssessmentAnswer` — removed as redundant once the centralized fix covers every path uniformly.) Fixed: split `SubmitAssessmentAttempt` into a thin ownership-verifying wrapper and a new `_SubmitAssessmentAttemptCore()` that operates directly on an attempt object (avoiding recursion through `GetAssessmentAttemptForStudent`); added `EnsureAssessmentAttemptActiveOrAutoSubmit()`, the same shape as the mock/practice fallbacks, wired into `GetAssessmentAttemptForStudent()`. Notification moved into `_SubmitAssessmentAttemptCore()` via `_ProcessAssessmentCompletionNotification()`, gated by a new `notification_processed_at` column on `assessment_attempts` (migration `a1e6838c5ea3`).

**3. `get_current_student()` had no defensive `is_active` check (medium, hardening not an active exploit).** `get_current_teacher()` already independently checks `teacher.is_active`; the student equivalent didn't, relying solely on `user.is_active`. Every current admin endpoint that deactivates a student does keep `student.is_active` and `user.is_active` in sync, so this wasn't live-exploitable, but added the same defensive second check to `get_current_student()` (`dependencies.py`) so a future desync (bulk import, data-repair script, new admin feature) can never silently leave a "deactivated" student with working login.

**4. Sliding JWT session refresh (Shailesh's explicit ask, "fix it once and for all," not a band-aid bump of the expiry number).** `get_current_user()` now checks remaining token lifetime on every authenticated request; once a token is more than halfway through its life, a fresh one is transparently issued via an `X-New-Access-Token` response header (added to CORS `expose_headers`, since custom headers are invisible to frontend JS cross-origin otherwise). Frontend's axios response interceptor (`lib/api.ts`) reads that header and swaps the stored token via a new `updateStoredToken()` helper (`lib/auth.ts`) — silent to the student. Net effect: a genuinely active session (answering questions, timer polls) renews itself continuously and can never hit a hard expiry wall mid-exam; a truly idle session still expires on schedule.

**5. Two new backfill scripts** (`backend/scripts/backfill_practice_attempt_notifications.py`, `backend/scripts/backfill_assessment_attempt_notifications.py`) close the historical gap for #1 and #2 — both find every completed attempt with `notification_processed_at IS NULL` (which, conveniently, is every pre-fix attempt, since the column and the gating didn't exist yet) and call the REAL production notification function directly (not a reimplementation, to avoid the class of bug found in the round-9 backfill script's own dedup logic), backdating created notifications to the attempt's real `submitted_at`. Not yet run — needs the code deployed first, same order as every prior backfill.

**Verification status:** same sandbox-staleness pattern as every round this week — confirmed again this session (dependencies.py and schema_migration.py both showed phantom truncation/syntax errors in the bash mount that don't reproduce against the real file via the file-edit tools). Not run via pytest/build in this sandbox; verified via full manual re-read of every edited file. **qa-reviewer must run real pytest + typecheck + build before this ships.**

**Files changed this session:**
- `backend/app/dependencies.py` — student `is_active` check, sliding JWT refresh.
- `backend/app/main.py` — CORS `expose_headers`, two new startup self-heal calls.
- `backend/app/models/models.py` — `notification_processed_at` on `Attempt` and `AssessmentAttempt`.
- `backend/app/services/schema_migration.py` — two new self-heal functions.
- `backend/app/services/attempt_service.py` — notification moved into `submit_attempt()`.
- `backend/app/services/assessment_engine_service.py` — lazy-completion fallback + notification moved into core grading.
- `backend/app/api/routes_student.py` — route handlers simplified, unused imports removed.
- `backend/alembic/versions/bfa28b9fc380_*.py`, `a1e6838c5ea3_*.py` — new migrations.
- `backend/scripts/backfill_practice_attempt_notifications.py`, `backend/scripts/backfill_assessment_attempt_notifications.py` — new, not yet run.
- `frontend/lib/api.ts`, `frontend/lib/auth.ts` — sliding-refresh interceptor + token-swap helper.

**Explicitly out of scope:** Collector's Vault (`frontend/app/student/achievements/vault/`) — still hardcoded dummy data with no backend and no `useProtectedPage` guard, deferred per Shailesh's instruction until the rest of the portal is confirmed solid.

## 2026-07-12 update: round 10 + heatmap tier-color fix + badge-notification commit-bug fix — delivered as PR #310, backfilled, verified

**tl;dr: all three fixes below are live in production (PR #310, commit `c2ebec6`, confirmed deployed on Vercel and Render), both one-time backfills have been run to completion, and there's nothing pending from this thread.** Delivered from the developer's own terminal with real verification this time (`pytest tests/ -q`: 20 passed; `npm run typecheck && npm run build`: clean, 41/41 routes) — Cowork's sandbox had a confirmed-stale repo mount this session (see the verification-caveat note further down) so those checks were deliberately skipped in Cowork and re-run for real before push.

Backfill results: `fix_auto_submitted_timestamps.py --apply` corrected 12 of 13 AUTO_SUBMITTED attempts (drift ranged seconds to ~8 minutes), their result summaries, and 36 notification timestamps. `backfill_missing_badge_notifications.py --apply` found and fixed exactly 1 missing notification platform-wide (Nishant Gantayet's "The High Achiever" badge — the one that surfaced the bug in the first place), out of 65 badges scanned across every student.

One process note worth keeping for next time: the first `--dry-run` attempt failed with `no such column: gamification_processed_at` because `DATABASE_URL` wasn't set in the terminal session, so the script silently ran against the local unmigrated SQLite dev DB instead of production. Fixed by setting `$env:DATABASE_URL` to the Render **External** Postgres URL (Internal only resolves from inside Render's network) before re-running — same gotcha as round 9's backfill hit.

## 2026-07-12 update (superseded by the entry above — kept for the investigation detail): round 10 (AUTO_SUBMITTED submitted_at fix + retro-correction script) + heatmap tier bar color/contrast fix — prepared, not yet delivered

Two independent fixes this session, both additive, no new migrations beyond what round 9 already shipped.

**1. Round 10 — the decision flagged at the end of round 9.** `backend/app/services/competition_mock_attempt_service.py`'s `SubmitCompetitionMockAttempt` now computes `submitted_at` for the auto (timer-expiry) path as `started_at + duration_seconds` (capped at "now" as a clock-skew guard) instead of `_now_utc()`. The old behavior stamped whatever moment the system happened to *detect* the expiry (a lazy GET, the defensive result-page repair) rather than the true exam-end time — wrong on its own, and also a risk to streak/heatmap day-bucketing since `recalculate_streaks.py` sorts on this column. `time_taken_seconds`/`time_utilization_percentage` were left unchanged — those were already correct for the auto path (100% time utilization is accurate for a timer-expiry submission; that part of the original report wasn't actually a bug).

Shailesh's call on the second open question: retroactively correct the attempts already backfilled under the old values. New one-time script `backend/scripts/fix_auto_submitted_timestamps.py` (`--dry-run`/`--apply`, same conventions as the round-9 backfill script — force-flushed prints, idempotent, safe to re-run). For every AUTO_SUBMITTED attempt whose stored `submitted_at` drifted from the recomputed correct value, it fixes three things together so nothing goes chronologically inconsistent: the attempt's own `submitted_at`, the matching `CompetitionMockResultSummary.completed_at`, and any `Notification.created_at` that was backdated to the old (wrong) value for that attempt. **Not yet run against production** — needs the round-10 code deployed first (same deploy-then-backfill order as round 9), then `--dry-run` to preview, then `--apply`.

**2. Heatmap tier bar colors — contrast fix**, following up on Shailesh reporting bars "meshing in with the background" during live student submissions. Computed actual WCAG contrast ratios for every tier color against the real card background (`bg-white/95` light / `bg-black/85` dark) rather than eyeballing it. The round-3 palette (PR #305) turned out to be tuned for dark mode only: S-TIER's gradient top (`yellow-300`) was ~1.3:1 against the white card (effectively invisible at the top of tall bars — the tier a student who submits well and fast earns most, matching what was observed), A-TIER (`emerald-500`, ~2.5:1) and B-TIER (`sky-500`, ~2.8:1) were both below the 3:1 minimum for graphical UI, and the REST/no-activity color (`slate-300` light / `white/10` dark) failed on both themes (~1.5:1 light, ~1:1 dark — genuinely invisible). All five tier colors passed comfortably in dark mode (5.7-15.9:1), so this was a light-mode-specific gap plus a dark-mode REST gap, not a uniform bug.

Replaced with a palette chosen for relative luminance ~0.13-0.28, the band that clears 3:1 contrast against BOTH white and black cards with the *same* color — no `dark:` variant needed on the base fill. `frontend/app/student/dashboard/page.tsx`: S-TIER `yellow-700→amber-600` gradient (gold), A-TIER `emerald-600`, B-TIER `sky-600`, C-TIER `violet-600` (moved off `amber-600` so it doesn't collide with the new S-TIER gold), D-TIER `rose-600`, REST `slate-500`. All six (5 tiers + rest) are now distinct hues, each passing contrast in both themes.

**Verification status — read before assuming this is done.** Same sandbox-staleness pattern documented in prior rounds recurred: this session's bash mount of the OneDrive-synced repo lagged behind the live file-edit-tool view — confirmed directly this time (not just inferred from mtimes) by finding `competition_mock_attempt_service.py` truncated mid-function in the bash mount (1267 lines vs. the true 1272) and `dashboard/page.tsx` throwing phantom "unterminated string literal" / unclosed-JSX-tag errors from `tsc` that don't exist when the same lines are read through the file tools. Running `pytest`/`npm run typecheck` from this sandbox would have tested a stale/corrupted snapshot, not today's actual changes, so those results were discarded rather than trusted. Verified instead by full manual re-read of both edited files end-to-end after editing (correct syntax, no naming collisions, `timedelta`/`_aware` already imported/defined in the backend file, single call site for the tier-color constants in the frontend file). New script (`fix_auto_submitted_timestamps.py`) syntax-checked clean with `py_compile` since it's a new file and synced correctly. **qa-reviewer must run real `pytest` + `npm run typecheck && npm run build`, and independently re-check the diff, before sre-devops delivers** — per this repo's standard split-workflow, same gate every prior round has gone through.

**3. Badge-unlock notifications silently dropped — a real bug inside round 9's own fix.** Shailesh reported that a student's "The High Achiever" badge showed correctly in the Trophy Room and the dashboard's "Latest Unlock" widget, but no "New Badge Unlocked" notification ever appeared in the notification panel — even though round 9 was supposed to be the permanent, universal fix for exactly this category of bug.

Root cause: `CreateNotification()` (`notification_service.py`) only does `db.add()` + `db.flush()` — it never commits, by design, so callers can batch several notifications into one transaction. Every OTHER side-effect in `_ProcessMockCompletionSideEffects()` has its own explicit `db.commit()` (the student/teacher/admin submission-notification block, the `gamification_processed_at` claim, `AchievementEngine.evaluate_mock_exam_submission()` internally), but the badge-notification loop (added in round 9) never did. Neither `SubmitCompetitionMockAttemptForStudent()` nor the route handler that calls it commits afterward either, and `get_db()` closes the DB session with no implicit commit. So every badge-unlock notification created through the live hook since round 9 shipped (2026-07-11 ~20:41 UTC) was created, flushed into the open transaction, then silently discarded the moment the request ended. The badge itself always persisted correctly (`evaluate_mock_exam_submission` commits its own writes *before* the notification loop even runs), which is exactly why Trophy Room and the "Latest Unlock" widget were right while the notification panel was empty — two different data sources, only one of which was actually being persisted.

Confirmed this wasn't specific to that one badge/student: traced the exact commit chain (`main.py`'s `get_db()` → route handler → `SubmitCompetitionMockAttemptForStudent` → `_ProcessMockCompletionSideEffects`) and found no commit anywhere after the badge loop, for any badge, for any student, since round 9 deployed. Also checked the only other three places `AchievementEngine.evaluate_mock_exam_submission` gets called (`routes_admin.py`, `schema_migration.py`'s dead/uncalled `ensure_mock_gamification_rewards_retroactive`, and the scripts under `backend/scripts/`) — all either commit correctly already or are unused, so this was isolated to the one live hook.

Fix: `competition_mock_attempt_service.py`'s badge-notification loop now calls `db.commit()` (with a matching `db.rollback()` in its except branch) right after each `CreateNotification()` call, mirroring the pattern the round-9 backfill script already used. New one-time script `backend/scripts/backfill_missing_badge_notifications.py` (`--dry-run`/`--apply`) closes the historical gap: for every `StudentBadge` a student has ever unlocked, it checks whether a matching `BADGE_UNLOCKED` notification exists (same title-match dedup convention used elsewhere in this codebase) and creates one backdated to `StudentBadge.unlocked_at` if it doesn't. Deliberately scoped to a student's entire badge history rather than just the round-9-to-now window, since checking existence is cheap and directly answers the question — no need to compute a precise time boundary. Not yet run against production.

**Not yet delivered** — Cowork has no route to push/PR/merge (see the 2026-07-10 entry below). Files changed this session:
- `backend/app/services/competition_mock_attempt_service.py` — `submitted_at` fix for the AUTO_SUBMITTED path, and the badge-notification missing-commit fix.
- `backend/scripts/fix_auto_submitted_timestamps.py` — new, one-time retro-correction script for round 10 (not yet run).
- `backend/scripts/backfill_missing_badge_notifications.py` — new, one-time backfill for dropped badge-unlock notifications (not yet run).
- `frontend/app/student/dashboard/page.tsx` — tier bar color palette (`TIER_BAR_CLASSES`/`REST_BAR_CLASSES`).

## 2026-07-11 update (rounds 6-9): mock-completion gamification bug — root-caused, fixed, delivered, backfilled, verified clean platform-wide

**tl;dr for tomorrow: there is nothing pending from this thread.** Rounds 6-9 are live in production (PR #309, commit `cd0a816`, confirmed deployed on both Vercel and Render). The historical data-repair backfill has been run to completion and independently re-verified with a full platform sweep — zero gaps remain. The only unresolved thread is a **product decision**, not code: see "Open decision for round 10" below. If nothing new has happened since this was written, there is no code work to resume — check with Shailesh whether he has a new task before doing anything.

### How this started

A second live batch of students (Pragya Ghosh, Sampreeti Mohapatra, Tanay Gupta, Sayantan Biswas) completed mock exams and got none of: XP/coins, badges, submission notifications (to themselves, their teacher, or admins). The developer also flagged that a prior round's leaderboard "time taken" numbers looked wrong. Explicitly instructed: this must be a **permanent, universal fix** covering every existing student and every student added in the future — not a one-off patch for the four named students.

### Root cause and fix (the permanent part — this is why nothing needs to be re-run for future students)

`backend/app/services/competition_mock_attempt_service.py` had two code paths that could grade/complete a mock attempt without ever running its notification/economy/achievement side-effects, because those side-effects only lived in a separate wrapper function that both paths bypassed entirely:
1. `EnsureCompetitionAttemptActiveOrSubmit()` — hit by any GET to load an attempt once the timer's already expired server-side (tab backgrounded, network dropped, student reopens the page after time's up).
2. The defensive result-summary repair inside `_result_payload()`.

Fixed by relocating the notification/economy/achievement side-effects into the core grading function itself (new `_ProcessMockCompletionSideEffects`, called from `SubmitCompetitionMockAttempt`), gated by a new `gamification_processed_at` timestamp column on `competition_mock_attempts` (Alembic migration `a1c9de3f7b21` + matching `schema_migration.py` self-heal entry) claimed atomically via `UPDATE ... WHERE gamification_processed_at IS NULL`. Every completion path now gets identical treatment exactly once, regardless of which one fires first, for every student past and future. **This is the core reason the fix is permanent and needed no per-student configuration.**

Also retired two fragile startup jobs (`ensure_mock_student_notifications_retroactive`, `recalculate_all_gamification_stats`) that used to run on every backend restart trying to paper over this same bug — the second did a full destructive wipe-and-rebuild of every student's badges on every deploy. Also fixed, same investigation: the dashboard's XP/coins wallet widget was never wired to the real economy ledger (fake local formula) — added `GET /student/economy` and wired `StudentWallet` to it. Also fixed: leaderboard `timeTakenSeconds` was fabricated from a hardcoded 60-minute assumption instead of reading the real column — pure read-path bug, no backfill needed.

### Delivery

Delivered via raw `git`/`gh` commands run directly in the developer's own terminal (not the AI-orchestrated qa-reviewer/sre-devops squad — same pattern as prior rounds, usage-cost driven). Manual verification substituted for qa-reviewer: `python -m pytest tests/ -q` (20 passed) and `npm run typecheck && npm run build` (clean, 62/62 routes) both confirmed green before push. One hiccup worth remembering: this repo has a server-side "MathPath safety guard" blocking direct pushes to `main` — the first push attempt was rejected; correctly redone as a feature branch (`fix/round-6-9-mock-gamification`) + PR, with local `main` reset back to `origin/main` afterward so it stayed clean. Merged as **PR #309** (commit `cd0a816`) after `gh pr checks --watch` passed CI. Confirmed live via direct Vercel and Render dashboard screenshots (both showing `cd0a816` deployed) before any data backfill was attempted.

### Backfill (historical data repair — this part is genuinely one-time, not recurring)

`backend/scripts/diagnose_mock_gamification.py` (read-only, safe anytime, takes names or `--all`) and `backend/scripts/backfill_mock_gamification.py` (`--dry-run`/`--apply`) were built and run from the developer's local terminal against production, guided step-by-step through several real terminal/environment issues along the way (missing `sqlalchemy` module, PowerShell paste/quoting, Render Internal-vs-External DB URL, a migration-not-yet-deployed column on a pre-migration schema — all resolved).

`--apply` was run twice. First run: 54 historical attempts scanned across 9 students, awarded missing XP/coins (6000 XP / 285 coins total), created missing notifications, and did a full badge/stat recompute (64 badges unlocked — see note below on why that number is much bigger than "notifications created"). A genuine bug was then found and fixed **in my own backfill script**: its notification-dedup logic gated ALL notification creation (student+teacher+admin) on one boolean ("does the student's MOCK_SUBMITTED notif exist"), which silently missed a real historical partial state — Meera Chatterjee's `MM_17` attempt had student+teacher notified but its admin notification was missing. Fixed the script to check student, teacher, and each individual active admin independently; re-ran `--apply`, closed that exact gap. **Final full-platform sweep via `diagnose_mock_gamification.py --all` (not scoped to named students) confirms zero remaining gaps across all 9 students and all 54 completed mock attempts** — every attempt shows OK for XP/coins, student notif, teacher notif, and admin notifs.

Why "badges newly unlocked" (64) was so much bigger than "notifications created" (a handful): the badge recompute does a full wipe-and-rebuild of `student_badges` every run, so it re-detects every badge a student has ever earned, not just new ones. But the now-retired startup job had already been organically notifying students about badges on every restart for months — so most of those 64 "newly unlocked" badges already had a notification sent long ago, correctly deduped against the `Notification` table (which the wipe never touches). Only genuinely first-time unlocks got a fresh notification. Confirmed by checking the retired job's source — identical title format and dedup logic to the new backfill script.

Two script-quality bugs were also found and fixed while running the backfill (bugs in my tooling, not the product): the dry-run summary undercounted notifications (fixed a counting bug where the counter only incremented in `--apply` mode), and the script went completely silent for minutes during the badge recompute with zero way to tell "working" from "hung" — root cause was Windows terminal stdout buffering plus `logging.info()` calls with no `logging.basicConfig()` ever configured, so nothing printed. Fixed by forcing `print(..., flush=True)` globally in the script and adding per-student progress lines.

### Open decision for round 10 (flagged, not actioned — needs Shailesh's call)

While investigating why the developer thought the backfilled notification timestamps "didn't look accurate," found a real, distinct, pre-existing bug: for `AUTO_SUBMITTED` (timer-expiry) attempts, `submitted_at` is set to `_now_utc()` — whenever the system happens to *detect* the expiry, not the true exam-end time — and `time_taken_seconds` is hardcoded to the full duration rather than measured (this is why every auto-submitted attempt shows exactly 100% time utilization). Correct fix would be `submitted_at = attempt.started_at + attempt.duration_seconds` for the auto path. **Two things need Shailesh's decision before any code changes:** (1) whether to fix this now as round 10, and (2) whether to retroactively correct the timestamps on the 8 attempts already backfilled under the old, inaccurate values. See `OPEN_ISSUES.md`'s "Active" section for the full technical writeup — full details also live in this thread's transcript if more context is needed.

### Rounds 6-8 (also delivered in the same PR #309, see `OPEN_ISSUES.md` for full per-round detail)

- Round 8: admin Live Radar widget + heartbeat were both silently 404ing due to a nonexistent `NEXT_PUBLIC_API_URL` env var (correct one is `NEXT_PUBLIC_API_BASE_URL`) — fixed by routing both through the shared `api` client.
- Round 7: admin Manage-tab pending-practice button bug, Trophy Room missing auth guard, Leaderboard missing auth guard — all fixed. Collector's Vault hardcoded-data finding remains unaddressed, needs a product decision (build real backend vs. hide the nav entry).
- Round 6: mock section renumbering extended to cover the live Attempt/Result pages (not just Instructions/Admin), heatmap timezone day-bucketing bug fixed.

## 2026-07-11 update (round 2): heatmap ghost-bar CSS bug + dynamic pace weighting + info icon — prepared, not yet delivered

Round 1 (below) shipped to main as PR #303 and was confirmed live by the developer via screenshots: tooltip data now correctly showed the two real mock exam completions (accuracy/time matched production records exactly), proving the multi-source data-pooling fix works. But the live screenshots also surfaced two things nobody had caught yet:

1. **Ghost bars.** The tooltip showed correct data on hover, but no visible colored bar rendered underneath it — a "ghost plot." Root cause: the heatmap row (`frontend/app/student/dashboard/page.tsx`) uses `items-end` on its flex container, which stops each day-column from stretching to the row's fixed height — each column's own height became auto/content-based instead. The bar inside is styled with `height: {percentage}%`, and a CSS percentage height cannot resolve against an auto-height parent, so it silently computed to zero. Pre-existing bug, not introduced by round 1 (round 1 never touched this render block) — this is very likely the exact thing `OPEN_ISSUES.md`'s long-standing "browser-QA the heatmap tooltip stack" item was warning about, just never confirmed until today. Fixed by giving each bar an explicit-height track (`h-20`) to size against, instead of an ambiguous auto-height parent.

2. **Tier didn't match intuition: 97% accuracy scored a lower tier than 92%.** Not a bug — a formula design issue, confirmed by the numbers: the pre-existing `flowState` formula (`accuracy × 0.7 + min(speed × 6, 30)`) uses a flat "5 questions/minute" threshold for the speed bonus. The 97%-accuracy day was a 60-minute mock exam (~1.7 q/min → small bonus); the 92%-accuracy day was a 30-minute mock of similar size (~3.3 q/min → much bigger bonus) — enough to flip the ranking. Discussed the tradeoff with the developer directly: speed is intentionally a major factor in this abacus program (not something to water down), but it needs to be judged relative to what each specific task was designed to take, not one flat number applied to a 5-minute drill and an hour-long exam alike. Resolution: score pace per attempt as `expectedDurationSeconds ÷ actualTimeTakenSeconds` (capped at 1.5×), averaged across a day's attempts, feeding the same ~30-point ceiling the speed bonus always had — the weighting balance is unchanged, only what "fast" is measured against changed.

Changes made this round (all additive):
- `backend/app/api/routes_student.py`: `/student/results` rows now include `expectedDurationSeconds` (from `DPS.default_duration_seconds`).
- `backend/app/services/competition_mock_attempt_service.py`: mock `attemptHistory` entries now include `expectedDurationSeconds` (from `CompetitionMockAttempt.duration_seconds`, already stored per-attempt).
- `backend/app/services/assessment_engine_service.py`: assessment `attemptHistory` entries now include `expectedDurationSeconds` (from `AssessmentAttempt.duration_seconds`).
- `frontend/types/assignment.ts`, `frontend/lib/api/student.ts`: added `expectedDurationSeconds` to the corresponding types.
- `frontend/app/student/dashboard/page.tsx`: bar-track CSS fix; `combinedActivityEvents` now carries `expectedDurationSeconds` through for all three sources; `grindData`'s speed calculation replaced with per-attempt pace-ratio math (falls back to a neutral 1.0 ratio when expected duration is unknown, rather than skewing the score); added an info-icon popover on the heatmap heading explaining what it tracks and what drives the tier, since students had no way to know before.

Verification status: same sandbox-staleness caveat as round 1 — this session's bash mount of the OneDrive-synced repo does not reflect live edits, so `pytest`/`npm run build` could not be run here. Verified by full manual re-read of every changed block after editing. qa-reviewer must run real verification before this ships, same as round 1.

Delivery process note: for round 1, the developer explicitly asked to bypass the AI-orchestrated qa-reviewer → sre-devops squad pipeline for the actual push/merge, citing Claude usage-limit cost concerns (subagent calls alone burned ~100k+ tokens for one delivery). Cowork gave raw `git`/`gh` commands to run directly in the developer's own terminal instead — same CI/branch-protection gates apply either way, just without the AI-agent ceremony wrapped around it. Expect the same request for round 2; the commands template is already established (see the developer's terminal history from round 1).

## 2026-07-11 update (round 1): Grind heatmap multi-source/multi-attempt fix — delivered as PR #303

Root cause (confirmed by reading the actual code, not assumed): `frontend/app/student/dashboard/page.tsx`'s `grindData` calculation only ever read `Results` (practice-sheet attempts from `/student/results`). `MockAssignments` and `Assessments` were already being fetched on the same page but never merged into the heatmap's day-bucketing — so mock exam and assessment-engine completions had zero path into the heatmap for any student. Additionally, both the mock-assignment and assessment-assignment backend payloads only ever exposed the *latest* attempt per assignment, so even after wiring them in, same-day multiple attempts on those two types would have collapsed to one.

Changes made this session (all additive, no existing fields removed/renamed):
- `backend/app/services/competition_mock_attempt_service.py`: new `_completed_attempt_history_payload()` helper; added `attemptHistory` field to `_assignment_with_current_attempt_payload()` (used by `/student/competition/mock-assignments`).
- `backend/app/services/assessment_engine_service.py`: new `_CompletedAssessmentAttemptHistoryPayload()` helper; added `attemptHistory` field to `AssessmentAssignmentPayload()` (used by `/student/assessments`).
- `frontend/types/assignment.ts`: new `AttemptHistoryEntry` type, added `attemptHistory?` to `Assignment`.
- `frontend/lib/api/student.ts`: added `attemptHistory?` to `StudentCompetitionMockAssignment`.
- `frontend/app/student/dashboard/page.tsx`: new `combinedActivityEvents` useMemo pools practice + mock + assessment events per calendar day; `grindData` now filters/aggregates from that pool instead of `Results` alone, so count/time/accuracy/flowState/tier are cumulative across every attempt and every activity type on a given day.

Verification status — read carefully before assuming this is done: this Cowork sandbox's mount of the OneDrive-synced repo is stale (confirmed via file mtimes lagging ~19 hours behind the actual edits), so `pytest`/`npm run build` could not be executed from this session against the real current files — running them here would have tested yesterday's snapshot, not today's changes. Verified instead by full manual re-read of every edited file end-to-end after editing (correct syntax, correct indentation, only already-imported symbols used, no existing field touched). Per this repo's own split-workflow (see `CLAUDE.md`), this is exactly the point where local Claude Code takes over: qa-reviewer must actually run pytest + npm build + independently re-check the diff before sre-devops delivers. Do not treat this change as verified until that happens.

Also confirmed while scoping this: only two consumers read the two touched backend endpoints (`/student/competition/mock-assignments` → dashboard + Mock Exams library page; `/student/assessments` → dashboard only), and neither reads beyond fields that already existed, so the new `attemptHistory` field should not affect them.

Known limitation intentionally left unfixed (logged in `OPEN_ISSUES.md`): mock assignments are filtered to the student's *current* level, so a mid-week level promotion would still hide that week's prior-level mock activity from the heatmap. Flagged, not fixed.

Delivered same day: qa-reviewer PASSED WITH NOTES (pytest 20/20, npm build clean, confirmed additive-only) via local Claude Code. Live-student pre-flight check couldn't reach production DB from that shell; developer explicitly authorized skipping it for this delivery (informed override — diff was read-only/additive and the developer confirmed no other students were realistically active). sre-devops hit a trust-boundary block (wouldn't accept a relayed authorization secondhand, by design) and a related Claude usage-limit concern from the developer led to finishing delivery via raw `git`/`gh` commands run directly in the developer's own terminal instead of the full AI-orchestrated pipeline. Merged as **PR #303**. Confirmed live by the developer's own screenshots: tooltip accuracy/time data matched real production mock exam records exactly.

## 2026-07-10 update: Apex Squad rebuilt as real subagents + delivery gate fixed

Diagnosed why #290–301 happened: `.github/workflows/mathpath-ci.yml` and the `MathPath Main Protection` branch ruleset (see `docs/project-memory/ENGINEERING_OPERATING_SYSTEM.md`) were already solid, but `.agents/apex_deliver.py` created the PR and immediately ran `gh pr merge --squash --admin` — bypassing branch protection and never waiting for CI to even finish. The "Apex Squad" in `.agents/AGENTS.md` was also self-contradictory with `.antigravity/instructions.md` (one said ship immediately with no approval, the other said wait for explicit approval).

Changes made this session:
- **`.claude/agents/*.md`**: 7 real Claude Code subagents (`frontend-architect`, `backend-architect`, `qa-reviewer`, `sre-devops`, `vfx-3d`, `data-telemetry`, `principal-designer`), each with scoped tool access and a concrete checklist instead of hype language. `qa-reviewer` is read-only (no Edit) so it can't rubber-stamp its own fixes. Only `sre-devops` runs git/PR/merge/deploy commands.
- **`.agents/apex_deliver.py`**: rewritten. No longer merges blind — waits for `gh pr checks --watch` before merging, merges without `--admin` by default (respects branch protection), and only bypasses via an explicit `--emergency-bypass` flag that requires typed interactive confirmation. Tags every successful delivery (`prod-YYYYMMDD-HHMMSS`) and pushes the tag.
- **`.agents/rollback.py`** (new): one-command rollback to any `prod-*` tag via a proper revert PR, so a bad deploy gets undone in one step instead of chased with forward-fix hotfixes.
- **Live-student safety check**: previously queried local SQLite (via default `DATABASE_URL`) and always silently reported "passed" — it could never see real production students. Now honestly reports UNVERIFIED and requires human override unless `PROD_DATABASE_URL` (a real prod connection string) is explicitly set.
- **`.agents/AGENTS.md`** and **`.antigravity/instructions.md`**: reconciled. Single operating mode now: low-risk UI/copy changes may go autopilot straight to qa-reviewer → sre-devops; anything touching schema/auth/payments/gamification state requires the Implementation Plan + explicit developer approval before any edit.
- **Memory doc fragmentation**: `CURRENT_STATUS.md`, `docs/project-memory/PROJECT_STATE.md`, `docs/project-memory/DAILY_HANDOFF.md`, and `.antigravity/session_status.md` were all stale and each prescribed a different "read this first" order (none of them even referenced this file). Added supersession banners to all four pointing here, and updated `docs/project-memory/README.md`'s canonical read order to lead with this file.

Not yet done (deliberately left for the developer): none of the above was committed/pushed. `PROD_DATABASE_URL` is not configured anywhere yet — the live-student check will report UNVERIFIED until Shailesh decides whether to wire it up. GitHub branch protection / required-status-checks were not verified from this sandbox (no `gh` CLI available here) — worth confirming the `MathPath Main Protection` ruleset is still active before relying on it as the real gate.

## 2026-07-10 update: squad expanded to 10 personas

Added on request:
- **`model-router`**: recommends Haiku/Sonnet/Opus/Fable per task against a concrete rubric (`.claude/agents/model-router.md`); the session's selected model never changes silently — a non-default recommendation requires developer approval before a specific task is delegated to a subagent running that model. Wired into `.antigravity/instructions.md` §6.
- **`browser-qa`**: closes a real, previously-unowned gap — nothing in the squad could drive a browser. Uses the Claude-in-Chrome tools to verify responsive/theme/tooltip behavior on the live site or a preview, targeting the still-open items in `OPEN_ISSUES.md` (heatmap tooltips, login responsiveness).
- **`scribe`**: owns end-of-session updates to `docs/project-memory/` (the exact upkeep that failed and produced the stale-doc mess fixed earlier this session). Accuracy-over-completeness, corrects in place rather than appending contradictions.

Also sharpened existing personas rather than further proliferating the roster: `qa-reviewer` now has an explicit security-review dimension and extra scrutiny for one-off data-repair scripts (the #292–299 failure category); `backend-architect` has the same data-script guardrail; `sre-devops` now explicitly owns running the pre-existing but previously unowned `load-testing/k6-load-test.js`.

## 2026-07-10 update: Cowork cannot push to GitHub — delivery moved to local Claude Code

Tested directly rather than assumed: this Cowork session has no route to `github.com`/`api.github.com` (proxy returns 403), and the repo's git credential helper is configured to call `C:/Program Files/GitHub CLI/gh.exe` — a Windows binary that only exists on the developer's actual machine, not in the Cowork sandbox. No GitHub connector is available in Cowork's MCP registry either. This means `sre-devops` cannot execute git push/PR/merge from within Cowork, full stop — no amount of process cleanup changes that.

Decision: delivery moves to **Claude Code running locally** on the developer's machine, where `gh.exe` is already authenticated and there's no network sandbox. Cowork sessions do implementation, review, and preparation; a local Claude Code session does verification + delivery (`qa-reviewer` → `sre-devops` → `apex_deliver.py`/`rollback.py`).

Set up to make this seamless:
- **New `CLAUDE.md`** at repo root — read automatically by Claude Code at session start. Points to this file, `git log`, `OPEN_ISSUES.md`, `.antigravity/instructions.md`, `.agents/AGENTS.md`, and explains the Cowork/local-delivery split. The `.claude/agents/` squad is picked up automatically by Claude Code — no extra setup needed there.
- `sre-devops.md` and `.antigravity/instructions.md` §5 both now state explicitly: don't attempt push/PR/merge from Cowork; hand off to local Claude Code instead.

Not yet done: nothing has actually been delivered via this new local path yet — it's configured but unexercised. First real Claude Code session on this repo should be treated as a dry run of the full pipeline, same as the Cowork squad itself was never exercised end-to-end this session.

## 2026-07-10 update: first real dry run caught two genuine Windows bugs

Ran the actual pipeline (qa-reviewer → sre-devops) on this session's own uncommitted rework via local Claude Code, exactly as planned. qa-reviewer correctly returned FAIL and blocked delivery — both bugs were real, both were artifacts of writing/testing these scripts in the Cowork Linux sandbox without accounting for Windows execution:

1. `.agents/rollback.py:74` — `git rm -rf --ignore-unmatch . > /dev/null` under `shell=True` on Windows runs through cmd.exe, not bash; `/dev/null` isn't a Windows path and was creating a stray `C:\dev\null` file instead of discarding output. Fixed: redirection removed entirely (unnecessary — `capture_output=True` already suppresses it).
2. `.agents/apex_deliver.py` (`check_live_students`, 4 call sites) and `.agents/rollback.py` (1 call site) — `print()` statements used emoji (⚠️, ❌) and an em-dash, which crash with `UnicodeEncodeError` on Windows' default cp1252 console encoding. Fixed: replaced with plain ASCII (`WARNING:`, `FAILED:`, `-`) in both files. Verified via a full non-ASCII scan of every `print()` line in both scripts — clean.

Both fixes applied directly from Cowork (same mounted repo), verified with `python -m py_compile`, not yet re-reviewed by qa-reviewer or delivered.

Non-blocking notes qa-reviewer also raised (acknowledged, not fixed yet — follow-up, not a gate): unescaped f-string interpolation of commit messages/tags into `shell=True` commands is a latent shell-injection risk (low real-world severity since this is a locally-invoked developer tool, not exposed to untrusted input); and inferring merge success purely from `gh pr merge --auto`'s return code has a theoretical race-condition edge case worth hardening later.

## 2026-07-10 update: live-status bridge between Cowork and local Claude Code

There's no session-bridging API between Cowork and Claude Code — confirmed when the developer asked whether Cowork could directly monitor a local Claude Code terminal. Closest real option (computer-use screenshots of VS Code) is view-only for IDEs/terminals and not continuous, so not a real substitute. Instead, added a lightweight shared-file bridge:

- **New `docs/project-memory/CLAUDE_CODE_STATUS.md`**: a snapshot (overwritten, not appended) that the *orchestrator* of a local Claude Code session updates at each milestone — task started, subagent verdict, blocked, completed. Deliberately not `scribe`'s job (scribe does full end-of-session consolidation; this is a live ticker).
- `CLAUDE.md` now instructs Claude Code to keep it current.
- `docs/project-memory/README.md`'s canonical read order now includes it as read #2, right after this file.

Not yet exercised — created empty/idle. First real use will be the next task given to the local Claude Code session.

## Who / What

- Shailesh is running MathPath as a freelance client project. Prefers concise, direct communication.
- MathPath: role-based abacus/mental-math platform. Admin, Teacher, Student portals.
- Stack: Next.js + TS + Tailwind + React Query + Sentry + react-three-fiber (frontend, Vercel), FastAPI + SQLAlchemy 2 + Alembic + Postgres (backend, Render).
- Live: https://math-path-platform.vercel.app/ (verified up 2026-07-10), https://mathpath-backend.onrender.com (/health returned ok, v1.0.0, production, 2026-07-10).
- GitHub: https://github.com/sg2499/MathPath-Platform

## Repo state as of 2026-07-10

- HEAD: `42fefd4` (2026-07-10, PR #301). NOTE: `PROJECT_STATE.md` / `DAILY_HANDOFF.md` are STALE — last updated 2026-07-07 at `dd9938e`, 93 commits behind.
- PRs #290–#301 (July 8–10): a 12-commit hotfix chain around a gamification retro-reset / master reset script (falsely awarded "unstoppable streak" badges, chronological streak recalculation, retro badge awarding). Multiple commits fixed crashes the previous commit introduced (ModuleNotFoundError server startup crash, import errors, SQL syntax, `submitted_at` AttributeError). All merged straight to production.
- Local worktree shows ~200 modified files vs git — likely OneDrive/line-ending noise, not real edits. Verify before assuming.
- There is a NESTED duplicate repo at `MathPath-Platform/` inside the root folder. Housekeeping candidate.

## Previous workflow (Antigravity) — assessment done 2026-07-10

- Workflow: agent edits code → `python .agents/apex_deliver.py "<msg>"` → branch, commit, `git push -f`, PR, `gh pr merge --squash --admin` (bypasses branch protections) → Vercel/Render auto-deploy → `python .antigravity/scripts/monitor_deploy.py` polls health.
- Key files: `.agents/AGENTS.md` (Apex Squad personas), `.antigravity/instructions.md` (engineering protocol — good discipline on paper), `.antigravity/session_status.md` (STALE: last updated 2026-06-12).
- Known weaknesses identified: `check_live_students()` in apex_deliver.py queries LOCAL SQLite, not production Postgres (safety check is ineffective); merges bypass branch protection; code was repeatedly shipped without being run first (see #290–#301); memory files not maintained.

## Agreed working principles for Cowork sessions

1. VERIFY BEFORE PUSH: run typecheck / pytest / build locally (or in sandbox) before any commit reaches main. The #290–#301 chain was preventable.
2. Keep this file + project-memory docs updated at end of every session.
3. Model strategy: Sonnet as default driver for routine fixes/UI work; Fable/Opus only for hard debugging, migrations, architecture. Haiku never for code changes.
4. First-session TODO: verify git push / gh auth works from the Cowork environment before claiming the full auto-deploy loop.

## Top open work (from OPEN_ISSUES.md 2026-07-07 + session findings)

1. Browser-QA on live site (Claude Cowork has Chrome tools — Antigravity could not do this; Playwright was blocked locally with spawn EPERM):
   - Student dashboard grind-heatmap tooltips (`ec9d041`..`dd9938e`): Sun–Sat week alignment, empty future days, readability, both themes.
   - Login responsiveness after `4ecd510`: 320px width, landscape, tablet, zoom.
2. Verify the July 8–10 gamification reset actually produced correct badge/streak state in production.
3. Fix `check_live_students()` to check production (API/health or prod DB) instead of local SQLite.
4. Confirm backend security-column migration `7f92d7d` executed in production.
5. Build warnings: move Sentry init to `instrumentation.ts`, set `metadataBase`.
6. `npm run typecheck` fails on clean checkout before `.next/types` exists (build-first requirement) — fix or document.
7. `flowState` tooltip metric estimates 5 questions/sheet when `totalQuestions` absent — decide if payload-backed count is needed.
8. Refresh stale memory docs (PROJECT_STATE.md, DAILY_HANDOFF.md, session_status.md) to reflect #290–#301.

## 2026-08-07 update: Bridge Module (BM) built and all 3 workflows verified end-to-end — ready for local delivery

New module added: **Bridge Module (BM)**, sitting between Preparatory and Intermediate in the platform hierarchy (YLM-PM-BM-IM-MM). Single level BM-L1, 40 lessons / 200 DPS. Fully dedicated engine (`question_engine/bm/`), seed config/script, and assessment/mock section registry (`BM_COMPETITION_LEVEL_REGISTRY`) — zero shared code with any PM/IM/MM engine, per Shailesh's explicit "dedicated engine, entirely independent" instruction. Per Shailesh's standing instruction this session ("implement all 3 workflows... verify everything end to end locally and only then we will move to pushing and merging"), all three flows (DPS practice, assessment generation, competition mock generation) were built and then verified live via Claude-in-Chrome against the developer's running local dev servers, not just unit tests. Backend suite: 281 tests passing (`backend/tests/test_bm_dps_assessment_mock.py`, new, ~730 lines).

Four real bugs were found and fixed during live verification — all the same underlying bug class (a module allow-list/gate that got BM's *implementation* added but not updated at every *place that lists modules*), each caught by actually exercising the UI rather than assuming unit-test coverage was sufficient:

1. **`assessment_blueprint_service.py`** — `SECTION_WISE_ASSESSMENT_MODULES`/`_SECTION_WISE_REGISTRIES` didn't include "BM", routing Assessment Studio's `create_blueprint()` down the wrong (lesson-wise) path. Caught by automated test before reaching the browser.
2. **`assessment_blueprint_service.py`** — `section_marks_metadata()` and `validate_section_distribution()` both gated their weighted-marks branch on `module_upper == "PM"` instead of `in {"PM", "BM"}`. Caused Assessment Studio to treat BM as fully flat, silently computing a 164-real-mark distribution that displayed as "100/100, ready to publish." Caught live in Chrome (server-side `ASSESSMENT_MARKS_MISMATCH` error on publish); fixed both gates, added 2 regression tests, re-verified live (weighted UI mode + successful 100-mark publish).
3. **`frontend/app/admin/competition/mock-studio/page.tsx`** — `CompetitionMockSupportedModuleCodes` (a frontend-only hardcoded `Set(["MM","IM","PM"])`) didn't include "BM", so BM was entirely absent from the Competition Mock Studio's Module dropdown — couldn't even start creating a BM mock. Fixed (added "BM" + BM-specific default question count/duration), verified live.
4. **`competition_mock_generation_service.py`** — `CompetitionMockSectionPlan()` (powers the admin's Section Allocation panel) had explicit branches for MM/IM/PM but none for BM, so BM fell through to a generic per-DPS-title fallback that invents its own ad-hoc section keys, completely unrelated to `BM_COMPETITION_SECTION_DEFINITIONS`'s real 6 keys that generation actually uses. Submitting the panel's mismatched keys back at generation time made every section resolve to 0 required questions — the mock silently saved as a DRAFT with `total_questions = 0` and zero persisted question rows, with a false "Draft mock generated" success message and no error anywhere. This was the most serious of the four: a phantom-success empty artifact, not a blocked action. Fixed with a proper BM branch in `CompetitionMockSectionPlan()`, plus a new defense-in-depth check in `GenerateCompetitionMockDraft()` (`COMPETITION_MOCK_GENERATION_EMPTY`, 400) so this bug *class* — for BM or any future module — can never again silently persist an empty mock. Added 3 regression tests (section-plan keys, full generate-with-real-keys-produces-60-questions, and the empty-mock safety-net itself). Re-verified live end-to-end: correct 6-section allocation panel, "BM-L1 Live Verify Mock v2" generated with 60/60 questions, spot-checked Question 1 (Add/Less, correct arithmetic) and Question 55 (Concept Drill, ADD=1028/TIMES=8 → 8224, TIMES correctly randomized in the 5–10 range).

**Not yet done**: none of this session's work has been committed, pushed, or merged. Per the established Cowork/local-Claude-Code split documented above, this Cowork session has no route to GitHub — delivery (qa-reviewer → sre-devops → `apex_deliver.py`) needs to happen from a local Claude Code session, which will find all the code changes already in the working tree. Two harmless leftover test artifacts exist in the live admin UI from this session's live verification (a BM-L1 "Live Verify Assessment" pair under Manage Assessments, and two BM-L1 mock DRAFTs under Manage Mocks, one of which — "BM-L1 Live Verify Mock" — deliberately demonstrates the pre-fix 0-question bug and can be deleted) — left in place rather than deleted, since permanent deletion isn't performed autonomously.

## Read order for a fresh session

1. This file
2. `CURRENT_STATUS.md`
3. `git log --oneline -30` (trust git over the memory docs until they're refreshed)
4. `docs/project-memory/OPEN_ISSUES.md`
5. `.antigravity/instructions.md` (engineering guardrails — still worth honoring: surgical edits, workbook compliance, plan-before-code)
