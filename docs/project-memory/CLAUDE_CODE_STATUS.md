# Claude Code — Live Status

This file is a snapshot, not a log — the active Claude Code session **overwrites** the "Current" section at each milestone rather than appending, so it stays short enough to be cheap to read. It exists so a Cowork session can check what's happening in a local Claude Code session without the developer copy-pasting terminal output back and forth. For full session history, see `docs/project-memory/COWORK_HANDOFF.md` and `DAILY_LOGS/`.

## Current

- **Updated:** 2026-07-11 (written by Cowork — developer confirmed live mocks have ended, rounds 6-9 CLEARED for delivery)
- **Task given (round 9):** A second live batch of students (named: Pragya Ghosh, Sampreeti Mohapatra, Tanay Gupta, Sayantan Biswas) completed mock exams and got none of: XP/coins, badges, submission notifications (student, teacher, or admin), and the leaderboard showed a fabricated "time taken" value. Root-caused and fixed all four permanently: (1) `competition_mock_attempt_service.py` had two hook-less completion paths (`EnsureCompetitionAttemptActiveOrSubmit`'s lazy GET-triggered auto-submit, and `_result_payload`'s defensive repair) that could silently complete an attempt without ever running notifications/economy/achievements, which only lived in a separate wrapper those paths bypassed. Fixed by relocating those side-effects into the core grading function itself, gated by a new atomically-claimed `gamification_processed_at` column (migration `a1c9de3f7b21` + schema_migration.py self-heal) so every completion path gets identical treatment exactly once. (2) Retired two fragile startup jobs (`ensure_mock_student_notifications_retroactive`, `recalculate_all_gamification_stats`) that ran on every backend restart trying to paper over this same bug — the second did a full destructive wipe-and-rebuild of every student's badges on every deploy, never touched XP/coins, and neither covered teacher/admin notifications. Replaced with a deliberate one-time backfill script. (3) Separately found and fixed: the dashboard's XP/coins widget never read the real economy ledger at all (fake local formula, didn't even count mocks) — added `GET /student/economy` and wired `StudentWallet` to it. (4) Separately found and fixed: leaderboard `timeTakenSeconds` was fabricated from `time_utilization_percentage` against a hardcoded 60-minute assumption instead of reading the real `time_taken_seconds` column — pure read-path bug, no backfill needed. Backfill tooling: `backend/scripts/diagnose_mock_gamification.py` (read-only) and `backend/scripts/backfill_mock_gamification.py` (`--dry-run`/`--apply`, idempotency-safe, backdates notifications, does the one-time badge/stat recompute). Neither script has been run yet.
- **Task given (round 8, still undelivered):** Developer reported the admin "Live Radar" widget shows zero active students even when students are logged in and live on the platform, and wants it fixed while the round-6/7 fixes are also held pending post-exam delivery. Root cause: `frontend/components/admin/LiveRadarWidget.tsx` and `frontend/hooks/useHeartbeat.ts` (mounted globally in `AppShell.tsx`) both built their fetch URL from `process.env.NEXT_PUBLIC_API_URL`, a variable that doesn't exist anywhere in this project's env config — the real one is `NEXT_PUBLIC_API_BASE_URL`. Every request silently 404'd against the frontend's own origin and was swallowed by a catch block, so the widget's live-student fetch never reached the backend (always empty) and the heartbeat ping meant to keep `last_active_at` fresh during idle browsing never fired either. Fixed by routing both through the shared `api` axios client instead of raw `fetch`, matching the rest of the codebase. Backend (`/api/admin/live-students`, `/api/auth/ping`, `last_active_at` tracking in `dependencies.py`) was already correct. Not delivered — same live-exam hold as round 7.
- **Task given (round 7, still undelivered):** Following a full end-to-end re-audit of two large shared components (`DetailWorkspaceViews.tsx`, `CollectorVaultWorkspace.tsx`) that hadn't had a line-by-line pass this session, the developer approved fixing 3 of the resulting 7-item cumulative findings list, but explicitly said NOT to push/deliver right now because 6-7 students are actively mid-mock-exam and a push would trigger a Vercel/Render redeploy. Applied as local, undelivered code edits: (1) `DetailWorkspaceViews.tsx` ~line 1966 — admin Manage-tab "Start Practice" quick-action now compares `Issue.label` (human-readable) instead of raw `row.status` (backend enum), matching the correct pattern already used elsewhere in the file. (2) `frontend/app/student/achievements/page.tsx` (Trophy Room) — added missing `useProtectedPage(["STUDENT"])` guard and a proper `ErrorState` render on fetch failure (was silently swallowing errors before). (3) `frontend/app/student/competition/leaderboard/page.tsx` — added missing `useProtectedPage(["STUDENT"])` guard (deliberately left its missing `AppShell` wrapper alone — looks like an intentional immersive-layout design choice, flagged not fixed).
- **Stage:** Implementation complete for rounds 6-9, all verified via direct re-read in Cowork. Round 9's read-only diagnostic was additionally verified against live production data by the developer (confirmed the diagnosis: 8/8 attempts across the 4 named students missing everything), and the backfill script's `--dry-run` was run twice by the developer from their local terminal — the first run surfaced a real counting bug in the script (fixed), the second run confirmed correct output (54 attempts scanned, 9 need economy award = 6000 XP / 285 coins, 24 attempts need notifications = 72 total). `--apply` has NOT been run yet (blocked by design until the migration is actually deployed). None of rounds 6-9 have been pushed, PR'd, or merged — this Cowork session has no GitHub credentials/route to github.com and cannot do so.
- **Active persona:** none (idle)
- **Last action:** Developer confirmed the live mock exam window has ended and gave the go-ahead to deliver. Cowork updated this file; no git/push/PR commands have been run (can't be, from this sandbox).
- **Blocked on:** nothing anymore for delivery timing — cleared. Still blocked on a human running the actual delivery: that has to happen from the local Claude Code terminal (this repo's `sre-devops` persona there has the authenticated `gh.exe`/git credentials Cowork lacks).
- **Next step (to run in local Claude Code, not here):** (1) qa-reviewer over the full rounds 6-9 diff, then sre-devops delivers via `python .agents/apex_deliver.py` (or the standard PR flow) once qa-reviewer PASSes — this covers the migration `a1c9de3f7b21`, the model/schema_migration.py self-heal, the `competition_mock_attempt_service.py` refactor, both leaderboard endpoints, the new `/economy` endpoint + dashboard wiring, the Live Radar/heartbeat fix (round 8), and the Manage-tab/Trophy Room/Leaderboard guard fixes (round 7); (2) after the migration is live, run `python scripts/backfill_mock_gamification.py --apply` (dry-run already confirmed clean — 9 economy awards, 72 notifications, badge/stat recompute); (3) re-run `python scripts/diagnose_mock_gamification.py "Pragya Ghosh" "Sampreeti Mohapatra" "Tanay Gupta" "Sayantan Biswas"` to confirm all four are now green, and spot check Nishant Gantayet / Meera Chatterjee / Sampreeti Mohapatra too since the backfill covers them as well; (4) spot-check the dashboard wallet, Trophy Room, and leaderboard live. Also still open: round 6's live-attempt verification (section badges gap-free through instructions -> attempt -> result review, heatmap day-bucketing correct) was never confirmed post-deploy — bundle into the same post-delivery verification pass. The Collector's Vault hardcoded-data finding remains unaddressed pending a product decision from the developer.

## Last completed milestone

- Round 9 (mock-completion side-effects permanently relocated into the core grading function + idempotency column; real economy endpoint + dashboard wallet wiring; leaderboard time fabrication fixed; diagnostic + backfill scripts written) was implemented and verified in Cowork on 2026-07-11, stacked on top of rounds 6-8, all four rounds still held undelivered for the same live-exam reason.
- Round 8 (Live Radar widget + heartbeat both silently 404ing due to a nonexistent `NEXT_PUBLIC_API_URL` env var — fixed by routing both through the shared `api` client) was implemented and verified in Cowork on 2026-07-11, stacked on top of rounds 6-7.
- Round 6 (mock Attempt/Result-page section renumbering + heatmap timezone fix) and round 7 (admin Manage-tab bug, Trophy Room guard, Leaderboard guard) were both implemented and verified in Cowork on 2026-07-11, but neither has been delivered yet — both are held pending the developer's go-ahead once the live student mock exam window closes.
- Round 5 (section-title stale-number fix on Instructions + Admin pages) and the full-portal audit that surfaced round 6's gaps were both completed 2026-07-11 in Cowork; round 5's delivery commands were handed to the developer but confirmation of merge/deploy was not received before round 6 started.
- Round 4 (PR #306) shipped 2026-07-11: renumbered `sectionNumber` sequentially on the admin Mock Studio page and student instructions endpoint when a section was omitted. Confirmed via developer screenshots that Vercel/Render both deployed it, but the developer then reported the gap was still visible live — root-caused via DevTools Network tab to the round-5 issue (title text still had the stale number embedded, superseding round 4 as incomplete).
- Rounds 1-3 of the grind-heatmap fix (multi-source pooling, ghost-bar CSS fix, dynamic per-task pace weighting, tier bar colors, scoped info popover, flip-on-close bug) delivered as **PR #303**, **PR #304**, **PR #305** — all merged and confirmed live via developer screenshots.
- Delivery-safety rework (CI-gated apex_deliver.py, rollback.py, 10-persona subagent squad, reconciled operating protocol) shipped to production 2026-07-10: qa-reviewer FAIL -> manual fixes -> qa-reviewer PASS WITH NOTES -> human-confirmed override of check_live_students() UNVERIFIED gate (tooling/docs-only diff) -> PR #302 merged -> tag prod-20260710-195723 -> deploy verified healthy. One self-corrected process note: sre-devops briefly ran `git config core.longpaths true` while unblocking a corrupted local ref, then reverted it in the same step; no config drift remains.

## Notes for whoever reads this next

- If "Blocked on" is non-empty, don't assume it resolved itself — check before proceeding.
- If "Updated" looks stale relative to the terminal's actual activity, trust the terminal, not this file, and flag the drift.
