# Open Issues

Last updated: 2026-09-01, later same day (Cowork — leaderboard rank-change notifications, XP rank-tier notifications, and a new teacher "Leaderboard" nav dropdown built end to end, per Shailesh's explicit request that neither students nor teachers were being notified about podium finishes or rank movement after finishing a DPS sheet or mock exam. New shared `leaderboard_service.py` (every ranking query, each accepting `exclude_attempt_id` so a submission's own contribution can be excluded to compute the "before" rank — no rank/position is ever stored in this schema, every leaderboard is computed live); new `rank_notification_messages.py` (396 message templates: 280 student-facing across 4 dimensions × 5 outcomes incl. `FIRST_PLACEMENT`, 96 teacher-facing podium-only, 20 for XP rank-tier-ups, all with anti-repeat selection so students rarely see the same phrasing twice); new `rank_notification_service.py` (orchestration, hooked into the same submission path as existing gamification side effects in `attempt_service.py`/`competition_mock_attempt_service.py`/`economy_service.py`, so a rank-change notification only ever fires on the student's own submission, never a passive overtake). Teacher sees the identical full leaderboard every student sees (own students flagged, not scoped down) via 6 new `routes_teacher.py` endpoints and two new pages (`frontend/app/teacher/leaderboard/dps`, `.../mock`) reached through a new sidebar "Leaderboard" dropdown (DPS Leaderboard, then Mock Leaderboard). `NotificationsBell.tsx` deep-links every notification to the exact leaderboard/tab/scope. DPS podium **badges** explicitly deferred by Shailesh to conserve build time/credits — DPS podium **notifications** are not deferred and are part of this build. Verified: full backend pytest suite 286/286 passing (run three times across the build, most recently after every backend change including the new teacher endpoints); every new/modified `.ts`/`.tsx` file esbuild-parse-checked against the actual committed content; a from-scratch SQLite integration test of the live before/after rank comparison including the `FIRST_PLACEMENT` edge case; all touched/new files confirmed byte-for-byte CRLF. Full writeup, including a candid list of remaining notification gaps (streak-at-risk, assignment due-soon, manual-intervention queue, class-wide digest, passive-overtake awareness, student inactivity, new-student-in-class), in `COWORK_HANDOFF.md`'s matching entry. **Status: prepared, not yet delivered — 18 files (7 new, 11 modified) sitting in the local working tree on branch `chore/repo-cleanup-and-doc-sync`, needs a local Claude Code session or Shailesh directly to branch off, commit, and push; nothing live-tested in a real browser yet since this Cowork session has no way to run the Next.js dev server against Shailesh's machine.**)

Prior "Last updated" (2026-09-01 (Cowork — PR #494's CI failure (3 `ASSESSMENT_MARKS_MISMATCH` test failures) diagnosed and fixed, confirmed merged by Shailesh; confirmed live and working -- Shailesh directly: "i have confirmed them live and they work fine". Separately, built the Mock Studio marks-balancing fix (Concept Drill/Skill Stacker sections now auto-balance every mock to exactly 100 marks, mirroring the existing assessment rule) across every level with a weighted section, plus removed the Avg Score column from the mock leaderboard. Full writeup in `COWORK_HANDOFF.md`'s matching 2026-09-01 entry. **Status: (1) [RESOLVED 2026-09-01] the mock-marks-balance + leaderboard-column change is CONFIRMED MERGED AND DEPLOYED as PR #495, commit `d00ec03082bf3fa08c8c41dfe23751d855e61cae`. Confirmed via Render (`mathpath-backend`, deploy `dep-dab66ogu01pc73e69js0`, status `live`) and Vercel (`math-path-platform`, deployment `dpl_E6dsA3ASWMWUaTfirCSxeWKF4ETj`, target production, state READY). (2) [RESOLVED 2026-09-01] PR #494 confirmed live and working; (3) the three items already flagged 2026-08-25 (deploy-pathway ambiguity, round-2 card-sizing merge confirmation, the leaderboard-revamp/12-music-track scope) remain open and unresolved.**))

Prior "Last updated" (2026-08-25 (Cowork — three teacher-facing bugs fixed: lesson-progress badge denominator (was PUBLISHED-only, now counts all 5 real DPS per lesson), Attempt Review stat-card label wrapping + a follow-up sizing-consistency round, and DPS practice-sheet timers across all 7 content seed scripts corrected to a flat <=10q=5min/>10q=10min rule, replacing a mix of wrong tiered/flat values none of which matched this rule. Round 1 (badge + wrap-fix + timer rule) CONFIRMED MERGED AND DEPLOYED — Render backend log + Shailesh's own live screenshot both confirm. Round 2 (card sizing polish) handed over as commands, not yet confirmed executed. **Three open items added to Active below, all needing Shailesh directly: (1) which deploy pathway is actually authoritative — Render auto-deploy-on-merge vs. the manual SSH `DEPLOY_PLAYBOOK.md` flow, both appear real but the relationship between them was never confirmed; (2) round 2's merge/deploy confirmation; (3) a pending leaderboard revamp + "new DPS leaderboard" + 12 music tracks that Shailesh referenced when closing this thread, with ZERO record anywhere in this repo's project-memory — full writeup in `COWORK_HANDOFF.md`'s matching 2026-08-25 entry.**))

Prior "Last updated" (2026-08-14, later still (Cowork — 7th round on the parent-report PDF arc, a second wave of live-testing polish asks on top of the still-undelivered 6th round (curriculum override fix + card overflow fix + Fable elevation pass). Six fixes in `report_export_service.py`/`routes_admin.py`, all verified via synthetic-payload rendering (not yet live-tested): (1) stat-card labels (Practice Sheets/Practice Accuracy/Assessments Cleared/Levels Promoted) center-aligned and enlarged. (2) Module names now render in full (Young Learners Module, Preparatory Module, Bridge Module, Intermediate Module, Master Module) instead of short codes, via a new shared `MODULE_DISPLAY_NAMES` constant in `routes_admin.py` plus a matching expanded local dict in `report_export_service.py`. (3) Removed the generic "Continued on page 2..." footer line. (4) Level-By-Level Breakdown table's "Zone" column header re-anchored to match the centered Zone chip below it (was left-aligned at the wrong X, drifting visibly left of the pill). (5) Progression Timeline empty-state message ("The first level movement will be recorded here after the next promotion.") had its flag icon and text independently positioned and overlapping for this sentence length — fixed by measuring text width and centering icon+text as one group with a fixed gap, mirroring the icon-then-text pattern already used correctly in the populated timeline rows. (6) Hero/identity card subtitle no longer shows "· Class {ClassSection}" — only the student code, per Shailesh's explicit list of what belongs in that card (photo, name, code, module chip). Verified: `py_compile` clean; built a synthetic `ReportData` payload, called `BuildParentProgressPdfBytes()` directly, rasterized both pages, visually confirmed both fixes. **Prepared, not yet delivered** — bundled with the still-undelivered 6th round below; nothing from either round has shipped. Not yet live-tested on `mock.mathpath.in`; the missing-logo bug remains open and unrelated.)

Prior "Last updated" (2026-08-14, later same day (Cowork — 5th round on the parent-report arc, bundled with a separate curriculum-correctness fix per Shailesh's "wrap them up together." (1) **Curriculum progression-path fix:** new `Level.next_level_id` self-referential FK, synced on every backend startup (`ensure_level_next_level_id_column()`, auto-derives same-module + IM→MM sequencing, hand-set overrides for the 3 fixed enrollment paths' module boundaries — YLM-L3→PM-L2, PM-L4→IM-L1, BM-L1→IM-L1). `NextLevelForLevel()` (backs real Promote) and `_admin_parent_report_next_learning_destination()` (backs the parent report's "next level" text — previously its own separate logic that got 2 of 3 boundaries wrong) now share this one source of truth. Verified via a standalone harness seeding the real curriculum shape — all 13 transitions correct, idempotent. (2) **Parent report PDF full redesign**, dispatched to Fable per Shailesh's explicit instruction: bespoke achievement-medallion headline (was a generic donut), Level-By-Level Mastery + Accuracy Profile breakdowns (was one aggregate percentage), full progression timeline, de-duplicated page 1/page 2 content, one consolidated palette, conditional photo-vs-initials-avatar handling. Also deleted a dead, silently-shadowed duplicate `BuildParentProgressPdfResponse` definition found during the rewrite. Verified via a real rendering smoke test (3 scenarios, PDF bytes actually generated, all pages visually inspected) — sample PDFs left in this session's outputs for Shailesh's review before shipping. (3) Fixed two bugs Shailesh flagged from a live teacher-login screenshot: redundant/oversized metric cards on `/teacher/progress-reports` (now matches the platform's established single-card convention), and separately diagnosed a `deploy-simple.sh` "hang" at the `gh run download` step as a real-but-silent large-artifact download (~266MB, no progress indicator), not an actual bug — confirmed by watching a manual run complete. **MathPath logo missing from the header across all 3 logins — investigated, code and the committed asset are both confirmed correct and unchanged, root cause NOT YET FOUND; needs Shailesh to check the live page's Network tab for what the logo image request actually returns.** **Prepared, not yet delivered** — needs a local Claude Code session; not yet live-tested, and the logo bug is still open.)

Prior "Last updated" (2026-08-14 (Cowork — 4th round on the parent-report feature, following 3 delivered/merged rounds and Shailesh's own live bug reports against `mock.mathpath.in`. Built a dedicated teacher "Progress Reports" tab (`/teacher/progress-reports`, new nav entry between Assessment Tracker and Promotion History) replacing the old embedded report panel on the student detail page, which he explicitly asked to be replaced ("unorganised and cannot be tracked properly"). Table columns deliberately exclude Score/Accuracy (already live on Assessment Tracker; showing both risks the two tabs disagreeing after a re-attempt) — Module/Level/Assessment/Published Date/Download only, his explicit call after I flagged the redundancy. Also fixed, from his screenshots: STUDENT_HAS_NO_TEACHER false positives on Publish (a real bug — students linked via the legacy `Student.teacher` name column instead of the `teacher_id` FK were wrongly treated as unassigned), download errors showing a generic Axios message instead of the backend's real reason (blob-typed error responses were never being parsed as JSON), and two layout bugs (cropped/wrapping action buttons, misaligned Generate button). New `ParentReportEmailLog.assessment_title` column captures the assessment name at generation time so the new tab doesn't need a second client-side join. Notification routing (`NotifyParentReportPublishedToTeacher` + `NotificationsBell.tsx`) repointed from the removed embedded panel to the new tab, with deep-link highlight/auto-expand. **Prepared, not yet delivered** — needs a local Claude Code session; not yet live-tested (rounds 1-3 were).)

Prior "Last updated" (2026-08-13 (Cowork — Parent progress report EMAIL SENDING REMOVED, replaced with a Generate/Download + explicit "Publish to Teacher" flow, plus a new teacher-facing notification + in-app report view, per Shailesh's explicit direction ("we need to remove the email sending functionality altogether... report generation option from where the reports can be downloaded by the admin and sent to the students by the teachers... also we need a feature for the teacher login where once the admin generates the progress report for a student it should be notified to the teacher in the notification dropdown... and also the teacher can then view the report on their end"). Presented a plan first per his ask ("so pls first tell me how can we proceed"); he then chose, via an explicit clarifying question, a **separate "Publish to Teacher" step** over auto-notify-on-generate — this is now the binding design: generating/downloading a report never notifies the teacher, only an explicit Publish action does. See the new Active entry below for full detail — **prepared, not yet delivered**, needs a local Claude Code session; not yet live-tested.)

Prior "Last updated" (2026-08-11, later same day (Cowork — YLM Assessment + Competition Mock workflows built end to end for all 3 levels, per Shailesh's explicit instruction after the digit-width fix below: "okay then go ahead and carefully implement both the flows for all the levels in this entire module." New dedicated `ylm_competition_mock_generation_service.py` (mirrors BM's shape, zero cross-module imports); YLM-L1/L3 get 3 sections (Addition/Subtraction/Add-Less), YLM-L2 gets 2 (no Add-Less-flavored lesson exists there); flat 1 mark/question throughout (joins MM's scheme, not IM's). Level 3's Addition/Add-Less sections have no source lesson at all (Lessons 25-31 are pure Subtraction, Lesson 32 is the final revision) — sourced instead from Lesson 32's own revision pool via the pre-existing `lesson_number=0` bypass in `enrich_config_with_lesson_rule()`, needing zero new generator code. All 4 BM-equivalent backend gate locations found and updated (checked against the BM rollout's own "4 missed locations" history so as not to repeat it) plus the frontend mock-studio module dropdown and assessment-blueprint marks labels. Caught and fixed one real capacity bug during verification (a lesson-sourced concept pool copying only its DPS-1 digit-width default left some sections unable to produce enough unique questions — widened to the lesson's full DPS digit range, which also better matches "all kinds of X sums throughout the level"). Verified via standalone scripts: section coverage, zero-duplicate-question mock generation at both 60 and 100 question totals across all 3 levels, assessment marks invariant, and all 6 pre-existing YLM tests still pass clean. See the new Active entry below for full detail — **prepared, not yet delivered**, needs a local Claude Code session; no live browser check done this segment.)

Prior "Last updated" (2026-08-11, earlier same day (Cowork — full YLM (Young Learners Module) audit against the real `DPS Junior Level.xlsx` workbook, the Golden Steps developer logic doc, and all 3 level image folders (32 lessons, 16/8/8 split, 160 DPS images), requested by Shailesh before setting up YLM assessments/mocks. Found and fixed a real, live-confirmed bug: every lesson's 5 DPS sheets generated statistically identical questions regardless of title/intent (`YLMConfig.dps_number` existed but was never read anywhere in generation) — live-confirmed on `mock.mathpath.in`'s admin Learning Path Studio before the fix (Lesson 2 DPS-1/DPS-3/DPS-5 all generated single-digit-only questions despite DPS-3/DPS-5 being titled "50, 60, 70, 80, 90" and "Number 50 Double Digit"). Fixed with a new DPS-level digit-pattern override table derived by parsing the real workbook DPS-by-DPS (not guessed), wired through `enrich_config_with_lesson_rule()`, plus 2 real bugs in the operand builders that silently ignored `digit_pattern` entirely. See the Active entry below for full detail — **prepared, not yet delivered**, needs a local Claude Code session.)

Prior "Last updated" (2026-08-11 (Local Claude Code — production deploy pipeline (`scripts/prod-deploy/deploy-simple.sh`) CONFIRMED WORKING END TO END for the first time. Fixed the script to point at the real repo (`sg2499/MathPath-Platform`), merged in the hosting team's static-IP rewrite (no AWS CLI needed), fixed a real Sentry org-mismatch build bug (`next.config.mjs`, PR #446), and resolved a server-side SSH key authorization block with the hosting team. First full run succeeded: build -> scp -> git sync -> deps -> service restart, confirmed live at `mock.mathpath.in`. See the updated CRITICAL deploy-script entry in Active below for full detail, live URLs, and one open watch item (server RAM hit 94.1% during restart -- flagged to the hosting team, awaiting their check).)

Prior "Last updated" (2026-08-10 (Cowork — Bridge Module (BM-L1) CONFIRMED DELIVERED AND MERGED. PR #443 merged to `main` as commit `1037ed3` ("Add Bridge Module (BM-L1): DPS, assessment, and competition mock workflows (#443)"), confirmed via `git log origin/main` and directly by Shailesh. See the "Resolved Recently" entry below for the full writeup; one item remains open — deleting the two leftover BM-L1 test artifacts in the live admin UI, see Active below. Next up per the standing queue: PM-L2, PM-L1 fixes, PM-L3/L4 fixes.)

Prior "Last updated" (2026-08-07 (Cowork + local Claude Code — Bridge Module (BM-L1) built end to end (dedicated engine, 200 DPS, assessment blueprint, competition mock), all 3 workflows live-verified via Claude in Chrome, 4 real module-registration bugs found and fixed during that verification (assessment marks gating x2, mock studio dropdown, mock section-key mismatch silently producing 0-question drafts). Delivery moved to local terminal: pushed as PR #443 on branch `fix/pm-l3-l4-dps-generation-bugs` (reused old branch name, harmless — PR diff is BM-only), hit and resolved a real merge conflict against `main` (which had moved forward with the PM-L3/L4 hotfix, #442, mid-flight) in `generation_service.py`, re-ran the full 281-test backend suite clean post-merge. PR #443 was open, conflict-resolved, and pushed (commit `45a2216`) at that point — now confirmed merged, see above.)

Prior "Last updated" (2026-08-05, later still (Cowork — PM-L2 built end to end across DPS/assessments/mocks, fully verified locally, PREPARED NOT YET DELIVERED. Dedicated `question_engine/pm_l2` engine (zero imports from PM-L1's), new Concept Drill question format (3 sub-types: multiply, divide-to-remainder, range-sum) worth 5 marks vs 1 elsewhere, new "Add/Less (Abacus)/(Visual)/Concept Drill" 3-section registry for assessments+mocks, PM-L1 confirmed unaffected. Full writeup in `CLAUDE_CODE_STATUS.md`'s top entry and the new Active bullet below. Full backend suite: 173/173 pass.)

Prior "Last updated" (2026-08-05, later same day (Cowork — exhaustive 3-workflow verification of PM-L1 (DPS/assessments/mocks) per Shailesh's explicit instruction not to push anything that would need reactive fixing later. Built and ran two exhaustive backend scripts covering all 75 DPS sheets (9000 questions, 0 failures) and all 41 mock/assessment concept-pool entries (2050+ questions, 4 full end-to-end mock runs, config-parity checks, 0 failures/warnings) — the PM math engine itself is now provably correct. Live re-verification then surfaced a second, real, more serious bug: PM-L1 assessment blueprints 500'd on "Generate Preview" (reproduced on a brand-new blueprint too, so not isolated). Root cause, found from the real traceback: `assessment_questions.lesson_id` was still `NOT NULL` in this deployment's actual DB — the alembic migration that made it nullable (`e5a2c9f14b7d`, authored 2026-07-22 for exactly this) was never really applied here (stamped `alembic_version` doesn't match any revision in the current chain), and `Base.metadata.create_all()` at startup only creates missing tables, never alters existing columns. Fixed with a new idempotent `ensure_assessment_questions_lesson_id_nullable()` in `schema_migration.py` (SQLite table-rebuild / Postgres `ALTER COLUMN`), wired into `main.py` startup — applied itself live via uvicorn's reload, re-verified working on 2 blueprints via the real admin UI, full 163-test suite still green. **Prepared, not yet delivered** — see the updated Active entry below for full detail.)

Prior "Last updated" (2026-08-05 (Cowork — full PM-L1 concept audit against the real Bridge Level Excel + 75 DPS images, requested by Shailesh before proceeding to PM-L2. Found and fixed a digit-width segregation bug (`digit_pattern` silently ignored across 4 complement base-builder functions in `question_engine/pm/operands.py`, meaning "1D-only" and "2D-only" DPS sheets were drawing from the same pool), corrected `PM_L1_LESSONS` digit_pattern values, widened mock/assessment concept pools back to "1D_AND_2D" to avoid an accidental variety regression, and fixed a stray title prefix. Fixing the bug correctly left some 1D DPS with too few unique question shapes (4-5 for 10 slots) — a real tradeoff, explained to Shailesh before acting — so the complement generator was fully redesigned (two modes, trigger-first vs. setup-then-trigger, reverse-engineered from real Excel column traces) to restore full per-question variety within the correct digit width. New/extended regression tests all pass; live-verified end to end via Claude in Chrome against the real running platform (DPS previews across 4 lessons spanning both widths/operations/complement-5/complement-10, plus a real 60-question mock generation). **Prepared, not yet delivered** — see the new Active entry below for full detail and the one leftover draft-mock artifact from live verification.))

Prior "Last updated" (2026-08-03e (Cowork — DPS questions converted from MCQ to typed free-text answers platform-wide, at Shailesh's explicit direction: students were picking options randomly without solving, parents complained. Scope is the shared DPS pipeline only (YLM/MM/IM generators, `GeneratedQuestion`/`AttemptAnswer`/`Attempt` models, the Learning Path Studio that generates/publishes DPS sheets) — confirmed via `Attempt.dps_id` being non-nullable that this table/pipeline is DPS-exclusive, entirely separate from `CompetitionMockAttempt`/`CompetitionMockQuestion` (mock exams) and `AssessmentAttempt`/`AssessmentQuestion` (level mastery/assessments), so those are provably untouched. New `backend/app/services/answer_matching.py`: single `answers_match()` function grading now goes through (`save_answer`/`submit_attempt` in `attempt_service.py` both call it instead of the old `QuestionOption.is_correct` lookup) — normalizes formatting noise (whitespace, unicode minus variants, leading zeros/plus signs, decimal padding, comma thousands-separators, fullwidth digits) via `Decimal` comparison, but never tolerates an actual value difference; falls back to case-insensitive string compare for any non-numeric answer (verified via `im/generator.py` that every concept family today, including IM's "Answer Position", stores a plain numeric `correct_answer` — the fallback is pure future-proofing, not exercised in production). `AttemptAnswer.selected_value`/`selected_option_id` columns already existed pre-migration, so no schema change was needed. Frontend: new `AnswerInputBox.tsx` replaces the options grid in `QuestionCard.tsx` (DPS-only component — `OptionButton.tsx` is untouched and still used by mock-exam/assessment attempt pages); auto-advance to the next question mirrors the old instant-MCQ-click feel via two independent debounce timers (450ms save, 1100ms advance-if-syntactically-complete-number) plus an immediate Enter-key override, deliberately never comparing against the correct answer client-side since `safe_questions_payload()` never sends it during an in-progress attempt. Result views (student/teacher/admin — all three share the same backend `result_payload()`) now show the plain typed value against the plain correct value instead of a lettered option. Admin Learning Path Studio preview (`AdminQuestionPreview.tsx`) keeps the existing MCQ options grid + correct-answer callout as-is (per Shailesh's explicit confirmation, for generator/distractor-quality auditing) and additionally shows a styled mockup of the new student-facing answer box. Verified: 136/136 backend tests pass (67 new exhaustive `answers_match()` cases covering every formatting-noise category plus adversarial wrong-value cases, 5 new end-to-end save→submit→result integration tests including a never-leak-the-correct-answer-mid-attempt check), all touched frontend files syntax-clean (project-wide `tsc --noEmit` could not complete inside this sandbox's 45s hard per-command timeout — same constraint noted in the 2026-08-03b entry below — recommend qa-reviewer run the full typecheck before this merges). **Prepared, not yet delivered** — this Cowork sandbox has no GitHub push access; needs a local Claude Code session to review the diff, run qa-reviewer, and deliver.)

Prior "Last updated" (2026-08-03d (Cowork — built the actual DPS badge backfill script Shailesh asked for after confirming the login-latency fix was live. `backend/scripts/backfill_dps_badges.py`: `--apply` calls the REAL `evaluate_dps_attempt_submission()` (not a reimplementation) against every student's full historical DPS attempt record in chronological order, wiping only `dps_`-scoped stats/badges first for a deterministic replay; badge-unlock notifications are backdated to each attempt's real `submitted_at` and deduped so re-running is always safe. **Caught a real bug during testing, before it ever touched anything real:** the first draft's `--dry-run` claimed to safely simulate-and-rollback, but `evaluate_dps_attempt_submission()` actually commits internally (same pattern already documented for `evaluate_mock_exam_submission()` in `backfill_mock_gamification.py`) — so that "dry run" was silently writing real rows. Caught via a throwaway local SQLite test DB, never ran against anything real. Fixed: `--dry-run` now reuses `diagnose_dps_badge_backfill.py`'s read-only logic instead of calling the real evaluator at all; `--apply` is the only path that writes. Verified against synthetic data: dry-run writes nothing (0 rows after), apply awards the correct badges + creates backdated notifications, re-running `--apply` is idempotent (no duplicate notifications, same final state), and a planted non-DPS badge/stat survives an apply run untouched (confirming the scoping is real). 64/64 backend tests still pass. **Prepared, not yet delivered, and not yet run against production** — this Cowork sandbox has no production DB access, so the real backfill numbers (how many students/badges this actually affects) are still unknown. Needs `--dry-run` first against production to see real numbers, then `--apply` when ready.)

Prior "Last updated" (2026-08-03c (Cowork — investigated Shailesh's report that login across all three roles (student/teacher/admin) had become noticeably sluggish since the Antigravity gamification sessions, "used to be instant." Ruled out the login endpoint/auth service/security middleware/rate limiter themselves — none touched since well before Antigravity's work. Root cause: `AchievementEngine.seed_badges()` runs synchronously in `on_startup()` on every server boot, and used to do one SELECT + one commit PER badge (~139 badges now — 100 literal + 39 dynamic Level Mastery — up from ~60 before this summer's gamification growth), i.e. ~278 blocking round-trips before the app could serve any request. Confirmed with Shailesh this backend is on Render's **free tier** (a different team hosts it on their own resources), which spins down on idle — so every cold boot re-paid that startup cost in full, hitting whichever role happened to log in first after a gap, which matches "feels sluggish across all roles" exactly. **Fixed:** rewrote `seed_badges()` to do one bulk SELECT + one commit instead of the per-badge loop — behaviourally identical (still upserts every badge's name/description/icon/required_count every boot) but cuts ~278 round-trips to 2. Verified idempotent against an isolated SQLite DB (100 badges both runs, correct DPS/Level-Mastery counts), 64/64 backend tests still pass, `py_compile` clean. Local timing: 122ms fresh-insert run, 3.3ms steady-state re-run — on production Postgres over a real network connection each of those ~278 round-trips would have cost real network latency on top of query time, so this should meaningfully shrink cold-start time. **Prepared, not yet delivered** — same no-GitHub-push-access constraint as always. Whether this fully explains the felt lag (vs. genuine free-tier cold-start being irreducible without a paid plan) still needs a live before/after comparison once this ships.)

Prior "Last updated" (2026-08-03b (Cowork — full pristine-check verification pass on the DPS badge system now that PR #418 is CONFIRMED DELIVERED AND MERGED to `main` (detection logic, leaderboard filter, colour repalette, Leather/Silver Tome overlap fix). Re-ran everything from scratch against the merged code, not just re-reading prior notes: 64/64 backend tests pass; colour verifier re-run clean (0 DPS-vs-DPS FAILs, worst pair dE00 15.99; the 109 DPS-vs-existing-88 FAILs remain, accepted per the leaderboard-fix rationale); both leaderboard endpoints confirmed still excluding `dps_*` while the Trophy Room `/achievements` endpoint correctly still shows everything; DPS unlock audio confirmed correctly routed to dedicated `dps_*.mp3` files (all 4 exist on disk) with safe synth-chime fallback. A full icon-by-icon geometric audit (141/141 icon components, exact coordinates not visual guess) found the Tome fix's own bug class — a wireframe box drawn at ~98% of a solid box's size, same position, so the wireframe edges crosshatch the solid box's own edges — **still present and unfixed in 5 places the original 2026-08-03 pass never touched: `EnvDpsIronAnvil`/`EnvDpsSteelAnvil`/`EnvDpsObsidianAnvil` (Ironclad Discipline family's BASE/SUPER/LEGENDARY tiers) and `EnvDpsAstralTome`/`EnvDpsBoundlessTome` (the Tome family's own LEGENDARY/MYTHIC tiers, missed because only the BASE/SUPER tiers were checked last time).** All 5 fixed the same way (solid inset emissive slab, `wireframe` removed), esbuild-clean. Also built `backend/scripts/diagnose_dps_badge_backfill.py` — a read-only diagnostic that replays a student's full historical DPS attempt record against all 10 families' exact detection logic to find students who'd already qualify for badges they were never awarded (since the detection hook only fires on new attempts going forward, not retroactively). Logic-tested against synthetic data, confirmed correct — but this sandbox only has an empty local SQLite dev DB, so it reports zero everywhere; **needs to be run against the real production DB to get real numbers before deciding whether an actual backfill/award pass is warranted.** A full monorepo `tsc`/`next build` could not be completed in this sandbox (45s hard timeout, hit repeatedly) — esbuild syntax-checked the 3 touched files clean instead; recommend qa-reviewer run the full typecheck before this new round merges. **This round (5 wireframe fixes + backfill diagnostic script) is prepared, not yet delivered** — same no-GitHub-push-access constraint as always.)

Prior "Last updated" (2026-08-03) (Cowork — closed out the two remaining Antigravity DPS-badge defects Shailesh approved fixing this session: (1) full 40-colour repalette of `frontend/lib/gamification/badgeVisuals.ts` — the original had 8 byte-identical duplicate pairs (`dps_midnight`/`dps_compass`/`dps_sage` were near-total copy-pastes of each other) plus over 100 more sub-12 dE00 collisions, confirmed by finally running `verify-badge-colour-distinctness.mjs` against it for the first time; new palette verified 0 internal FAILs (worst DPS-vs-DPS pair dE00 15.99), `DPS_NEW` block added permanently to the real verifier so this can't silently regress again; (2) `get_mock_exam_leaderboard()`/`get_cumulative_leaderboard()` in `routes_student.py` now exclude `dps_*` badges from `topBadges` — that leaderboard is mock-exam-scoped and was the only surface where DPS and non-DPS badges ever rendered side by side, so excluding them there also closes out the remaining ~100 dps-vs-existing-88 colour collisions as a non-issue (documented, not chased to zero — see `DPS_BADGE_COLOR_AUDIT_2026-08-03.md`). 64/64 backend tests pass, `py_compile`/`esbuild` clean. **Prepared, not yet delivered** — this Cowork sandbox has no GitHub push access; needs a local Claude Code session to review the diff, run qa-reviewer, and deliver.)

Prior "Last updated" (2026-08-02) (Cowork — audited everything shipped to `main` on 2026-07-31 outside this thread (PRs #413-#417, apparently run through Antigravity rather than a Cowork/Claude Code session) against this repo's own delivery standard. Headline finding: PRs #415/#416/#417 shipped a full frontend/audio treatment for 10 new DPS badge families (40 badges) and removed the Trophy Room's "DPS Badges Coming Soon" placeholder, but **zero backend detection logic existed anywhere for any of the 40 badges** — confirmed by grepping `achievements.py` and `attempt_service.py`, and by the fact `AchievementEngine` is never even imported into the DPS attempt-submission path (only the mock-exam path). Every DPS student would have seen 40 permanently, structurally unearnable badges — worse than the honest placeholder it replaced. No PR description, no tests, no qa-reviewer evidence, and no entry in this file, `COWORK_HANDOFF.md`, or `CLAUDE_CODE_STATUS.md` for any of that day's work, despite `.agents/AGENTS.md` naming gamification/badge logic as a mandatory qa-reviewer-gate category. Detection logic for all 10 families has now been designed and implemented this session (`AchievementEngine.evaluate_dps_attempt_submission()` in `achievements.py`, wired into `attempt_service.py`'s existing DPS gamification hook) plus 16 new tests, all passing (64/64 full suite). See the new "Active" entry and "Resolved Recently (2026-08-02)" entry below for full detail — **prepared, not yet delivered**, and several of the badge-family interpretations still need Shailesh's explicit sign-off before this should be treated as final, per this project's own standing convention that gamification design decisions get confirmed with him directly, not inferred silently.)

Prior "Last updated" (2026-07-30): (Cowork — gamification Phase 3 "Level Mastery" session wrap, now fully closed out and live-verified. Backend rebuilt after being found missing in production, shipped (PR #409); a follow-up position-aware-visibility feature caused a real production incident (schema migration silently didn't apply, every `Student`-loading endpoint 500'd, PR #410) and was emergency-rolled-back same session (PR #411); a simpler no-migration redesign was implemented, independently reviewed (1 real bug caught and fixed), pushed, merged (PR #412), and confirmed live via a real student's Trophy Room. Full writeup in `COWORK_HANDOFF.md`'s matching 2026-07-30 entry.)

Prior "Last updated" (2026-07-25): (Cowork — corrected a stale doc: Security audit Phase 2 was actually merged to `main` on 2026-07-22 as PR #363/`7db4ae3`, not "prepared, not yet delivered" as this file previously said. Verified directly via `git log`/`git branch --contains`, not from prose. See the Active-section Phase 2 entry below for detail.)

Prior "Last updated" (2026-07-24): Cowork — pre-rollout QA closeout phase is now CLOSED OUT. All bugs from the 2026-07-23 8-bug round, plus further ones found across several more sweeps of Admin/Teacher/Student, totaling 16 files, are now CONFIRMED DELIVERED AND LIVE-VERIFIED — see the new "Resolved Recently (2026-07-24)" entry below and `CLAUDE_CODE_STATUS.md`/`COWORK_HANDOFF.md` for full technical detail. The 3 planned live-attempt tests were superseded by broader real-account verification across 3 students spanning all 3 modules (MM/YLM/IM) and 3 data states, which is a stronger check than the originally-planned single-account attempt tests. **Only one item remains open for this phase, and it's a product decision, not a bug:** the `TEMPORARY_ASSESSMENT_READINESS_BYPASS` flag — see Active below. Next work should be new-features-only per Shailesh's stated intent for this phase.

Prior "Last updated" (2026-07-23): Cowork — pre-rollout QA closeout phase: two full QA sweeps across all 3 logins, followed by 8 bugs root-caused and fixed in the same session. PREPARED, NOT YET DELIVERED — see the matching "Resolved Recently" entry below and `CLAUDE_CODE_STATUS.md` for full technical detail. **Still needs a local Claude Code session** to run `qa-reviewer`/`sre-devops` and deliver, plus a live browser session with Shailesh to run the 3 planned live-attempt tests (DPS practice, mock exam, assessment submission) and a final re-verification pass across all 3 logins for each fix.

Carried from 2026-07-25 (Cowork, two rounds that day, both confirmed delivered and live via real browser verification against production, not just merged. Round 1: 6 real bugs Shailesh found live-testing PR #393 (section ordering, wrong operator symbols, BODMAS overlap, position-number wrapping, extra preview chips, Step 1 field misalignment) — all fixed, delivered as PR #394 (`a67e90f`), live-verified. Round 2, straight after: 3 more real bugs found on top of that — BODMAS not matching the mock exam's no-wrap single-line convention, a systemic bug silently collapsing every IM assessment's multiplication/division/positional/BODMAS-variant questions to one hardcoded pattern regardless of the level's real curriculum variety, and a chip-color inconsistency. All 3 root-caused, fixed, delivered, and live-verified — see the matching "Resolved Recently" entry below and `CLAUDE_CODE_STATUS.md` for full technical detail. **One decision still needed from Shailesh, now more urgent than before:** the live "IM L4" published assessment (and possibly other already-published IM assessments across other levels) still contains data generated before any of today's fixes existed, so it needs regeneration — see the new Active entry below.

Carried from 2026-07-21 (three rounds that day. Newest first: (0) a full security/data-safety audit (`docs/SECURITY_AUDIT_2026-07-21.md`) found two critical, already-live credential-exposure issues — see the matching Active entries above for what's fixed vs. what still needs Shailesh directly. (1) IM-L1 full build — the final IM level — **CONFIRMED DELIVERED AND LIVE**, independently re-verified end to end via Cowork browser check against production (DPS previews, assessment generation, competition mock generation + section-omission all confirmed correct; marks-weighting confirmed intentional — 5/1 split in DPS+assessments, flat 1 in mocks, matching every other IM level). (2) Login page "visual elevation" — the last open login-page item — all four discussed ideas (living background, abacus-bead flourish, login confetti, extra micro-interactions) built and self-verified in Cowork, **PREPARED NOT YET DELIVERED**, see the matching Active entry below. Carried from 2026-07-20: student-portal QA sweep — 3 real bugs found and fixed, all **CONFIRMED DELIVERED AND LIVE**: Practice tab Needs-Re-Attempt undercounting, dashboard's fabricated "Top 5%" stat, and a Leaderboard/Mock Exams/Insights accuracy mismatch that took 3 fix rounds plus a false-alarm chase before landing correctly — cross-verified against a real student account, not just the test account. See the "Resolved Recently" entry below for the full writeup, including the debug-field diagnostic technique worth reusing if a similar "fix looks right but live number still wrong" situation recurs. One small cleanup PR from that round (removing the temporary debug fields) needs merge-confirmation. Carried from 2026-07-19: repo-wide "clean slate" cleanup — **CONFIRMED DELIVERED AND LIVE**, including a second bug found and fixed during the round's own live verification (MM/IM assessment digit-shape mismatch, PR #347). Carried from 2026-07-18 round 4: IM-L2 full build + IM-wide assessment marks-weighting fix + trivial-operand fix (MM+IM mult/div/BODMAS) all **CONFIRMED DELIVERED AND LIVE** — PR #344 (`2c6e0dc`) and PR #345 (`2d414db`). Carried from 2026-07-17 round 3: MM 150-DPS audit -> 8-finding fix (PR #342, merged) -> live browser verification -> DPS-title cosmetic pass (PR #343, confirmed merged). Also from 2026-07-17: Login page hover/crop/copy fixes delivered clean; feature-grid reverted to 2x2 after an overcorrection; dark-mode logo box went through two wrong fixes — one of which visibly broke light mode — before landing on a targeted glow fix in PR #339, confirmed live.)

## Active

- **Leaderboard rank-change/podium notifications + teacher Leaderboard nav — prepared 2026-09-01, not yet delivered.** 18 files (7 new, 11 modified) sitting in the local working tree on branch `chore/repo-cleanup-and-doc-sync`, ready to be branched off, committed, and pushed from a local Claude Code session or by Shailesh directly. Full writeup in `COWORK_HANDOFF.md`'s 2026-09-01 entry. Nothing live-browser-tested yet (this Cowork session has no way to run the Next.js dev server against Shailesh's machine) — worth a real end-to-end check (submit a DPS sheet/mock exam as a test student, confirm the right message pool/outcome/deep-link fires, confirm the teacher podium notification and new Leaderboard nav dropdown render correctly) before this ships.
- **Notification backlog surfaced while building the above (product decisions needed, not started):** streak-at-risk / streak-broken notifications (none exist today); assignment due-soon reminders (none exist); a push notification when a new item lands in the teacher's manual-intervention queue (currently pull-only, `teacher_manual_intervention_queue()`); a class-wide "N of your students moved up this week" digest for teachers (this round's podium notification is real-time/per-student only); whether a low-frequency "you were passively overtaken" digest is wanted for students (deliberately excluded from this round per Shailesh's own rank-change spec, which fires only on the student's own submission); student-inactivity nudges; and a notification when a new student is assigned to a teacher. None of these are built — flagging for Shailesh to prioritize.
- **Deploy-mechanism ambiguity, surfaced 2026-08-25 — needs Shailesh to confirm which pathway is actually authoritative before `DEPLOY_PLAYBOOK.md`'s manual SSH steps are handed over again without question.** Shailesh pasted a Render deploy log (`https://mathpath-backend.onrender.com`) that appears to have auto-triggered straight off a merge to `main` and successfully deployed the 2026-08-25 backend fix (confirmed via the seed-sync summary line showing the corrected `duration_rule`). This is a different mechanism than the manual `ssh ubuntu@15.206.108.37` + `systemctl restart mathpath-backend` + `pm2 restart mathpath-frontend` flow in `DEPLOY_PLAYBOOK.md`, which this session (per Shailesh's own standing instruction) kept handing over as "the" deploy commands. Both may be real and both may matter (e.g. two separate hosted environments), or the SSH playbook may be stale/legacy now that Render appears to auto-deploy on merge — genuinely unclear, not resolved in that thread. **Update (2026-09-01):** confirmed directly via the Render API that `mathpath-backend` (`srv-d87flim7r5hc73fja470`) has `autoDeploy: "yes"`, `autoDeployTrigger: "commit"`, and every recent deploy in its history shows `trigger: "new_commit"` -- Render auto-deploy-on-merge IS the confirmed live mechanism (e.g. PR #495's commit `d00ec03` auto-deployed and went `live` within ~2 minutes of the merge, no manual script run). Whether the manual SSH `DEPLOY_PLAYBOOK.md` flow is still needed for anything (a second environment? a fallback?) is still unconfirmed -- ask Shailesh directly before assuming it's dead, but for the actual production Render/Vercel services, no manual deploy step is required for a normal merge to `main`.
- **BM-L1 leftover test artifacts, not yet confirmed cleaned up.** Two harmless draft artifacts created during live verification: an assessment pair under Manage Assessments, and a mock draft under Manage Mocks that deliberately demonstrates the pre-fix empty-mock bug. Permanent deletion isn't something Claude does unilaterally — left for Shailesh (Admin -> Manage Assessments / Manage Mocks -> find BM-L1 test entries -> Delete). Zero risk if left alone.
- **Root cause never diagnosed: why did the `entry_path` Alembic migration silently not apply in production (2026-07-30 incident)?** `alembic upgrade head` ran in Render's build step but the column never got added, causing every `Student`-loading endpoint to 500 until an emergency rollback. The feature that needed this migration has since been redesigned to not need a schema change at all, so this isn't blocking anything right now — but the underlying "a migration can silently no-op in this deploy pipeline" risk is unexplained and could bite the next schema change. Needs a Render build-log inspection (the migration step specifically) before trusting `alembic upgrade head` blind again. Three unconfirmed hypotheses logged in `CLAUDE_CODE_STATUS.md`'s 2026-07-30 history: multiple Alembic heads, a non-fatal swallowed failure in the build step, or a `PROD_DATABASE_URL`/`DATABASE_URL` mismatch.
- **Level Mastery visuals: Batches 4-5 not started (24 of 39 badges).** Batch 1 (BM-L1), Batch 2 (MM-L1), Batch 3 (YLM-L1/L2/L3) are done and Opus-elevated/live-verified (15 of 39). Batch 4 (PM-L1 through PM-L4) and Batch 5 (IM-L1 through IM-L4) remain — 12 badges each, same build->verify->live-preview cycle as the completed batches. Must route through an explicit Opus `Agent` call, not whatever model the session is running on by default — a standing correction from earlier in this project. After both batches, a full 39-badge verification pass (distinctness script, contrast, live preview) is still needed before the visual side of Phase 3 is done. Badges without a shipped visual currently render as a generic octagon fallback in the Trophy Room (`getShapeStyles()` in `frontend/app/student/achievements/page.tsx` has no `LevelMastery*` entries yet) — cosmetic, not blocking.
- **Product decision needed: restore the strict assessment-readiness gate before rollout, or keep the bypass?** `backend/app/core/config.py` lines 55-59 — `TEMPORARY_ASSESSMENT_READINESS_BYPASS` defaults ON (`ASSESSMENT_READINESS_FORCE_STRICT` env var defaults `false`). This is currently live in production: teachers can assign live assessments to students regardless of DPS/practice completion (confirmed live via the "Readiness Bypass Active" banner on Ashalatha Gupta's `/teacher/assign-assessment`, where IM students with 0/60 DPS cleared still appeared in the assignable queue). A documented, intentional Phase 8.8 convention whose own comment says it's awaiting Shailesh's explicit call to restore strict enforcement — not a bug, nothing to fix in code, just needs a decision before real users are on the platform. This is now the **sole remaining open item** from the entire 2026-07-23 through 2026-07-24 pre-rollout QA closeout phase (16 files fixed, all delivered and live-verified — see "Resolved Recently (2026-07-24)" below).
- **Product decision needed: should the mock-exam achievement counter (`mock_exams_completed` in `achievements.py`) re-sync to only currently-active assignments, or stay a lifetime counter?** Found while investigating the Trophy Room badge-count-vs-average QA finding (7 badges vs. 6 visible completed mocks for Rohan Sen) — concluded this is very likely NOT a bug (the counter correctly never decrements when an assignment is later superseded, and the displayed 81%-vs-84.2% average gap is explained by marks-weighted pooling, not phantom data), but flagging the design question in case Shailesh wants the achievement display to track only-active assignments instead.
- **Decision needed: regenerate the live "IM L4" published assessment (and possibly other already-published IM assessments), which still contains pre-fix data.** Across the two 2026-07-25 rounds (PR #394 then the follow-up round, both delivered and live — see Resolved Recently below), the live "IM L4" assessment (92 or 84 questions depending on when last regenerated, PUBLISHED/LIVE, real students could see it) was generated before these fixes existed, so it still has: wrong section order (Skill Stacker first instead of last), wrong operator symbols on multiplication/division, and — the bigger one — every multiplication/division/positional/BODMAS-variant question collapsed to the same single hardcoded pattern instead of the level's real curriculum variety. Deploying the code fix does not retroactively regenerate already-persisted questions; only "Generate Preview" (or republish) does that. Since bug #2 from the second round was old and platform-wide (not specific to this one blueprint), other already-published IM assessments across other levels likely have the same stale-variety problem too. **Ask Shailesh: regenerate just "IM L4," or run a broader pass across every published IM assessment?** This is a live-data change, not a code change, so it wasn't done unilaterally either round.
- **Re-surface the full 2026-07-20 QA report with Shailesh if it matters beyond the 3 bugs already fixed.** A student-portal QA sweep (Dashboard/Practice/Assessments/Results/Competition/Achievements) was run and shared as a report via Cowork's file-sharing flow, not committed to the repo and not retained in `COWORK_HANDOFF.md`'s summary beyond the 3 bugs that came out of it (see "Resolved Recently" below). If it flagged anything lower-priority, that list isn't preserved anywhere durable — ask Shailesh if he still has it, or re-run the sweep if needed.
- **Five stray unassigned draft artifacts left in production/local dev, harmless but not cleaned up.** "MM-L1 Verification Mock" (code `CMP-MM-MM-L1-20260717133952-D639F1`, MM-L1, 100 questions), created during the 2026-07-17 round-3 live-verification pass. Two more from 2026-07-18 round 4: assessment blueprint **"Verification Check IM-L2 (delete after)"** and competition mock **"Verification Check IM-L2 Mock (delete after)"**. One more from 2026-07-19's live verification: assessment blueprint **"Verification Check MM Decimal Crash Fix (delete after)"** (MM-L1, 40 questions, the one used to confirm both the decimal-crash and digit-shape fixes live). One more from 2026-08-05's PM-L1 live verification (local `127.0.0.1:3000` dev environment, not necessarily production): competition mock **"PM-L1 Verification Mock"** (code `CMP-PM-PM-L1-20260805075648-1FBDA7`, PM-L1, 60 questions, used to confirm the digit-width fix computes correctly through the full mock-generation pipeline). Two more from the same day's later exhaustive-verification session: assessment blueprints **"PM-L1 Full Verification Assessment"** (100 questions, the one that first surfaced the `lesson_id` NOT NULL bug) and **"PM-L1 Repro Test 2"** (100 questions, created fresh specifically to prove the bug wasn't isolated to one blueprint — both now successfully generate previews post-fix). None ever assigned to any student, all still DRAFT. All use a hard "cannot be undone" delete confirmation dialog, which falls under actions Cowork won't execute itself even with a go-ahead — left for Shailesh to delete manually (Admin -> Assessment Blueprints -> search "Verification Check" -> Delete; Competition -> Manage Mocks -> search each name -> Delete -> Delete Permanently) if he wants them gone. Zero risk if left alone.
- **Login page "visual elevation" round 2 — delivered and merged (PR #360), not yet independently re-verified live.** Fixed the 3 bugs Shailesh found live-testing round 1 (Teacher/Student heading size, Admin headline wrapping, confetti not appearing) — see `CLAUDE_CODE_STATUS.md`'s "Last completed milestone" for what shipped. This Cowork session moved straight into the security audit before doing a live re-check; worth one next time there's a browser session on this project, especially the confetti (still can't self-verify that one — won't type a password into any login field, even for a named test account).
- Minor doc-consolidation candidates flagged 2026-07-14, still needs human judgment not a code fix: root `SETUP_GUIDE.md` (144 lines) vs. `docs/SETUP_GUIDE.md` (20-line stub, looks like an orphaned early draft); `CURRENT_STATUS.md` already self-marks as superseded by this file (2026-07-10) but hasn't been archived/deleted.
- **Repo-wide CRLF/line-ending noise in `git status` (~230 files showing "modified", confirmed whitespace-only) is a recurring, not-yet-permanently-fixed pattern.** Root cause: this sandbox's git has `core.autocrlf` unset. Deliberately not renormalized-and-committed from Cowork again on 2026-07-19 (large blast radius, safer to decide once from the authenticated local terminal — `git config core.autocrlf` + optionally a `.gitattributes` with `* text=auto eol=lf`) — re-diagnose via `git diff --ignore-cr-at-eol --stat` before assuming any of it is a real pending change.
- **Student-side live verification still open for three older items** (timer urgency-pulse animation, dark-mode timer contrast, heatmap round-2 rendering — flagged since 2026-07-12/13). Attempted 2026-07-19 via the already-logged-in admin panel; blocked on not having a test-student login (resetting a real student's password to manufacture one is a live-account-affecting action, out of scope for Cowork to do unilaterally). If there's a standing test-student account, share credentials next round; otherwise this needs Shailesh's own pass.
- A full `git bundle --all` backup of every branch deleted today (`mathpath-full-branch-backup-20260714.bundle`) was shared with Shailesh as a safety net — worth archiving it somewhere durable if he wants to keep it long-term, since it was delivered via Cowork's file-sharing flow, not committed to the repo.
- Browser-QA the 2026-07-13 timer fix (PR #318) live: confirm the sub-5-minute urgency pulse actually renders (both themes), confirm dark-mode timer chip contrast looks right, and confirm a genuinely backgrounded tab past expiry resyncs to the correct submitted state instead of showing stale time.
- Collector's Vault (`frontend/app/student/achievements/vault/`) still runs on hardcoded dummy data with zero backend calls and no `useProtectedPage` guard. Explicitly deferred by Shailesh (2026-07-13) until the rest of the student portal is confirmed solid — needs a product decision (build the real backend vs. hide the nav entry) before a code fix makes sense. Same underlying issue flagged since round 7. Also explicitly excluded from the unified economy system (no loot-pack drops for DPS/assessments until the Vault exists).
- Local-machine suggestion, not a repo issue: exclude `frontend/node_modules`/`.next` from OneDrive sync — flagged during the cleanup round below as a plausible contributor to this session's stale-sync issues.
- All 60 IM Level 4 DPS rows remain DRAFT (unpublished) by design — Shailesh publishes individually from Learning Path Studio himself once he's reviewed each one. Not a bug, just noting nothing auto-publishes.

## Resolved Recently (2026-09-01, round 3 -- Shailesh confirmed two open items directly, both now closed)

Shailesh confirmed directly in this session: (1) the DPS practice leaderboard (PR #491/#492) IS what he meant by the "new DPS leaderboard" referenced when closing the 2026-08-25 thread -- the leaderboard-revamp/music-track Active bullet is now closed rather than carried forward as an open scope question. (2) The 4 ambiguous DPS-badge rule interpretations from PR #418 (Ironclad Discipline's weekly window, Rising Phoenix's chain scoping, Master's Anvil's threshold, Midnight Oil's timezone) are all correct as implemented -- no changes needed, closing out the last open thread from that PR.

## Resolved Recently (2026-09-01, round 2 -- DPS badge detection logic, colour repalette, wireframe-tangle fixes, and backfill script all confirmed merged; two stale Active bullets left over from Aug 2026 removed)

A second git-log cross-check (same 2026-09-01 session, run right after the first stale-Active cleanup above) found two more Active bullets describing work as "PREPARED, NOT YET DELIVERED" that was actually merged back on 2026-08-03 -- both survived the earlier cleanup because a later bullet in the same list already referenced one of the same PRs as merged, but the original bullets themselves were never removed. Verified via `git log origin/main`:

- DPS badge detection logic (`AchievementEngine.evaluate_dps_attempt_submission()`, 10 families) + leaderboard `topBadges` filter + 40-badge colour repalette -- merged as PR #418 (`22c547a`, "DPS badges: detection logic, leaderboard filter, color fix, Tome overlap fix"). **Confirmed by Shailesh directly (2026-09-01):** all 4 rule interpretations (Ironclad Discipline's weekly window, Rising Phoenix's chain scoping, Master's Anvil's threshold, Midnight Oil's timezone) are correct as implemented -- no changes needed.
- 5 wireframe-tangle bug fixes (Ironclad Anvil x3 tiers, Astral/Boundless Tome) -- merged as PR #419 (`10a300c`, "Fix wireframe-tangle bug in Anvil/Astral/Boundless Tome cutscenes; add DPS badge backfill diagnostic").
- Badge-seeding batched into one query (cold-start latency fix) -- merged as PR #420 (`5140c67`).
- DPS badge backfill script -- merged as PR #421 (`732bab0`, "Add DPS badge backfill script (dry-run confirms nothing to backfill yet)") -- the title itself answers the open question the removed bullet raised (whether production had students needing a retroactive backfill): the dry-run against production found nothing to backfill.

## Resolved Recently (2026-09-01, stale Active-section cleanup -- 14 items were sitting in Active claiming to be open/undelivered/unresolved when real `git log` evidence shows they were merged to `main` weeks ago, some over a month ago)

Prompted directly by Shailesh catching a brand-new chat thread reciting this stale list as current status ("backdated as fuck"). Cross-checked every Active bullet against `git log origin/main --grep` before trusting any "not yet delivered"/"prepared" claim -- the same discipline this file's own 2026-07-13 "documentation-sync bug" entry already flagged as a recurring risk, now confirmed recurring again. Removed 14 bullets, each verified via a real merged commit on `origin/main`:

- Attempt Review card-sizing polish -- merged as PR #480 ("Match Attempt Review top-row card sizing to the rest of the platform").
- Parent progress report email-removal + Publish-to-Teacher rework -- merged as PR #460, with 4 follow-up polish PRs (#461, #462, #464, #465).
- YLM Assessment + Competition Mock workflows (all 3 levels) -- merged as PR #448 ("YLM: digit-width fix + section-wise assessments & competition mocks"). Note: YLM was later collapsed to a single level (PR #454, "Collapse YLM to a single level (YLM-L1, 32 lessons)") -- the 3-level section design this bullet described is itself now superseded by that later structural change, worth confirming with Shailesh whether the L1/L2/L3 section split still applies post-collapse.
- YLM DPS-level digit-width fix -- merged bundled into the same PR #448.
- CRITICAL deploy-repo mismatch (`contactvilltek` vs `sg2499`) -- this bullet's own text already said "RESOLVED (2026-08-11)" inline; it had just never been moved out of Active.
- PM-L2 built end to end -- merged as PR #434, with 2 follow-up fixes (#435, #436).
- PM-L1 digit-width fix + complement-generator redesign + schema-drift fix -- merged as PR #433.
- Level Mastery visibility fix confirmation -- this bullet was already struck through and marked "DONE" inline; stale formatting only.
- Security audit Phase 0 / Phase 1 / Phase 2 -- all three bullets already said "CONFIRMED DELIVERED AND MERGED" inline (PR #361, #362, #363); stale formatting only.
- Leaderboard debug-field cleanup -- merged as PR #357 ("chore: remove temporary debug fields from leaderboard query").
- YLM competition-mock support (old partial-mitigation note) -- superseded by the full YLM build, PR #448, above.
- "All four IM levels now have registered competition-mock structures" -- an informational completeness note, not open work; removed as noise.

Two bullets were updated in place rather than removed, since they're not fully resolved: the deploy-mechanism-ambiguity bullet now records that Render's auto-deploy-on-commit is confirmed live (checked directly via the Render API this session) though the SSH playbook's continued relevance is still unconfirmed; the pending-leaderboard-revamp bullet now notes that a DPS practice leaderboard (PR #491/#492) has since shipped and may or may not be what Shailesh meant.

**Not re-verified this pass, still taken at face value -- flag if you rely on these:** Level Mastery visual Batches 4-5 (PM/IM badges), the DPS badge wireframe-tangle fixes + backfill script status, Collector's Vault backend wiring, and the older 2026-07 product-decision/manual-cleanup items further down this list. A `git log --grep` sweep for each would take the same few minutes this cleanup took -- worth doing before trusting any of them as still-open either.

## Resolved Recently (2026-09-01, PR #494 CI-failure fix + PR #495 mock-marks-balancing/leaderboard-column fix -- both CONFIRMED MERGED AND DEPLOYED, live and working)

3 `ASSESSMENT_MARKS_MISMATCH` pytest failures (`expectedMarks=100.0`, `actualMarks=140.0`) on PR #494's `MathPath CI/backend-tests` check, diagnosed as a genuine backend bug rather than patched away in the tests, fixed, and verified with real test execution. Shailesh confirmed the PR merged, then handed the deploy commands (per `DEPLOY_PLAYBOOK.md`), and this session confirmed directly: "i have confirmed them live and they work fine". Full technical writeup in `COWORK_HANDOFF.md`'s 2026-09-01 entry.

Separately, Mock Studio's Section Allocation never knew Concept Drill/Skill Stacker sections are worth 5 marks/question -- it just split Total Questions evenly, so a 100-question MM mock could total 140 marks. Fixed so the admin sets only the weighted section's question count and flat sections auto-balance to fill the remaining marks, landing every mock on exactly 100 -- mirroring the assessment blueprint's own marks invariant -- plus dropped the Score/Avg Score column from the mock leaderboard (Rank/Student/Avg Accuracy/Avg Time only). Delivered as PR #495, commit `d00ec03082bf3fa08c8c41dfe23751d855e61cae`. Confirmed via Render (`mathpath-backend`, deploy `dep-dab66ogu01pc73e69js0`, status `live`) and Vercel (`math-path-platform`, deployment `dpl_E6dsA3ASWMWUaTfirCSxeWKF4ETj`, target production, state READY). Commit authored directly by Shailesh (delivered outside this Cowork thread, likely via a local Claude Code session), confirmed back in this thread with real deploy evidence rather than taken on word alone.

## Resolved Recently (2026-08-10, Bridge Module BM-L1 — CONFIRMED DELIVERED AND MERGED, PR #443, commit `1037ed3`)

Built end to end: dedicated `question_engine/bm/` engine (zero shared code with PM/IM/MM), 200 DPS across 40 lessons, assessment blueprint wiring, 6-section competition mock registry (Add/Less Abacus, Add/Less Visual, Multiplication, Division, BODMAS, Concept Drill @5 marks). All 3 workflows live-verified via Claude in Chrome against a running dev platform, not just unit tests. 4 real bugs found and fixed during that live verification, all the same class — a module allow-list updated for BM's implementation but not every place that lists modules: `SECTION_WISE_ASSESSMENT_MODULES` missing "BM"; `section_marks_metadata()`/`validate_section_distribution()` weighted-marks gates checking `== "PM"` instead of `in {"PM","BM"}` (silently showed a real 164-mark distribution as "100/100 ready to publish"); Mock Studio's frontend module dropdown missing "BM" entirely; and the most serious — `CompetitionMockSectionPlan()` missing a BM branch, so the admin's Section Allocation panel invented ~30 wrong section keys and every mock silently saved as an empty DRAFT (0 questions, false "success" message, no error anywhere). All 4 fixed with regression tests, plus a new defense-in-depth `COMPETITION_MOCK_GENERATION_EMPTY` check so that bug class can't recur silently for any module. Backend suite: 281/281 passing.

Delivery hit two snags, both resolved: `MathPath CI` didn't auto-fire on the push (one-off GitHub webhook miss, worked around via manual `gh workflow run`), and a real merge conflict against `main` (which had moved forward mid-flight with the PM-L3/L4 hotfix #442) in `backend/app/services/generation_service.py` — resolved cleanly (BM's whole new block inserted where `main`'s side of the conflict was empty), verified via `ast.parse()` + a conflict-marker grep before committing. Merged as commit `1037ed3` ("Add Bridge Module (BM-L1): DPS, assessment, and competition mock workflows (#443)"), confirmed on `origin/main` via `git log` and directly by Shailesh.

**One item remains open, not a code issue:** two leftover BM-L1 test/draft artifacts in the live admin UI need manual deletion by Shailesh — see Active above.

## Resolved Recently (2026-07-24, pre-rollout QA closeout — FINAL PASS, phase now CLOSED OUT: 16 files fixed total across the whole phase, all CONFIRMED DELIVERED AND LIVE-VERIFIED)

Following delivery of the 2026-07-23 round's 8 bugs (below) plus a further round finding the same `0`-vs-`null` root-cause pattern in `teacher/students/page.tsx`'s team-average computation (a dead-fallback-path bug that PR #397 didn't actually fix — see `CLAUDE_CODE_STATUS.md`'s "Prior entry" detail) and `student/practice/page.tsx`/`admin/results/page.tsx`, this final pass asked the sharper question: does every fix hold for *any* student account, in *any* module/level, not just the one or two accounts tested so far? Shailesh rejected a code-review-only answer and logged into two more real accounts to make live verification possible: Tanaya Patra (YLM, zero completed attempts — a data state never previously tested) and Anaisha Agrawal (IM, real production account, **read-only** viewing only per Shailesh's explicit constraint that her account not be touched).

This surfaced 2 more real bugs, found only because Tanaya's zero-attempt state exercised a code path no prior account had hit:
1. `frontend/app/student/results/page.tsx` ("My Progress" page) — false "AVERAGE ACCURACY 0%" for any student with no completed work, at 4 render sites. `AverageValues()`/`HierarchyAverageForRows()` returned `0` instead of `null` for an empty array. Fixed with a new `FormatAccuracyOrDash()` helper at all 4 sites; also fixed a latent second bug in the same function (a `> 0` filter that would have wrongly excluded a level with a genuine 0% average, not just no-data levels).
2. `frontend/components/common/DetailWorkspaceViews.tsx`'s `accuracyToneClass()` — false "red/low accuracy" tone instead of neutral for missing data; fixed to match its already-correct sibling `accuracyTone()`.

**All 16 files across the whole phase now confirmed delivered and live-verified** via 3 real student logins spanning all 3 curriculum modules and 3 data states (Rohan Sen/MM real data, Tanaya Patra/YLM zero data, Anaisha Agrawal/IM real data viewed read-only). Full file list, exact fixes, and the PowerShell command-quoting lesson learned mid-delivery are in `COWORK_HANDOFF.md`'s matching 2026-07-24 entry and `CLAUDE_CODE_STATUS.md`'s Current section.

**Bottom line:** this phase is genuinely pristine — every accuracy/average display across Admin, Teacher, and Student roles, across all 3 modules and multiple data states, is either fixed and live-verified or investigated to ground and confirmed correct as designed. Only the readiness-bypass flag above remains, and that's a product decision, not a bug.

## Resolved Recently (2026-07-23, pre-rollout QA closeout — 8 bugs found via 2 full QA sweeps across all 3 logins, root-caused and fixed same-session — CONFIRMED DELIVERED AND LIVE as of the 2026-07-24 final pass above)

Full technical detail in `CLAUDE_CODE_STATUS.md`'s Current section; QA reports at `docs/QA_AUDIT_2026-07-23.md` (round 1) and `docs/QA_AUDIT_2026-07-23_DEEP.md` (round 2, deeper sweep across both teacher accounts + full Learning Path publish-status audit). Summary of the 8 fixes:

1. Assessment Readiness "CLEARED DPS" counted a Needs-Re-Attempt sheet as cleared, on all 3 logins — frontend was reading the wrong one of two backend fields (`completedDpsCount` instead of `passedDpsCount`). Fixed on all 3 readiness pages.
2. Teacher "My Students" Average Accuracy diluted by no-attempt students (8%/18% shown vs. the real average of students who'd attempted anything) — fixed to exclude no-data students from the denominator, matching Admin's already-correct logic.
3. Admin Performance Reports didn't reset the Module filter on student switch, silently showing an all-zero report for the wrong module — fixed with an auto-sync effect.
4. Student Assessments page hard-errored — `float(None)` crash in the assessment payload builder whenever an attempt existed but wasn't fully graded yet. Fixed with a null-safe float helper.
5. Mock leaderboard showed "0/0%" for every rank below the podium — a `requestAnimationFrame` count-up animation starting at 0 that browsers throttle for backgrounded tabs, while the podium rendered statically. Fixed by rendering real values directly.
6. A fresh login could briefly render the previous user's cached dashboard data (React Query cache not scoped to user identity, nothing cleared it on account switch) — fixed by clearing the cache on the existing auth-changed event.
7. One component used the browser's default date-locale instead of the shared `en-IN` formatter — fixed; swept the rest of the frontend and found no other real instances.
8. Investigated (not code bugs): Trophy Room badge count vs. average mismatch has a legitimate explanation (lifetime counter + marks-weighted average, no phantom data); "View Result" click-target flakiness during QA looks like a browser-automation artifact (stale pre-hover coordinates), not a real user-facing bug; parent-report resend has no existing retry feature at all (a feature gap, not a regression).

Verification: full existing backend suite 48/48 pytest pass, app imports cleanly, all changed files compile clean. Frontend full-project `tsc --noEmit` timed out in this sandbox (recurring OneDrive-latency limitation, same as prior rounds) — every edit individually type-checked against its exact interface instead. **Now confirmed delivered and live-verified as of the 2026-07-24 final pass above.**

## Resolved Recently (2026-07-25, two rounds: 6 live-found #393 bugs fixed as PR #394, then 3 more live-found bugs including a systemic IM question-variety collapse — both CONFIRMED DELIVERED AND LIVE via real Chrome verification against production)

Immediately after the previous session's #393 "100-marks-always" feature was merged, Shailesh live-tested it and found 6 real, visible bugs the previous verification pass had missed entirely (it only checked marks math, never looked at actual generated question content). Full technical detail for both rounds in `CLAUDE_CODE_STATUS.md`; summary here.

**Round 1 (PR #394, `a67e90f`):**
1. Skill Stacker/Concept Drill section generating and listing FIRST instead of last — `distributeWeighted100()` in `page.tsx` (a #393-round regression) built weighted section rows before normal rows, which propagated into `display_order`. Fixed by re-sorting to the level's real registry order before returning.
2. Multiplication/division showing "+" instead of "×"/"÷" — pre-existing, platform-wide, not new. `QuestionText()`'s fallback in `assessment_engine_service.py` was operand-only and ignored the real operator, and that wrong text got persisted (frontend prefers persisted text over rebuilding). Fixed by making it operator-aware.
3. BODMAS questions overlapping the answer options — `ExpressionQuestion`'s character-count-gated `nowrap` didn't account for the Assessment Studio's narrower preview column. Fixed to always allow wrap (superseded by round 2's real fix, see below).
4. ANSWER_POSITION numbers wrapping onto a second line — fixed with dynamic font sizing based on value length.
5. Question preview showing 4 chips (section number, section title, concept tag, "Mixed") instead of the 2 the mock exam convention uses — fixed to match exactly (later found to still have a color inconsistency, see round 2 #3).
6. Step 1 "Skill Stacker/Concept Drill Questions" field visibly misaligned with "Duration Minutes" next to it — fixed with `items-end` on the grid row.

All 6 confirmed live via a real test blueprint generated in production (not just the mechanism): section order correct, weighted section literally last in question numbering, real `×`/`÷` symbols on generated questions, BODMAS no longer overlapping, both position directions rendering cleanly, exactly 2 chips.

**Round 2, straight after (same day, live-verified, no separate PR number recorded here — see `CLAUDE_CODE_STATUS.md`):** Shailesh flagged 3 more issues after seeing round 1 live, one of which turned out to be much bigger than it looked.

1. **BODMAS must never wrap — always one line, exactly like mock exams**, not just "allow wrap instead of overlap" (round 1's stopgap). Root cause: the assessment preview always used a narrow 360px question column regardless of question type; the mock exam preview already solves this by switching to a full-width single-column layout for BODMAS/expression/financial questions. The assessment preview never had that switch. Fixed by copying the identical conditional from the mock preview.
2. **Multiplication/division only ever showed 2D×2D and 4D/2D — never the level's other real digit patterns.** This was the real find: `ImSectionRegistryConfig()` in `assessment_engine_service.py` discarded every field a section's concept-pool entry carries except `conceptFamily`/`title` — `multiplicationDigits`, `divisionDigits`, `isLongDivisionEstimation`, `answerPositionDirection`, `positionRange`, `hasSquareTerm`, `bodmasTemplate`, `borrowingMode`, `isDecimal`, `fixedTimes`, `addRange`, and more all got silently dropped, so every question for a given concept family fell back to `operands.py`'s hardcoded default no matter which curriculum variant should have been used. This wasn't limited to multiplication/division or IM-L4 — it silently affected every IM level and multiple concept types, and **"Find the Position of the First Natural Number" could never generate at all**, since its direction flag never reached the generator. Fixed by merging the concept-pool entry's own fields straight into the generator config, mirroring how MM's equivalent function already does it. New regression test added and passing (48/48 backend tests total).
3. **Section-name chip didn't match the section-number chip's blue styling** — only applied to MM, plain gray for every other module including IM (the exact same pattern also exists in the mock studio's per-question chips, not something this round introduced — flagged, not touched there since Shailesh only flagged the assessment view). Fixed by removing the module conditional.

**Live-verified end to end (2026-07-25, real Chrome session against production):** built a fresh test blueprint (IM-L4, 92 questions), confirmed via computed style both chips share identical class and color; sampled 11 of 13 multiplication questions and got real variety across all 5 registry digit patterns (2D×2D through 4D×2D); sampled 7 of 13 division questions, same result across all 5 patterns including the long-division-vs-exact distinction working correctly; confirmed both `WRITE_FROM_POSITION` and `FIND_POSITION` questions now appear in the same generated set; confirmed a 36-character BODMAS expression renders on one line at full width with no overlap. Test drafts deleted after each round; Assessment Library confirmed back to just the real published assessments each time, no test data left behind.

**Still open, see Active above:** the live "IM L4" published assessment (and possibly others) still contains data generated before either round's fixes existed and needs regeneration — awaiting Shailesh's decision on scope.

## Resolved Recently (2026-07-20, student-portal QA sweep — 3 bugs found and fixed, all CONFIRMED DELIVERED AND LIVE: PR #353, #354, #355)

Shailesh asked for a full QA pass on the student portal with a zero-tolerance bar for discrepancies, fixed for all students now and in the future. Three real bugs found and fixed; full narrative (including a 3-round chase on the third one that ended up being a false alarm from deploy-propagation lag, not a code bug) in `COWORK_HANDOFF.md`'s matching "2026-07-20 update" entry. Summary:

1. **Practice tab "Needs Re-Attempt" stuck at 0 for unstarted re-attempts.** `needsReattempt()`/`statusLabel()` in `DetailWorkspaceViews.tsx` only checked re-attempt status on already-completed rows; an unstarted re-attempt (backend already stamps `requiresAttention`/`isReattempt`/`retryAttemptNumber` on these) fell through to generic "Pending." Fixed by adding the same checks to incomplete rows. Confirmed live: shows "1" matching the one actual pending re-attempt DPS.
2. **Dashboard's "Consistency: Top 5% this week" was a hardcoded, fabricated stat**, shown even for a student with zero weekly activity. Replaced with a real `weeklyConsistencyLabel` computed from the existing per-day heatmap data. Confirmed live: shows "Consistency: active 1/7 days this week."
3. **Leaderboard "Overall Journey" accuracy didn't match Mock Exams/Mock Performance Insights (71% vs 42%).** Root cause: leaderboard used a plain unweighted `AVG()` of per-mock accuracy percentages; Insights correctly pools `sum(correct)/sum(total_questions)` across mocks of different sizes. Fixed across 3 rounds (PR #353 weighted-formula switch, PR #354 `is_active` scoping to match Insights, PR #355 correcting which FK column the `is_active` join uses) — the first two rounds each closed part of the gap; a live re-check after PR #355 still showed a lower number than expected (29% vs 42%), which triggered an extensive diagnosis (temporary debug fields added to the leaderboard response, plus an independent ground-truth cross-check against a **real student account, Rohan Sen**, via direct admin-API calls) before concluding PR #355 was actually correct all along and the discrepant readings were deployment-propagation lag, not a live bug. Confirmed live via a hard-reload + direct API call: Ishan Banerjee 42% (exact match), Rohan Sen 81% (exact match to hand-computed ground truth: 57 correct / 70 questions).

**All three confirmed delivered and live**, cross-checked against a real student account in addition to the test account. **One follow-up not yet confirmed:** the temporary diagnostic fields added during bug 3's investigation were removed in a cleanup pass — merge not yet confirmed, see Active list above.

## Resolved Recently (2026-07-19, repo-wide "clean slate" cleanup + a second bug found during its own live verification — CONFIRMED DELIVERED AND LIVE, both PRs merged: #346/decimal-crash+YLM-gate round and PR #347, `131a44a`)

Shailesh asked to fix everything still open or halfway before starting new work. Full writeup (including the live-verification narrative) in `COWORK_HANDOFF.md`'s matching two entries; summary here:

1. **MM assessment decimal-lesson crash (flagged 2026-07-18, fixed and confirmed live 2026-07-19).** `QuestionText()` fallback in `assessment_engine_service.py` crashed on decimal Add/Less operands (pre-signed strings like `"-12.75"`) because it assumed numeric operands. Fixed by coercing through `Decimal(str(...))` before the sign check.
2. **YLM gated out of admin Create Mock — confirmed live.** `frontend/app/admin/competition/mock-studio/page.tsx`'s module dropdown now only lists MM/IM (`CompetitionMockSupportedModuleCodes`). Confirmed via the real DOM in production: dropdown options are exactly "Select Module" / IM / MM.
3. **Repo hygiene:** `.gitignore` gaps closed; stale `.agents/AGENTS_backup.md` deleted after migrating its two still-accurate conventions into `.agents/AGENTS.md`; three untracked docs files deleted per Shailesh's choice.
4. **New bug found live-verifying #1 above, root-caused and fixed same session, merged as PR #347 (`131a44a`):** generating any MM-L1 full-level assessment failed outright (`"Master Module generator produced invalid question 1 for WHOLE_NUMBER_MULTIPLICATION"`), unrelated to #1. Root cause: `DPSSection.generator_config_json` nests the real per-section config (`multiplicationDigits`/`divisionDigits`) under an `"activeSection"` key (both `seed_master_module.py`/`seed_intermediate_module.py` build it that way), but `MmSectionConfig()`/`ImSectionConfig()` read it flat — so those keys were invisible to `_MultiplicationDigits()`/`_DivisionDigits()`, causing the generator (stage-based fallback) and validator (title-parse fallback) to permanently disagree on question 1's digit shape. Fixed with a shared `_ResolvedSectionGeneratorConfig()` helper that flattens `activeSection` correctly — same fix covers both MM and IM.

**All four confirmed delivered AND live, end to end, not just merged:** PR #347 (`131a44a`) confirmed via `gh pr checks --watch` (11/11 green) and `gh pr merge --squash`. Independently re-verified live via Claude-in-Chrome against production after deploy: regenerated the same MM-L1 full-level assessment preview that failed before — now returns `200`, all 40/40 questions generated, and Question 1 is a genuine `DECIMAL_ADD_LESS` question (`1995.3 + 1537.8 - 9278.7 = -5745.6`) rendering and computing correctly. This is the first real end-to-end live confirmation of fix #1 too, since bug #4 had been silently blocking it the whole time.

**Verified pre-delivery:** 24/24 backend pytest (both rounds); a script reproducing the exact real DB storage shape for #4 confirms the previously-0/12-valid case is now 12/12, plus 4 edge cases (no-nesting config, null config, malformed data, key-precedence) all pass; frontend typecheck + build confirmed clean via the local terminal (41/41 routes) for the #1/#2 round.

**Two harmless leftover drafts, need manual deletion alongside the pre-existing ones (see Active above):** "Verification Check MM Decimal Crash Fix (delete after)" (MM-L1, DRAFT) and the older "MM-L1 Verification Mock"/"Verification Check IM-L2..." pair already flagged.

## Resolved Recently (2026-07-18 round 2, trivial-operand bug in MM + IM multiplication/division/BODMAS generators — CONFIRMED DELIVERED AND LIVE as PR #345, `2d414db`)

**Delivery + verification, added round 4 (same day):** Shailesh committed/pushed as PR #345 (`2d414db`), confirmed via `git log`/`git fetch origin main` matching `origin/main`, `git diff --stat 2c6e0dc 2d414db` confirming the diff is scoped to exactly `im/operands.py` and `mm/operands.py`, and Render/Vercel deploy-dashboard screenshots showing `2d414db` Live/Ready in Production. Independently re-verified live via Claude-in-Chrome against the real production admin panel (not just sandbox tests): IM-L2 multiplication regenerated twice, 0 trivial operands; IM-L2 BODMAS regenerated, 0 trivial embedded divisors; MM's BODMAS (MM-L1 Lesson 19 DPS4) regenerated live, 0 real trivial hits (one scan false-positive on a `×10%` percentage term — correctly excluded, since percentage terms are a different concept family this fix deliberately left untouched — corrected and re-run clean).

Shailesh flagged a live admin-preview screenshot: IM-L2 multiplication questions like "9663 x 1 = ?" — a student can answer without computing, since anything x 1 is itself. Asked that this be checked for both multiplication AND division, across every level in both MM and IM, plus that MCQ distractors not be guessable by pattern alone.

**Root cause (IM):** two independent bugs in `im/operands.py` — `_IsTrivial()` only flagged exact multiples of 100 >= 100 (never caught a literal 1), and `GenerateWholeNumberMultiplication()`'s retry loop only re-rolled when BOTH operands were trivial (AND, not OR), so a single trivial 1-digit side was accepted on the first attempt every time. Fixed by porting MM's existing, correct `_IsTrivialScaleOperand` definition (0, 1, or any value whose only nonzero digit is the leading one: 10, 20, 50, 100, 300, 1000, ...) into IM, fixing the AND->OR logic, and extending the guard to `GenerateWholeNumberDivision`'s divisor (previously only excluded literal 1) and to embedded division/multiplication terms inside both IM BODMAS templates (L2/L3's exact-division shape and L4's own basic/squared-term shape, the latter having zero guard at all on its embedded terms).

**Root cause (MM):** MM's *standalone* multiplication/division generators were already fully protected via the same `_IsTrivialScaleOperand` guard (confirmed via audit, no bug there). But MM's BODMAS composite-expression family (6 stage-specific builders, reachable across lessons 1, 2, 4, 6, 10, 11, 16, 19, 21, 22, 23, 27 — essentially every lesson band with a BODMAS section) had the identical unguarded-embedded-term gap in 5 places, plus a hand-picked `BracketDivisor` choice list that was majority-trivial by design (10, 20, 40, 50, 100 of 7 choices). All fixed with the same guard already used elsewhere in that file.

**Deliberately not touched:** percentage-specific round numbers (10%, 20%, 25%, 50%, etc., used in several BODMAS percent terms) — a different concept family from the "multiplication/division sum" complaint, and workbook-native convention that shouldn't change without being asked.

**MCQ distractor quality:** audited separately, confirmed already solid — the shared `smart_distractors.py` engine (used by both MM and IM) already enforces last-digit matching, a plausibility magnitude band, and operation-specific "genuine mistake" candidate pools (digit-transpose, middle-digit-shift, missed-row, sign-flip). Not the cause of the reported bug and needed no changes.

**Verified:** 24/24 existing pytest pass. IM: 40,100 standalone mult/div questions across all 3 levels, 0 trivial operands; 2,700 BODMAS questions across all 3 levels, 0 trivial embedded divisors; full 60-DPS stress test, 16,275 questions, 0 errors (no regression). MM: 2,400 BODMAS questions across all 12 affected lessons, 0 trivial embedded operands; full 30-lesson stress test, 7,830 questions, 0 errors (no regression). **Delivered and live — see round 4 verification note above.**

## Resolved Recently (2026-07-18, IM-L2 full build (DPS/assessment/competition mock) + IM-wide assessment marks-weighting fix — CONFIRMED DELIVERED AND LIVE as PR #344, `2c6e0dc`)

**Delivery + verification, added round 4 (same day):** Shailesh committed/pushed as PR #344 (`2c6e0dc`), confirmed live on both Render and Vercel. Independently re-verified live via Claude-in-Chrome against the real production admin panel: assessment marks fix confirmed via raw API response on a real draft blueprint (`totalMarks: 80`, exact 5/1-mark split matching `ImConceptWeightedQuestionMark()`); mock registry confirmed via raw API response on a real draft mock (exactly 6 sections, `marksPerQuestion: 1` flat including Skill Stacker/Concept Drill, 0 trivial operands across 50 scanned questions). Also confirmed: the existing section-omission redistribution/dense-renumbering engine (2026-07-17) covers IM-L2's new registry automatically, no extra code needed — `_RedistributeSectionCounts()`/`_DenseSectionNumbering()` are shared, level-agnostic helpers keyed off whatever `SectionDefinitions` a level's registry provides.

See `CLAUDE_CODE_STATUS.md`'s "Last completed milestone" section for the full technical writeup. Short version: (1) full cell-by-cell audit of `IM2 Lvl 6.xlsx` (60 DPS) confirmed IM-L2 is genuinely independent of L3/L4 — plain-product Skill Stacker formula (not doubling), positive-only Borrowing, lower multiplication/division ceiling, no Solve Equation/Answer Position; DPS engine built and wired in (`curriculum_map.py`/`operands.py`/`validators.py`/`seed_intermediate_module.py`). (2) IM-wide (all current + future levels) assessment marks-weighting fix — assessments previously split a flat 100-point pool evenly regardless of concept; now 1 mark per question except Skill Stacker/Concept Drill at 5 marks, matching DPS practice exactly, via a new `ResolvedAssessmentQuestionMark()` resolver wired into all 4 marks-computation call sites in `assessment_engine_service.py`. Master Module explicitly untouched (falls through to the original flat function, byte-for-byte, confirmed via direct unit test). (3) IM-L2 competition-mock registry added (`IM_COMPETITION_LEVEL_REGISTRY["IM-L2"]`) — 6 sections (Squares and Positional/Placement both confirmed absent from this level, dropped). All three pieces verified against real seeded scratch DBs, not just unit-level: IM-L2 DPS 9,960-question stress test 0 errors; assessment marks fix end-to-end on real IM-L2 (12Q) and IM-L3 (120Q, actually exercised the 5-mark branch) blueprints, `Version.total_marks` summed correctly both times; mock registry end-to-end at 60 and 120 questions, all 6 sections populated correctly, every question 1 mark. One new pre-existing bug found during this verification, not fixed — see the MM `QuestionText()` crash entry under Active above. **Delivered and live — see round 4 verification note above.**

## Resolved Recently (2026-07-17 round 3, MM 150-DPS audit + 8-finding fix + live verification + DPS-title cosmetic pass — PR #342 merged, PR #343 CI-green)

Shailesh uploaded 3 Excel workbooks covering all 30 Master Module lessons x 5 DPS (150 sheets) and asked for a full audit against `MM_CURRICULUM_MAP`. 8 findings delivered in chat first, approved with two explicit rules: leave Skill Stacker/Concept Drill counts at 5 (challenge level intentional), never alter already-attempted mock/practice content. 7 of 8 finding categories fixed in `curriculum_map.py` (~35 edits) plus one stale test assertion removed, delivered as **PR #342, merged, all 11 CI checks green**. Two edits initially planned but not executed were caught by a smoke-generation script before anything was reported done, fixed in a second pass, full 150-DPS regenerate-clean re-run after. A self-inflicted LF/CRLF false-alarm diff (Edit tool vs. working-copy line endings) was caught and fixed before commit both here and again in PR #343 — see `COWORK_HANDOFF.md`'s matching entry for the reusable diagnostic (`git show HEAD:<path> | xxd` vs. guessing).

Live browser verification followed (Claude-in-Chrome against production `math-path-platform.vercel.app` admin panel): all 7 MM fix categories confirmed correct in the live "Generate Preview"/"Show Answers" tool; IM-L3 and IM-L4 both spot-checked healthy and unaffected (separate curriculum map); a live MM-L1 competition mock draft generated cleanly (100Q, 10 even sections). **Key architecture finding, stronger than assumed going in:** `attempt_service.py` generates every student attempt's questions fresh at attempt-start from whatever `curriculum_map.py` is currently deployed — DPS publish only stamps a seed/timestamp, never freezes content — so PR #342's fix applied automatically to all future attempts platform-wide the moment it deployed, with zero DPS republishing needed, while already-attempted content stayed untouched per Shailesh's constraint.

Verification surfaced one follow-on bug: the admin panel's DPS title labels come from a separate hardcoded `DPS_TITLES` dict in `seed_master_module.py`, decoupled from the actual curriculum content, so 5 labels still described pre-fix content after PR #342 (e.g. L14 DPS5 still said "...& Less Percentage (Visual)" though the actual section list no longer has one). Shailesh asked this be fixed too. All 150 `DPS_TITLES` entries cross-referenced against actual generated sections via script; 5 genuine content-inaccurate labels fixed (L12 DPS1, L13 DPS1, L14 DPS5, L19 DPS1, L29 DPS4) — purely stylistic differences elsewhere (character/typography/conjunction-word variance, consistent across the whole file) deliberately left alone. Confirmed no separate DB backfill needed: `seed_master_module()` runs unconditionally on every backend startup and unconditionally overwrites `dps_title` on existing rows, so the fix self-applies on deploy, same mechanism as PR #342. Delivered as **PR #343, all 11 CI checks green — merge not yet confirmed, see Active section above.**

Full narrative, exact finding list, and file-level diff detail in `COWORK_HANDOFF.md`'s matching "2026-07-17 update (round 3)" entry.

## Resolved Recently (2026-07-17 round 2, IM-L4 bug-fix pass + IM-L3 net-new build + IM-L3 competition registry + MM/IM section-omission redistribution & dense renumbering — implemented and self-verified in Cowork, PREPARED NOT YET DELIVERED)

See `CLAUDE_CODE_STATUS.md`'s "Current" section for the full technical writeup — kept there rather than duplicated here since it's still pre-delivery. Short version: (1) 4 real IM-L4 bugs found via a full cell-by-cell audit of LEVEL 8.xlsx and fixed in `operands.py`/`validators.py`/`curriculum_map.py`; (2) IM-L3 built net-new, same evidence-first discipline, from `IM3 Lvl 7 New.xlsx`; (3) `IM_COMPETITION_LEVEL_REGISTRY["IM-L3"]` added — Section 6 is "BODMAS" only (Solve Equation confirmed absent from the entire L3 workbook), multiplication/division digit patterns and Skill Stacker/Concept Drill ranges all pulled from what L3 actually contains, not scaled down from L4; (4) a general engine fix applied to both MM and IM: omitting a competition-mock section now redistributes its share to the remaining sections instead of shrinking the mock, and renumbers every remaining section densely/gap-free everywhere a number is shown — including, new this round, the admin's Section Allocation panel itself updating live as the admin types, per an explicit request for real-time feedback. All four pieces verified against a real scratch DB (not just unit-level): 960-generation stress test, full pytest (24/24), a real IM-L3 generation + omission smoke test with assertions on the actual persisted section numbers/titles, and a client-side algorithm cross-check against the backend's redistribution logic across 6 scenarios. **Blocked purely on delivery** — needs a local Claude Code session (qa-reviewer then sre-devops) to commit and push; this Cowork sandbox has no GitHub route.

## Resolved Recently (2026-07-17 correction, IM Long Division & Estimation — actually delivered as PR #331, this doc's "Active" entry was stale)

The "needs pytest/typecheck/build + delivery" entry that lived here since 2026-07-15 was wrong — `git log` shows `4e07847` ("fix: IM Long Division & Estimation now requires a remainder and rounds correctly", PR #331) already merged to `main`, ahead of the 2026-07-17 login-page work. Shailesh caught the stale doc; verified directly against `git log -- backend/app/question_engine/im/operands.py` and `validators.py` rather than trusting this file. The working tree shows `operands.py`/`validators.py` as "modified" in `git status`, but that's the repo-wide CRLF/line-ending noise (256 files affected, confirmed whitespace-only via `git diff -w`), not real pending changes. Nothing left to do on this item.

## Resolved Recently (2026-07-17, login page hover/crop/copy — delivered as PR #335, #336; verified via CI, not yet all live-visually-confirmed)

Role-tab hover restored to the pre-redesign behavior (fills with the role's gradient + white text) — correct on the first attempt. Desktop story-panel feature-card cropping traced through three real attempts to its actual root cause: `overflow-y:auto` had been added to `.math-login-story` (the whole panel) as a safety net, but that panel also holds a purely decorative glow-blur circle intentionally positioned 112px off-panel (`bottom:-112px`) — putting the scroll behavior on the panel made the browser count that decorative bleed as real overflow, so CI's overflow measurement was reporting exactly 112px every single run regardless of three rounds of genuinely different real-content sizes. Fixed by scoping the scroll safety-net to `.math-login-story-content` (the actual logo/copy/feature stack) instead, leaving the panel on its original `overflow:hidden` so the decorative bleed clips cleanly again. A stopgap 4-column/truncated-text feature-grid layout adopted mid-chase (before the real cause was found) was reverted back to the original 2x2 grid with full text once Shailesh flagged the truncation looked bad. Copy across all 3 roles' Description/Promise/Feature text tightened per Shailesh's "more professional, crisp" ask — no follow-up issues. Full narrative in `COWORK_HANDOFF.md`'s 2026-07-17 entry, including the reusable lesson: if a CI overflow number is suspiciously identical across genuinely different content, suspect the measurement target before the content.

**Dark-mode logo box — confirmed fixed live by Shailesh (2026-07-17), after PR #339's deploy.** Logo box reads clean in dark mode, light mode unchanged, role-switch animation feels fine. This closes out the 3-round logo chase (PR #337/#338/#339) for real this time. The broader visual-elevation ask (living background, abacus flourish, confetti-on-login) remains open in Active — explicitly deferred, not part of this confirmation.

## Resolved Recently (2026-07-15, live browser visual verification of IM mock-studio preview — confirms PR #328's fixes hold in production)

Followed up on the one open item from 2026-07-14 (PR #326/#328/#329): actually looked at a real IM Level 4 competition mock in the admin mock-studio preview via a live Chrome session logged in as admin on the production Vercel deployment, rather than trusting typecheck/build + isolated unit tests alone.

**BODMAS/Solve Equation rendering (Section 6):** checked multiple questions across the section, including a plain 5-term BODMAS expression (`853 ÷ 76 + 6591 × 3 + 1475 - 6281`) and one with a squared term (`751 ÷ 19 + 79² + 74 × 66 + 7879 - 2540`). Both rendered on a single clean line in the wide layout with no clipping or overlap — confirms `needsWideQuestionLayout()` from PR #328 is working correctly live, not just in the sandbox. Also checked a Skill Stacker question (Section 8, the other section carrying the heavier 5-marks-per-question weight) — same clean wide-layout rendering, no issues.

**Equal concept distribution:** pulled the full Coverage Check breakdown for all 100 questions across all 8 sections of the live `IM_01` mock. Every section's question count splits as evenly as the math allows across its concepts: Section 6 (BODMAS/Square-Term/Solve-Equation, 13 questions) splits 5/4/4; Section 7 (2 concepts, 12 questions) splits exactly 6/6; Section 8 (2 concepts, 10 questions) splits exactly 5/5; Sections 1-4 (5-6 concepts each, 13 questions) all split within 1 question of perfectly even. No concept dominates its section. Confirms the `_CollectImCompetitionSectionLockedQuestions()` rewrite from PR #328 holds in the live generated data, not just the synthetic isolated test.

**Verdict: nothing broken.** Both open questions from the 2026-07-14 handoff are now closed — this was the last item blocking a clean "good to go" on that round of work.

**One non-blocking observation, not confirmed as a real bug:** while separately trying to generate a brand-new draft mock via Create Mock (rather than just previewing the existing `IM_01`), the browser tab hung for an extended period on the generation request and the draft was never actually created (confirmed absent from Mock Paper Library afterward). Retried in a fresh tab and could not reproduce the hang against the same already-generated mock, so this may have been a Chrome-extension connection hiccup rather than an app issue — but flagging in case creating a fresh 100-question IM mock is ever observed to hang for real. Worth a quick sanity check next time a new IM mock is generated from scratch.

## Resolved Recently (2026-07-14, IM Level 4 in assessments + competition mocks, equal-distribution/layout fix, level-registry robustness, and repo-wide branch/ref hygiene — delivered as PR #326/#328/#329, plus non-code repo cleanup)

**Delivery:** PR #326 (assessments + IM 8-section competition mock structure), PR #328 (tag `prod-20260714-1856`, equal concept distribution + mock-studio layout width), PR #329 (tag `prod-20260714-2019`, level-keyed registry robustness) — all merged, all 11 CI checks green each time, backend health-checked after each. Full technical detail in `COWORK_HANDOFF.md`'s matching 2026-07-14 entry. Summary:

1. **Assessments were silently degrading every MM/IM lesson to trivial single-digit add/less**, regardless of the real lesson concept, because the generic YLM-shaped config path was being used for all three modules. Fixed with module-aware dispatch (`MmSectionConfig`/`ImSectionConfig`), confirmed level-agnostic (dispatches on module code only, correctly covers every lesson in every level).
2. **IM Level 4 competition mocks** now have the exact 8-section structure Shailesh specified, with admin section-count allocation matching MM's existing no-auto-redistribution convention (admin decides every count, including 0 to omit, nothing auto-rebalances).
3. **BODMAS questions were displaying clipped/overlapping in the admin mock-studio preview** — root-caused (after a first attempt made it worse) to a hardcoded MM-only wide-layout gate in `mock-studio/[mockId]/page.tsx`; fixed by generalizing to the actual resolved display type via `needsWideQuestionLayout()`.
4. **One concept within a competition-mock section could dominate the others** instead of an equal split — root-caused to batch-then-greedy-consume question generation desyncing from the pre-computed equal schedule; rewrote to generate exactly one question per scheduled slot in strict order.
5. **MM/IM competition-mock section structures were bare module-level constants with no level check** — a request for any level besides the one currently live (MM-L1, IM-L4) would silently generate the wrong level's content instead of erroring. Fixed with `MM_COMPETITION_LEVEL_REGISTRY`/`IM_COMPETITION_LEVEL_REGISTRY`, keyed by `level_code`, failing loudly for any unconfigured level. Confirmed safe for live students beforehand: mock generation is a one-time admin action, persisted to the DB before students ever see it — this refactor could not affect an already-generated mock. 4 new tests added (24 total backend tests, up from 20).
6. **Platform-wide audit** (investigation only) confirmed assessments and the DPS/practice smart-distractor fix are solid across all three modules and every level. **Confirmed gap, not fixed:** YLM has zero competition-mock support (see Active section above).
7. **Repo-wide branch/ref hygiene** (not code) — root-caused a recurring Windows "filename too long" git corruption to hundreds of stray branches with excessively long auto-generated names from the `apex_deliver.py` delivery tool; full 430-branch audit, two independent content-verification passes (byte-for-byte tree-hash matching, not just ancestor-checking) confirmed everything flagged for deletion was either already merged into `main` or fully redundant, including specifically double-checking the highest-stakes item (a security-hardening branch, confirmed byte-identical to what's already live). A full backup bundle of every branch was made before deleting anything. 300 branches deleted (77 local, 223 remote) plus 18 auto-pruned stale refs; 8 branches actively used by a separate tool (Gemini/Antigravity) were deliberately left untouched. Root cause fixed via `git config core.longpaths true` (left enabled this time, unlike a one-off unblock-then-revert of the same setting on 2026-07-10, since this time the root cause is systemic).

**Nothing pending from the code side of this thread.** The only follow-up work is the visual verification and the two confirmed gaps (YLM, future IM levels) listed under Active above.

## Resolved Recently (2026-07-13, IM Level 4 marks-per-question fix — delivered as PR #323, verified end to end)

Follow-up to the IM + Level 4 launch (PR #321, docs close-out PR #322 — see `CLAUDE_CODE_STATUS.md` for the full launch writeup). Concept Drill and Skill Stacker sections only carry 2 questions per sheet by design, so scoring them at the same flat 1 mark/question as every other IM concept made a sheet's total marks non-whole and undervalued those two concepts. Requirement: 5 marks each for Concept Drill/Skill Stacker (0 if wrong), everything else in IM stays at 1 mark, zero effect on MM/YLM scoring.

**Fix:** new nullable `DPSSection.marks_per_question` column — `NULL` (default, every existing section in every module) means "inherit `DPS.marks_per_question`," unchanged behavior. Only IM's seed script sets `5.0` on Concept Drill/Skill Stacker sections. `attempt_service.py`'s `start_attempt()`/`submit_attempt()` now look up each question's own section override before falling back to the DPS's flat value. `backend/app/models/models.py`, `schema_migration.py`, `main.py`, `seed_intermediate_module.py`, `attempt_service.py`, `routes_student.py` (exposes correct `totalMarks` on `/student/assignments`).

**Verification:** a standalone throwaway-SQLite script (`verify_marks_per_question.py`) run directly in the developer's own terminal (Cowork's sandbox hit its usual stale-bash-mount issue on the 5 edited files — confirmed spurious via fresh Read-tool re-reads, not fought further). All 12 checks passed: an IM-style DPS (10 Add/Less@1 + 2 Skill Stacker@5 + 2 Concept Drill@5) computed `max_score == 30` and scored a mixed correct/wrong/unanswered attempt exactly right; an MM/YLM-style DPS (single flat section, no override) matched the old pre-existing flat formula exactly — zero regression outside IM.

**Delivery:** PR #323, merged via `gh pr merge --squash --delete-branch` after all 11 CI checks passed, no `--admin` bypass (this touches live scoring). Two process hiccups along the way, both self-corrected with no data loss: an initial direct push to `main` was rejected by the repo's server-side safety guard (recovered by moving the already-made commit onto a feature branch via `git branch` + `git reset --hard origin/main`), and a merge attempt was tried before the commit/push had actually happened (caught via `gh pr create`'s own refusal, re-sequenced correctly). **Nothing pending from this thread.**

## Resolved Recently (2026-07-13, premature mock-exam auto-submit — investigated and fixed as PR #318, verified)

Students reported mock exams auto-submitting while the on-screen timer still showed minutes remaining. Investigated before any code changed. Backend deadline enforcement was confirmed correct (`expires_at = started_at + duration_seconds`, re-checked on every answer-save and page load). Root cause was the client-side countdown: `frontend/hooks/useAttemptTimer.ts` (shared by mock/assessment/DPS-practice attempt pages, unchanged since the initial commit) counted down via a plain `setTimeout` chain with no anchor to wall-clock time and no resync on tab-focus regain (React Query's `refetchOnWindowFocus` is globally off). A backgrounded tab (student switches apps, phone locks) freezes the display while the server's real deadline keeps advancing; the next answer-save correctly detects real expiry and submits, while the frozen display still showed time left — reproducible for any student who left the tab mid-attempt, on any timed activity type, which is why only some students hit it.

**Fix:** `useAttemptTimer.ts` now anchors to an absolute deadline and self-corrects every tick regardless of throttling, plus a `visibilitychange` listener that resyncs immediately and refetches the attempt from the server the moment the tab regains focus. Wired into all three attempt pages (mock, assessment, DPS/practice). Also fixed in the same pass: `.math-timer-critical`'s pulsing animation referenced the wrong keyframes name (`math-timer-critical` vs. `@keyframes mathTimerCritical`) so the sub-5-minute urgency pulse never actually ran — one-line name fix; and `TestTimer.tsx` had no dark-mode variants on its non-urgent state — added.

**Delivery:** PR #318, squash commit `fa246c0`, all 11 CI checks passed (`npm run typecheck && npm run build` run for real in the developer's terminal, clean, 41/41 routes). Frontend-only, no backend/schema touched. **Not yet independently browser-verified live** — see Active item above.

## Resolved Recently (2026-07-13, repo-hygiene cleanup — delivered as PR #315, verified)

Shailesh asked for a full repo sweep for dead/unused files and anything bloating or slowing down the platform, across everything, not just the student portal. Findings, all confirmed unreferenced by any code, docs, or CI workflow before flagging:

1. **`MathPath-Platform/` — a 946MB duplicate nested clone of the entire repo**, tracked as a broken/orphaned gitlink (mode `160000`, dangling commit hash) with no `.gitmodules` registering it as a real submodule. Includes its own `.git`, its own 459MB `node_modules`, duplicate `backend/`/`frontend/`/`docs/` — pure dead weight, most likely from an accidental `git add` after cloning the repo into a subfolder at some point. Removed via `git rm --cached` + manual delete (submodule/gitlink entries aren't cleanly removed by a normal `git rm -r`).
2. **4 unreferenced root-level files:** `original_page.tsx` (stray "before" snapshot of a page component), `teacher-rules.css` (64KB, not imported anywhere — real styling is `frontend/app/globals.css` + Tailwind), `trigger_gamification_sync.py` and `trigger_production_sync.py` (pre-dated the proper `backend/scripts/backfill_*` convention, fully superseded).
3. **5 pre-git versioning artifacts:** `README_MATHPATH_v2_2_PHASE2A_STABLE.md`, `README_MathPath_v2_3_Phase_2_Stable_Freeze.md`, `README_STABLE_v2_1_PREMIUM_UI.md`, `VERSION.txt`, `STABLE_PACKAGE_DO_NOT_EDIT_DIRECTLY.txt` — all from an old "duplicate the whole folder per phase" workflow, obsolete now that git branches/tags/PRs handle versioning. Only the plain `README.md` stays.
4. **`mathpath.db`** — empty (0 bytes), untracked (caught by `.gitignore`'s `*.db` rule already), just deleted from disk.

**Not a repo/git problem, flagged for awareness only:** `frontend/node_modules` (818MB) and `frontend/.next` (2.4GB) are properly gitignored, not bloating git history — but both, plus the nested clone's own `node_modules`, sit inside the OneDrive-synced folder, meaning several GB of build cache/installed packages are likely being continuously indexed by OneDrive. Plausible contributor to this session's repeated "stale sync" issues. Worth excluding `node_modules`/`.next` from OneDrive sync, or moving local dev work outside the synced folder — a local-machine setting, not something a code change can fix.

**Delivery:** PR #315, squash commit `040a946`, all 11 CI checks passed (backend-tests, frontend-build, frontend-typecheck, frontend-e2e-tests, governance-audit, repository-safety) — confirming everything removed was genuinely unused.

## Resolved Recently (2026-07-13, unified economy system + assessment timestamp fix + cleanup — delivered as PR #312, backfilled, verified)

**Delivery:** PR #312, squash commit `6145dca`, all 11 CI checks passed (`pytest tests/ -q`: 20 passed; `npm run typecheck && npm run build`: clean, 41/41 routes). Confirmed deployed on both Vercel and Render. Full detail in `COWORK_HANDOFF.md`'s latest 2026-07-13 entry. Summary:

1. **Unified economy system** — DPS, assessments, and mock exams now all earn XP/coins under one formula (`EconomyService.evaluate_activity_performance()`), based on each attempt's allotted `duration_seconds` × the existing accuracy multiplier curve × a per-activity weight (DPS 1.0 / Assessment 1.3 / Mock 1.5). Previously only mocks earned anything, despite the dashboard explicitly promising MathCoins for DPS completion. New `gamification_processed_at` columns on `attempts` and `assessment_attempts`. **Backfilled:** `backfill_practice_dps_economy.py --apply` — 8 historical attempts (Sakshi Agarwal x4, Shailesh Gupta x1, Meera Chatterjee x3), 210 XP / 7 coins awarded total, hand-checked against the formula (duration 300s × DPS weight 1.0 × accuracy multiplier band per attempt — all matched exactly).
2. **Assessment AUTO_SUBMITTED timestamp bug** (same class as round 10, never applied to assessments) — fixed in `_SubmitAssessmentAttemptCore()`. **Backfilled:** `backfill_assessment_economy.py --apply` and `fix_assessment_auto_submitted_timestamps.py --apply` — 0 attempts affected by either (no assessment completions exist historically yet, consistent with PR #311's finding).
3. **DPS `marks_per_question` fix** — `submit_attempt()` now scores using the DPS's real configured value instead of hardcoding 1.
4. **Cleanup** — `recalculate_streaks.py`'s broken column reference fixed; dead `NotifyPracticeReattemptUnlocked()` deleted; dead synthesized `GET /student/notifications` endpoint deleted.

**Nothing pending from this thread.** All fixes live in production, all three backfills applied, zero errors.

**Also this round — docs-sync fix, delivered as PR #313.** `CLAUDE_CODE_STATUS.md` and this file briefly carried stale "not yet delivered" content after PR #312 actually merged (a sync gap between the Cowork sandbox's edits and what `git add` captured at commit time — same root cause flagged in the note below from the PR #311 round, now confirmed to have also affected PR #312). Corrected via a docs-only PR #313 (squash commit `9871632`). If a memory doc's status ever looks inconsistent with `git log`/PR history again, trust the git history, not the doc.

## Resolved Recently (2026-07-13, full student-portal audit — five fixes, delivered as PR #311, backfilled, verified)

**Delivery:** PR #311, commit `434f357`, all 11 CI checks passed, merged. `pytest tests/ -q`: 20 passed. `npm run typecheck && npm run build`: clean. Full detail in `COWORK_HANDOFF.md`'s 2026-07-13 entry. Summary:

1. **Practice/DPS lazy-auto-submit notification gap** (critical, same bug class as pre-round-9 mocks) — the lazy timer-expiry fallback graded attempts correctly but never notified anyone. Fixed by moving the notification into `submit_attempt()` itself, gated by new `notification_processed_at` column (migration `bfa28b9fc380`). **Backfilled:** `backfill_practice_attempt_notifications.py --apply` — 8 historical attempts (Sakshi Agarwal x4, Shailesh Gupta x1, Meera Chatterjee x3), 30 notifications created.
2. **Assessments had no server-side lazy-completion safety net at all** (high) — could get stuck `IN_PROGRESS` forever if the frontend's auto-submit call never landed. Added the same fallback mocks/practice already have (`EnsureAssessmentAttemptActiveOrAutoSubmit`), notification gated by new `notification_processed_at` column on `assessment_attempts` (migration `a1e6838c5ea3`). **Backfilled:** `backfill_assessment_attempt_notifications.py --apply` — 0 attempts affected (no historical gap existed).
3. **`get_current_student()` defensive `is_active` check** (medium, hardening) — now matches `get_current_teacher()`'s pattern.
4. **Sliding JWT session refresh** — tokens more than halfway through their lifetime are transparently renewed via an `X-New-Access-Token` response header, picked up silently by the frontend. Permanent fix for "student could theoretically get logged out mid-exam," not just a bigger expiry number.
5. Two new backfill scripts, both run to completion (see #1/#2 above): `backfill_practice_attempt_notifications.py`, `backfill_assessment_attempt_notifications.py`.

**Nothing pending from this thread.** All five fixes live in production, both backfills applied, zero errors.

## Resolved Recently (2026-07-12, round 10 + heatmap tier-color fix + badge-notification commit-bug fix — delivered as PR #310, backfilled, verified)

**Delivery:** PR #310, squash commit `c2ebec6`, merged after `pytest tests/ -q` (20 passed) and `npm run typecheck && npm run build` (clean, 41/41 routes) were run for real in the developer's own terminal (Cowork's sandbox had a confirmed-stale repo mount this session and couldn't be trusted for verification — see caveat below). Confirmed deployed on both Vercel and Render before either backfill script was run.

- **Round 10 — AUTO_SUBMITTED `submitted_at` fix.** Both developer decisions from the round-9 writeup below were made: (1) fix it going forward — `competition_mock_attempt_service.py`'s `SubmitCompetitionMockAttempt` now computes `submitted_at` for the auto path as `attempt.started_at + attempt.duration_seconds` (capped at "now") instead of `_now_utc()`; `time_taken_seconds`/`time_utilization_percentage` were left as-is since those were already correct for the auto path. (2) retroactively correct the already-backfilled attempts — approved; `backend/scripts/fix_auto_submitted_timestamps.py --apply` run against production: 13 AUTO_SUBMITTED attempts scanned, 12 had drifted timestamps (seconds to ~8 minutes), all corrected along with `CompetitionMockResultSummary.completed_at` and 36 notification timestamps (12 attempts x student/teacher/admin).
- **Heatmap tier bar colors.** Developer reported bars "meshing in with the background" during live student submissions. Root cause: the round-3 palette (PR #305) was tuned for dark mode only — S-TIER's gradient top (`yellow-300`) was ~1.3:1 contrast against the light-mode card (near-invisible), A-TIER/B-TIER (`emerald-500`/`sky-500`) were ~2.5-2.8:1 (below the 3:1 minimum for graphical UI), and REST (`slate-300` light / `white/10` dark) failed on both themes (~1.5:1 / ~1:1). All five tier colors were fine in dark mode (5.7-15.9:1) — a light-mode gap plus a separate dark-mode REST gap. Fixed in `frontend/app/student/dashboard/page.tsx` with a palette in the ~0.13-0.28 luminance band that clears 3:1 against both white and black cards with one color (no `dark:` variant needed): S-TIER `yellow-700→amber-600`, A-TIER `emerald-600`, B-TIER `sky-600`, C-TIER `violet-600` (moved off `amber-600`, now used by S-TIER), D-TIER `rose-600`, REST `slate-500`.
- **Badge-unlock notifications silently dropped (bug inside round 9's own fix).** Reported: a student's "The High Achiever" badge showed correctly in Trophy Room and the dashboard's "Latest Unlock" widget, but no notification for it ever appeared. Root cause: `CreateNotification()` only `db.add()`s + `db.flush()`es, never commits. Every other side-effect in `_ProcessMockCompletionSideEffects()` has its own explicit `db.commit()`, but the badge-unlock notification loop added in round 9 never did, and nothing later in the call chain commits either — so every badge-unlock notification created through live mock submissions since round 9 shipped (2026-07-11 ~20:41 UTC) was silently discarded, while the badge itself persisted fine (`evaluate_mock_exam_submission` commits its own writes first). Fixed with `db.commit()`/`db.rollback()` around each badge notification. `backend/scripts/backfill_missing_badge_notifications.py --apply` run against production: 65 badges scanned across all students, exactly 1 missing notification found (Nishant Gantayet's "The High Achiever" — the one that surfaced this bug) and created.
- **Verification caveat, for the record:** this session's Cowork sandbox had a confirmed-stale bash mount of the repo (not inferred — `competition_mock_attempt_service.py` was missing its last ~5 lines in the mount, `dashboard/page.tsx` threw phantom syntax errors from `tsc` at lines valid in the real file), so pytest/typecheck/build could not be trusted from Cowork and were skipped there. Real verification happened afterward in the developer's own terminal before pushing — both passed clean.
- Follow-up: browser-QA the new tier bar colors on live in both themes (confirm they render as intended), same as the still-open round-2 heatmap browser-QA item below.
- Terminal note for future backfill scripts: local `python scripts/*.py` defaults to the local SQLite dev DB unless `$env:DATABASE_URL` is set to the Render **External** Postgres URL first (Internal only resolves inside Render's network) — this tripped up the first `--dry-run` attempt this round (`no such column: gamification_processed_at`, because it was querying the unmigrated local SQLite file).

## Resolved Recently (2026-07-11, rounds 6-9 — delivered as PR #309, backfilled, and verified clean platform-wide)

**Delivery:** PR #309, commit `cd0a816`, merged same day via `gh pr merge --squash --auto` after `gh pr checks --watch` passed CI. Confirmed live on both Vercel (Production, commit `cd0a816`) and Render (`mathpath-backend`, deploy live for `cd0a816` at 2026-07-11 8:41 PM) via direct dashboard screenshots. Delivered from the developer's own local terminal using raw `git`/`gh` commands (not the AI-orchestrated qa-reviewer/sre-devops squad) — manual verification substituted for qa-reviewer: `python -m pytest tests/ -q` (20 passed) and `npm run typecheck && npm run build` (clean, 62/62 routes) were both run and confirmed green before push. Note: the repo has a server-side safety guard blocking direct pushes to `main` — first push attempt was rejected, redone correctly as a feature branch + PR (`fix/round-6-9-mock-gamification`) after resetting local `main` back to `origin/main`.

- **Round 9 — universal mock-completion gamification fix.** After a second live batch of students (Pragya Ghosh, Sampreeti Mohapatra, Tanay Gupta, Sayantan Biswas, +others) completed mock exams and got no XP/coins, no badges, no submission notifications (student, teacher, or admin), and a garbage leaderboard "time taken" value, root-caused and fixed all four, universally, for existing and future students:

  **Root cause (XP/coins/badges/notifications):** `competition_mock_attempt_service.py` had two code paths that could grade/complete a mock attempt without ever running its notification/economy/achievement side-effects, because those side-effects only lived in a separate wrapper function (`SubmitCompetitionMockAttemptForStudent`) that those paths bypassed entirely: (1) `EnsureCompetitionAttemptActiveOrSubmit()`, hit by any GET to load an attempt once the timer's already expired server-side (tab backgrounded, network dropped, or the student just reopens the page after time's up), and (2) the defensive result-summary repair in `_result_payload()`. Once an attempt's status flipped to completed via either path, the raw grading function's own "already completed" guard made it a permanent no-op for the real submit/auto-submit endpoints too — so the side-effects silently never ran, for however many students hit this (very plausible for most of a live class, since letting the timer run out or navigating away is common).

  **Fix:** relocated the notification/economy/achievement side-effects into the core grading function itself (`SubmitCompetitionMockAttempt`, now delegating to a new `_ProcessMockCompletionSideEffects`), guarded by a new `gamification_processed_at` timestamp column on `competition_mock_attempts` (Alembic migration `a1c9de3f7b21` + a matching `schema_migration.py` self-heal entry, per this repo's dual-safety convention) claimed atomically via a conditional `UPDATE ... WHERE gamification_processed_at IS NULL`. Every completion path — manual submit, explicit auto-submit, the lazy GET-triggered auto-submit, and the defensive result-page repair — now gets identical treatment, exactly once, with no dependency on which one happens to fire first. This is now live in production, so every future student and every future mock submission is covered automatically — nothing left to configure per-student.

  **Also found and retired:** `main.py`'s `on_startup()` used to unconditionally run `ensure_mock_student_notifications_retroactive()` and `recalculate_all_gamification_stats()` on every single backend restart — a pre-existing, fragile attempt to paper over this same bug. The first created a *differently-typed* (`MOCK_EXAM_COMPLETED` vs the real `MOCK_SUBMITTED`) student-only notification with no teacher/admin coverage; the second did a full destructive wipe-and-rebuild of every student's badge/stat tables on every deploy, and never touched XP/coins at all. Both are now retired from startup (left in the codebase, just not called) in favor of the real fix plus a deliberate one-time backfill. Important downstream consequence discovered during backfill verification: because this old job ran on *every* backend restart for months, it had already organically sent most students their legitimate badge-unlock notifications over time — this is why the backfill's "badges newly unlocked" count (64) was much higher than its "notifications created" count (a handful) on the full recompute: most badges were being re-detected against a freshly-wiped table, not newly notified.

  **Also found and fixed (separate bug, same investigation):** the student dashboard's "Acquired XP / MathCoins" wallet widget never read the real economy ledger at all — it computed a locally-fabricated approximation that didn't even include mock exams. Added `GET /student/economy` (real `UserEconomy` read) and wired `frontend/app/student/dashboard/page.tsx`'s `StudentWallet` to it.

  **Also found and fixed (leaderboard, unrelated read-path bug):** both `get_mock_exam_leaderboard` and `get_cumulative_leaderboard` in `routes_student.py` computed the displayed "time taken" by rescaling `time_utilization_percentage` against a hardcoded 3600-second (60-minute) assumption instead of reading the real, already-correct `time_taken_seconds` column. Fixed to read the real column directly; no backfill needed since the underlying data was always correct.

  **Backfill — run to completion and verified clean.** `backend/scripts/diagnose_mock_gamification.py` (read-only) and `backend/scripts/backfill_mock_gamification.py` (`--dry-run`/`--apply`) were run from the developer's local terminal against production. `--apply` ran twice: 54 historical attempts scanned across 9 students; first pass awarded missing XP/coins (6000 XP / 285 coins total) and created missing notifications; badge/stat recompute unlocked 64 badges (of which only 3 needed a fresh notification — see above for why). A real gap was found afterward: the script's original notification-dedup logic gated ALL notification creation (student+teacher+admin) on one boolean check ("does the student's MOCK_SUBMITTED notif exist"), which missed a genuine historical partial state — Meera Chatterjee's `MM_17` attempt had student+teacher notified but its admin notification was silently missing from whatever code ran before this was ever fixed. Fixed the script to check student, teacher, and each individual active admin independently; re-ran `--apply`, closed the gap. Final full-platform sweep via `diagnose_mock_gamification.py --all` (not scoped to the originally-reported students) confirms **zero remaining gaps across all 9 students and all 54 completed mock attempts** — every attempt shows OK for XP/coins, student notif, teacher notif, and admin notifs. This partial-notification failure mode cannot recur going forward: the live production hook is atomic per-attempt (one try block, one commit for all three recipient types together), so it was only ever possible in pre-fix historical data.

  Also fixed two script-quality issues found while running the backfill (not product bugs): the dry-run summary undercounted notifications created (fixed a counting bug), and the script went silent for minutes during the badge recompute with no way to distinguish "working" from "hung" (added per-student progress printing + forced `print(..., flush=True)` since Windows terminals were buffering output during the long loop).

- **Round 8 — Live Radar widget fix.** Root-caused why the admin "Live Radar" widget (`/admin/dashboard`) always showed "No students active" even while students were actively taking mocks. Two frontend files — `frontend/components/admin/LiveRadarWidget.tsx` and `frontend/hooks/useHeartbeat.ts` (mounted globally in `AppShell.tsx`) — both built their request URL from `process.env.NEXT_PUBLIC_API_URL`, a variable not defined anywhere in this project (the real one is `NEXT_PUBLIC_API_BASE_URL`). Both `fetch()` calls resolved to a relative path against the frontend's own origin, 404'd, and were silently swallowed by `catch` blocks. Fixed both by switching to the shared `api` axios client, matching the pattern used everywhere else in the codebase. Backend side was already correct.

- **Round 7 — Manage-tab, Trophy Room, Leaderboard fixes.** Full end-to-end audit of `DetailWorkspaceViews.tsx` and `CollectorVaultWorkspace.tsx` surfaced a 7-item cumulative findings list; 3 were fixed this round: (1) admin Manage-tab "Start Practice" quick-action compared the raw backend `row.status` enum against human-readable labels it never equals — changed to compare against the already-computed `Issue.label`. (2) Trophy Room (`frontend/app/student/achievements/page.tsx`) had no `useProtectedPage` guard and silently swallowed fetch errors — added the guard plus a proper `ErrorState` render. (3) Leaderboard (`frontend/app/student/competition/leaderboard/page.tsx`) had no `useProtectedPage` guard either — added it; deliberately left the page's missing `AppShell` wrapper alone (looks like an intentional immersive-layout design choice, not a bug — flagged, not fixed). **Still open, deliberately not addressed:** the Collector's Vault (`CollectorVaultWorkspace.tsx`) runs entirely on hardcoded dummy inventory data with zero backend calls despite being a live, reachable nav item — needs a product decision (build the real backend, or hide the nav entry) before a code fix makes sense.

- **Round 6 — section renumbering + heatmap timezone fix.** (1) Section renumbering now covers the full mock flow, not just Instructions/Admin: `backend/app/services/competition_mock_attempt_service.py` gained a shared `_section_display_number_map()` helper (real fixed 1-10 section_number -> gap-free 1..N display number). `_attempt_payload`/`_question_payload` now pass a `display_section_number` through to each question's `metadata.section_number`, so the live Mock Attempt page's "Section N" badges are gap-free. `_result_payload` now calls a new `_apply_section_display_numbers()` on `question_review` — but only *after* `_section_performance_from_review()` has already run on the real-numbered data, since that function's `MM_COMPETITION_SECTION_TITLES` lookup depends on the real fixed number, not display position; renumbering it first would have silently mislabeled concept strengths/weaknesses. Verified this ordering carefully since it's the one place a naive renumber-everywhere approach would have broken something. (2) Heatmap timezone bug: `frontend/app/student/dashboard/page.tsx` had two separate UTC-conversion traps — the week-column dates were built with local `Date` methods but serialized via `.toISOString()` (converts to UTC, can shift late-night activity to the wrong day for students behind UTC), and the month/year label re-parsed a "YYYY-MM-DD" string with `new Date(...)`, which JS treats as UTC midnight (can misread the month at a month boundary). Replaced both with explicit local-timezone-safe helpers (`toLocalDateKey`, `toActivityDateKey`, `parseLocalDateKey`) so every date decision in the heatmap consistently uses the student's own local calendar day. Delivered with PR #309; not yet independently browser-verified live (no live-student breakage reported since delivery, but a deliberate check with a student in a non-IST timezone, or confirming Section badges are gap-free during a fresh mock attempt + result review, hasn't been done as its own step).

- Full-portal audit (2026-07-11, read-only, no code changed): confirmed rounds 1-5 of the heatmap and section-numbering work are logically sound and additive-safe (checked payload null-safety, type consistency across every consumer, popover stacking, and non-MM-mock safety of `getCleanMmSectionName`). Found two things, both now fixed above: (1) the section-renumbering fix (round 4/5) only covered the admin Mock Studio page and student pre-exam Instructions page, not the live Mock Attempt page or post-exam Result/Review page. (2) The heatmap's week-boundary calculation could shift late-night activity into the wrong day bucket for students in UTC-behind timezones.

- Browser-QA the 2026-07-11 heatmap round 2 (ghost-bar CSS fix, per-task dynamic pace weighting, info/explainer icon) once delivered — confirm bars actually render (not just tooltips), confirm the info icon popover opens/closes correctly on both themes and mobile widths, and sanity-check a few more real multi-attempt days against the new pace-ratio math.
- Known limitation left open from the 2026-07-11 heatmap fix (not blocking, flagged for a future pass): `ListStudentCompetitionMockAssignmentsForAttempt` filters mock assignments to the student's `current_level_id`, so if a student is promoted to a new level mid-week, that week's mock activity from the prior level drops out of `/student/competition/mock-assignments` entirely (and therefore out of the heatmap) even though it happened this week. Would need the endpoint to stop filtering by current level for activity-feed purposes, or a separate activity-only endpoint.
- Browser-QA the 2026-07-07 student dashboard heatmap tooltip stack from `ec9d041` through `dd9938e`, especially current-week Sunday-to-Saturday alignment, empty future-day handling, tooltip readability, and dark/light theme contrast.
- Decide whether the shipped `flowState` formula should keep estimating `5` questions per sheet when `totalQuestions` is absent, or switch to payload-backed counts before relying on tier/rank copy. (Still open — the 2026-07-11 fixes reuse the same 5-question fallback for mock/assessment events for consistency, and now also fall back to a neutral 1.0 pace ratio when expectedDurationSeconds is unknown; neither fallback heuristic itself was resolved.)
- Re-run `npm.cmd run verify:student-login-responsive` in an environment that can actually launch Playwright browsers; the 2026-07-05 local attempt hit `spawn EPERM` for all 60 cases before any page assertions ran.
- Browser-QA the student login page on live or preview after `4ecd510`, especially 320px width, phone landscape, tablet widths, tab readability, theme-toggle overlap, and zoom/scroll behavior.
- Make `npm.cmd run typecheck` reliable on a clean checkout, or document that it must follow `npm.cmd run build`, because the pre-build 2026-07-06 run failed on missing `.next/types` references from `frontend/tsconfig.json`.
- Address the current frontend build warnings by migrating Sentry init into `instrumentation.ts` and setting `metadataBase`.
- Verify that deployed backend auth works after schema-migration commit `7f92d7d`, and confirm the missing security columns self-heal in the target environment.
- Add deployment/live-smoke evidence for the pushed frontend stack from `ec9d041` through `dd9938e`, and keep the earlier July 4 login/dashboard sequence bundled into that verification pass.

## Watch List

- The current local environment can build the frontend but blocks Playwright browser launches with `spawn EPERM`; do not treat the 60 failing responsive-matrix cases as product regressions until they are rerun elsewhere.
- The current local verification path is order-sensitive on a clean checkout: `npm.cmd run typecheck` may fail before a build repopulates `.next/types`, even though the command passed in this audit because generated types were already present.
- The new `flowState` tooltip signal is partly heuristic because the code falls back to `5` questions per sheet when `totalQuestions` is missing.
- The recent 2026-07-04 and 2026-07-07 changes are frontend-heavy and visual, so browser QA remains higher-signal than another typecheck/build pass.
- Historical MM browser confirmations are still not explicitly recorded in repo memory for:
  - the originally failing Admin Learning Path Studio preview for `MM-L1`, Lesson 1, DPS 1
  - Lesson 12 DPS 3 visual labels in browser UI after the successful live API verification

## Resolved Recently (2026-07-11 round 5, prepared not yet delivered)

- Round 4's section-numbering fix (below) shipped as PR #306 but the gap was still visible live, including in incognito (ruling out caching). DevTools Network inspection confirmed the raw API response: `sectionNumber` was correctly sequential (1-9), but `sectionTitle` — the actual string both the student instructions page and the admin page render — still contained the stale original number baked into the text itself (e.g. `"sectionTitle": "Section 7 - Cubes and Cube Roots"` on the row now numbered 6). Round 4 only renumbered the numeric field; the visible label never used it. Fixed in both places: `backend/app/api/routes_student.py`'s `student_competition_mock_instructions` now strips the embedded `"Section {old_number} - "` prefix from the raw title (regex, case-insensitive) and rebuilds it as `"Section {new_display_number} - {clean_concept_name}"`; `frontend/app/admin/competition/mock-studio/[mockId]/page.tsx`'s `groupQuestionsBySection` now builds each section's `sectionTitle` from the existing `getCleanMmSectionName(question)` helper (which already strips the embedded number using the question's real `sectionNumber`) instead of the raw stored title, so the Overview and Coverage Check tabs show a clean concept name that matches the adjacent renumbered `S{n}` badge. Not yet verified live — needs delivery, then a live check on MMP-001 specifically (the mock the developer used to catch this) confirming both the badge number and the title text agree with no gaps.

## Resolved Recently (2026-07-11 round 4, prepared not yet delivered — incomplete, see round 5 above)

- Competition mock section numbering had gaps whenever a section was omitted for a student level that hasn't covered that concept yet (e.g. Section 6 skipped, jumping straight from S5 to S7) — both on the admin Mock Studio detail page (Overview, Question Preview, and Coverage Check tabs, `frontend/app/admin/competition/mock-studio/[mockId]/page.tsx`) and the student-facing mock instructions page (`backend/app/api/routes_student.py`'s mock-assignments instructions endpoint). Fixed by sorting on the real fixed section number (to preserve correct concept order) and then renumbering sequentially for display only — the real section number is still used everywhere concept-specific logic depends on it (e.g. the admin page's BODMAS/financial/positional question-type detection), only the displayed "S{n}"/"Section {n}" label changed. **This fix alone was insufficient** — it renumbered `sectionNumber` but not the stale number embedded in the `sectionTitle` text itself, which is what's actually displayed. See round 5 above for the complete fix.

## Resolved Recently (2026-07-11 round 3, prepared not yet delivered)

- After round 2 shipped, the developer's own screenshots caught three more real issues: (a) the info popover rendered as a small anchored dropdown that visually overlapped the bars underneath it — the card uses 3D transforms for its tilt-hover effect, which changes how nested absolute/fixed positioning stacks, so it was tangling with the bars instead of sitting cleanly on top; fixed by making it a dedicated overlay scoped to just the card's own boundary (centered within the card, not the whole screen — confirmed with the developer that page-wide centering would feel disconnected). (b) All bars were the same color regardless of tier — fixed by giving each tier (S/A/B/C/D) its own distinct bar color, gray for rest days. (c) Clicking the info icon a second time to close it was also flipping the whole card to the quote-display side — root cause was the close-on-click-outside handler not calling stopPropagation, so the click kept bubbling to the card's flip handler after closing the popover; fixed by stopping propagation at every layer (trigger, backdrop, and popover content).

## Resolved Recently

- 2026-07-11 (round 2, prepared not yet delivered): Two more real bugs found by looking at the live heatmap after round 1 shipped. (a) Bars weren't visually rendering at all — only tooltips — because the heatmap row used `items-end` on a flex container, leaving each day-column's own height as auto/content-based, so the bar's `height: X%` had no defined ancestor to resolve against and collapsed to zero. Fixed by giving each bar an explicit-height (`h-20`) track to size against. (b) The flowState formula rewarded raw speed (flat "5 questions/minute" threshold) over accuracy, so a 97%-accuracy/60-minute mock scored a lower tier than a 92%-accuracy/30-minute one — because the two mocks have very different intended durations and a flat threshold can't tell them apart. Fixed by scoring pace per attempt against that specific task's own admin-defined expected duration (new additive `expectedDurationSeconds` field on `/student/results` rows and both `attemptHistory` payloads) instead of one global number; overall accuracy/speed weighting balance kept the same, since speed is intentionally a major factor in this abacus program. Also added an info-icon popover on the heatmap heading explaining what it tracks and what drives the tier, since students previously had no way to know.
- 2026-07-11 (round 1): Grind heatmap was silently undercounting activity — it only read practice-sheet `Results`, never mock exam or assessment-engine completions, even though both were already fetched by the dashboard page. Fixed by adding an additive `attemptHistory` array (full completed-attempt history, not just latest) to the mock-assignment and assessment-assignment backend payloads, and rewriting the dashboard's `grindData` calculation to pool practice + mock + assessment events per calendar day before computing one flowState/tier per day. Delivered to main as PR #303.
- Student dashboard JSX compilation was repaired in `134eeb2`.
- The student dashboard hydration crash from hook ordering was repaired in `8979bfc`.
- Conquest Matrix date presentation and mock slideshow support were refined in `303b004`.
- Student login no longer forces disabled browser zoom and now has dedicated responsive regression coverage wired into CI in `4ecd510`.
- The student dashboard grind heatmap now exposes weekly flow-state tooltips with corrected tooltip text colors and larger containers through `ec9d041`, `7e4212b`, and `dd9938e`.
- The earlier leaderboard/library premium styling sequence remains merged, synced to `origin/main`, and locally buildable.
