# Daily Log: 2026-06-18

## Context

This was a project-memory handoff audit. The repo already had late 2026-06-17 commits on `main`, but the project-memory docs had only partially captured them.

## Repository State

- Branch state at audit time: `main` = `origin/main` = `aae8814`.
- No new product code was changed during this audit; only project-memory files were refreshed.
- Local unrelated items were preserved:
  - `MathPath-Platform`
  - `copy_titles.py`
  - `old_seed.py`
  - `seed_function.py`

## Late 2026-06-17 Work Captured In This Audit

The final pushed June 17 changes that were missing or under-recorded in memory were:

- `9d5a268 Polish student mock attempt metrics`
  - moved the live timer into the student metric card grid
  - tightened the metric-card layout on the attempt page
- `8642be7 Tighten visual add less generation rules`
  - separated fast-visual and decimal-visual MM add/less validation rules
  - enforced valid row counts and operand ranges for visual questions
- `f6c5f6e Fix visual add less mock constraints`
  - made decimal visual add/less generation follow explicit whole-digit plans
  - passed section-aware add/less config into MM competition generation metadata
- `aae8814 Polish mock attempt expression layout`
  - widened the desktop question workspace for expression-style questions
  - shrink-fit long expression prompts instead of clipping or awkward wrapping

Earlier 2026-06-17 work that remains current:

- MM freshness window enforcement for the previous 15 active same-level mocks
- MM generator quality fixes for write-position variety and non-trivial multiply/divide questions
- Competition Mock Studio assignment scope fix
- Student competition mock attempt layout rebuild and low-time timer warning

## Verification Performed Today

Passed locally on 2026-06-18:

- `.venv\Scripts\python.exe -m pytest tests`
- `npm.cmd run typecheck`
- `npm.cmd run build`

Result:

- Backend full suite: 15 passed.
- Frontend typecheck: passed.
- Frontend production build: passed.

## Push And Deployment Evidence

- The latest code is pushed to GitHub because `HEAD` matches `origin/main`.
- Repo deployment memory already contains explicit live verification for the earlier 2026-06-17 freshness, MM quality, and assignment-scope pushes.
- Repo deployment memory does not yet contain explicit live Render/Vercel smoke for the later commits `8642be7`, `f6c5f6e`, `9d5a268`, or `aae8814`.

## Exact Next Recommended Steps

1. Open the live student competition mock attempt screen and QA long expression, dense vertical-sum, and visual add/less questions.
2. Generate or inspect live MM mocks and confirm fast-visual and decimal-visual add/less questions follow the intended row and digit patterns.
3. If the live checks pass, append explicit deployment evidence for the late June 17 commits to `DEPLOYMENT_LOG.md`.
4. Add regression coverage for MM visual add/less constraints and student attempt expression rendering.

## Admin Learning Path MM DPS Preview Fix

The user shared a live Admin Learning Path Studio screenshot showing a generic server error while generating preview for `MM-L1`, Lesson 1, DPS 1: `Decimal Number Add-Less, 2D x 2D (Visual) & 4D / 2D (Visual)`.

Root cause:

- The selected DPS uses `Decimal Number Add-Less (Visual)` with `borrowingMode = NEGATIVE_ONLY`.
- That routed generation through the borrowing add-less path.
- The validator correctly applied decimal visual row/digit rules, but the borrowing path was still producing generic negative decimal stacks that did not satisfy those visual rules.
- A broader curriculum sweep also found `Lesson 10 / DPS 2` could fail because fast visualisation validation was hard-coded to 2-digit operands while the attached concept is `3 Digit Add-Less (Fast Visualisation)`.

Implemented:

- Negative-answer decimal visual add-less now generates approved visual decimal stacks while still forcing a negative final answer.
- Fast visualisation validation now uses the section's explicit digit count instead of assuming two-digit operands.
- Added regression coverage for the exact `MM-L1` Lesson 1 / DPS 1 generation plan.
- Added regression coverage that walks every mapped Master Module DPS plan and verifies each one generates a non-empty question set.

Verification:

- Direct local reproduction for `MM-L1` Lesson 1 / DPS 1 now returns 30 questions.
- Local database-backed `generate_preview` for the selected DPS now returns 30 questions.
- `.venv\Scripts\python.exe -m pytest tests\test_generator.py tests\test_mm_competition_mock_generator.py`
- `.venv\Scripts\python.exe -m pytest tests`
- Result: 17 backend tests passed.
- Pushed commit `5bce2ed Fix MM DPS preview generation` to `main`.
- Live Render smoke for `MM-L1`, Lesson 1, DPS 1 returned 30 questions.
- Live Render smoke for `MM-L1`, Lesson 10, DPS 2 returned 15 questions for `3 Digit Add-Less (Fast Visualisation) & BODMAS`.

Remaining:

- User should retry the same Admin Learning Path Studio DPS in the browser.
- Continue live QA for the student competition mock attempt screen.

## Source Asset Intake System

The user clarified that repeated re-uploading of the same 150 DPS images and 3 Excel sheets is unacceptable. The project must retain durable source-asset context across daily conversation threads.

Implemented:

- Added `docs/project-memory/SOURCE_ASSETS.md` as the committed manifest for external source files, folders, DPS images, Excel workbooks, and extraction notes.
- Added `reference-assets/` as a local-only source asset vault inside the project folder.
- Added local vault subfolders:
  - `reference-assets/incoming`
  - `reference-assets/dps-images`
  - `reference-assets/workbooks`
  - `reference-assets/extracted`
- Added `reference-assets/.gitignore` so bulky source files remain local while the folder structure and README stay committed.
- Updated `docs/project-memory/README.md`, `DAILY_HANDOFF.md`, and `CURRENT_STATUS.md` so every new conversation reads `SOURCE_ASSETS.md`.

Current known candidate source path:

- `C:\Users\shail\OneDrive\Shailesh\Work\Math Path\Modules\MM`

Still missing:

- Exact durable paths for the full 150 DPS images.
- Exact durable paths for the 3 Excel workbooks.

Next intake step:

- When the user provides the final files/folders, record them in `SOURCE_ASSETS.md` immediately and use `reference-assets/` only if the user wants the files copied into the project folder.
