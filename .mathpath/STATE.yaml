current_epic: annual-competition-plan.md
active_package: pkg-09-rehearsal-and-regression.md
status: IN_PROGRESS

# Previous epic (competition-mock-practice-plan.md) is COMPLETED and shipped.
# Annual Competition (2026-09) is now the active epic. Package 1 (data
# model), Package 2 (assignment engine), Package 3 (Admin Annual Competition
# Studio -- backend AND frontend), Package 4 (section-timer + pause engine --
# the highest-risk/most novel piece of this whole build), Package 5
# (student live-attempt UI -- backend AND frontend: discovery, pre-section
# instructions, multi-section heartbeat-driven timer/attempt screen,
# slot-gated start, session-superseded handling), Package 6 (Scoring +
# Results -- automatic finalize-on-submit scoring hook, the confirmed
# accuracy/time/mistake-order ranking formula, admin rank/release actions,
# student result endpoint + a minimal inline result card on the existing
# attempt-submitted screen), Package 7 (Teacher/Admin Monitoring --
# live per-student/per-slot status incl. STUCK reusing Package 4's own
# heartbeat-grace threshold, a roster-scoped release-gated results review
# for teachers, admin UI finally added for Package 4's reconcile sweep and
# Package 6's rank/release actions, and a new read-only teacher monitoring
# page), and Package 8 (Certificates/Leaderboard -- see below) are all
# COMPLETE -- see .mathpath/packages/pkg-0{5,6,7,8}-*.md. Package 6b (admin
# "technical issue" retry override, REQUIREMENTS.md item 6) is also
# COMPLETE -- see .mathpath/packages/pkg-06b-retry-override.md.
# Package 5 also added answer capture (CompetitionEventAttemptAnswer),
# which Package 4's own doc had explicitly scoped out -- a considered,
# documented scope addition, not a silent expansion. Package 6 required
# zero schema changes -- CompetitionEventResult was already fully built in
# Package 1. Package 7 required zero schema changes either -- pure read
# layer over Packages 1/4/6's existing state, and confirmed zero writes
# (no db.add/db.commit anywhere in its service module), matching its own
# checklist's "teacher has no write path" requirement.
#
# Package 8 (Certificates/Leaderboard, 2026-09-05) is COMPLETE -- see
# .mathpath/packages/pkg-08-certificates-leaderboard.md. Rather than a
# further client round-trip, Shailesh gave the two outstanding product
# decisions directly: leaderboard scope is "own result only" (no public/
# shared leaderboard -- already fully satisfied by Package 6's existing
# student result endpoint, nothing new needed there; admin's leaderboard
# is Package 6/7's existing Results tab, polished with rank-medal styling
# this package), and certificate fields are name/level/rank/score/date +
# MathPath branding, issued to every finalized participant (not just
# rank-holders), downloadable from the student's own portal once
# released. New module `annual_competition_certificate_service.py`
# (reportlab, landscape A4) renders on demand from an already-computed
# CompetitionEventResult -- zero new schema, zero new scoring logic.
# Admin can always download regardless of release (matches this epic's
# "admin sees everything" convention). Sample certificate was rendered to
# an image and visually reviewed before shipping.
#
# REQUIREMENTS.md item 6's admin-triggered "technical issue" retry override
# (Package 6b, 2026-09-05) is COMPLETE -- see
# .mathpath/packages/pkg-06b-retry-override.md. Built directly on
# Shailesh's explicit go-ahead in between Package 7 and Package 8. New table
# CompetitionEventAttemptRetryGrant mirrors the pre-existing DPS
# AssignmentReattemptPermission's APPROVED->USED lifecycle; StartCompetitionEventAttempt's
# existing single-attempt rejection now checks for one before rejecting. A
# genuine pre-existing gap was found and fixed as part of the same change:
# competition_event_attempt_answers (Package 5) had no Alembic migration or
# startup-safety-net coverage at all -- backfilled into both. API-only, no
# admin UI yet (matches this epic's own established precedent of shipping
# admin actions API-first) -- see the package doc for full detail.
#
# The client's follow-up answer on items 1+2 (YLM/PL-1 age-boundary rule)
# arrived 2026-09-05 and resolved the ambiguity -- Package 2's
# DIRECT_LEVEL_MAPPING[("PM","PM-L1")] now targets "PM-L1" instead of
# "YLM-L1" (see .mathpath/packages/pkg-02-assignment-engine.md's
# "Post-completion amendment" section and REQUIREMENTS.md's item 1). No
# other package needed changes. One soft, non-blocking question remains
# open (is "Bloomers" a display label or a content-filter name) -- not a
# code blocker.
#
# All 8 core build packages are now COMPLETE. Package 9 (rehearsal + full
# regression) and Package 10 (go-live checklist) remain per the original
# plan's sequencing and Shailesh's "section by section" cadence -- not yet
# started, awaiting explicit go-ahead.
#
# Everything so far is built and verified locally only -- push/merge/deploy
# is being bundled across all packages per Shailesh's instruction
# (2026-09-04), not done per-package.
